# Phase RC-2 — Benefits at the counter (Registration Counter series, 2 of 4)

**Lane: LIGHT** (5 tasks, no full-module build — the machinery exists; this phase arms and gates it).
**Stop-loss: 1,500,000**, re-derived rather than copied (§2.143a — *a term nobody re-derives is a term nobody notices is zero*):
`1.5 × (86,000 measured per-task main rate × 5)` = **645,000** · inline-recon carry **150,000** · task-subagent term **0** (LIGHT) · review `240,000 × (1 + 2.0 remediation factor)` = **720,000**.
Two terms changed from RC-1's 1,700,000 and both are measured: the per-task rate is RC-1's *actual* (~430k main over 5 tasks) instead of the 200,000 placeholder, and **recon runs inline in the main session rather than as subagents** — RC-1 spent 400,000 on three recon agents for this same class of answer; this phase's whole spike below cost ~60,000 in greps, so the carry is set at 150,000 and the saving is banked, not re-spent.
**Session token balance at kickoff: ~14.96M** (recorded per §2.141; deltas at each task boundary in CLOSE).
**Parallel-lane state at kickoff:** three lanes share this checkout. **VD-1 vitals-desk** holds uncommitted `opd/{errors,events,index,realtime,vitals}.ts` + the whole `escalation.*` set and owns migration `0049_vitals_bay`; **18a radiology** is between T5 and T6. Stage BY PATH and commit WITH A PATHSPEC, reading `git diff --cached --stat` against the Files list first (§2.152 — RC-1 swept a peer's 54 staged lines despite by-path staging). All RC-2 runs use `TEST_DATABASE_URL=postgres://hmis:hmis@localhost:5433/hmis_rc2_scratch`, named in every evidence-citing commit (§2.137). Never bare `pnpm verify`: `pnpm --filter @hmis/core exec jest -w 2` then `pnpm --filter @hmis/web exec vitest run`, sequentially, box slot negotiated by message (§2.151).
**Config change already taken this session:** `maxWorkers: 2` landed at `42e7efc` on the owner's 2026-09-01 ruling — the pending item RC-1's handoff carried. It is the durable fix for both the local OOM and CI run 33436302396's false red, and it is why this phase can expect a full verify to survive.

## 1. Why this phase

RC-1 built the rails; RC-2 makes the money on them correct before RC-3 renders it. The owner's design demands that picking a returning member **reprices the bill column before billing is ever opened** — a benefit that only appears at invoice time is a benefit the clerk cannot quote — and the owner's **BEST SINGLE BENEFIT** ruling (2026-08-31) demands the seat show *which chip won and why the others lost*.

Recon (this session, inline) **refuted the phase's own premise, which is the cheapest thing a spike can do**: the counter quote already runs the entire Plan 09 benefits contest. `feeQuote` → `previewInvoice` → `priceDraftWithBenefits`, and `PricedLine` already carries `candidates[]` **and** `winner`. There is no membership wiring to build and no winner/loser wire type to design. What is actually missing is narrower, sharper, and mostly about **refusal**: coupons cannot physically reach the quote, referral proposes nothing, member benefits do not yet stop at the self-pay share, and one existing writer would make a naive referral discount pay money out on visits nobody referred.

This is the sixth phase running in which the honest answer was *arm and gate*, not *build*. **No deploy anywhere in this series** — production has never left `commissioning`; code-complete + green evidence is the finish line.

## 2. Spike — answered by reading, before authoring

- **S1 — does the counter quote path already compose member benefits?** **YES, fully.** `feeQuote` (`charge-rules.ts:64`) calls `previewInvoice`, which is one line: `priceDraftWithBenefits(db, input, now).priced` (`invoices.ts:594`). `PreviewInvoiceInput` already declares `patientId` **and `couponCodes`** with a comment saying exactly why ("a counter that quotes high and bills low teaches its clerks not to trust the quote"). Membership needs **zero** integration work. **The one real gap: `feeQuote(db, encounterId, now)` accepts no codes and forwards none, and `feeQuoteRoute` is `@Param("encounterId")` only — so a coupon presented at the counter can reach the invoice but never the quote.** That asymmetry is T1.
- **S2 — does the wire carry what BEST SINGLE BENEFIT needs to render?** **YES.** `PricedLine.candidates: AdjustmentCandidate[]` + `winner: AdjustmentCandidate | null`, and each candidate carries `reason`, `amountPaise` ("for rejected candidates: the amount that was ASKED (audit)") and `rejected: { code, detail } | null`. The losers and *why* they lost already travel. RC-2 designs no new type for this; it owes only that the field survives the quote and is proven non-empty by execution.
- **S3 — §2.149's WRITER grep on the predicate a referral discount would key off. THIS IS THE PHASE'S SHARP FINDING.** Readers of `referralSource` are harmless. The **writer** is not: `openLabWalkinInTx` (`encounters.ts:340`) does `referralSource: input.referralSource ?? "external_rmp"` — **every direct lab walk-in whose caller names no referrer is stamped `external_rmp`.** A referral discount keyed on the `referralSource` enum would therefore discount bills nobody referred and open partner ledger entries against no partner — and `external_rmp` is precisely the payee class the department brainstorm holds **structurally OFF** (§11.19-C fix 1, IMC 2002 cl. 6.4: external-RMP payout is unlawful). The enum is *attribution capture*, not a payee. D3 below is the consequence.
- **S4 — next free migration.** Journal head measured at authoring: `0049_vitals_bay`, 50 entries — **VD-1's, taken last night**. Re-measure `_journal.json` immediately before and after generating; the VD-1 lane may take another. RC-2 is expected to need **no migration at all** (D4/D7), which is itself the finding.
- **S5 — the permission ground truth.** `membership.instrument.read` + `.recognise` + `.grace_honor.request` are held by `front_office`, `front_office_supervisor` and the billing roles — **only *apply* exists.** `membership.catalog.manage` sits in `NOT_YET_MODELLED` with a measured reason ("IT GUARDS NO ROUTE (measured 2026-08-26)"); `membership_admin` holds `import.run` + `reconcile.operate` and 0 users. Every `partners.*` permission is parked pending O-8. So the enrol/apply split is a **new permission**, not a regrant — T4.
- **S6 — does arming the flag change anything already shipped?** §2.150's question, asked of RC-2 because RC-2 is the phase it was written for. `memberBenefitsEnabled()` is read at ONE call site (`invoices.ts:484`) and is load-bearing: with it off, `composeBenefits` is never called, no membership table is read, no source is appended. Every consumer of a priced draft therefore becomes a consumer of *new* numbers the moment it flips. The census of those consumers is T5's explicit obligation, run as if the code were new.

## 3. Design decisions

- **D1 — Membership at the counter is ARMED, NOT BUILT, and no money-path line is written for it.** S1 settles it. RC-2 adds no membership call, no new source, no quote plumbing for the percentage benefit. What RC-2 owes membership is *proof by execution with the flag on* that the quote and the invoice that follows it agree to the paise, plus the refusal gates below. **DECIDED:** RC-2 does not flip `MEMBER_BENEFITS_ENABLED` in any deployed environment — the flag is armed *in tests*, and the `.env` flip is a commissioning act the owner holds, like the deploy. Catalogs are empty by Plan 09 DD3 and the synthetic book lives out-of-git at `/opt/hmis-context/`.
- **D2 — Coupons reach the quote through the same door as the invoice.** `feeQuote` gains `couponCodes?: string[]` and forwards it into the `previewInvoice` input it already builds; `feeQuoteRoute` takes them as a repeatable query parameter, zod-parsed at the boundary. **The boundary is declared, not inherited** — RC-1 T1 was born from `receiptBlockSchema` silently stripping a field the web was already sending, and a quote route that accepts codes it does not declare would resurrect that exact class one screen over.
- **D3 — A referral proposes from a RESOLVED ATTRIBUTION, never from the `referralSource` enum.** S3's trap is the whole argument. The new `AdjustmentSource` reads the encounter's attribution link (`attribution_ids` / `partner_ref_map`, resolved once before pricing exactly as `composeBenefits` resolves instruments — `propose` stays pure and synchronous, `tariff` stays byte-untouched). No counterparty resolved ⇒ **no candidate, not a zero candidate.** `external_rmp` never proposes under any resolution: it is a *capture value*, its payout is unlawful (IMC 2002 cl. 6.4), and the source refuses it explicitly with its own reason string rather than by falling through. Source order becomes `[rule, manual, membership, coupon, referral]` — referral last, because RC-1's tie-break ruling ("a standing hospital rule beats a commercial instrument; a paid durable membership beats a one-shot") extends to "a benefit the patient bought beats one a channel partner brought", which is the version explainable across a counter.
- **D4 — The partner LEDGER entry is the attribution link, NOT a commission accrual.** The design asks for "a partner ledger entry for month-end commission"; `COMMISSION_ACCRUAL_ENABLED` stays **OFF** (O-8 unruled, and the brainstorm's O-1 default keeps every gated lane structurally off). RC-2 records *which counterparty this discount was proposed under* on the priced line's candidate `reason` and the existing attribution row — the fact travels, the money does not move. **DECIDED:** no accrual is written, no `commission.accrued` event is emitted, and no `partners.*` permission leaves `NOT_YET_MODELLED`. The month-end lane is Plan 21's; RC-2 must not open a second home for it.
- **D5 — Enrol and apply are two permissions from day one, and enrol is new.** *Apply* is the shipped `membership.instrument.recognise` (front_office holds it — correct, the clerk applies). *Enrol* becomes `membership.instrument.enrol`, declared on the membership manifest and granted to `front_office_supervisor` and `membership_admin`, **never to `front_office`** — the owner's ruling that this is "a ruling, not a UX nicety". **DECIDED:** `membership.catalog.manage` STAYS in `NOT_YET_MODELLED` and its reason is amended to name this phase; RC-2 builds no catalog screen (that is Plan 22 T1) and will not mint a key to a door that still does not exist — the same argument `seed-roles.ts` already makes for it. Enrolment's *route* is deliberately minimal: the permission and its refusal exist and are proven; the enrolment UI is RC-3's.
- **D6 — Member benefits stop at the self-pay share (brainstorm O-3's default, made executable).** When an encounter's `intendedPayer` is not `self`, the membership, coupon and referral sources **do not propose** — TPA/corporate/scheme rates are tariff substitution, not contestants, and a member discount stacked on a panel rate is money given away twice on a bill the hospital does not collect. The refusal is recorded as a rejected candidate with its reason, so the seat can say *why* the chip greyed out instead of silently omitting it. This is the phase's second money path and it gets fail-first treatment.
- **D7 — Corporate v0 and package v0 are naming, not machinery.** Corporate v0 = the already-recorded `intendedPayer` surfaced on the quote beside the shipped credit-extended exit, so the screen can say "bill to panel — nothing to collect" in RC-3; **no panel table, no e-authorisation record** (Plan 21 owns those; a second home is worse than a missing one). Package v0 rides the shipped `membership_plans.kind = 'package'` plus the existing `entitlement_counters`/`entitlement_movements` — a covered consult is an entitlement consumption the contest already knows how to narrow (`narrowToUsableEntitlements` drops exhausted counters rather than refusing at commit, so a member past their four free consults is simply billed). **No new table and no migration.**

## 4. Tasks

### T1 — CRITICAL · coupons reach the quote, and the contest outcome survives to the wire
**Files:** `apps/core/src/modules/billing/charge-rules.ts`, `apps/core/src/modules/billing/billing.controller.ts`, `apps/core/src/modules/billing/charge-rules.test.ts`, `apps/web/src/lib/billing-api.ts` (wire type, additive).
Per D2. `feeQuote(db, encounterId, now, opts?: { couponCodes?: string[] })`; route declares the codes in zod at the boundary and passes them through. Assert the quote's `draft.lines[0].winner` and `candidates` are populated with the flag ON.
**Assertion book:** assertion — a quote carrying a valid coupon code returns the same `netPaise` the invoice then charges, and names the winner; mutant — a scratch `feeQuote` that drops `couponCodes` on the floor (today's shipped behaviour); discriminating input — one redeemable coupon on a `new` visit; expected kill — quote net equals gross (undiscounted) vs the discounted net the invoice charges. **This is the RC-1 T1 class run deliberately: the shipped code IS the mutant, so the fail-first red is the kill.**
**Commit:** `feat(core): a coupon presented at the counter reaches the QUOTE, not just the invoice (RC-2 T1)`

### T2 — CRITICAL · referral-as-discount, keyed on a resolved attribution
**Files:** `apps/core/src/modules/partners/sources.ts` (new), `apps/core/src/modules/partners/index.ts`, `apps/core/src/modules/billing/invoices.ts` (source composition only), `apps/core/src/modules/partners/sources.test.ts` (new).
Per D3/D4. Factory mirrors `membershipSource`/`couponSource` exactly (`sources.ts` is the template — pure, sync `propose`; the resolution happens once, before the transaction). Purity asserted, not claimed, per `membership/sources.test.ts`'s precedent.
**Assertion book:** (a) assertion — a lab walk-in stamped `external_rmp` by `openLabWalkinInTx`'s default proposes **NO candidate** and no attribution row is touched; mutant — a scratch source keyed on the `referralSource` enum instead of the resolved counterparty; input — S3's exact path (`openLabWalkin` with no `referralSource`); expected kill — a discount and a partner-attributed candidate on a bill nobody referred. (b) assertion — with a real resolved counterparty the referral candidate proposes and **loses to a larger membership candidate**, appearing in `candidates` as a non-winner with its reason (BEST SINGLE BENEFIT, by execution). (c) assertion — no `commission_accruals` row and no `commission.accrued` event exists after either path.
**Commit:** `feat(core): referral-as-discount proposes from a resolved attribution — never the referralSource enum (RC-2 T2)`

### T3 — CRITICAL · benefits stop at the self-pay share
**Files:** `apps/core/src/modules/billing/invoices.ts`, `apps/core/src/modules/billing/benefits-payer.test.ts` (new).
Per D6. The gate sits in `composeBenefits` where `resolveEncounter`'s `intendedPayer` is already in hand — one read, no new query.
**Assertion book:** assertion — an encounter with `intendedPayer: "corporate"` and a live membership prices at FULL gross, with the membership present as a rejected candidate naming the payer; mutant — the gate removed; discriminating input — the same patient, same plan, `self` vs `corporate`; expected kill — discounted vs full net on the corporate bill. Both directions executed (the `self` case must still discount — a gate that refuses everything passes a one-sided test).
**Commit:** `feat(core): member, coupon and referral benefits apply to the self-pay share only (RC-2 T3)`

### T4 — ROUTINE · the enrol/apply permission split, day one
**Files:** `apps/core/src/modules/membership/manifest.ts`, `apps/core/scripts/seed-roles.ts`, `apps/core/src/modules/membership/membership.controller.ts`, **plus every census the manifest move touches** — run both §2.138 greps at directory scope AND `README` permission tables. **This task owes a FULL verify at its boundary (§9.9 rule 6): RC-1 T2's derived-census class fires here.**
Per D5. Declare `membership.instrument.enrol`; grant to `front_office_supervisor` + `membership_admin`; amend `membership.catalog.manage`'s park reason to name RC-2.
**Assertion book:** assertion — `front_office` is refused enrolment and `front_office_supervisor` succeeds, both by execution; mutant — inline mutant-shaped assertion (18a precedent, disclosed): the guard swapped to `recognise` instead of `enrol`; expected kill — the clerk who may only apply is admitted to enrol (403 vs 200 inversion).
**Commit:** `feat(core): enrolling a member is not applying one — membership.instrument.enrol split from recognise (RC-2 T4)`

### T5 — ROUTINE · package v0, corporate v0, and §2.150's arming census
**Files:** `apps/core/src/modules/billing/charge-rules.ts` (payer + package naming on `FeeQuote`), `apps/core/src/modules/membership/entitlements.test.ts` (package kind), `apps/core/src/modules/billing/charge-rules.test.ts`, `apps/web/src/lib/billing-api.ts` (wire, additive).
Per D7 — and the census §2.150 demands: **every consumer of a priced draft re-read as if new code**, because arming the flag changes what they receive. Enumerate them in the commit body, not just in a grep.
**Assertion book:** assertion — a `kind='package'` plan with a consult entitlement counter zeroes the consult line and records one `entitlement_movements` row; a second visit past the counter's limit bills in full with no movement; mutant — the exhausted-counter narrowing removed; expected kill — the fifth visit cannot be invoiced at all (the exact failure `composeBenefits`'s header predicts).
**Commit:** `feat(core): package v0 rides the shipped entitlement counters; the quote names the payer (RC-2 T5)`

**Verify economy (§9.9):** T1–T3 batch into one narrow suite each plus a combined money-path run; **T4 owes a full verify at its boundary**; T5 batches into the phase-end full pass. `pnpm typecheck && pnpm lint` before every launch (§2.132). Tree frozen during any launched run; the box slot negotiated with the VD-1 and 18a lanes by message. With `maxWorkers: 2` now in the config, a full pass is expected to survive without the OOM killer — if it does not, that is new information and it goes in CLOSE.

## 5. CLOSE

**Commits:** `42e7efc` (the owner's `maxWorkers: 2` ruling, its own commit) · `c5b5eb7` (doc) · `d953223` (T1) · `ab01603` (T2) · `d922a1a` (T3) · `15194fe` (T4) · `e2a1589` (T5). **CODE-COMPLETE, NOT DEPLOYED** — the series' standing rule. **THE REVIEW LANE HAS NOT RUN; see the debt below before treating this as closed.**

### What the phase turned out to be

Three of five tasks were **arm-and-gate, not build**, and the spike said so before any code was written — which is the cheapest thing a spike can do:

- **T1's premise was refuted in recon.** `feeQuote` → `previewInvoice` → `priceDraftWithBenefits`: the counter quote already ran the entire Plan 09 contest, and `PricedLine` already carried `candidates[]` **and** `winner`. The owner's BEST-SINGLE-BENEFIT ruling needed no new wire type and membership needed no wiring. What was actually missing was one door: `feeQuote` accepted no codes, so a coupon could reach the invoice and never the quote.
- **T5's premise likewise.** `loadInstances` filters on patient and codes and **never on `kind`**, so `kind='package'` already resolved, contested and consumed counters exactly as a membership does. Package v0 is a proof; no table, no column, **no migration in the whole phase**.
- **T3 was mostly refusal**, and **T4 was a boundary** rather than a feature.

### The findings that were not in the plan

1. **§2.149's writer-grep caught a live money trap before it shipped (T2).** `openLabWalkinInTx` (`encounters.ts:340`) writes `referralSource: input.referralSource ?? "external_rmp"`, so every direct lab walk-in that named no referrer carries the value that looks most like a referral. A discount keyed on that enum would have paid out on bills nobody referred, in the one payee class whose per-patient payment is unlawful (IMC 2002 cl. 6.4). The source is keyed on a resolved attribution instead; the rule-21 mutant priced the damage at ₹50 a visit and died.
2. **`partners` already imports `billing`,** so the referral source is REGISTERED through a new inversion seam (`billing/benefit-sources.ts`, mirroring `registerFeeSettledHook`) rather than imported into a cycle.
3. **T1 exposed a defect in the opposite direction and T2 closed it.** `IssueInvoiceInput.couponCodes` had carried a comment since Plan 09 saying no HTTP caller could set it. Once the quote could take a coupon, a quote that discounted and an invoice that could not would be RC-1 T1's defect reversed — promise ₹450, charge ₹500. Both invoice bodies now declare it.
4. **§2.150's arming census outgrew the phase title, and this is the most consequential finding here.** Six consumers of a priced draft; two are not the counter — `lab/lab-desk.controller.ts:183` and `ot/bill.ts`, both passing `patientId`, both therefore resolving instruments. **Arming `MEMBER_BENEFITS_ENABLED` is a hospital-wide act, not a counter one.** Correct behaviour (scope is per-category) but not what "benefits at the counter" describes. RC-2 arms it in tests only; the `.env` flip stays the owner's commissioning act.

### DECIDED, and one of them overrules this document

- **The payer refusal is NOT recorded as a rejected candidate** — a correction to §3's own D6. `AdjustmentCandidate.rejected` means *contested and refused*, and a payer-ineligible instrument was never in the contest; emitting one would render a losing chip that never ran, and would have cost a widened `rejected` union in `tariff/types.ts` plus the web wire to say what `PricedDraft.intendedPayer` already says. Gated instead, payer lifted onto the quote (T5).
- **RC-2's non-table permission pairs got their OWN constant and their OWN README sentence**, not an append to `PLAN_09_PAIRS` and not an edit of "Plan 09's four `membership.*` strings" to read five. Appending would have typechecked and passed while recording an RC-2 decision as Plan 09's DD18; that count is scoped to Plan 09 and is still true. **A census kept green by making the record false is worse than one that fails.**
- **`membership.catalog.manage` stays parked**, re-measured at 0 occurrences outside the manifest. `membership.instrument.enrol` is granted because it guards a real route locked by a flag: *a permission guarding no route is a key to a door that does not exist; one guarding a door locked by a flag is a lock.*
- **No accrual is written for a referral** (D4). `COMMISSION_ACCRUAL_ENABLED` stays off, O-8 unruled, Plan 21 owns the payout lane.

### Mechanical verification, stated honestly

- **Full core pass: 335 suites / 3349 tests — 330 suites and 3337 tests PASS.** Exit 1 came solely from five `src/modules/radiology/` suites belonging to the 18a lane. Not caused by RC-2: radiology's entire billing surface is `withIdempotency` + `BillingError`, it reaches no pricing code, and the reds reproduce **independently on the VD-1 lane's tree**, and **in isolation on a virgin database at BOTH `-w 2` and `-w 7`** (identical 5 suites / 12 tests). That last run **exonerates the owner's `maxWorkers` ruling** — it would have been red at the old default too. Diagnosis handed to 18a: `schedule.test.ts` truncates per test, so two orders collide *within* one test, and two cases with different `serviceId`s both collide with the same order `R2608310001`.
- **Web: 61 files / 374 tests, exit 0.** typecheck 0 and lint 0 at every commit.
- Per-task: T1 3 suites/9 (fail-first red recorded: `Expected 45000, Received 50000`, winner null — the shipped code was the mutant); T2 21/21 incl. the enum mutant; T3 6/6 incl. the gate mutant; T4 29/29; T5 31/31. Money-path batches 73 suites/725 and 74/731. All on `hmis_rc2_scratch` (§2.137).
- **Rule-21 mutants: three, all died** — the enum-keyed referral (₹50 damage on every lab walk-in), the removed payer gate (₹100 given twice on a corporate bill), and the removed entitlement narrowing (the fifth visit un-invoiceable). Two were inline and disclosed rather than scratch files, because what needed showing was the DAMAGE, not a second copy of the arithmetic.

### THE OUTSTANDING DEBT — read this before calling RC-2 closed

**The two independent review passes §9.10 requires have NOT been run.** This session was constrained from spawning subagents. Every phase since 09a has had reviewers find a CRITICAL or MAJOR over a tree that was already green: RC-1's pass 1 found 1 CRITICAL + 4 MAJOR against 55 green tests, a died mutant and clean typecheck/lint, and its pass 2 found the M2 fix incomplete plus a new screen regression. **RC-2 touched three money paths** (the quote's coupon door, a new pricing source, the payer gate) and one permission boundary. On the base rate, this tree has findings in it. That is the single largest risk on this phase and it is the owner's call, not something to bury in a commit message.

### Carries
- **The five radiology suites → the 18a lane**, with the discriminating experiment already spent.
- **M3 → RC-3** (unchanged from RC-1): no un-settle push; `reverseAllocation`, `markEnteredInError`, `issueCreditNote` reach no hook. Rename `queue.fee_settled` → `queue.fee_status_changed` while it is still unconsumed.
- **§2.154's open question → RC-3**: RC-1 T3's join-queue race mutant died on an unwarmed connection pool, where a right kill and a lucky interleaving are indistinguishable. Warm and re-run before trusting that guard.
- **O-15 (opening the sales lane) remains the owner's** — `membership.instrument.enrol` exists, is granted, and refuses.

### Actuals

| | |
|---|---|
| session balance | kickoff ~14.96M; **measurement is DISCONTINUOUS** — the context was refreshed mid-phase, so this is reconstructed: ~360k to the refresh + ~60k after ≈ **420k main-session** |
| subagents | **0** — recon ran inline (~60k of greps) where RC-1 spent 400k on three recon agents; reviewers not run (see the debt) |
| total vs stop-loss | ≈ **420k of 1,500,000 (28%)** — but the 720k review term is UNSPENT because UNRUN, not because it was efficient. The coding half came in at ~420k against RC-1's ~830k for the same class of work; **the inline-recon decision is what bought that** |
| tasks | 5 coded, 7 commits, 0 migrations, 0 deploys |
| catches before review | 1 live money trap (T2's enum), 1 reversed asymmetry (T1→T2's invoice bodies), 1 arming-scope finding (T5) — all by spike or census, none by a test that was already passing |

### Lessons bound for the ledger
1. **A pathspec protects against a dirty INDEX, not against a peer's dirty WORKING TREE** — §2.152's blind spot, identified in the morning and then suffered in the afternoon when a peer's pathspec commit captured this lane's uncommitted census edits, leaving `main` red in a split state. Neither a stat nor a `--cached --stat` can see it.
2. **One permission moved TWELVE censuses here**, against §2.155's three and the peer's corrected ten. The two nobody had counted are a bare-integer array that appears **twice** (once for the idempotence leg), and an index that must be derived rather than pattern-matched: `membership_admin` is index 14, `staff_auditor` also holds two grants, and changing the first `2` you find leaves the suite red with a diff that names no role. **Grep tells you where to edit; only the run tells you what you broke.**
3. **Verify with an instrument that can see what you are looking for.** Twice in one session a confident all-clear came from a pattern that could not match: `pkill -f "jest.js…"` and `ps | grep "jest.js"` are both blind to `processChild.js` workers, and `grep -E "^\+…"` is blind to a diff's `+` column. The first left orphaned workers truncating a database under a running batch and produced reds in an innocent module; the second produced a false all-clear on a sweep that had actually happened.
4. **An exhaustive `toEqual` is worth its maintenance cost.** Adding `intendedPayer` to `FeeQuote` was caught instantly by a field-for-field assertion — the exact opposite failure mode to the bare-integer censuses, which fail silently when missed.
5. **A shared-blame story that fits can still be wrong.** Two lanes converged quickly on "your unrun suite plus my over-wide batch", which was tidy, mutual, and false; the cause was orphaned workers. Both rules were worth keeping; neither explained the evidence.
