# HANDOFF — RC-4 mid-phase: T1 and T3 are done, T2 and T4 are yours, T5 is the owner's

Paste everything below the line into a fresh Claude Code session opened at `/opt/hmis` on the build host. Written 2026-09-01 at RC-4's T3 boundary, per EXECUTE-METHOD-V3 §5A.2. **Every fact here is a POINTER — trust the pointed-at file over this summary if they ever disagree.** Where this file and the phase document disagree, **this file is newer**; §4A of the phase doc and §5 below record why.

---

## Mission state in one paragraph

You are the **Registration Counter lane** of an agentic hospital OS, building the owner-approved **Desk One** seat. **RC-1 (rails), RC-2 (benefits) and RC-3 (the seat) are CLOSED, code-complete, NOT deployed.** You are mid-**RC-4 (the counter that finishes a patient)**: **T1 and T3 are done and pushed; T2 and T4 remain; T5 is gated on the owner.** `main` is at **`d8cae7d`**, in sync, and green. The series-wide prohibition holds — **no deploy anywhere**; production has never left `commissioning` and code-complete + green evidence is the finish line.

**Migration head `0051_close_review_remediation`, 52 entries. MEASURE it, never trust this line.**

## 1. Read these, in this order

0. **`docs/superpowers/plans/2026-09-01-SCOPE-registration-counter-remainder.md`** — what is left in the whole series, measured against its four acceptance demos, and the proposed **three-phase cut (RC-4/RC-5/RC-6)**. The cut is **PROPOSED AND UNRATIFIED**; RC-4 assumes it. Read this before believing any statement about how much is left.
1. **`docs/superpowers/plans/2026-09-01-phase4-rc4-finishing-a-patient.md`** — YOUR phase doc. §1 (why this is a wiring phase), §2 (spike, answered), §3 (D1–D7), §4 (tasks), **§4A (what measuring for T2 found, and the re-order)**, §6 (rulings).
2. `docs/superpowers/plans/2026-09-01-phase3-rc3-the-seat.md` — RC-3's CLOSE. **§5.10** (what two reviews found over a green tree), **§5.11** (three of my checks that could not fail), **§7** (eight findings deliberately declined — that list is partly yours).
3. `docs/superpowers/EXECUTE-METHOD-V3.md` — **§5A is where this series' lessons landed**: 5A.1 the contract pass · 5A.2 this handoff · **5A.3 the assembled-artifact assertion** · **5A.4 revert every fix**. §6 stop-loss, §9.9 verify rules.
4. `docs/superpowers/AGENT-RULES.md` — the binding contract. Then ledger **§5 only**; cite §2 entries BY NUMBER (`grep -A 30 "^### 2\.NNN" docs/superpowers/plans/reports/EXECUTION-LESSONS.md`). **NEVER open the whole ledger — it is 461KB / ~115k tokens and this lane has not opened it for four phases.**
5. Auto-memory `registration-counter-build.md` loads with your session.

## 2. What RC-4 has done

| | |
|---|---|
| `8c2a953` | **T1** — the seat REGISTERS IN FOUR FIELDS IN PLACE and opens a visit. `Register new` used to navigate to `/registration?new=true`, abandoning the dossier, the search and the patient session; **leaving is the defect the seat exists to remove**, so the route no longer hands a navigation down at all. The design's fourth field `ageYears` was accepted by `registerBody` all along. |
| `3a87430` | T2's measurement re-ordered the phase — see §5 below. |
| `bcb1397` | **T3 — D7 RULED: the PAID stamp goes on the BOARD** (`opd-desk.tsx`), which is literally what the demo calls it. `feeStatus` had been on the wire since RC-1 T3 with **no web type declaring it** — no core change needed. The seat also renders the `tokenNo` it already held from the walk-in result, closing D2's "token" noun for free. |
| `d8cae7d` | D7 recorded as ruled in the phase doc. |

**Evidence at T3:** full web `vitest run` **68 files / 459 tests, exit 0**; `pnpm typecheck` workspace-wide exit 0; eslint clean. **No core source has been touched by RC-4 at all**, so no core batch is owed and no test database has been used.

## 3. YOUR TASKS

### T2 — CRITICAL · the deferred join, its first consumer since RC-1 wrote it

**It was blocked on T3 and is now unblocked.** Bill-first: `walkIn({… join: "defer"})` returns **`WireWalkInDeferredResult`** — null `tokenNo`, null `sessionId`, null `queueEntry` — then `POST /opd/visits/:id/join-queue` fills them after the money.

**Measured seams, so you do not have to re-derive them:**
- `apps/web/src/lib/opd-api.ts:336` declares `join?: "queue" | "defer"` and `:342` declares `WireWalkInDeferredResult`, whose own docstring says *"the shipped counter never sends `join`"*. **`grep -rn '"defer"\|join-queue' apps/web/src` returns the declaration and its comments and NOTHING ELSE.** You are the first caller ever.
- The route is `apps/core/src/modules/opd/opd-visits.controller.ts:289`, `POST visits/:id/join-queue` → `joinQueue` in `encounters.ts`.
- **`joinQueue` has NO settlement gate, and that is CORRECT.** It checks actor, encounter exists, `status === "registered"`, service date, responsible doctor, and no live entry. The PAID stamp is **derived** from `encounterFeeStatuses`, never stored — so a token joined after payment reads PAID and one joined before reads UNPAID, both correctly. **Nothing needs a gate.** The mutant is therefore *"the join fires before the money"*, and the kill is an UNPAID token in the one lane whose entire purpose is that it never appears.

**Assertion book:** assertion — a bill-first walk-in has null token, session and entry until the money is taken, then joins with a token already PAID; mutant — the join fires before settlement; kill — an UNPAID token on the board in the bill-first lane. **Fail-first treatment: this is the phase's highest-risk task precisely because nobody has ever called this path.**

### T4 — ROUTINE · the flow pill, worn openly

**A CORRECTION TO THE PHASE DOC'S D5, measured while writing this handoff — D5 says the pill reads "the department the seat is working under". IT IS WRONG.** `counterSequence` and `tokenLane` live on the **OPD CONFIG, hospital-wide** (`kernel/db/schema/opd.ts:36`, read by `modules/opd/config.ts:189-202`), not on `opd_departments`. One setting for the counter, not one per department. Fix D5 when you get there rather than inheriting the error.

**The route already exists and is purpose-built:**
- `PUT /opd/config/counter-flow` — `opd-masters.controller.ts:229`, permission **`opd.counter.flow.manage`**, body `counterFlowBody = { counterSequence?, tokenLane? }`.
- Its own comment states the design: *"the flow pill's own body: EXACTLY the two flow keys. zod strips anything else, so a holder of the narrow permission cannot reach danger ranges or the letterhead through this route no matter what the request carries."* **That is a security property — do not widen the body.**
- `COUNTER_SEQUENCES` / `TOKEN_LANES` are the enums; read them rather than retyping the strings.

**Assertion book:** assertion — the pill shows the current `counterSequence`, and a clerk without `opd.counter.flow.manage` cannot change it; mutant — the pill rendered from client state rather than the server's value; kill — two counters showing different sequences for one hospital.

### T5 — **DO NOT START.** Deleting `/counter` is the owner's call

It is the one hard-to-reverse action in the phase: it deletes a shipped, proven money path. **D6 orders it LAST**, after T1–T4 have shown the seat finishing a patient — that ordering is the argument for the ruling, not an obstacle to it. Both consequences are already staged: `/counter/seat` is **the only NAV row in the application with no module-manifest entry**, and `caddyfile-parity.test.ts`'s pin comment already says 45 → 44.

## 4. OWNER RULINGS OUTSTANDING — three, none of them yours to default

1. **Is a plan-bundled coupon a BEARER instrument?** A stranger presenting it **permanently spends the member's single-use coupon** — `couponUnusableReason` never checks the holder, and the redemption's uniqueness is `(coupon_id, cycle_no)` with no patient term. **MONEY.** Gates RC-5, not you.
2. **Does `/counter` go, and in this phase?** See T5.
3. **Should a fully-refunded consult put the token back to UNPAID?** **Confirmed by execution: a credit note CANNOT un-settle** — `settlementState` computes `covered = credited + allocated`, so a credit note counts toward coverage and only ever moves settled-ward. RC-3's D4 claimed the opposite and the test asserting it checked only `via`. **MONEY and patient-visible.** Gates RC-5.

**A fourth was ruled by me and should never have been escalated** (`250979b`): `Ctrl+N` is Chrome-reserved, so the new-patient chord is **`Alt+N`, global in `keyboard.tsx`** beside its seven siblings. The standing rule is *decide on industry standard; stop only for money, procurement or law* — apply it, and correct yourself out loud when you get it wrong.

## 5. THE FIVE THINGS THIS SERIES HAS PAID THE MOST FOR

**5.1 — THE RAIL IS ALREADY BUILT. MEASURE BEFORE YOU WRITE.** Three phases, three times: `matchedOn` (RC-3 T4), `avgConsultMinutes` (RC-3 T4), `feeStatus` (RC-4 T3). Each was filled server-side, returned over the wire **with no serializer**, read by screens that could not see it — and **each needed no core change at all**. RC-4's §1 table lists the two still open. Before writing a line of T2 or T4, grep the server.

**5.2 — UNIT MUTANTS CANNOT REACH ASSEMBLY DEFECTS (§2.162 / method §5A.3).** RC-3 built thirteen mutants, applied and killed every one, over clean typecheck, clean lint and a full green suite — then two reviewers returned **3 CRITICAL + 12 MAJOR and every CRITICAL was in the assembly**: a screen handing `quote={null}` into its own panel; a quote with no lifetime showing patient A's bill and PHI under patient B's name; a hard-coded `issued={null}` printing "Collect ₹400" on an already-paid encounter. **Every task owes an assertion over the ASSEMBLED screen driven through a full cycle — two patients, not one.**

**5.3 — REVERT EVERY FIX AND WATCH IT GO RED (§2.163 / method §5A.4).** **Four checks of mine could not fail this session, and the revert found all four where reasoning found none:** a `git checkout` that discarded a task's uncommitted route mount while the suite stayed green; a refused-refetch test with no prior success to leave standing; an alias-consumer test that grepped the source for a string appearing elsewhere in the file; and **T3's own token test, which asserted the token vanishes after Escape — true whether or not the state cleared, because the line lives inside the `inHand !== null` branch.** That last one is F1's exact shape on a second field, inside the phase that fixed F1.

```
cp <file> /tmp/fix.orig      # NEVER `git checkout`: a revert is a WRITE, and a write to a file
                             # with uncommitted work is a deletion you did not type
<restore the defect> ; <run>  # MUST be red, in the test you just wrote
cp /tmp/fix.orig <file> ; <run>  # MUST be green
```
Record each pair in the commit as `R<n> <what was restored> … N failed / M passed`. RC-4 is at R21.

**5.4 — A LABEL THAT PRINTS REGARDLESS IS NOT A CHECK.** `echo "(empty = free)"` after a `grep` prints whether or not the grep matched — I did that three times today. Before trusting any check, ask **what it would report if the thing you are looking for were present**. If the answer is "the same", it is not a check.

**5.5 — AN APP-BOOTING E2E IMPORTS EVERY MODULE (§2.164).** A peer lane's compile error, *or a migration that throws*, makes `test/*.e2e.test.ts` unrunnable **repo-wide** — and `tsc` is structurally blind to the migration case. Attribute with `tsc 2>&1 | grep -E "^src/modules/(your|dirs)/"` returning EMPTY rather than assuming.

## 6. The shared checkout — three lanes

Find current names with `ListAgents`; **message before colliding.** **`hmis-d9`** (18a radiology — closed a 33-finding review remediation at `4ffe5b9`) and **`hmis-63`** (VD vitals — VD-1 done, VD-2 unstarted, takes locale namespace `vitalsBay`). Both are responsive and **both caught real defects of mine today; all three of the day's defects were found by another lane's instrument, never the owning lane's.**

- **§2.152 AND ITS AMENDMENT:** `git commit -- <path>` commits the **working tree** for that path, so it captures a peer's uncommitted edits to any file you name. Check `git status` before every commit and name files, never directories.
- **Locale files are the guaranteed collision point:** `apps/web/src/locales/{en,hi}.json` (NOT `src/i18n/`), pinned key-for-key by `lib/i18n.test.ts`. This lane's namespace is **`registrationCounter`** (plus `opd.feeStatus.*` and `nav.counterSeat`). **A key-parity check is pure set arithmetic over two JSON files and needs no test runner and no box slot.**
- **Your test DB: `hmis_rc3_scratch`** (or mint `hmis_rc4_scratch`). Name it in every evidence-citing commit (§2.137).

## 7. Verify economy

**Web needs no box slot** — `pnpm --filter @hmis/web exec vitest run` is in-process, no postgres, **~30s for all 68 files**. Run the FULL suite, not a slice; RC-3's close cited stale numbers from five commits earlier and had to be corrected.

**Never a bare `pnpm verify`** (§2.151 — it OOMs the box). Core is `pnpm --filter @hmis/core exec jest -w 2 <paths>`, sequentially, box slot by message; `maxWorkers: 2` is already in `apps/core/jest.config.cjs` by owner ruling.

**`pnpm typecheck` AND `eslint` before every launch** — vitest strips types, so a green web suite can sit over code that does not compile.

## 8. Cost

**Stop-loss 1,280,000**, derived in the phase doc header from RC-3's *measured* per-task rate × 5 (§2.95 — per-task rate, never a previous phase's total), with a **543,000 review term** that is RC-3's actual two-pass-plus-remediation cost. **The review term is not a place to save:** on RC-3 it found 3 CRITICAL over thirteen dead mutants and a full green suite. **Record the session balance at kickoff and a delta at each task boundary (§2.141), and say so if it goes discontinuous on a context refresh — mine did.**

## 9. First actions

1. `git pull --rebase`, `git log --oneline -8`, `git status --porcelain` (**whose files are dirty?**), `ps -eo pid,cmd | grep -E "[j]est|[v]itest"` (**it must match `processChild` WORKERS, not only supervisors**), `uptime`.
2. `ListAgents` and message the peers.
3. **Re-measure before writing:** for T2, grep the server for what `join-queue` already does; for T4, read `COUNTER_SEQUENCES`/`TOKEN_LANES` and the `counter-flow` route. Three phases running, the answer has been "it is already built".
4. Full web `vitest run` to confirm the tree is green before you change it.
