# Ground rules for every agent in this workflow (read first)

## Where to read
- READ-ONLY checkout of `origin/main` @ `b04cbd9` (2026-09-06): `/tmp/claude-0/-opt-hmis/f3306fc1-230f-44e8-8e99-c5d128865528/scratchpad/main-ro` — use this for every repo file. `/opt/hmis` is 90 commits BEHIND; do not read repo docs from it (exception: the untracked Hermes brainstorm at `/opt/hmis/docs/superpowers/brainstorms/2026-09-01-hermes-ops-copilot/00-BRAINSTORM.md`).
- Key docs (paths relative to main-ro):
  - `docs/superpowers/2026-09-06-ROADMAP-v2.md` — the CURRENT roadmap (a proposal, merged as docs). §1 Q1–Q6 decided; §2 the 13-week sequence; §3 the seven gates G1–G7 (what "open" means); §4 IPD gate; §5 what the quarter does NOT build; §6 the owner's four rulings + two facts; §8 the S-gate.
  - `docs/superpowers/plans/2026-09-06-phase1-11i-the-stand-up-path.md` — Phase 11i (T0–T9), §2b 24 Indian-day edge rows.
  - `docs/superpowers/2026-09-06-HANDOFF-commissioning-lane-11i.md` — §10 has 76 more edge rows.
  - `docs/superpowers/brainstorms/2026-08-27-department-series/00-INDEX-AND-SYNTHESIS.md` — §3 the reconciled plan numbering (12a–c, 13, 14–19, Track C 20–31, IPD cluster 40–54, service lines 60–67), §4 themes, §5 kernel asks, §6 risks.
  - `docs/superpowers/brainstorms/2026-08-27-department-series/00-OWNER-RULINGS-REGISTER.md` — 262 rows R-001…R-262.
  - `docs/superpowers/brainstorms/2026-08-27-department-series/00-CROSS-MODULE-CHAOS.md` — cross-module failure scenarios.
  - `docs/superpowers/brainstorms/2026-08-27-department-series/NN-*.md` — 22 department brainstorms (75–116 KB each; read the sections you need with grep -n + sed -n, not whole files).
  - `docs/superpowers/specs/2026-08-10-hmis-architecture-design.md` — spec; §16 (line ~774) is the agentic layer (tiers T0–T4; clinical capped at T2–T3; roster of agents/automations; guardrails).
  - `docs/superpowers/specs/2026-08-25-clinical-copilot-design.md`, `docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md`, `docs/superpowers/specs/2026-08-11-hmis-staffing-kpi-design.md` (event-derived KPIs).
  - `docs/PROJECT-BRIEF-2026-08-30.md` (330 KB — grep, never read whole).
  - `docs/superpowers/EXECUTE-METHOD-V3.md` (§1 one doc per phase, §5A close, §8 topology), `docs/superpowers/AGENT-RULES.md`, `CLAUDE.md`, `tools/lane.sh`, `docs/runbooks/*.md` (four go-live runbooks), `docs/superpowers/2026-09-0*-HANDOFF-*.md` (lane handoffs).
- Read-only shell is fine (`grep`, `sed -n`, `git -C main-ro log/ls-tree/show`, `gh pr list/view`). NEVER edit any repo file, never run tests/builds, never touch `/opt/hmis-prod` or any `hmis-prod-*` container, never deploy.

## The position (RE-MEASURED 2026-09-06 ~19:00 UTC by the position-state reader — supersedes every earlier figure)
- **Production was deployed at 12:35 UTC today by a jest mutation run that executed deploy.sh** (memory `deploy-sh-executed-by-a-test`). It now runs images `hmis-prod/{server,web}:399f92c` — a LANE merge commit (`origin/lane/commissioning-t3`), NOT an ancestor of `main`: = `main@f211075` + 11 commissioning-lane commits (11i T0/T1/T2/T3/T4/T7/T8/T9). 78 migrations applied (`0000`–`0077`). `main` (`b04cbd9`; remote tip `8de8fc0`) has 80 migrations → **production is 2 migrations behind `main` (`0078`, `0079`) and carries 11 lane commits `main` lacks.** No backout image exists (the `c11833d` image was overwritten; T8's hand-tag step never ran). Two never-deployed modules are now LIVE and INERT: pharmacy (13 routes, empty shelf) and 18c's ionising gate (refuses every X-ray/CT against an empty `aerb_licences`). Runbook status lines saying "NOT DEPLOYED" are false. Every document dated 2026-09-06 that says "deployed base c11833d / 56 applied / 22 pending" describes the state BEFORE 12:35 UTC. Full evidence: `wf/understand/position-state.md` §0.
- Production has NEVER left `commissioning` mode, has ONE full administrator, ~500 events (no real patient in earnest). Four go-live runbooks exist on `main` (a fifth, radiology, merged as #140); NONE has an executed, dated section. The owner tests on lane-built demo stacks. `/opt/hmis-uat/.env` exists; no UAT container is running.
- ROADMAP v2's thesis stands and is now sharper: the constraint is ABSORBING (deploy → establish → load → staff → rehearse on UAT → pilot → hospital-wide exit from commissioning = G1…G7), and the one deploy the calendar scheduled for the owner's hand has already happened unrun and ungoverned. Phase 11i: T0/T1/T2/T4 merged (#113–#116); T3/T5/T6a/T7/T8/T9 open (#117–#122, #117 CONFLICTING); T6b (the S-gate execution on UAT) NOT RUN; §8 CLOSE empty; the `deploy-blocker` label EXISTS.
- 22 open PRs. Lanes: commissioning, front-desk (FD-26/27/28 = 17 commits with NO PR and no remote branch, 19 dirty files, a migration serial `0075` already taken on main), lims (8 stacked PRs #125→#137, train frozen), pharmacy-launch (#123→#128→#136), radiology (#133, #139, #142, #143 = 18a-iv phase doc FOR APPROVAL), roadmap (history), roadmap-v3 (this brainstorm). An ORCHESTRATOR session (`hmis-lanes-a2`; state under `/opt/hmis-lanes/.orchestrator/state/`) manages lanes and the merge train; its `deployed-base.txt` still says `c11833d`. There is no developer; every session is an AI agent.
- Plan numbering is FROZEN by the index §3; new work takes a letter inside an existing block (11i, 11j, 17-E, 18a-iii, 18a-iv) or a free number inside the right block. Never renumber.

## Owner standing rules (bind every proposal)
- Owner rulings are for MONEY, PROCUREMENT and LAW only. Everything else: pick the standard Indian-corporate-hospital answer, mark it DECIDED with one line of reasoning, keep going. Name the ruling with its register id (R-nnn) or runbook id (O1…O6) where one exists.
- Clinical agents cap at T2–T3 forever (draft; a human signs). Agents act only through permission-enforced APIs, fail-open, kill switch, draft provenance. Nothing with inference runs on production before DPIA v0.2 (counsel).
- Evidence over assertion: cite file:line or PR/commit for every claim about what exists. Distinguish authored / merged / deployed / open (G-gates). A green test suite is not evidence a department can be stood up.
- A "parameter that confirms the goal" must be MEASURABLE from something the system records (an event, a census row, a drill log, a KPI the registry can compute, a dated runbook section) or from a fact a human can read in one query. Vague targets ("better UX") are not parameters.
- Edge cases are written the way 11i §2b writes them: the Indian day it must survive — one row = scenario, the behaviour expected, the artefact that proves it.

## Output discipline
- Write your FULL notes to the file path your prompt names (markdown, ≤ 30 KB, with file:line citations). Return the structured summary via the StructuredOutput tool. The file is what downstream agents read; the summary is the index.

## NIGHT UPDATE (2026-09-07 ~03:00 UTC, from the orchestrator hmis-lanes-a2 + re-measured on origin/main 3f596e4)
- `origin/main` = `3f596e4`, green; 43 PRs landed overnight. Merged that matter to the roadmap: **#150 Phase 20 (workforce & roster) phase doc AUTHORED, FOR APPROVAL** (`docs/superpowers/plans/2026-09-06-phase1-20-workforce-roster.md`); **#154 17-E T7b** (`interface_down` by heartbeat lapse) — Q5 fully executed; **#148 18a-iii CLOSE** (both passes, T1–T5); **#144 `import:item-master`** (materials; the hospital's own file, never an invented row) — the first spreadsheet loader of ROADMAP v2 §2 wk 4–6; **#146 "bring 11i T8's backout path onto current main (rebuild of #117)"**; #149 drizzle snapshot-chain repair; #143 (18a-iv phase doc) merged as a DOC ONLY — the orchestrator HOLDS 18a-iv code until the owner rules; #157 (open) adds `docs/runbooks/ot-go-live.md` + an OT census row; #158 (open) `ALL_MANIFESTS` census population; #159 pcpndt read route; #160 membership boot-time warn.
- The orchestrator closed the "establish-once" family structurally: a seed CAN perform a Class C activation and CANNOT perform Class A, so every Class A activation fell to "a human" whose runbook may not exist — seven departments were uncommissionable for that one cause.
- HELD by the orchestrator, deliberately, pending the owner's explicit line: #117–#122 (the 11i stack; landing it ratifies the 12:35 accidental deploy), #73 (never merge), 11j (not dispatched: section numbering is not an instruction to execute), 18a-iv code. Plan 20 BUILD also waits on the owner's approval line (authoring is done).
- `deployed-base.txt` corrected to `399f92c` by the orchestrator. Production is unchanged since 12:35 UTC 09-06.
- Front-desk lane: tree CLEAN, 5 commits today, two migration serials (0074/0075) taken on main and must regenerate (0080 went to LIMS T7b); NO remote branch, NO PR; the lane will push only on the owner's ask. A defect it reported to the owner twice as "live in production" (unbillable repeat lab test) is only on its branch — a pre-landing blocker, not an outage. This is the class of prose fact a measured STATUS exists to kill.
- Lesson from the orchestrator tonight: a runbook pointed at "T9's runbook", which does not exist, and stranded a module on that pointer. Any document a roadmap item depends on must be checked to exist before it is named.

## NIGHT UPDATE 2 (2026-09-07 ~04:00 UTC, from the orchestrator, verified on origin/main)
- Two authoring rules v3 MUST apply to every item it names: (1) when an item names a SEAM, cite the function SIGNATURE that carries the property that matters, not the HTTP entry point — e.g. `upsertOrderable(exec: Db | Tx, …)` in `modules/lab` states "one transaction for the whole file" is possible; `@Controller("lab/catalogue")` is true about the wrong object (one request = one transaction). A survey claim relayed by the orchestrator became an instruction to a lane; only an explicit "refute this" saved it. (2) For every item that names NEW work, first name what ALREADY occupies that name (e.g. `apps/core/scripts/seed-lab-catalogue.ts` — a JSON fixture seed from #127 — coexists with the operator loader `import:lab-catalogue`, as `seed:lab-demo` does with `import:item-master`); the critic must sweep v3's items for name collisions with existing scripts/routes/plans.
- OPENED as PRs since the last update (both OPEN at 04:00 UTC, not merged): #164 (mini-OT seam defects: the resequence client/server shape and the missing `list.resequenced` event — REAL and fixed; the "absconded recall task" was correctly REFUTED: there is no task primitive in the kernel, so honouring the comment would mean inventing a subsystem — the comment is the defect); #162 (formulary interaction-pairs census row, placed on the `hospital` census not `pharmacy`, because the consumer is `opd/rx-checks.ts` — prescribing, not dispensing); `import:lab-catalogue` is in flight with the LIMS lane (one `withTx`, the controller's zod schemas reused, no HTTP).

## NIGHT UPDATE 3 — CURRENT FIGURES, measured 2026-09-08 11:32 UTC (OVERRIDE for every later stage)
- **OVERRIDE:** wherever your prompt says "measured at b04cbd9 on 2026-09-06", write **"measured at `91c34fc` on 2026-09-08 11:32 UTC"** and use the figures in this section. `main-ro` may still be checked out at an older tip; run `git -C <main-ro> fetch origin && git -C <main-ro> log --oneline -1 origin/main` and read files with `git show origin/main:<path>` when a path you need is newer than the checkout.
- `origin/main` = `91c34fc`; **82 migrations** on main (`0000`–`0081`; newest `0080_lab_instrument_interface`, `0081_lab_catalogue_imports`). Production still runs `hmis-prod/server:399f92c` (built 2026-09-06 12:35 UTC; 78 migrations) → **4 pending** (`0078`–`0081`). No deploy since the accidental one. `.orchestrator/state/deployed-base.txt` = `399f92c` (corrected). Lane heartbeats under `.orchestrator/state/*.status` are unchanged since 09-05/09-06 — stale by two days, and nothing shows their age.
- **Open PRs: only #118–#122 (11i T9/T7/T3/T5/T6a, still FROZEN pending the owner's ruling on the accidental deploy) and #73 (never merge).** #117 is no longer open (its content landed as #146 "rebuild of #117"). Everything else drained.
- **Merged 09-07/09-08 (#151–#170), the ones that matter:** #168 `import:lab-catalogue` (the second spreadsheet loader; ONE `withTx`; migration `0081`; template at `docs/runbooks/lab-catalogue-template.md`); #165 the spreadsheet-loader design note; #163 the item loader applies the whole file in ONE transaction; #164 mini-OT seam fixes (resequence event + client/server shape); #162 formulary interaction-pairs census row on `hospital`; #158 `ALL_MANIFESTS` census population with compulsory classification; #157 OT runbook + five census rows; #155 OPD definition seeded; #159 pcpndt read route; #160 membership boot-time warn; #156 "census of commissioning acts that exist only in a `beforeEach`" (the establish-once family, closed); #161/#167 CI job timeouts; #170 main went red because the calendar moved (a date-dependent test).
- **Runbooks on main now: NINE** (`lab-catalogue-template`, `lab`, `opd`, `ot`, `pcpndt`, `pharmacy`, `radiation-safety`, `radiology`, `radiology-pacs`). **Only pharmacy's has an `## Executed` section and it reads NOT YET RUN. Zero runbooks have been executed on any environment.** UAT is still not up (the freeze).
- Lanes now: commissioning `lane/commissioning-t3`; lims `lane/commissioning-t7` (LIMS took the T7 runbook rebase); pharmacy-launch `lane/ci-timeout-resize`; radiology `lane/formf-backfill-clock`; front-desk `lane/front-desk-fd25` (+70, local only, no PR); roadmap-v3 (this brainstorm, docs only). `/opt/hmis` was fast-forwarded to 32 behind.
- Nothing from the OWNER has arrived: no ruling on the deploy, no names, no catalogue file, no certificates, no GST/CA line, no DPIA. Every Track S row's "next act" is still his.

## NIGHT UPDATE 4 — CURRENT FIGURES, measured 2026-09-21 20:45 UTC (FINAL OVERRIDE: supersedes updates 1–3)
- **OVERRIDE:** the document header must read **"measured at `035af769` on 2026-09-21 20:45 UTC"**. Fifteen days passed since the readers ran; every position fact in `wf/understand/position-state.md` and in the pillar drafts is now HISTORY. Re-measure any fact you state with `git -C <main-ro> fetch origin` + `git show origin/main:<path>` / `gh pr list`; cite the measurement, not the draft.
- `origin/main` = `035af769`, **119 migrations** on main. **The 11i stack (#117–#122) LANDED; the catch-up deploy was EXECUTED on 2026-09-14** (`docs/runbooks/catch-up-deploy-2026-09.md` §11 "Executed — 2026-09-14"), and production was deployed again on 09-19 and 09-20 (images `hmis-prod/server:b3aaf712`, `:3e6c0e9d`, `:61906e51` built 2026-09-20 05:57 UTC — the SHA tags T8 introduced are live and the backout image now exists). So ROADMAP v2 §2's week-1 and week-2 rows are entirely landed/overtaken; G-B ("production tracks main weekly, with a way back") has its first evidence.
- **Still NOT executed on UAT:** `lab-go-live.md` §14 and `pharmacy-go-live.md` §6 both read "NOT YET RUN" on main — the S-gate for the lab has still never happened; no other runbook has an Executed section. Whether UAT containers run today is unmeasured here.
- **Plan 20 (workforce & roster) is IN EXECUTION**: roster phase R PRs #273–#286 merged 09-21 (departments/duty positions, versions, units, leave, on-duty scoping, "the duty manager at 02:14 is whoever is ON", calendars, requirements/validator/simulate, the proposer + monthly draft R9); #287 (close phase R, two review passes) and #264 (T1 roster periods) OPEN; docs #263 (brainstorm, owner rulings, phase doc 20-U, boards, stress test). Pillar 1's E1 ("one named human") milestones must be re-stated against THIS, not against "#150 FOR APPROVAL".
- **New since the readers:** the "obligation spine" phase O (docs #266/#270/#281: review, every seat's census, edge cases, phase plan; T2 #267 approvals tell every holder); alerts "seen/owned/handed over" + deep links (#282); workflow rung at 70 % of budget + a clock for silence (#283); notify reach profiles + Web Push + a ladder that climbs on silence (#284); OPD day report on the letterhead PDF/CSV (#257/#262); pharmacy desk PD-series (#254 prescriber authorises what the check would refuse, migration 0107; #255/#259/#260/#261); two IST-midnight clock bombs fixed (#256/#271); approvals routed to a queue no role could open (#265); the owner's inbox in plain words (#258); the OPD unpaid-token door (#269); vitals emergency charted before billed (#268).
- Open PRs: #287, #264 (roster), #202 (pharmacy-formulary handoff), #196 (staff-money), #189 (desk-upcoming). 25 lane worktrees exist; a cleanup session is dropping dead ones tonight; `lane/roadmap-v3` is this brainstorm's and is being committed now.
- The owner's rulings that were open on 09-06 (deploy ratification, GST on MRP, O1, O6, names, catalogue, certificates, DPIA, second server): NOT re-measured here — the critic must check the rulings register / phase docs on main before listing any as still open.

## NIGHT UPDATE 5 (2026-09-21 21:00 UTC, orchestrator, verified by content on 035af769) — BINDING for the mechanism and the synthesis
- **The census instrument already exists and is the spine: `apps/core/scripts/standup-check.ts` ("PHASE 11i T2, THE READINESS CENSUS")** — a declared table of rows with three verdicts `ok` / `RED` / `NOT MODELLED`; every row cites a runbook file and section and its `fix` string is the runbook's own sentence (e.g. "§6: create one `resources` row of kind `bench` per real bench"); RED is the only failing verdict; NOT MODELLED is a recorded fact about the schema, never printed green. PR #287 extends it with roster rows. **v3's STATUS mechanism MUST be rows in / around this census, never a parallel instrument:** (a) G1–G4 stay the census's rows; (b) G5–G7 and the platform facts the DB cannot see (origin/main tip, open PRs + labels, docker SHA tags on the box, the drill log, dated `## N. Executed` runbook sections, lane worktrees + heartbeat age) are gathered by ONE thin wrapper that runs the census and appends those rows in the SAME row grammar (module · gate · code · verdict · fix · evidence), then writes `docs/STATUS.md` with a `measured-at <sha> <utc>` header. Name the wrapper (`tools/status.sh` or `pnpm status`), say which rows are census rows and which are wrapper rows, and never describe "a second census".
- Verified by content on main: `lab-go-live.md:499` "## 14. Executed on UAT — NOT YET RUN"; `pharmacy-go-live.md:318` "## 6. Executed on UAT — NOT YET RUN"; `catch-up-deploy-2026-09.md:448` "## 11. Executed — 2026-09-14". Backout image confirmed on the box: three SHA tags (b3aaf712 09-19 21:28; 3e6c0e9d 09-19 23:08; 61906e51 09-20 05:57 = :latest) — TWO deploys on 09-19, one on 09-20; `HMIS_DEPLOY_ROLLBACK_TO` has a real target.
- Lanes reaped today: front-desk, lims, oblig-t1, oblig-t4, roster-r7, roster-r8, slip-capture; roster-r10 appeared. **Any sequence row that names a lane must derive it from content, never from a name** — several names no longer exist. Open PRs: #287 BLOCKED, #264 draft, #202/#196 BEHIND, #189 UNSTABLE.
- The landing freeze on the roadmap-v3 PR is the orchestrator's convention, not a machine gate; branch protection evaluates the PR's own head. Docs-only PRs still run full CI (~15 min per push).

## NIGHT UPDATE 6 (2026-09-21 21:10 UTC, orchestrator; verified by grep on 035af769) — four facts inside `apps/core/scripts/standup-check.ts` that shape the wrapper
1. **The seam exists:** `runCensus(db, moduleName): Promise<RowResult[]>`, `censusLines(results)` and `anyRed(results)` are exported. The wrapper IMPORTS `runCensus` and gets structured rows — no screen-scraping.
2. **The row type is closed:** `export type Gate = "G1" | "G2" | "G3" | "G4"`; `RowResult = { module; gate: Gate; code; verdict; fix; detail? }` — no `evidence` field, and `detail?` carries a loader's own throw text (do not overload it). **Widen in the WRAPPER's file, never in the census:** `type BoardGate = Gate | "G5" | "G6" | "G7"`; `type BoardRow = Omit<RowResult, "gate"> & { gate: BoardGate; evidence: string }`. The synthesis must not propose editing `standup-check.ts`'s types (PR #287 is +55/−2 on that file; it adds rows and touches neither type).
3. **Cite runbook sections by TITLE TEXT, never by number** — the census's own rule on `NotModelledRow.runbook` (D15): `lab-go-live.md` carried two sections numbered 11 and a renumber went stale in the same phase. So the G5 row matches `Executed on UAT` (lab, pharmacy) and `Executed — ` (catch-up runbook) as text, and reads "NOT YET RUN" vs a date from the same line; "§14 / §6 / §11" are worth nothing next phase.
4. **The exit code is a decision:** `anyRed` drives `process.exitCode = 1`; while the S-gate is open the census is red by construction, so a scheduled STATUS regeneration that inherits it reads as a broken build forever. DECIDE and state in the header: the board GENERATES with exit 0 (a board is a measurement, not a gate); the gate is a separate invocation (`--gate <module>`) that fails on RED — the deploy uses the gate, the orchestrator loop uses the board.
5. **The header must name the DATABASE the census read, beside the main SHA and the UTC time** — a census against a lane's test DB is a different hospital from production; contaminated test DBs are a live problem on the board. `measured-at <sha> <utc> db=<host/dbname identity, never the password>`; a board without the DSN identity is a timestamp, not evidence. `docs/STATUS.md` is free on main.

## NIGHT UPDATE 7 (2026-09-21 21:20 UTC, orchestrator; verified) — the gate already exists; the wrapper is BOARD-ONLY
- `apps/core/package.json` declares `"standup:check": "tsx scripts/standup-check.ts"`; `main()` parses `process.argv[2] ?? "all"` (no flag parser — a `--gate` argument would be read as a module name and throw) and exits 1 on RED via `anyRed`. It is the deploy gate TODAY: seven runbook call sites (`standup:check lab|front-desk|ot|pcpndt|pharmacy`) and the deploy runbook runs `node dist/scripts/standup-check.js all`.
- Therefore: **no `--gate` anywhere.** The gate IS `standup:check <module>` (G1–G4, "can the hospital open this department on Monday"), untouched, one implementation of the verdict. The wrapper (`tools/status.sh` / `pnpm status`) is BOARD-ONLY: it calls `runCensus` per module for G1–G4 rows, appends G5–G7 and the platform/development rows (main tip, PRs + labels, docker SHA tags, drill log, dated Executed titles, lane worktrees + heartbeat age), writes `docs/STATUS.md`, exits 0. **G5–G7 and the board facts are facts about US, never consulted by a deploy** — a deploy must never block because a PR is open or a worktree went stale.
