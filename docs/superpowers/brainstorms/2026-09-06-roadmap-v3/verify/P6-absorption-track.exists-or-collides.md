# P6-absorption-track — verify: EXISTS-OR-COLLIDES (re-run 2026-09-08)

Target: `wf/design/P6-absorption-track.md` (46 KB, written 2026-09-06 21:40 UTC against `b04cbd9`). **Measured at `origin/main` = `41a4be4`, 2026-09-08 ~12:05 UTC** (= `91c34fc` + #118, merged 11:59:29Z; `main-ro` checkout is `ff07cbc`, 17 behind). Method: every file:line in §0–§5 opened at the ref the design cites (`b04cbd9`, or `fbe8fc4` where it says "origin/main") — **137 citations, 132 hold, 5 off by a few lines** (§4); every "Exists"/"Build" name grepped on `origin/main` (`package.json` scripts, `apps/core/scripts/`, `standup-check.ts` row codes, `deploy.sh`, `docs/runbooks/`, `tools/`); `gh pr list --state all --limit 120`; the index §3; RM §7; the five sibling designs and their verifiers; `/opt/hmis-lanes/.orchestrator/{bin,state}` (read-only). This supersedes the 2026-09-06 22:20 run of the same lens: the repo moved 30+ merges under the design and four of its milestones are now on `main`.

**Verdict: needs-amendment.** Four milestones presented as builds are merged (K1–K4). No plan number collides with the index §3 (the 11 series is outside it; `11l`/`11m` are free on the repo — `git grep -wE '11[lm]' origin/main -- docs` finds only "11m 20s" in a design HTML). But three homes sit under CLOSED numbers without a letter, two capabilities are homed in two pillars, and the whole E1/G-B "Exists" is a day stale (T8 and T9 are on `main`).

---

## 1. KILL — the item must go or move

### K1 · E2-M1 "Lab catalogue loader dry-runs the owner's file on UAT · 3 d" — MERGED as `import:lab-catalogue` (#168)
- `origin/main` `18f358e` "feat(lab): import:lab-catalogue — the owner's own catalogue, and not one row this loader invented (#168)", merged 2026-09-07T08:10:41Z. `apps/core/package.json:43` `"import:lab-catalogue": "tsx scripts/import-lab-catalogue.ts"`; migration `apps/core/drizzle/0081_lab_catalogue_imports.sql`; the owner's template `docs/runbooks/lab-catalogue-template.md:1-5` ("This is the template `import:lab-catalogue` reads … never invents a value").
- What it already does (`scripts/import-lab-catalogue.ts:17-56`): three CSVs (analytes, orderables, ranges), any subset; **one transaction for the whole import** (:40-41); dry run without `--apply` (:17); writes through `upsertAnalyte`/`upsertOrderable`/`putReferenceRange` so `foetal_sex_refused`, the formula parser and the range-overlap guard apply (:48-51); **not in `deploy.sh`, not in `SEED_STEP_SCRIPTS`** (:44-47); coexists with `seed:lab-catalogue` (the JSON fixture) exactly as the design's E2 "Exists" describes — but the design lists that coexistence as the *precedent* and then builds the thing that already landed.
- Design E2-M1 acceptance: "`load:lab-catalogue --dry-run` row report · 0 uncaught throws, every rejection carries a module refusal code". The name `load:lab-catalogue` collides with the shipped `import:lab-catalogue`; the flag `--dry-run` collides with the shipped default-dry-run + `--apply` (A10).
- **Fix:** E2-M1 becomes "adopt #168; the remaining act is the dry run on UAT with the owner's file (F3)" — 0 d build, one human act. Its census check (`lab_orderables_priced`, `lab_range_sources_present` ok after the real run) stays. Note the loader note §7 (`docs/superpowers/specs/2026-09-07-spreadsheet-loader-design.md:123-136`): prices stay DEV-PLACEHOLDER-flagged until O6 — E2-M1's "priced" row is green only in that flagged sense until E8-M1.

### K2 · E2-M2 "Pharmacy item loader · 3 d" — the drug-item half is MERGED as `import:item-master` (#144, #163, #165)
- #144 `8749e0a` merged 2026-09-06T22:11:44Z (31 minutes after the design's timestamp), `package.json:35`; #163 `5d5aeea` (2026-09-07) made it one transaction; #165 `b528a8d` wrote the design note it now follows. `scripts/import-item-master.ts:10,39` — dry run by default, `--apply` writes; refuses the whole file on any bad row; "deliberately not in `deploy.sh`" on the `import:holder-book` precedent.
- **Residual that is real:** sale registration and the NPPA ceiling — `grep -ciE 'registerSaleItem|ceiling|nppa|saleItem' scripts/import-item-master.ts` on `origin/main` = **0**. PH §2 (`origin/main` `docs/runbooks/pharmacy-go-live.md:93-149`, "FOUR people, not one") still has those as human steps. The census row `pharmacy_items_priced_within_ceiling` is still absent from SC (0 hits) — that part of E2-M2 is new.
- **Fix:** E2-M2 = "adopt #144/#163; build the sale-registration + ceiling step on the same file shape, and the ceiling row" — size it as the residual (≈1 d), name it `import:*`.

### K3 · E3-M1 "Front-desk runbook exists and the census sees it · 2 d" — the runbook and the row set EXIST as `opd-go-live.md` + `"front-desk"` (#155)
- `docs/runbooks/opd-go-live.md` — `ff07cbc` "fix(opd): §OPD-UNSEEDED — the definition every patient needs, checked and written down (#155)", merged 2026-09-07T03:31:46Z. Title: "OPD go-live runbook — **the front desk**, and the definition nobody wrote down" (:1); its command is `standup:check front-desk` (:29, :123). `apps/core/test/standup-check.test.ts:70-73` (#158): `opd: "front-desk"` — "the census's name wins". `standup-check.ts:237-294` on `origin/main` is the `"front-desk"` row set: `opd_config_present`, `opd_visit_definition_active`, `active_doctor_in_a_department`, `cashier_held`, `front_office_held` — **five rows**, so the design's acceptance "`front_desk` rows ≥ 4 in SC" is already true (under a different key).
- Design E3-M1 builds `docs/runbooks/front-desk-go-live.md` and asserts "the mapping test green with 6 files". The mapping is one runbook per manifest key; a second front-desk file has no key to map to, and there are **nine** runbook files on `origin/main` already, not six.
- **Residual that is real:** `opd-go-live.md` §6 (:174-186) explicitly does NOT cover "appointments, the queue and the display board"; it has no six-screen walk, no panel/TPA line, no test-print step, no `## Executed` section (grep = 0), and does not fold README:62/:361/:645 (0 mentions of README). Those are E3-M1's real content.
- **Fix:** E3-M1 = "extend `opd-go-live.md` (the six-screen walk, the panel/TPA line, one test print per device, the three README runbooks folded) and add the missing `front-desk` rows" — keep the key `front-desk`, drop the new filename, fix "6 files" to the measured count. DECIDED 4 ("the front-desk runbook moves from README to `docs/runbooks/`") is half-done by #155 and should say so.

### K4 · E6-M1's OT third — `ot-go-live.md` + six `ot_*` census rows are MERGED (#157); and the OT stand-up still has three homes across the brainstorm
- `docs/runbooks/ot-go-live.md` — `b2baade` "fix(ot): §OT-UNROSTERED — the page `seed-ot.ts` has been pointing at, and five census rows (#157)", merged 2026-09-07T04:02:44Z; sections §0–§9 (:15-173), incl. §2 the Class-A ceremony twice, §3 publish the three seeded drafts, §4 draft *and* publish `privileges`. `standup-check.ts:458-523` (`origin/main`): `ot_approval_types_registered`, `ot_theatre_present`, `ot_workflow_definitions_active`, **`ot_definitions_published`** (:510-518 — reads `activeDefinitionRow` per kind, i.e. the active row, never the seed's return: the design's edge #17 mutant is already the shipped check), `ot_surgeon_held`, **`ot_anaesthetist_held`** (:522, the design's `anaesthetist_role_held` under another name).
- Design E6-M1 "Build: census rows — OT `ot_definitions_published` … `anaesthetist_role_held`; three runbooks (OT's from README:1354 + Plan 15 T9 :387-392)". Two of three OT rows exist by name or function; the OT runbook exists. **Residual:** `daycare_package_services_priced` (0 hits in SC) and E6-M2's UAT walk.
- **Three homes, all overtaken:** P3-E6 (`P3-diagnostic-surgical.md:40,91-93`, home Plan 15, E6-M1 "`docs/runbooks/ot-go-live.md` on main … `standup:check ot`", E6-M4 production pilot wk 10–13); P5-E1-M3/M4/M5 (`P5-discharge-audit.md:76-78`, home `15e`, "`docs/runbooks/mini-ot-go-live.md`", "module keys 4 → 5", production pilot); P6-E6 (11m, no production this quarter, DECIDED 6). The P3 verifier (its K1, :15-18) and the P5 verifier (:19-30) both found #157 under their pillar; neither routes the residual to P6 any more.
- **Fix:** E6-M1's OT third = "adopt #157; add `daycare_package_services_priced`". Then ONE home for the residual UAT ceremony (E6-M2), stated in §5 DECIDED with the two sibling ids it absorbs, and a line on whether DECIDED 6 (no production pilot this quarter) overrides P3-E6-M4/P5-E1-M5 or yields to them. The `mini-ot-go-live.md` filename in P5 must die — the key is `ot` and the file exists.

---

## 2. AMEND — fix the citation, the home, or the stale fact

### A1 · E1 "Exists" and goal G-B are a day stale: T8 and T9 are on `main`
- #146 "merge: bring 11i T8's backout path onto current main (rebuild of #117)" merged 2026-09-07T00:39:04Z (`374ea1d`); **#117 itself merged 2026-09-08T11:24:59Z (`91c34fc`)**; **#118 (T9 redirects) merged 2026-09-08T11:59:29Z (`41a4be4`)**. On `origin/main` `docker/prod/deploy.sh`: `HMIS_DEPLOY_ROLLBACK_TO` (:49, :56, :232, :250), SHA tags on every image (:247-249), the outgoing configs snapshotted to `$DEPLOY_DIR/previous` (:275); `kernel/retention/events.ts:143` `backup.drill_rehearsed` (the design's G-B says "`main` has only `drill_passed/failed`, :120-121" — false since 09-07); `deploy-parity.test.ts:546-613` pins the backout path and the rehearsal seed list.
- Design: "T3/T5/T6a/T7/T8/T9 open (#117 DIRTY, #118–#122 CLEAN)"; "none on `main` (DS grep for those four names: empty)"; §0 "12 open PRs". **Now open: #119 (T7 runbook), #120 (T3 UAT target), #121 (T5 synthetic door), #122 (T6 lab runbook) and #73 — five.** `HMIS_TARGET` is still absent from `main` (0 hits) — that part holds.
- **Fix:** E1-M1 = "the train lands #119–#122" (E1-M1's "rebase #117 onto the squash twins" is done, cite #146/#117); G-B's instrument reads `drill_rehearsed` from `main`; §0's position line is rewritten per the CONTEXT override ("measured at `91c34fc`/`41a4be4` on 2026-09-08").

### A2 · Three epics are homed under CLOSED numbers with no letter — and HO:173 already homes two of them in 11k
- E4 "Plan home: 16c (existing)" — 16c CLOSED 2026-09-04 (memory; PRs #53/#62/#65/#66/#72). E5 "18c + 18a-iii (existing)" — 18c both close passes run; **18a-iii CLOSE merged as #148** (2026-09-07). E8 "11f (existing)" — 11f shipped and live 2026-08-25. The repo's own practice for new work under a closed number is a letter (`18a-iii`, `18a-iv`, `17-E`, `07a–07d`), which CONTEXT:24 states as the rule ("new work takes a letter inside an existing block").
- `docs/superpowers/2026-09-06-HANDOFF-commissioning-lane-11i.md:173`: "The next handoff: **11k** (the back-book and catalogue loaders, **the pharmacy opening on UAT, the radiology census walk**)"; HO:269 (F3, "the pharmacy seat drill (11k)"); HO:313 (the catalogue loader carried to 11k). The design homes E4-M1 (pharmacy G5 on UAT) at 16c and E5-M2 (radiology G5 on UAT) at 18c/18a-iii — **two homes each**.
- **Fix:** either E4-M1 and E5-M2 move under 11k as the handoff wrote it, or E2/E4/E5 say in one line that 11k is split (loaders / pharmacy UAT / radiology UAT) and give E4/E5/E8 letters (`16c-ii`, `18c-ii` or `18a-v`, `11f-ii`) or fold them into 11k/11m. E8 could also live in 11m (the "operability" series is where `validate:config` was born, 11c).

### A3 · E5-M1 (`seed-radiology` joins `deploy.sh`) has a second home in P3-E1 (18a-iv), and the repo points at that lane
- `P3-diagnostic-surgical.md:52` E1-M2 "`seed:radiology` in `deploy.sh` … `radiology_study_types_active` ok with no manual seed"; P3 D15 (:150) "`deploy.sh` runs `seed:radiology`"; P3 §7 wk 1–2 (:199). P6 E5-M1 + DECIDED 5 say the same. `standup-check.ts:627-632` on `origin/main`: "`deploy.sh` DOES NOT RUN IT … **Fixing it belongs to the radiology lane's own stand-up**, not inside the lab's phase". Still genuinely open: the seed list at `deploy.sh:574-612` runs ops/opd/patients/billing/tariff/membership/formulary/materials/ot/pharmacy/lab — no radiology (P3's verifier N11 agrees).
- **Fix:** one home. If P6 keeps it (it edits `deploy.sh` and `deploy-parity.test.ts:354-381`'s seed census, which is S's file), say so and have P3-E1-M2 depend on P6-E5-M1; else drop E5-M1 and depend on P3.

### A4 · `11l` is claimed twice inside the brainstorm (P5-E2 and P6-E7); `11m` chains on it
- `P5-discharge-audit.md:39-40` "Plan home: `11l` — … 11j and 11k are taken; `11l` free (grep)"; D7 (:162) "Letters `15e` and `11l`". P6-E7 = 11l; P6-E6 = "11m (next free letter after 11k/11l)". Free on the repo (`git grep -wE '11[lm]' origin/main -- docs` → only `docs/design/…/WaitAnswer.dc.html:124` "11m 20s" and its bundle :1067 — a duration, not a plan). The P5 verifier's K (:9-15) proposed P5-E2 → `11n` or a fold into 12a.
- **Fix:** P6 keeps 11l (its content is the session layer the 11 series owns) and records the resolution in §5 DECIDED so 11m does not silently depend on P5 moving. Also E6's pointer "RM §7 :461-470" for 11k/11l is wrong: RM §7 names only 11i and 11j; 11k's source is `11i:411` and `HO:173`.

### A5 · E6-M1's materials/membership rows collide with a recorded decision in the census test (#158)
- `apps/core/test/standup-check.test.ts:88-121` (`origin/main`, #158 merged 2026-09-07T06:22Z): `NOT_DEPARTMENTS` carries `materials: "OPEN — vendors, items and opening stock are master data no seed supplies"` and `membership: "OPEN — the holder book is loaded from the owner's own files (Plan 09 DD3)"`, with the rule "moving one to `DEPARTMENTS` turns this suite red until somebody writes both halves — and it belongs to the **board**, not to the lane that happened to be here. This map records today's answer (they are not)". `pcpndt` was ruled a department the same day (:78-85); `aerb` ruled not.
- Design E6-M1: "`standup:check all` prints `ot.*`, `materials.*`, `membership.*` rows ≥ 3 each · 8 runbook files under `docs/runbooks/`, the mapping test green". As written, the acceptance cannot go green without editing the classification map — which the test reserves for a board decision the design does not name.
- **Fix:** E6 states it TAKES that call (it is exactly the "standard Indian-corporate-hospital answer, mark DECIDED" class — not money/procurement/law), lists the two map moves in its build, and corrects the counts: **nine** runbook files exist now (`lab-catalogue-template`, `lab`, `opd`, `ot`, `pcpndt`, `pharmacy`, `radiation-safety`, `radiology`, `radiology-pacs`); with materials + membership = eleven (the template is not a go-live file — the mapping test's glob is `*-go-live.md`). E3-M1's "6 files" → the same correction.

### A6 · E5 omits PCPNDT — a department with a runbook, five rows and a statutory gate radiology cannot open without
- `docs/runbooks/pcpndt-go-live.md` (#159 `5957c41`, "The seventh go-live runbook … a hospital could deploy this system, scan patients, and hold no PCPNDT register at all"); `standup-check.ts:544-594` `pcpndt`: `pcpndt_registration_active`, `pcpndt_machine_registered`, `pcpndt_person_registered`, `pcpndt_incharge_held`, `pcpndt_certificate_displayed`. The design's RULINGS row "L2 / R-256 / R-237 … the real PCPNDT §19 registration" names the law but E5's "three runbooks (RS, PACS, RAD)" and its gates never name the instrument that measures it.
- **Fix:** E5 "Exists" cites the pcpndt row set + runbook; E5-M2/E5-M5 read "the four runbooks"; E5-M4's G3 includes `pcpndt_registration_active` ok (real, not DEMO) before an ultrasound study is acquired.

### A7 · Every pharmacy-runbook citation is against `b04cbd9`; #128 (merged) rewrote it, and the design half-knows
- Design: "PH:37-52 master data", "PH:54-90 seat drill", "PH:92-106 eleven named refusals", "PH:35 precondition 5", "PH §1.5", E4-M1 "11 refusals each seen once". `origin/main` PH: §1 preconditions :61-92 with **§1.9 the GST hard stop** (:84-91 "the taxable base for a pharmacy line is the printed MRP, which is tax-inclusive by statute … Owner ruling R-2 answered *which slab*; it never asked inclusive-versus-exclusive"); §2 master data :93-149; §3 drill :150-201; **§4 "all 33 codes"** :202-240; **§6 "Executed on UAT — NOT YET RUN"** :257 already scaffolded; §7 rollback :293.
- Two instrument consequences: (i) E4-M1's "11 refusals" → the 33-code table or a named subset; (ii) G-A's instrument `grep -l '^## Executed' docs/runbooks/*-go-live.md` will **never match PH's heading** (`## 6. Executed on UAT — …`, numbered) even after it is filled — anchor the grep on `Executed on` and require a date.
- **Fix:** re-cite at `origin/main`; "PH §1.5" → §1.9; NEW-P6-2 cites §1.9 as the existing statement of the question (it is; the census row `pharmacy_gst_treatment_ruled` is still new — 0 hits).

### A8 · `priceForBatch` is misattributed
- Design E4: "`priceForBatch` adds CGST/SGST on top of the base (`tariff/pricing.ts:101-106`)". `apps/core/src/modules/tariff/pricing.ts` has no `priceForBatch` (grep :1-110 = 0); :101-106 is `priceLine`'s return. `priceForBatch` is `apps/core/src/modules/pharmacy/price.ts:55`, exported at `pharmacy/index.ts:31`. The diagnosis (GST added on a tax-inclusive MRP unless the category is exempt) is right and PH §1.9 says the same; the function name is wrong.

### A9 · `file-demo.sh` is not in any git ref
- `git grep file-demo origin/main` → prose only: `docs/superpowers/2026-09-05-HANDOFF-radiology-lane.md:84` and `ROADMAP-v2.md:62` name `/opt/hmis-aerb-demo/file-demo.sh` (outside git); `11i:284` says its shape becomes `scripts/seed-aerb-demo.ts` (#121, open). E5 "Exists" and E5-M2's instrument → `seed:aerb-demo` (#121).

### A10 · Loader grammar and naming: the repo has decided, three times
- Design DECIDED 8 / E2: `load:*`, `--dry-run`. HO:313-316: `load:lab-catalogue`, "dry-run by default … `--commit` writes". Repo: `import:holder-book`, `import:item-master`, `import:lab-catalogue` (`package.json:34,35,43`); **dry run by default, `--apply` writes** (`import-item-master.ts:10,39`; `import-lab-catalogue.ts:17`; loader note §2 :50-56). `remint-uhids.ts:8` uses `DRY_RUN=1` — a fourth grammar, the one commissioning script that touches `legacy_uhid` (:31-63) and the nearest precedent for E2-M3's back-book loader.
- **Fix:** DECIDED 8 adopts `import:*` + `--apply`; E2-M3 is `import:back-book`, cites `remint-uhids.ts` and the loader note's open item 4 (provenance, note :147) as what it must add.

### A11 · `cashier_held` / `front_office_held` are cited as hospital rows; they moved
- Design E3 "Hospital rows `second_administrator` (SC:173), `cashier_held`, `front_office_held` (SC:201-206)" — true at `b04cbd9`. On `origin/main` they are `"front-desk"` rows (SC:286-291); `hospital` is now eight rows incl. `formulary_interactions_loaded` (#162, SC:183). E3-M2/E3-M3's "hospital census gains …" should say which key each new row joins.

### A12 · Two stand-up executables on `main` are miscited or uncited
- `dev-radiology-standup.ts` is now on `main` (#139 `0b7b442`) — DECIDED 14's "exists" holds today (it did not on 09-06). `dev-lab-standup.ts` (#129 `35f5b6c`, "the laboratory's stand-up, as an executable — for a dev database and nothing else") is on `main` and uncited; it is E1-M3's instrument on UAT and records that activating `opd_visit` on a fresh DB needs two distinct humans — E1-M3 needs two `seed:staff` users for that act.

### A13 · E6 "Exists" misses README:822 (Plan 09's membership go-live runbook)
- README has six "Go-live runbook" headings: :62 patients, :174 tariff, :361 OPD, :645 billing, **:822 membership**, :1354 mini-OT (all verified at `b04cbd9`). E6-M1's membership runbook folds :822 as OT's folded :1354 (now done by #157).

### A14 · E1-M4 hard-codes the figure G-C forbids
- "applied count = journal length at the tip (80 at `fbe8fc4`)" — 82 at `91c34fc` (`0080_lab_instrument_interface`, `0081_lab_catalogue_imports`); production 78 → **4 pending**. Write the rule ("= journal length at the deployed tip"), not the number.

---

## 3. NOTES — minor, or verified as genuinely new

- N1 Off-by-a-few citations (the content is where the design says, a few lines away): LAB "POST /lab/catalogue/analytes|orderables (LAB:120-123)" → `:115-116` (refusals `:121-127`); PH:31 `PHARM-OPD`/`pharmacy_dispense` → `:32`; `import-holder-book.ts:2-9` "not in deploy.sh" → `:11-14`; `tools/lane.sh:79-84` "refuses over unpushed work" → the unpushed check is `:81-84` on `origin/main` ("commits that were never pushed") — claim holds.
- N2 E7 "Exists" verified: `/opt/hmis-lanes/.orchestrator/bin/board.sh:13` `for l in front-desk lims pharmacy radiology`; `state/deployed-base.txt` = `399f92c`; heartbeats `*.status` dated 09-05/09-06 with nothing showing their age. **But** `O/bin` also holds `train-driver{,2,3,4,5}.sh` (v5 8.7 KB, edited 09-08 11:24) and `land-pr.sh` — a release-train tool exists outside git while §6 says "no … release-train tool"; "`O/bin` moves under `tools/`" would bring it into the repo. Name which scripts move.
- N3 E7-M1's "one new `deploy.sh` line writes `log/deploy-<sha>.json`" is still new: T8 snapshots configs to `$DEPLOY_DIR/previous` (`deploy.sh:275`) and writes no per-deploy record; `log/` holds backup and drill logs (:810, :819). Cite :275 as the hook.
- N4 Verified absent on `origin/main` (genuinely new): `docs/STATUS.md`, `tools/milestones.sh`, `lane.sh stale` (subcommands: new/drop/list/status/gc, `lane.sh:136-140`), `pilot:harvest`, `JEST_WORKER_ID`/`NODE_ENV=test` in `docker/`, `tools/`, `scripts/` (0 hits — edge #1's guard does not exist), `config_validated_within_24h`, `config_hash_matches_mode`, `roster_credentials_rotated` (`must_change_password` 0 hits in SC; Plan 20's doc silent on O2), `pharmacy_gst_treatment_ruled`, `pharmacy_retail_licence_on_file`, `print_destination_registered`, `in_consult` in `kernel/ops` (0 hits — edge #14's guard is new). `ops.config_validated` exists (`kernel/ops/events.ts:92`).
- N5 E1-M2's container names: #120 sets `PROJECT="hmis-uat"` (`origin/lane/commissioning-t3:docker/prod/deploy.sh:81-83`), so compose names are `hmis-uat-<svc>-1`.
- N6 E4 "Exists" `seed:pharmacy-demo` (#136, `package.json:39`) — holds. E6 "Exists" `seed-ot` drafts and activates none, `seed-materials` two approval types, `seed-membership` no catalog — hold at `b04cbd9` and on `main` (`deploy.sh:585,599,600` run all three).
- N7 The design's §0 "five runbooks on `origin/main`, zero executed" → nine, zero executed; one (PH:257) scaffolded NOT YET RUN. G-A baseline "G5 0 of 5" → 0 of 8 go-live files.
- N8 Sibling dependence on "the weekly deploy" (P1:96-104,222-228; P3:30,53,199; P4:120,191; P5:78,210) names it as P6's/T7-T8's — no second builder; not a collision.

## 4. Citations that held (137 checked; the 132 not listed above)
All SC/DS/RM/11i/HO/LAB/RS/RAD/11f/REG/P15/P09/16c/README/auth/patients/seed-*/mode.ts/ops.controller/ops schema/validate-config/events.ts/deploy-parity/bench.ts/lane.sh ranges in §0–§5 resolve to the text claimed at the ref cited (`b04cbd9`, or `fbe8fc4` for RAD and "origin/main SC:369-373"/"PH:3-8"). The five flagged: `pricing.ts` `priceForBatch` (A8), LAB:120-123, PH:31, `import-holder-book.ts:2-9`, `lane.sh:79-84` (N1).
