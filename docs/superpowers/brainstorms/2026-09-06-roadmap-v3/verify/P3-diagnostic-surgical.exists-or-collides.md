# P3 — Diagnostic & surgical: EXISTS-OR-COLLIDES verification

Target: `wf/design/P3-diagnostic-surgical.md`. Lens: does anything the design presents as new already exist (merged / authored / deployed); does any plan home collide with index §3 or an authored plan; is any "what exists" citation wrong; does any capability already have a home elsewhere. Read-only; paths relative to `main-ro`.

## 0. Baseline drift — the design's ref is behind the checkout it names

The design pins `origin/main @ b04cbd9`. `main-ro` HEAD is **`ff07cbc`** (#155), 28 commits past `b04cbd9` (`git log --oneline | grep -n b04cbd9` = line 29); `origin/main` is **`91c34fc`** (16 more, incl. #157 and #164, both merged 2026-09-07). Every citation below was checked **at `b04cbd9`** (`git show b04cbd9:<path>`) for correctness, and existence was re-checked on `origin/main`. Where the design says "not in main-ro", it is describing a checkout that no longer exists.

Verdict: **needs-amendment**. One milestone must go (built and merged), one is half-built, one epic rests on a measured claim that is wrong, three capabilities have a second home the design does not name, and the baseline paragraphs are stale. The numbering is clean.

---

## 1. KILL

### K1 — E6-M1 "Runbook written; definitions published; census section" is MERGED (#157)
- Design E6 build: "`docs/runbooks/ot-go-live.md` from P15 §6.3 + §19; `standup:check ot` rows …"; E6-M1 acceptance: runbook on main with §0 · `standup:check ot` ≥ 6 rows · `deploy.sh` runs it · size 4 d; D10 and D11 are listed as DECIDED; §0.8 says "`standup:check` has no `ot` section (standup-check.ts:128,210,322,361); no OT runbook exists (`ls docs/runbooks` = lab, pharmacy, radiation-safety, radiology-pacs)"; §7 schedules "E6-M1 (OT runbook + `standup:check ot`) by S" in wk 3.
- Repo: **PR #157 `fix(ot): §OT-UNROSTERED — the page seed-ot.ts has been pointing at, and five census rows` MERGED 2026-09-07T04:02:44Z (`b2baade`)**. Files: `docs/runbooks/ot-go-live.md`, `apps/core/scripts/standup-check.ts`, `apps/core/src/modules/ot/definitions.ts`, `ot/index.ts`, `test/standup-check.test.ts`. On `origin/main`: runbook §0 "WHAT WILL BITE YOU", §2 "Activate `daycare_case` and `ot_gate` — the Class A ceremony, twice", §3 "Publish the three seeded drafts", §4 "Draft *and* publish `privileges`", §5–§9 (`git show origin/main:docs/runbooks/ot-go-live.md` :15,65,100,116,132-173). Census: **six** `ot_*` rows — `ot_approval_types_registered`, `ot_theatre_present`, `ot_workflow_definitions_active`, `ot_definitions_published`, `ot_surgeon_held`, `ot_anaesthetist_held` (`origin/main:apps/core/scripts/standup-check.ts:460-522`). `deploy.sh` already runs `standup-check.js all` (`origin/main:docker/prod/deploy.sh:663`; at `b04cbd9` `docker/prod/deploy.sh:542`), so the `ot` section is covered by the deploy with no change.
- Consequence: E6-M1's build content is done; D10/D11 are executed facts, not decisions; §0.8's two sentences and the §7 wk-3 row are false on `origin/main`. What remains of E6-M1 is the human ceremony (MS publishes with a distinct drafter on UAT — G3), which is already E6-M2/E6-M3's content. The only possibly-new row is edge #18's `ot_incharge_held` (not among #157's six). **Kill E6-M1 as written; fold the ceremony into E6-M2 and cite #157.**

---

## 2. AMEND

### A1 — E5-M1 is half-built by #164, and its event shape contradicts the merged decision
- Design E5 "Defects on the way": `resequenceList` posts `order`+`reason` (`ot-api.ts:120`) against a server wanting `caseIdsInOrder` (`ot-cases.controller.ts:79`); E5-M1 acceptance: "`list.resequenced{listId, version, order, reason}` from `resequence` (fail-first test) · `resequenceList` posts `caseIdsInOrder` and `/ot/list` calls it"; build list: `ot_resequence_proposals {list_id, version, …}`.
- Repo: **PR #164 `fix(ot): the re-sequence left no trace, and its two ends could not talk` MERGED 2026-09-07T05:09:55Z (`a0148ad`)**. Files: `ot/events.ts`, `ot/lists.ts`, `ot/ot-cases.controller.ts`, `ot/recovery.ts`, `apps/web/src/lib/ot-api.ts`, tests. `listResequenced = defineEvent("list.resequenced", MODULE, z.object({ listDate, theatreResourceId, caseIdsInOrder, reason: nullable }))` (`origin/main:apps/core/src/modules/ot/events.ts:136-140`), and the docstring above it: **"There is no `version` here on purpose … The event is the trace; the version is the sheet's identity"** (:132-134). `resequenceList(body: {listDate, theatreResourceId, caseIdsInOrder, reason?})` (`origin/main:apps/web/src/lib/ot-api.ts:123`). The controller's schema comment records the fix (`origin/main:ot-cases.controller.ts:82-88`).
- Both citations were correct at `b04cbd9` (`ot-api.ts:120` had `order: string[]; reason: string`; controller :79 had `caseIdsInOrder` only). They describe a defect that is now fixed.
- Consequence: two of E5-M1's five acceptance items are done; the proposed payload (`listId, version, order`) and `ot_resequence_proposals.version` collide with #164's explicit no-version ruling. Still open on `origin/main`: no screen calls `resequenceList` (`git grep resequenceList origin/main -- apps/web/src` outside `lib/ot-api.ts` = 0), `planned_start` (no `planned`/`list_items` in `schema/ot.ts`), the `turnover` refusal, `clearTheatre`, the LA-only late flag. **Rewrite E5-M1 to the residual, cite #164, and make the proposal row key on the event's `{listDate, theatreResourceId}` not a version.**

### A2 — E7 and §0.7 rest on a wrong measurement: `ot/lists.ts` does not call `usersHoldingRole`
- Design §0.7: "`modules/ot/lists.ts` is one of its five consumers (measured: `grep -rln usersHoldingRole apps/core/src` = `ot/lists.ts`, `workflow/timers.ts`, `notify/consumer.ts`, `workflow/roles.ts`, `alerts/consumer.ts`)"; E7-M1: "`ot/lists.ts` behind 20's flag; fallback proven — flag off: existing `lists` tests green"; D19: "20's OT consumer ships behind 20's flag with the static resolver as fallback — RM Q4's shape, blast radius five files"; E7 home: "Plan 20 (Plan 20's OT consumer)".
- Repo: the only occurrence in `ot/lists.ts` is a **comment** — `* The adversarial pass found that \`usersHoldingRole("anaesthetist")\` is a LIST, not an assignment` (`apps/core/src/modules/ot/lists.ts:26`, identical at `b04cbd9` and HEAD). Actual call sites: `kernel/workflow/timers.ts:148,153`, `kernel/notify/consumer.ts:230`, `kernel/alerts/consumer.ts:179,229,266,300,322`; `kernel/workflow/roles.ts:71` is the definition. `grep -l` counted a comment and a definition.
- The design inherited the error from ROADMAP v2 (RM:279-281 names the same five). **Plan 20's authored doc corrects it by name**: "the roadmap's list names `modules/ot/lists.ts` — which **mentions the function in a comment and never calls it** — and `kernel/workflow/roles.ts`, which is the DEFINITION" (`docs/superpowers/plans/2026-09-06-phase1-20-workforce-roster.md:29`); ground truth row: "**3 runtime files + 2 seed scripts**" (:28); T3 moves exactly `alerts/consumer.ts`, `workflow/timers.ts`, `notify/consumer.ts` (:100-102). Plan 20 §6 out-of-scope (:122-131) and §7 (:134-141) name no OT consumer.
- Consequence: E7-M1 has no call to put behind a flag, and "inside 20" is a scope addition to an authored FOR-APPROVAL doc (#150, merged `d6c27ed`) that does not contain it. The OT's real seam is `publishList` refusing a list item without an assigned anaesthetist (`lists.ts:26-30`, F18/F24g) and `signIn`'s substitution record (`cockpit.ts:156-182`) — those take a user id, not a role resolution. **Amend E7 to name that signature, drop "fallback proven" for `lists.ts`, and either add a T8 to Plan 20 explicitly or home E7 in 15d.** (The `opdDoctorSchedules` table E7-M2's `surgeon_clash` needs does exist: `kernel/db/schema/opd.ts:92`.)

### A3 — D18 puts the contrast-reaction seat inside 18a-iv, which 18a-iv excludes by name
- Design E1 build: "#143 T1–T4; **reaction screen**"; E1-M2: "`POST /radiology/contrast-reactions` … web callers ≥ 1"; D18: "The reaction seat is built inside E1, not a new phase — #143 §6 calls it 'the one to raise first'".
- Repo: 18a-iv §6 (`docs/superpowers/plans/2026-09-06-phase1-18a-iv-radiology-ordering-door.md:108-113`): "The contrast administration record, the contrast reaction and the outside-study register have no web surface either … They are the same shape as this and they are **not** this phase: this one is the department's entrance, and they are three separate seats' worth of work. **The reaction is the one to schedule next**". The quoted words are "schedule next", not "raise first"; the sentence's operative content is an exclusion.
- Consequence: E1's home (18a-iv) explicitly excludes E1's fifth deliverable. **Either amend 18a-iv §6 by name in the phase doc, or give the seat its own letter — `18a-v` is free** (`grep -rn -o "18a-(ii|iii|iv|v)"` across docs shows ii..iv only; `18a-ii` is "statutory output", RM:423, and must not be reused).

### A4 — E1's human act names a PR that is already merged
- Design E1 header: "Home 18a-iv (PR #143 FOR APPROVAL …; §7 empty)"; E1-M1 human act: "orchestrator approves #143 (§7 empty)".
- Repo: #143 is **MERGED** (`cbacf98`, in `main-ro` at `docs/superpowers/plans/2026-09-06-phase1-18a-iv-radiology-ordering-door.md`); §7 reads "**None.** This phase invents no price, buys nothing and decides no statute" (:119-123) — not empty, "none". Per CONTEXT (night update) the orchestrator holds 18a-iv **code** pending the owner's approval line for the phase. **Amend the act to "owner's approval line for 18a-iv; orchestrator dispatches T1"**.

### A5 — D13 / E5-M3: the C-arm's `device` kind is radiology's, its status vocabulary has no `out_of_service`, and 18c already gates it
- Design §0.7: "Equipment is registry `device`, unclaimed by `ot` (`modules/ot/kinds.ts:47-51`), governed by Plan 29"; D13: "The C-arm is a Plan 13 `device` row with a manual status until Plan 29"; E5-M3 acceptance: "`theatre_fit` open for `cArm: true` while the C-arm `device` is `out_of_service`".
- Repo: `ot/kinds.ts:47-51` is correctly quoted (ot does not claim `device`). But the kind **is claimed**: `apps/core/src/modules/radiology/kinds.ts:53-56` declares `kind: "device", statuses: ["available", "in_use", "down", "qa_blocked", "maintenance", "retired"], initial: "available"` — the only `kind: "device"` declaration in `apps/core/src`. `out_of_service` is not a status of the kind. A C-arm is an ionising device: 18c refuses `startAcquisition` with `device_not_licensed` and withholds it from the MWL (`docs/runbooks/radiation-safety-go-live.md:14-16`); `aerb/manifest.ts:9` names "The C-arm in the cath lab (63)" as the register's object; `aerb/units.ts:10-11` carries `fluoro_seconds` for exactly this machine.
- Consequence: two homes for the same row — E5-M3's `theatre_fit` on a device status, and radiology's kind declaration + `aerb`'s licence gate. **D13 must say: the C-arm is a radiology `device` (with an AERB licence row, or it is `qa_blocked`), and `theatre_fit` reads that kind's `down | qa_blocked | maintenance`; nothing in 15d declares or extends the kind.**

### A6 — D16 / E3: the `partners` module is Plan 09's commission book, not a data-processor register
- Design D16: "Store, teleradiology partner and AI processor are all `partners` rows with `dpa_id, region, deid_level` — one register for every byte that leaves"; E3 build: "partner as a `partners` row with `dpa_id, region, deid_level`".
- Repo: `kernel/db/schema/partners.ts` defines `counterparties {code, name, payeeClass, status, gstin, …}` (:59-98), `partnerAgreements` (:99), `attributionIds` (:129), `partnerRefMap` (:157), `commissionAccrualSubjects` (:214), `commissionAccruals {kind: accrual|reversal|adjustment|kicker}` (:255-267), `receivableExpectations` (:339); `modules/partners/index.ts` exports `resolveReferral`, `AccrualTerms`, `ResolvedAgreement` (:38-44). doc01 never names the module: `imaging_sendouts {study_id, partner_id, deid_level, …}` (`01-radiology-imaging.md:174`) and "Teleradiology partners … send-out register; DPA" (:536).
- Consequence: a cloud object store or a CDSCO image-AI processor as a `payeeClass` counterparty is a semantic collision with a money module every lane imports. **Name a processor register (e.g. `imaging_processors` / `data_processors {dpa_id, region, deid_level, kind: store|teleradiology|ai}`) owned by 18b-iii, or state explicitly that `counterparties` gains a non-payee class and why the accrual code must ignore it.**

### A7 — Stale "what exists" paragraphs (checkout moved)
- G-A: "`radiology_definitions_active` arrives with #140 on remote `main`, not in main-ro" — it is in main-ro: `apps/core/scripts/standup-check.ts:398` (HEAD `ff07cbc`). E1 header: "`docs/runbooks/radiology-go-live.md` (#140, merged to remote main, not in main-ro)" — `ls docs/runbooks` at HEAD = `lab-go-live, opd-go-live, pharmacy-go-live, radiation-safety-go-live, radiology-go-live, radiology-pacs-go-live` (six; at `b04cbd9` it was four, `git ls-tree b04cbd9 docs/runbooks/`). On `origin/main` there are nine (`ot-go-live.md`, `pcpndt-go-live.md`, `lab-catalogue-template.md` added). §0.8 "runbooks executed 0/4" → 0/9. **Re-pin the header to `origin/main 91c34fc` and rewrite G-A, E1 "Exists", §0.8 and §6's last bullet.**

### A8 — "18a (`0047`–`0052`)" includes two front-desk migrations
- `git ls-tree b04cbd9 apps/core/drizzle/`: `0047_radiology_core`, `0048_counter_flow` (`ALTER TABLE "opd_config"`), `0049_vitals_bay` (`ALTER TABLE "opd_queue_entries"`), `0050_form_f_completion`, `0051_close_review_remediation`, `0052_form_f_completion_allowlist`. **18a = 0047, 0050–0052.** (0053/0054 = 18b; 0073, 0075–0077 = 18a-iii; 0074 = 17-E `lab_plate_maps`; 0078 = `radiology_chasers` — all as the design says.)

### A9 — "`fibre|bandwidth|Mbps` occur nowhere in the repo" is false
- `git grep -c -i -E "fibre|bandwidth|Mbps" b04cbd9`: `apps/web/src/screens/lab-downtime.test.tsx:43` ("fibre cut"), `05-video-consultation-opd.md:369,401`, `16-pharmacy.md:369,566` ("a fibre cut takes the pharmacy down"). None sizes the radiology link, so §0.2's point survives; the sentence does not. **Say "no link sizing anywhere; the word appears only as a downtime cause".**

### A10 — E6-M2 lists a deploy fact as a G3/G4 acceptance
- "`OT-1` + two `daycare_recovery` beds in the registry": `seed-ot.js` runs in the deploy (`docker/prod/deploy.sh:479` at `b04cbd9`) and creates `OT-1`, `RB-1`, `RB-2` (P15:136); #157's `ot_theatre_present` is a **G2** row. **Move it to the G2 column (already green on any deploy) or drop it.**

---

## 3. NOTE

- N1 `deploy.sh:542-545` — the file is `docker/prod/deploy.sh`; the lines are the standup-check verdict and match the claim ("prints it"). Add the path; on `origin/main` the block is at :663-667.
- N2 18a-iii off-by-one: "O-1 plus a signed DPA" is `:166` (not :165); "Image upload for outside studies — belongs with 18b-ii's storage tiering" is `:171` (not :172).
- N3 P15: `daycare_package` "created by the go-live runbook" is `:454-455`; `:453` says "created idempotently by `seed:ot`" (a different object).
- N4 `spec:785` is the T2 roster line "Radiology Report Drafter (PACS phase)"; it supports the drafter's phase, not "R-008 files image-AI under 18b" — that rests on REG:20's plan column alone.
- N5 R-262 (REG:274) lists staff geo, nursing tables, KPI packs, feedback triage, face matching, camera/telemetry egress, AI-care notice — **imaging is absent**; the row supports "per-module addenda after", which strengthens §0.5 but is not "R-262 names an imaging addendum".
- N6 D19 "blast radius five files" inherits RM:279-281's set; Plan 20:28 measured 3 runtime + 2 seed scripts. RM:337's "7 consumer files — five runtime, two seed scripts" disagrees with both (Plan 20:29 says so).
- N7 "R-206 env sensor kit ₹40–80 k" — REG:218 names no price; doc15:527 prices *tablets* at ₹60–80k. The sensor figure has no source in the repo.
- N8 Numbering is clean: 15d is P15's own definition (P15:44 "theatre-time bands, the cancellation charge matrix, equipment checks and telemetry"; record §7.3 `00-RECORD-AND-PLAN.md:214` routes "B2 (full cascade), … G9, G10, M7" to 15d); the OT List Optimiser is 48's (index §3:90 "OT List Optimiser, Op-Note Drafter"); 18b-ii is an existing name (18b:10,21,88; RM:338; 18c:9,76; 18a-iii:119,171; Plan 20:5); **18b-iii / 18b-iv are unused** — free; 11b is "storage / hybrid decision" (RM:402). No collision with index §3:55-136.
- N9 Edge 21 verified: `git show 399f92c:docker/prod/prometheus/alerts.yml | grep -c sweepCriticalChaser` = 0; `b04cbd9` = 3. `399f92c` contains #108 (`f211075`) and #79 (`2511184`), 78 migrations — the header's "18a, 18b, 18c, 18a-iii T1–T4 and `ot` are all in it" holds.
- N10 doc19 is `19-support-services-bme-diet-mortuary-security.md`; `:215` names `ot.list_resequenced` (the spec's name, spec:454) — #164 shipped it as `list.resequenced` under MODULE `ot`. Edge 12's "doc19:215 consumes the event" must use the shipped name.
- N11 E1-M2's "`deploy.sh` runs `seed:radiology`" is still genuinely open: not in `docker/prod/deploy.sh` at `b04cbd9`, HEAD or `origin/main`; #133 fixed the seed's actor only (`gh pr view 133`: "`seed:radiology` … could not succeed on a fresh deployment", `actor_not_user`); the census row's own fix text still says "NOTE deploy.sh does not run it (11i finding)" (`standup-check.ts:427` HEAD).
- N12 E5's other claims verified at `b04cbd9`: `signIn` calls `assignResource` with no theatre-status check (`cockpit.ts:149-171`) and the registry admits `onRelease` (`registry.ts:475`) — the "uncleared theatre" defect is real; `returnTheatreToService` is `blocked`-only (`cockpit.ts:621,635`); `flagLateSurgeons` at 60 s (`jobs.ts:258-262`) off the typed NPO start (`gates.ts:66-72`); LA-only unflagged (`lists.ts:245-248`); `OT_EVENTS` has no overrun/npo/theatre events (`ot/events.ts:217-224`); `cArm` in the criteria book (`definitions.ts:93`); `theatre_fit` "deliberately absent: 15b/15c/15d own them" (`schema/ot.ts:88`); `postponeCase` exists (`booking.ts:361`); `in_holding` is a workflow state (`cockpit.ts:131,153`).
- N13 Radiology/E2/E3/E4 claims verified: `imaging.study_acquired` / `image_viewed` (`radiology/events.ts:66,105`); `IMAGE_SOURCES` :83, `pacs_settings` kind :102, `provenance` :434, `IMAGE_ARRIVALS` :823 (`schema/radiology.ts`); `views.ts:14-37`; `mwl.ts:36-37,95-97` (`withheld`, `malformedAeTitle`); `GET /aerb/licences/gaps` (`aerb.controller.ts:157`); `chasers.ts:75-95` reads `critical_categories`; `POST contrast-reactions` (`radiology-acquisition.controller.ts:221`); `/radiology/reception` (`router.tsx:809`); 0 `/orders` web callers; 0 `sendout|teleradiology` in `apps/core/src`; no `kernel/copilot`; `SpeechClient` (`kernel/inference/types.ts:32`); no Orthanc/OHIF in any `package.json`; `interface.down` by `sweepInterfaceHeartbeats` (`kernel/ops/events.ts:112`, `jobs.ts:370-372`); `pacs_not_configured` (`views.ts:72`); `backup.drill_passed` (`kernel/retention/events.ts:120`); `imaging.critical_flagged/critical_overdue/report_unread` (`radiology/events.ts:86,162,167`); `fallbackExhausted` (`timers.ts:150-154`); `HmisSchedulerJobMissing` (`alerts.yml:8`); `radiation_safety_officer` (`seed-roles.ts:1208`); DPIA :5,:19,:28,:66-70 and 0 image/DICOM/pixel hits; spec :24-33 (v4.7), :454, :778, :780 ("Where a rule is safer … the rule wins"), :596; chaos :42; 18b :21 row 2, :49 D7, :82-84, :88-90, :103 F1; doc01 :53,:83,:127,:145-147,:174,:184,:275,:276,:330,:497 ("Storage Forecaster … 18b"),:487 (Extractor → 18b),:534,:558,:564,:578,:598; REG :13,:16,:18,:20,:21,:138,:176,:177,:180 (R-168),:218,:249,:259 (R-247),:266,:274; RM :276-302,:326-347,:373-381,:391-410,:414-418,:423,:428,:452-455; 18c :80-84; index :57,:61,:90,:155; walk report §8 "Every study in this walk was created with `curl`" (`plans/reports/2026-09-06-radiology-commissioning-walk.md:223,237`); `alerts-parity.test.ts` pins interval + daily `job=~` legs (:11,:28).

---

## 4. Amendments (what v3 must do)

1. Delete E6-M1's build content; cite #157 (`b2baade`): runbook §0–§9 and six `ot_*` census rows exist; `deploy.sh` runs the census. Keep only the UAT ceremony, inside E6-M2/M3. Rewrite §0.8, D10, D11 and §7 wk 3 accordingly; add `ot_incharge_held` as the one new row if edge 18 keeps it.
2. Rewrite E5-M1 to its residual (planned_start, `turnover` refusal + `clearTheatre`, LA-only late flag, a screen caller for `resequenceList`); cite #164 (`a0148ad`); adopt the shipped payload `{listDate, theatreResourceId, caseIdsInOrder, reason}` and drop `version` from the event and from `ot_resequence_proposals`; delete the "Defects on the way" sentence about the seam.
3. Correct §0.7 and E7: `ot/lists.ts:26` is a comment; the call sites are `timers.ts:148,153`, `notify/consumer.ts:230`, `alerts/consumer.ts:179,229,266,300,322` (Plan 20:28-29, T3 :100-102). Name the OT's real seam (`publishList` F18 refusal, `lists.ts:26-30`; substitution `cockpit.ts:156-182`) and give E7 an explicit home — a named Plan 20 T8 or 15d — rather than "inside 20".
4. Resolve D18 against 18a-iv §6:108-113: amend 18a-iv by name or open `18a-v` for the reaction seat; fix the quote to "the one to schedule next".
5. E1 human act: #143 is merged; the act is the owner's approval line for 18a-iv (§7 says "None").
6. D13/E5-M3: the C-arm is radiology's `device` (`radiology/kinds.ts:53-56`, statuses `available|in_use|down|qa_blocked|maintenance|retired`) under 18c's licence gate; `theatre_fit` reads those statuses; replace `out_of_service`.
7. D16/E3: do not overload `partners.counterparties` (a `payeeClass` commission book); name a processor register owned by 18b-iii, or state the non-payee class and its accrual exclusion.
8. Re-pin the baseline to `origin/main 91c34fc`; rewrite G-A, E1 "Exists", §0.8 (nine runbooks, 0/9 executed), §6.
9. "18a (0047, 0050–0052)"; "no link sizing anywhere" instead of "nowhere in the repo"; `docker/prod/deploy.sh:542-545`; 18a-iii :166/:171; P15 :454-455; `ot.list_resequenced` → `list.resequenced` in edge 12; drop the unsourced ₹40–80 k or source it; move `OT-1`/RB beds to G2.
