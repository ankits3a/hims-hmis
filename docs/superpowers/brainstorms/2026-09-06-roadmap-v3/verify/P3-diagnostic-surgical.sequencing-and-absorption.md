# VERIFY — P3 diagnostic & surgical · lens: sequencing-and-absorption

Target: `wf/design/P3-diagnostic-surgical.md` (written against `b04cbd9`, 2026-09-06). **Measured at `91c34fc` on 2026-09-08 11:32 UTC** (CONTEXT NIGHT UPDATE 3 override; `git -C main-ro fetch` done, files read with `git show origin/main:<path>`; `gh pr view` for #117–#122, #133, #143, #150, #153, #157, #164; the orchestrator's `/opt/hmis-lanes/.orchestrator/state/` read-only). Paths relative to main-ro. Adversarial by brief: every row tried to refute; §F lists what survived. This supersedes the 2026-09-07 03:30 pass at `3f596e4` — its findings were re-measured, three of its facts moved (§0 rows 1, 4, 5), and one new lens-2 finding (§B6) is the sharpest in the file.

**Verdict: needs-amendment.** The pillar's thesis — openings before builds, every build epic behind a dated S-gate — is RM Q2 applied and is right. Its own tables break it (E3-M1 wk 1–2, E5 on the OT's G2, E7 "E6 open"); three milestones are overtaken by merged PRs (#157 → E6-M1, #164 → half of E5-M1, #150 → "Plan 20 authored wk 3"); E7-M1's subject does not exist in the code; the OT's opening depends on a THIRD governance human the design says it does not need; the R lane is booked ~45 lane-days into a 35-day window RM already fills; and five deploy-day / config-before-guard shapes have no edge row, one of them (R-168) the exact 18c shape the roadmap exists to stop.

## 0. Position drift the design could not see (measured)

| # | fact | where | moves |
|---|---|---|---|
| 1 | **#117 (11i T8) MERGED 2026-09-08 11:24 UTC by the orchestrator's 11i train**; #118 CONFLICTS and the train stopped; #119–#122 (T7/T3/T5/T6) still OPEN, no labels, no owner line in the state dir | `train-11i.log` passes 1–6 11:25–11:29 UTC; `gh pr view 120/121/122` = OPEN, mergeable | the stack is moving without a recorded ruling; UAT (T3) is still not on main. Every `depends_on 11i T3/T5` row (E1-M1, E1-M4, E6-M1, E6-M3) waits on an act nobody has written down (§B1) |
| 2 | **#143 (18a-iv) MERGED as a DOC ONLY**, header "NOT APPROVED, NOT EXECUTED", §7 "None"; the orchestrator holds 18a-iv code until the owner rules | `plans/2026-09-06-phase1-18a-iv-radiology-ordering-door.md:3,119-123`; CONTEXT night update 1 | E1-M1's human act "orchestrator approves #143" names the wrong actor (§B2) |
| 3 | **#150 Plan 20 MERGED "FOR APPROVAL. NOT EXECUTED."**; its ground truth: `modules/ot/lists.ts` "mentions the function in a comment and never calls it"; T3 moves three runtime call sites, none in `ot` | `plans/2026-09-06-phase1-20-workforce-roster.md:3,29,100`; `modules/ot/lists.ts:26` is the only hit, inside `/** … */`; real callers = `alerts/consumer.ts`, `notify/consumer.ts`, `workflow/timers.ts` + `seed-ops.ts`, `seed-roles.ts` (`git grep usersHoldingRole`) | §7 wk 3 is done; wk 4–6 "built by R" waits on the owner's line (§B3); E7-M1 has no subject (§C1); §0.7 "measured: five" matched a comment |
| 4 | **#157 MERGED**: `docs/runbooks/ot-go-live.md` (§0 WHAT WILL BITE YOU, §1 preconditions, §2 Class A ceremony ×2, §3 publish the three drafts, §4 draft+publish `privileges`, §5 unit, §6 people, §7 money) + **six** `ot` census rows (`ot_approval_types_registered`, `ot_theatre_present`, `ot_workflow_definitions_active`, `ot_definitions_published`, `ot_surgeon_held`, `ot_anaesthetist_held`); `deploy.sh:600` runs `seed-ot.js`, `:663` runs `standup-check.js all` | `docs/runbooks/ot-go-live.md:15-172`; `scripts/standup-check.ts:458-530` (`code:` at ot-section lines 3, 20, 36, 52, 63, 65) | E6-M1 (wk 3, 4 d) is mostly done; D10/D11 are executed, not decided (§C2) |
| 5 | **#164 MERGED**: `list.resequenced{listDate, theatreResourceId, caseIdsInOrder, reason}` emitted by `resequence`; `resequenceList` now posts `caseIdsInOrder` + `reason`; e2e test added; still **no `.tsx` caller** | `modules/ot/events.ts:136-140`; `apps/web/src/lib/ot-api.ts:123`; `git grep resequenceList apps/web/src` = the lib only | two of E5-M1's five acceptance legs are done; the "called by no screen" half stands (§C3) |
| 6 | **#153/#166 MERGED**: a re-run of `seed:radiology` drafted v+1 and activated it over the MS's approved book; fixed. `seed:radiology` still creates **five** `device` rows (XR-1, USG-1, CT-1, MRI-1, MMG-1) and is "the only writer of an imaging device"; `deploy.sh` does not run it (`grep -i radiology deploy.sh` = one comment, `:653`) | `scripts/seed-radiology.ts:20,59,70-79,186-187`; `docs/runbooks/radiology-go-live.md:127-157` "THERE IS NO RESOURCES SCREEN"; `kernel/resources/resources.controller.ts:104` GET-only; `createResource` callers = `seed-ot`, `seed-radiology`, `lab/instruments`, `materials/stores`, `opd/masters` | D15/E1-M2 seeds a fleet the hospital may not own (§E1) |
| 7 | `radiology-go-live.md` exists (10 sections, no `## Executed`); `radiology-pacs-go-live.md:3` still says "CODE-COMPLETE and NOT DEPLOYED" (false since 12:35 UTC 09-06); **0 of 9 runbooks executed** (pharmacy's `## 6. Executed on UAT — NOT YET RUN`) | `git ls-tree origin/main docs/runbooks/`; grep `Executed` per file | E1's "#140 … not in main-ro" and G-A's "arrives with #140" are stale; E1-M2's status-line item survives (§C4) |
| 8 | 82 migrations on main (`0000`–`0081`); production at 78 → **4 pending** (`0078` chasers, `0079`, `0080` lab interface, `0081` lab catalogue imports) | CONTEXT NIGHT UPDATE 3 | E1-M3 "weekly deploy with `0078`" → "with `0078`–`0081`"; edge 21/22 still right in shape |
| 9 | `deploy-blocker` label EXISTS | `gh label list` | RM §0 row 10 is no longer inert; edge 19's "no compose diff lands before R1" could be a label, not prose |

## A. Building outpaces absorbing (lens 1)

**A1 — E3-M1 is built in wk 1–2, before any radiology gate, by no named lane, against the pillar's own rule. AMEND.** §3 E3-M1: `depends_on —`, gate `build`, "migration on main", 4 d, "none (designable now)"; §7 wk 1–2 "E3-M1 (outbound register, no ruling) authored as 18b-iii's first task" — the lane cell names R for E1-M1/M2 and nobody for E3-M1. §6 last bullet and §7's closing paragraph: "a build epic starts only when its opening epic's G5 section is dated" — E1-M4, which §7 puts in wk 4–6. RM §5:414 "No new module from the department series except Plan 20"; RM Q2:235-237 "Everything else the lanes build this quarter comes out of the pilots, not out of the department series" — 18b-iii's content is doc01 `:174`, i.e. the series. D2's reason ("R-254 as a property, not a memo") is a fair exception; it must be *stated* as an exception to RM §5 with a lane named, or E3-M1 moves behind E1-M4.

**A2 — E5 (15d) starts on the OT's G2, not its G5, contradicting the design's own rule; and it is series content built before the OT's pilot — the pattern RM §0c.5 removed 17-M for. AMEND.** E5-M1 `depends_on E6-M1` (runbook + census = G2). §6/§7: builds start when E6-M3 (G5) is dated; §7 puts E5-M1/M2 in wk 7–9 while E6-M3 is wk 4–6 "**if** the six OT names arrive" and E6-M4 wk 10–13 "if O6 prices land". P15 DD18 (`…15-mini-ot-daycare.md:184`) routes bands/matrix/telemetry/equipment to 15d; the record's §7.3 (`brainstorms/2026-08-28-plan-15-mini-ot/00-RECORD-AND-PLAN.md:213`, not `plans/…:214` as the design cites) routes "B2 (full cascade)" there too — none of it is a pilot finding. RM §0c.5:176-181's test applied to 15d fails the same way it failed 17-M. What IS pilot-shaped: E5-M1's seam residue (no `.tsx` caller for `resequenceList`; `signIn` walks into `turnover` because `registry.ts:474` admits `onRelease`). Split: E5-M1a (seams) `depends_on E6-M3`; E5-M1b/M2/M3/M4 `depends_on E6-M4`'s harvest — or one line saying why the OT is exempt from §0c.5.

**A3 — E2-M1 (18b-ii authored) in wk 4–6 may precede E1-M4 dated. NOTE.** Authoring is not building and it is conditional on 11b; RM §2:338 puts "18b-ii if the PACS rulings land" in wk 7–9. Say E2-M1 is authoring-only and waits on E1-M4 as well.

**A4 — E7 "Depends: 20; E6 open" — "open" is not a gate. NOTE.** RM §3 has seven; E7-M1/M2 sit in wk 10–13 beside E6-M4's pilot. Name G5 or G6.

## B. Human-held gates written as engineering dependencies (lens 2)

**B1 — Four milestones depend on the 11i stack and none names whose act lands it. AMEND.** E1-M1 (`11i T3`), E1-M4, E6-M1 (`11i T3/T5`), E6-M3 need #120/#121/#122. RM §2 wk 1 gate: "11i approval (owner: one line)"; CONTEXT: held "pending the owner's explicit line". Today's train merged #117 and stopped on #118's conflicts (§0 row 1) — so the hold is either lifted unrecorded or being worked around; either way the design's human-act cells ("orchestrator approves #143", "none", "MS approves", "seats sit") carry no owner line. Add it once at the top of §3 and let the rows inherit.

**B2 — E1-M1's "orchestrator approves #143 (§7 empty)" is the wrong actor. AMEND.** §0 row 2. The act is the owner's approval of 18a-iv — RM's "one line" shape.

**B3 — "Plan 20 authored by R wk 3" is done; "built wk 4–6" and "R after 20" wait on the owner's approval line. AMEND.** §0 row 3; CONTEXT: "Plan 20 BUILD also waits on the owner's approval line". E5, E7 and D19 inherit it and none says so.

**B4 — E1-M4 (wk 4–6) `depends_on E1-M3` (wk 7–9): the table and the calendar contradict, and the seats need people RM dates later. AMEND.** RM §3 G5 "a real person at each seat"; RM §6:455 dates "the RSO and physicist by week 7" and no radiologist/radiographer at all; the design folds all five names into E1-M3 (wk 7–9). Split names (wk 4, asked with pharmacy's) from certificates (wk 7), or move E1-M4 to wk 7.

**B5 — "counsel wk 4–8" exists on no calendar. AMEND.** RM names counsel twice (`:405`, `:428`) and dates nothing. E3-M2 and E4-M2 hang on "the counsel session (L1)". Name it as the owner's act — book counsel — with a week, or E3's Q1-2027 row has no trigger.

**B6 — The OT's establishment needs a THIRD governance human the design says it does not need, and E6-M1's acceptance skips the Class A leg that every booking requires. AMEND (strong).** E6 epic: "G2 MS publishes with a distinct drafter (R-247 honesty, **not O1**)"; E6-M1 acceptance: "`criteria` + `privileges` `active` on UAT via `ot_definition_publish` with drafter ≠ approver". That is Class B only. `booking.ts:178,280` call `startInstance(OT_GATE_DEF_KEY)` / `startInstance(DAYCARE_CASE_DEF_KEY)` — both **Class A**, seeded by nothing (`ot-go-live.md:15-24` row 1; census `ot_workflow_definitions_active` G3, `standup-check.ts` ot-section `:36-50`, ok "only when both are active"). The ceremony (`ot-go-live.md:65-99`): `opd_admin` drafts (`workflow.definitions.draft` = **`opd_admin` only**), MS approves, owner approves ("two distinct people — per person, not per role key"), owner activates (`activate` = **`owner` only**), and "**the owner must not be the drafter**" (`:57-62`). §1.3: "the three governance humans exist". Production has `admin` (owner) + `anand.rao` (MS) (`ot/approval-types.ts:30`; `plan-11d-gate-report.md:445`). So a third person holding `opd_admin` at hospital scope — or a grant of it to a non-owner human — is a precondition of E6-M1, E6-M3's "one synthetic case booking→discharge" and everything after. It is not literally O1 (a second *administrator*) but it is the same shape: a named human the owner has not named. Add to F4's OT row: "`opd_admin` drafter (not the owner)"; add the Class A leg to E6-M1's acceptance; and say the drafter ≠ approver rule is enforced per person (`ot-go-live.md:86-87`).

**B7 — E5-M3 "BME enters the C-arm" is a script, not a hospital act. AMEND.** §0 row 6: no route creates a `device`; `radiology-go-live.md:145-155` calls adding a machine "a deployment act, not a hospital one". On production that is G3 by seed, which RM §3 G3 forbids. Say who runs it and under which rule, or add the `lab/instruments.ts`-shaped door to E5-M3's build.

## C. Order against RM §2/§5, index §3, and the code, without saying why (lens 3)

**C1 — E7-M1 has no subject. KILL as written; recut E7.** Acceptance: "`ot/lists.ts` behind 20's flag; flag off: existing `lists` tests green". `lists.ts` never calls `usersHoldingRole` (§0 row 3). There is nothing to flag and no fallback to prove. What E7 actually is: NEW code — a publish-time "listed anaesthetist is on the day's roster" check and a booking-time `surgeon_clash` — reading 20's `whoIsOn(tx, roleKey, at)` (plan-20 `:96`). That is a consumer 20's T3 does not include, so "Build (inside 20)" is also wrong: it is 15d's or a 20-letter's. Recut E7-M1 as "the OT's first read of 20's resolver, behind 20's flag, with the F24 refusal as today's fallback" and drop the fallback-proof leg; correct §0.7's "measured: five".

**C2 — E6-M1 is overtaken by #157. AMEND.** All three deliverables exist (§0 row 4). What #157 lacks and E6 still needs: a row per whitelisted class for `daycare_package` (P15 §6.3 `:455-456`: a class without one is refused `bill_not_composable`); the four unheld-role rows (`ot_incharge`, `daycare_coordinator`, `ot_nurse`, `recovery_nurse` — runbook §6 lists them, census checks only surgeon + anaesthetist); a recovery-bay row. D10/D11 → "executed by #157".

**C3 — E5-M1 is half done by #164 and the design's D6/D7 text reads as if none of it is. AMEND.** `list.resequenced` exists and `resequenceList` posts the server's shape (§0 row 5). Left: the `.tsx` caller, `planned_start`/`planned_min`, the `turnover` refusal + `clearTheatre`, the LA-only late flag. Re-cut the acceptance to the four that remain; note the event payload carries no `version` **by #164's own refutation** (the version is the sheet's identity) — the design's `{listId, version, order, reason}` contradicts a merged decision.

**C4 — Two "absent" claims are stale on the tip the roadmap runs from. NOTE.** §0 row 7. Strike "not in main-ro" (E1, G-A); keep E1-M2's status-line fix (still false on main).

**C5 — §7 wk 7–13 books R on E5/E2/E7 and displaces RM §2's own R bookings without saying so. AMEND.** RM §2:338 wk 7–9 R: "20 close review, both passes. Plan 41 phase doc AUTHORED"; `:341-344` wk 10–13 R: "the IPD cluster starts (41) only if …, else the lanes stay on pilot fixes". The design's §7 says "E5/E6/E7 are proposed for R after Plan 20 (RM already assigns 20 to R)" and never mentions 41's authoring or the pilot-fix duty. Either 41's authoring moves (say to whom) or E5/E7 move to Q1 2027. RM §5:414-418 (no 29/31), §5:428 (no 12a), §4 (IPD gate) are all respected with reasons — no other §5 conflict found.

## D. Size and shared files without coordination (lens 4)

**D1 — The R lane is over-booked in every window after wk 6. AMEND.** Design §7, R only: wk 7–9 (15 working days) = E5-M1 6 + E5-M2 5 + E2-M2 8 = **19 d** (+ E1-M3 2 d labelled S); wk 10–13 (20 d) = E7 6 + E5-M3/M4 9 + E2-M3/M4 9 = **24 d**. Add RM's own bookings (20 close review ×2 passes; 41 authored; pilot fixes) and the "R after 20" premise — 20 is ~2 weeks (RM §4:400) and cannot start before the owner's line (§B3). Either move E2-M2 to Q1 2027 (its own §6 row 2 says 18b-ii is "not this quarter unless 11b lands by wk 7") or state that E5 waits for 20's close and re-sum. S in wk 4–6 gets E1-M4 + E6-M1/M2/M3 (10 d) on top of RM's lab pilot, pharmacy on UAT + production, six front-desk screens — plausible only because S = P + L + F by then (RM §2:337, §0c.5); say so.

**D2 — Shared and kernel files are edited by five milestones and none names CLAUDE.md's coordination. AMEND.**
- **E5-M2 cascade job**: a scheduler job registers in `kernel/worker/jobs.ts` (`flagLateSurgeons` `:259-262`, 60 s); `alerts-parity.test.ts:160` pins **`toHaveLength(18)`** by name and `:166` exactly two `job=~` legs; `jobs.ts:250-251` "a third leg of its own is not available". A new job = `kernel/worker/jobs.ts` + `docker/prod/prometheus/alerts.yml` + the parity test's named list (three everyone-files) **and the count moves to 19** — the design's "alerts-parity count unchanged" is true only if the cascade rides inside `flagLateSurgeons`' tick, which the milestone must then say.
- **E2-M3 `interface.down`**: `registerInterface` exists (`kernel/ops/interfaces.ts:129`, reachable via `ops.controller.ts:296`); #154 shipped the first heartbeat writer. Orthanc is a row plus a poster — **no kernel change**; the epic text ("`kernel/ops/events.ts`; `sweepInterfaceHeartbeats` precedent") reads as one. Say so.
- **E5-M1 D7**: `cockpit.ts:600-612` already documents the registry's rule and why `returnTheatreToService` exists; a status check in `signIn` before `assignResource` is module-local. Do not edit `kernel/resources/registry.ts:474`; say so.
- **E1-M2 / E6**: `docker/prod/deploy.sh` (S's file; #117/#146 touched it) and `scripts/standup-check.ts` (touched by #140, #155, #157, #158, #162 in two days; #158 makes classification compulsory for every manifest's rows). Name the merge order and the owner-lane.
- **E1-M2 reaction seat** adds a route → `apps/web/src/router.tsx` (everyone-file) and a locale key → `locales/*.json`. Unnamed.
- New tables (E3-M1 `imaging_sendouts`, E4 `imaging_ai_flags`, E5 `ot_list_items`/`ot_resequence_proposals`) need **no** `kernel/db/schema/index.ts` edit (`:22,:42,:76` re-export per module) — one fewer shared file than a reader assumes; serials at rebase (edge 22 covers it).

**D3 — E2-M2 + E2-M3 = 14 lane-days for an Orthanc with 0 files and 0 dependencies, on a box at its ceiling. NOTE.** UAT's Orthanc + index is a second container pair on the 15 GB host (RM §6.1; 11i T3 gives UAT db 1 GB / api 768m / worker 512m, `11i…md:249`); unpriced in memory. E2-M1's ruling row should carry the UAT copy's cost.

## E. Missing edge rows — deploy-day / role-unheld / config-before-guard (lens 5)

**E1 — D15 seeds a fleet, and the seed's re-run shape was a real defect until 09-07. AMEND + two rows.** (a) `seed-radiology.ts:79` seeds XR-1, USG-1, CT-1, MRI-1, MMG-1; every ionising one enters `GET /aerb/licences/gaps`; a hospital without a mammography unit never reaches E1-M3's "gaps = `[]`" except with `DEMO` (forbidden on production, RM §2 wk 1) or a retire route (none). Row: "the seed's fleet vs the hospital's" → Class C only (services, `study_types`) from the deploy under D15; devices through a door or the runbook; `radiology_device_present` RED names it. (b) #153: "a plain re-run with no changes left v1 `superseded` and v2 `active`" — the weekly deploy would have re-drafted the MS's book every Friday. Fixed; the design has no row for the class. Row: "the weekly deploy re-runs the seed after the MS published v3" → `alreadyActive`, versions unchanged in the transcript.

**E2 — `imaging_cloud_region` RED with no partner is a permanent RED on radiology's census for two quarters. AMEND (change the row's semantics).** E3-M1 acceptance and edge 4 say RED with no partner; §7 lands E3-M1 wk 1–2 and the first partner Q1 2027 (E3-M3). G-A's "RED = 0 on production by wk 9", E1-M4's "`standup:check radiology` exit 0" and E2-M4's "green" become unreachable by construction. RM §3 rule 5:386-389: a must-not-open-without row is RED "while the configuration exists and the fix does not". Write it RED only when a store/partner row exists without `dpa_id` or with `region ≠ IN`; NOT MODELLED (never green, `standup-check.ts:48-49`) while nothing leaves.

**E3 — R-168 on a production where no reading has ever been typed is the 18c shape. AMEND.** REG `:180`: "Block theatre if no reading > 2 h; warning before; manual log clears". E5-M3 acceptance: "block after 2 h without a reading, warn at 1 h"; D14. From the deploy that carries it, OT-1 blocks two hours later unless an OT nurse has typed a reading — a guard that needs configuration, shipped live; RM §3 rule 4 requires the phase doc to choose deploy-dark or surface-first. Row: "the deploy lands at 18:00; nobody types a reading" → the gate arms on the first reading, or `ot_env_reading_present` RED and the rule-4 line.

**E4 — `theatre_fit` for `cArm: true` classes on a production with no C-arm row and no screen to make one. AMEND.** `ot/definitions.ts:500-510`: three whitelist classes carry `cArm: true`; E5-M3 gates them on a `device` row's status; no door writes one (§0 row 6). On deploy day every C-arm case is refused `theatre_fit` for a machine the system has never heard of. Row: "no C-arm `device` exists" → satisfied-by-absence with `ot_carm_device_present` RED, or the door ships first.

**E5 — Lists published before 15d carry null `planned_start`. NOTE.** `schema/ot.ts:299-323` has no such column; additive means pre-migration rows are null and "+25 % of `planned_min`" has nothing to compute. Row: scope "100 % non-null" to lists published after the deploy; D8's NPO fallback is the artefact.

**E6 — 20's flag on with no roster published for the day contradicts Plan 20's own D2. AMEND.** E7-M1 flag-on: "publish refuses an anaesthetist not on the day's roster". Plan 20 D2 (`:68-70`): flag off **or no published roster covering the instant** "resolves exactly as today". A Sunday with no roster (P15 edge 24) must therefore publish exactly as today, not refuse. Row: no roster row for the date → today's F24 refusal stands; `escalation.triggered` names the missing roster.

**E7 — No ortho discharge before O6. NOTE.** P15 §6.3 `:455-456`: a class without a `daycare_package` is refused at discharge; runbook §7 "an unpriced package fails at booking, in front of a patient"; RM §2:341 puts the CA session at wk 10–13 at the earliest. E6-M4's harvest row "cases without a package" reads 100 % until then. Write the row so a session does not read the refusal as a defect.

**E8 — `interface.down` on a production with no Orthanc: benign, say so. NOTE.** `interfaces.ts:20-21` downs only rows `up` with `last_seen_at` older than their own `stale_after_ms`; "never seen" stays never seen (`:78,:126`). Row it so the next lane does not re-derive it.

**E9 — The Class A ceremony with two humans on production. AMEND (row for §B6).** Row: "`admin` drafts `daycare_case` because nobody else holds `opd_admin`" → `activate` refuses `workflow_drafter_activator`, the draft is discarded, and the phase doc names the third human before the first attempt (`ot-go-live.md:57-62`).

## F. What survived

- E4 (machine reads) is correctly outside two quarters: R-008's 90-day condition, 12a excluded (RM §5:428), DPIA unsigned, CDSCO unconfirmed — every gate named and human; E4-M3 behind 40/48 (RM §4). Sound.
- E1's shape — the door (18a-iv) before any PACS — matches RM Q1 ("radiology is third") and 18a-iv's own §1. Sound once B1/B2/B4 land.
- §6 row 2 (18b-ii not this quarter unless 11b by wk 7) matches RM §2:338 and §5:423. Sound; §7 must read the same way (D1).
- D9 (ortho first; gynae behind R-237/15b) is the corporate default, not a ruling. Sound.
- Index §3: 15d, 18b-iii, 18b-iv are letters inside existing numbers (CONTEXT numbering rule; index §3:57); 29/31 deferred with the reason (RM §5:414-418); nothing behind the IPD gate is built. Sound.
- Edge rows 18–22 already carry role-unheld, ruling-asleep, stale-status, rules+worker-in-one-deploy and serial-collision shapes; 24–25 are good absence rows. §E adds the instances missed.
- No milestone needs `kernel/db/schema/index.ts`.

## G. Amendments, in the order a reviser should apply them

1. Re-measure at `91c34fc`: strike "not in main-ro" (C4); E6-M1 → "#157 landed; add the package/role/bay rows" (C2); E5-M1 → the four remaining legs, no `version` in the event (C3); §7 wk 3 → "Plan 20 approval line (owner)" (B3); `0078` → `0078`–`0081`.
2. Add F4's OT row "`opd_admin` drafter, not the owner", the Class A leg to E6-M1's acceptance, and row E9 (B6).
3. Recut E7: delete E7-M1's flag/fallback acceptance; "the OT's first read of 20's resolver", NEW code, `depends_on` 20 T2 merged + flag; fix §0.7's count (C1); honour 20 D2 in E7-M1's flag-on leg (E6).
4. E5: split seams (E5-M1a `depends_on E6-M3`) from cascade/equipment/charges (`depends_on E6-M4`), or one line on why 15d is exempt from RM §0c.5 (A2).
5. E3-M1: `depends_on E1-M4` or a stated RM §5 exception with a lane; rewrite `imaging_cloud_region` as RED-only-when-misconfigured (A1, E2).
6. Put the owner's two lines (11i approval; 18a-iv approval) at the top of §3 and let E1-M1/E1-M4/E6-M1/E6-M3 inherit (B1, B2).
7. E1-M3 → names (wk 4) and certificates (wk 7); E1-M4 `depends_on` the names only (B4). Date the counsel session as an owner act (B5).
8. D15/E1-M2: Class C only from the deploy; devices through a door or the runbook; rows E1(a)(b). Same door or rule for the C-arm (B7, E4).
9. Add rows E3 (R-168 arms on first reading), E5, E7, E8.
10. D2: per milestone, name the everyone-files and the merge order; state the cascade rides `flagLateSurgeons`' tick or the parity list moves 18 → 19; `router.tsx` + locales for the reaction seat.
11. Re-sum R per window against RM §2's bookings and 41's authoring; move E2-M2 to Q1 2027 or say what R drops (C5, D1). Price UAT's Orthanc in E2-M1's ruling row (D3).
