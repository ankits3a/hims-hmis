# P6 absorption track — verification, lens: SEQUENCING AND ABSORPTION

Target: `wf/design/P6-absorption-track.md` (written 2026-09-06 21:40 UTC at `b04cbd9`).
Measured against `origin/main` **`41a4be4`, 2026-09-08 ~12:00 UTC** — one commit past the OVERRIDE's `91c34fc`: the orchestrator's 11i train merged **#117 (T8) and #118 (T9) at 11:25–11:29 UTC today** and is updating #119 now (`/opt/hmis-lanes/.orchestrator/state/train-11i.log`; `train.holder` = `list=117 118 119 120 121 122`). Open PRs: **#119–#122, #73**. Paths relative to `main-ro`; `RM` = `docs/superpowers/2026-09-06-ROADMAP-v2.md`; `HO` = `docs/superpowers/2026-09-06-HANDOFF-commissioning-lane-11i.md`; `SC` = `apps/core/scripts/standup-check.ts` (line numbers are `origin/main`'s unless marked `b04cbd9`); `SCT` = `apps/core/test/standup-check.test.ts`.

**Verdict: needs-amendment.** The thesis (absorb before build; G1–G7 per department; G7 = O6) survives every attack I made. What does not survive is the *state* under three of the eight epics — E2, E3-M1 and E6-M1 name as new work things that now exist under other names — one *false* human dependency (E5-M3 on O1), two *hidden* human dependencies (the owner's ask behind E3-M1; an agent standing in for "a real person" at G5), shared-file coordination that no milestone names (four lanes into `standup-check.ts` in one fortnight; two lanes plus two open PRs into `deploy.sh`; a kernel change in E8), and five edge shapes the table lacks — one of which (#153) already happened.

---

## 0. Position drift the design must absorb (every figure it cites has moved)

| design says | measured now | evidence |
|---|---|---|
| 80 migrations, pending 2 (`0078`, `0079`) | **82** (`0080_lab_instrument_interface`, `0081_lab_catalogue_imports`); **pending 4** | CONTEXT night update 3; `git ls-tree origin/main apps/core/drizzle` |
| 12 open PRs; #117 DIRTY, #118–#122 CLEAN | **5 open** (#119–#122, #73); #117 and #118 **merged 11:2x UTC today by the train** | `gh pr list`; `train-11i.log` |
| five runbooks, RAD only at `origin/main` | **nine** under `docs/runbooks/` incl. `opd-go-live.md` (#155), `ot-go-live.md` (#157), `pcpndt`, `lab-catalogue-template` | `git ls-tree origin/main docs/runbooks/` |
| loaders to build: `load:lab-catalogue`, `load:pharmacy-items` | **`import:lab-catalogue` (#168) and `import:item-master` (#144) exist**, dry-run by default, whole-file `withTx`, refusals by row | `apps/core/package.json:35,43`; `scripts/import-lab-catalogue.ts:214-345`; `scripts/import-item-master.ts:10-40`; `docs/superpowers/specs/2026-09-07-spreadsheet-loader-design.md:16-54` |
| the OT has no census rows and its runbook lives in README | **six `ot_*` rows** incl. `ot_definitions_published` (SC:458-522) and `docs/runbooks/ot-go-live.md` (#157) | SC:509 `code: "ot_definitions_published"` |
| the front desk has no runbook the census sees | **`opd-go-live.md` → module key `front-desk`** with five rows (`opd_config_present`, `opd_visit_definition_active`, `active_doctor_in_a_department`, `cashier_held`, `front_office_held`) | SCT:133-141; SC:237-295 |
| T8's events `backup.drill_rehearsed` are on a lane only | on `main` (#117 merged; `kernel/retention/events.ts:126-129`) | |
| E1-M1's human act (NEW-P6-1) precedes the landing | **the train is landing the stack without it** | `train-11i.log` passes 1–6 |

None of this changes the *shape* of the track. It changes what three epics say they build (§1.1), and it makes E1-M1's ordering ambiguous (§1.2 item e).

---

## 1. Findings, by lens item

### 1.1 "Building outpaces absorbing" — authored/built before the department has passed G5/G6

**(a) E2 — two of its three loaders exist under other names, and the epic's name (11k) is already spoken for with a different scope. — AMEND.**
- `import:lab-catalogue` (#168, `package.json:43`) does what E2-M1 specifies: any subset of files, the whole file judged before a write, one bad row refuses all, `Verdict = create|update|unchanged|refuse` (`import-lab-catalogue.ts:203-223,337-345`). `import:item-master` (#144, `package.json:35`) is dry-run by default, `--apply` to write, blanks counted not filled, GST slab per item (`import-item-master.ts:10-40`). The design's rule (CONTEXT night update 2, rule 2: "name what ALREADY occupies that name") was not applied: E2 names `load:*` beside `seed-lab-catalogue.ts` but not `import:*`. E2-M1's *build* is a phantom; its *acceptance* (dry-run on the owner's file on UAT; `lab_orderables_priced`/`lab_range_sources_present` ok) is the real, unexecuted milestone and should stay.
- What is genuinely missing for E2-M2 is the pharmacy half `import:item-master` does not do — the **sale registration** and the NPPA ceiling check (PH §2; `pharmacy_item_present` reads `sale_items`, not `items`). E2-M2 should be re-cut as "the sale-registration loader over an item master that `import:item-master` already loaded", size ≤ 2 d, and name `pharmacy_items_priced_within_ceiling` as the one new row (no collision in SC).
- **11k's scope collision.** HO:173 defines 11k as "the back-book and catalogue loaders, **the pharmacy opening on UAT, the radiology census walk**"; HO:313-318 carries `load:lab-catalogue`'s spec into it. The design's E2 = 11k (loaders only) and hands pharmacy-UAT to E4 (16c) and radiology to E5 (18c). Either adopt HO's 11k or say in one line that 11k is re-cut and why — otherwise the next commissioning handoff (HO:173 "the successor's prompt names it first") and the roadmap disagree on what 11k is.

**(b) E2-M3 — the back-book loader is built this quarter for a production load the design itself puts next quarter (§6), for a desk that has not passed G5. — AMEND.** RM §5:432-434 scopes loaders to "the owner's **two** spreadsheets"; 11i §6:411-412 sends "the back-book and catalogue loaders" to 11k. The design follows 11i and does not say it is exceeding RM §5. Its own §6 says "the back-book bulk load on production: next quarter"; E2-M3 (wk 4–6) then builds a loader whose only production use is Q1 2027, before the front desk's G5 (E3-M3, also wk 4–6) — RM §0c.5's pattern in miniature. Move E2-M3 after E3-M3 (or to Q1 2027) and state the RM §5 exception in DECIDED 8.

**(c) E6 (11m) — half done, half a board decision it does not cite, and scheduled into a lane RM assigns elsewhere. — AMEND.**
- OT: `ot-go-live.md` and six `ot_*` rows are on `main` (#157; SC:458-522, incl. the very row edge row 17 says to build — `ot_definitions_published`, SC:509). E6-M1's OT third is done; the runbook has 0 `## Executed` headers, so E6-M2 (G5 on UAT) is the live milestone.
- Materials and membership: SCT:89-125 on `main` **records them as `NOT_DEPARTMENTS` — "OPEN … a scope call with real cost … it belongs to the board, not to the lane"** (SCT:97-100, 122-124). DECIDED 6 makes that call — legitimately, this is the board's document — but it must cite that it overturns SCT's recorded answer and that flipping either into `DEPARTMENTS` turns the suite red until the runbook and the row set both exist (SCT:98-99). As written, a lane executing E6-M1 will find the test telling it the opposite of the roadmap.
- Lane: §7 puts E6-M1 "by the LIMS lane after 11j (RM §0c.5)". RM §0c.5:176-181 says L joins S for "the pharmacy opening on UAT, the radiology census, the pilots' fix list" — not the silent three. RM §2 wk 4–6:337 says the same. The citation is mis-aimed and E6 displaces calendar work without saying what it displaces.

**(d) E8's build items are unsized, unassigned and unscheduled.** "Two hospital census rows; a transition guard + runbook line; a COMMISSIONING watermark on the invoice — a template test, front-desk lane" — no milestone in E8's table carries them (E8-M1/M2 are 0 d lane), §7 never schedules them. They are code built for a wk 10–13 act; see §1.4(c) for the kernel seam.

### 1.2 Dependencies on human-held gates not named as such (or named wrongly)

**(a) E5-M3 depends on O1 for "a third distinct account" — the dependency is unproven and probably false. — AMEND.** RAD §3 (`radiology-go-live.md:74-91`) says "the smallest lawful set is three". The guards it cites are `duplicate_approval` per `(definitionId, approverId)` (`kernel/workflow/definitions.ts:154`) and `workflow_drafter_activator` (`:208`). **There is no drafter ≠ approver guard** (`definitions.ts:128-215`: approve checks role-held + duplicate; activate checks approvals-complete + SoD pair). Plan 15's Q3 measured exactly this on production: drafter `admin` → approvals `admin`(owner) + `anand.rao`(MS) → activator `anand.rao` — **two humans, both already active on production**, 35 users / 22 active (`plans/2026-08-28-phase1-15-mini-ot-daycare.md:119,201`). What radiology's ceremony needs beyond those two is a holder of `workflow.definitions.draft`, which `seed-roles.ts:284-288` grants to **`opd_admin` only** — a grant at `/admin/users` to any existing active account that is not the owner, *not* O1's second administrator. O1 has been open since 24 August (RM §4:398); tying E5-M3 to it parks the ceremony on the one human act with no date, when it could ride the first governed deploy (E1-M4). Amend: E5-M3 depends on "an `opd_admin` holder ≠ `admin` on production (census row, `fix` = the grant)"; prove the two-account path on UAT in E5-M2; drop "O1 gives it" from DECIDED 5 and edge row 18 (whose "production has two accounts" is also wrong in the letter — plan 15 :119).

**(b) G5 "on UAT" is performed by the lane, i.e. by an agent, in E1-M3, E4-M1 and E5-M2 — and the roadmap's G5 says "a real person at each seat". — AMEND.** RM §3:362 "done by a real person at each seat … who answers: the department head + the commissioning lane". HO D8:249 allows one *person* five times. The design's E1-M3 human act is "the commissioning lane at five seats in a browser" (wk 2), before the pathologist is named (F4, wk 3). Either DECIDE it — "G5-on-UAT may be performed by the lane in a real browser; the department head performs G5 on production (E3-M3)" — with the reasoning that the S-gate is about the *path* (HO D8), or make E1-M3 depend on F4. Silently substituting the actor is the one thing a G-gate must not do. (G-A's target row already drifts on this: "wk 3: lab G5 dated on UAT" vs §7 "Wk 2: E1-M3".)

**(c) E3-M1 depends on "the front-desk lane pushes FD-26/27/28 first" — that push is the owner's act, and it is a 70-commit landing, not a 2-day runbook. — AMEND.** CONTEXT night update 1: "the lane will push only on the owner's ask". `/opt/hmis-lanes/front-desk/hmis` is **70 commits ahead of `origin/main`, tree clean, no remote branch**; the diff touches `kernel/printing/*` (+2,700 lines), `kernel/retention/events.ts`, `apps/web/src/router.tsx` (+165), **both locale files (+534 each)**, `test/seed-roles.test.ts` (+146), `test/caddyfile-parity.test.ts` — every one a CLAUDE.md "belongs to everyone" file — and two migrations `0074`/`0075` whose serials are taken on `main` (`git diff --stat origin/main...HEAD -- apps/core/drizzle`; `tools/renumber-lane-migrations.py` exists for exactly this). E3-M1 must be split: **E3-M0 "the front-desk lane lands" (owner: the ask; orchestrator: the train; serials → `0082+`)**, then the runbook work. And the runbook work is not a new file — see §1.4(a).

**(d) E1-M2 "2 d · none" retires the owner's two test benches.** RM §0 row 3:30-31: the owner's absorption channel today *is* the `:8443` front-desk preview and the `:8444` AERB demo; RM Q3:243-246 replaces them with UAT. Turning them off is not "none" — it is the one visible change to how the owner tests. One line: "the owner is told which URL replaces which, before the stacks go".

**(e) E1-M1 is being overtaken by the train.** The design makes NEW-P6-1 (the owner's one line) E1-M1's human act and E1-M4's dependency. #117 and #118 merged today without it; #119 is being updated. The design should say what E1-M1 means once the stack has landed anyway: the artefact is the catch-up runbook's dated §11 row ("executed 2026-09-06 12:35 UTC by a mutation run; no backout"), and NEW-P6-1 gates **E1-M4 only**. As written a reader cannot tell whether the merge or the line comes first.

**Named correctly (checked, not refuted):** O6/R-255 → E8-M1, E4-M2; F3 → E2; F4 → E3-M2, E4-M3, E5; L2/R-256 → E5-M4; R-004 → E5-M5; the owner's hand → E1-M4/E1-M5; O2 → E3-M2 (11f:263-265, `auth.ts:30`); no DPIA and no 11b on the path (verified: nothing in the track infers; `pilot:harvest` is a script). RM §6.1 (second server) is correctly "nothing waits".

### 1.3 Order vs the index §3 dependency order and RM §5's "not this quarter"

- **Index §3** orders *builds* (14 → 15 → 16 → 17 → 18; `00-INDEX-AND-SYNTHESIS.md:57`); *opening* order is RM Q1's (lab, pharmacy, radiology; RM:211-219) and the design follows it. Opening 15/14/09 last with the reason given (schema in production since `0034`–`0037`, behaviour unverified) is consistent with RM §5's "not 29/31 (no consumer until 15 opens for real)". **No contradiction found.**
- **RM §5 exceeded twice without saying so:** the third loader (§1.1 b) and — arguably — E6's three runbooks + UAT walks for modules RM §2 never schedules. Both are defensible; both need the one line "RM §5 says X; this does Y because Z".
- **E8-M2 is stricter than RM** (G7 also waits on E3-M4, the lab's G6) and says why (RM:339 "the harvest becomes the fix list"). Fine — note only.
- **E5-M1 in wk 3** (seed-radiology into `deploy.sh`) is consistent with RM rule 2:372-374 (Class C by the deploy) and deploy-dark. But see §1.5(a): the seed is now safe *because of #153*, and the design's edge table does not carry the shape #153 proved.

### 1.4 Size implausible for one lane, or a shared-file change with no coordination named

**(a) E3-M1 — a second front-desk runbook file would collide with `opd-go-live.md`. — AMEND.** SCT:129-151 maps each runbook file to one module by name and **fails the suite for a runbook not in the map**; `opd-go-live.md` is already the front desk's (`"front-desk"`, SCT:141). DECIDED 4 ("move from README to `docs/runbooks/`") was executed by #155 under a different filename. E3-M1 = extend `opd-go-live.md` with the six-screen walk, the panel/TPA line and one test print per device; and note that the lane's print runbook is at `docs/superpowers/2026-09-06-RUNBOOK-print-relay-go-live.md` (front-desk lane tree) — **outside `docs/runbooks/`, the exact defect DECIDED 4 names**, in the lane about to land it.

**(b) `standup-check.ts` + its pinned test are edited by four of this design's epics in the same fortnight and the design does not name it as a shared file. — AMEND.** E2 (`pharmacy_items_priced_within_ceiling`), E3 (`roster_credentials_rotated`, `print_destination_registered`), E4 (`pharmacy_gst_treatment_ruled`, `pharmacy_retail_licence_on_file`), E6 (nine rows across three modules), E8 (`config_validated_within_24h`, `config_hash_matches_mode`) — in four different lanes (front-desk, S, LIMS, unnamed). SC took five PRs from five lanes in the last 48 h (#140, #155, #157, #158, #162) and its test pins the runbook↔rows mapping (HO G4:277) and, since #158, compulsory classification of `ALL_MANIFESTS`. Every one of those edits is also an edit to a runbook's `## Preconditions`. Name SC + SCT + `docs/runbooks/*` as "belongs to everyone" for this quarter and give the rows one landing order (a row is added in the PR that adds its runbook line, never separately).

**(c) E7 (11l) touches `deploy.sh`, `CLAUDE.md`, `tools/lane.sh`, CI and `deploy-parity.test.ts` "in the roadmap lane, tonight" — the roadmap lane is docs-only and `deploy.sh` has two open PRs against it. — AMEND.** CONTEXT: "roadmap-v3 (this brainstorm, docs only)". `deploy.sh` is being edited by #119 (T7) and #120 (T3, `HMIS_TARGET=uat`) right now; E7-M1 adds a `log/deploy-<sha>.json` line and E7-M3 a `JEST_WORKER_ID` refusal (0 occurrences on `main` today: `grep -c JEST_WORKER_ID deploy.sh` = 0) to the same file; E5-M1 adds a seed to it a week later. CI just changed twice (#161, #167). Name the lane (commissioning, after #120 lands, is the only one that does not conflict) and the order: #119/#120 → E7-M3's guard → E7-M1's record line → E5-M1's seed. Size 3+1+2+1 d is plausible; the collision is the risk, not the size. (No name collision: no `tools/milestones.sh`, no `docs/STATUS.md` on `main`.)

**(d) E8's transition guard is a `kernel/ops/mode.ts` change and the invoice watermark is a `billing`/printing change — neither is named as shared.** `transitionMode` (`kernel/ops/mode.ts:169-191`) is the gate; reading `opd_queue.status = 'in_consult'` needs only the kernel schema (`kernel/db/schema/opd.ts:376`), so no layering inversion — but `kernel/**` is CLAUDE.md's first "belongs to everyone" entry and `billing` is "imported by nearly every other module". The `pilot.harvest_read` event is a new event family with no home named (`git grep '"pilot\.'` = 0; lab events are per-module `defineEvent`). Name the files, the lane and the week.

**(e) Sizes that do not hold:** E3-M1 "2 d" (§1.2 c: a 70-commit landing first). E2-M1 "3 d" (the loader exists; 1 d for the dry-run on UAT + the census read). E6-M1 "3 d" (OT third done; the other two need the SCT flip, two runbooks and ~6 rows — 3 d holds only if the board decision is written first). E8's build: unsized.

### 1.5 Missing edge rows — deploy-day / role-unheld / config-before-guard

The table has 27 rows and covers the jest deploy (1), the stale SHA (2), the missing backout (3), the guard-before-config for 18c (4), the unheld pharmacy role (5), the money ruling while asleep (6), the scope mismatch (7), the UAT dump (8), the harvest gap (9–10), the tariff edit after validate (15), the seed-green/MS-never-published trap (17), the Sunday deploy question (24–25). Missing, by shape:

| # | shape | scenario (the Indian day) | expected behaviour | artefact | why it is missing matters |
|---|---|---|---|---|---|
| A | **config-before-guard / deploy-day** | **The weekly deploy re-runs every seed; a seed that is not find-or-create supersedes a book the MS published through the governed route** — #153 proved it: `seed:radiology` drafted v(n+1) and activated it with `approvalId: NULL` on every run (`gh pr view 153`). E5-M1 puts that seed on the weekly path | every seed on the deploy path is idempotent *over governed rows*, asserted by a test that counts definitions, not devices (#153's own gap); `deploy-parity`'s seed list gains a "leaves an active book alone" contract | the idempotence test per seed; the rehearsal transcript (HO I17:311) shows 0 new `imaging_definitions` rows on a re-run | **AMEND** — it already happened; E5-M1 widens the exposure to every Monday |
| B | **role-unheld at runtime** | The sole holder of `pathologist` (G4 = "a named human", one) is on leave on pilot day 4; nobody can sign; reports HELD all day; Plan 20 is not live | the runbook names the cover *by name* before G5 on production; the harvest records "no signer" as a seat defect, not "training"; the census's `fix` text says "one holder is a single point of failure" — informational, never RED (a hospital with one pathologist is lawful) | `lab.report_print_blocked` / HELD count that day; the runbook's cover line | **AMEND** — every G4 row in the design reads "held by a named human", singular; RM §0c.4 says humans are the long pole |
| C | **deploy-day** | The Saturday 22:00 UTC drill FAILED or did not run (box memory, HO A2) and Monday is deploy day | G1 is unmet → no deploy; STATUS.md "deploy-ready: no — drill `failed`/`absent` <date>"; the rehearsal is re-run by hand under `HMIS_DRILL_REHEARSAL=1` before any window | `backup.drill_failed` or no `drill_*` event for the week; the §11 row says why the Monday slipped | **AMEND** — G-B's Monday rule assumes a passed drill; nothing says what a failed one does |
| D | **deploy-day / the human long pole** | The owner (the only deployer, E1-M5) misses two Mondays; pending reaches 8; no fixing PR exists so no `deploy-blocker` can be opened | G-B goes red with "waiting: the owner's hand"; the rule "a label needs a fixing PR" (DECIDED 7) does not fit — the artefact for *this* is the STATUS.md line, and the design should say so | STATUS.md `deploy lag = 14 d, cause = unstaffed` | **AMEND** — RM §0c.4 applied to G-B; the design covers the agent asked to deploy (25), not the human who does not |
| E | **shared-file collision** | Two lanes add census rows for two modules in one week; HO G4's runbook↔rows pin (SCT) fails for whichever lands second, and both PRs turn red for each other's module | the row lands in the PR that adds the runbook line; the train updates the second PR; nobody "fixes" the pin by loosening it | the pin unchanged; the train log | **AMEND** — §1.4(b) |
| F | **Indian day / G6** | Of the "seven consecutive read days", two are Sunday and a public holiday; the department head is not in | G6 counts *working* days of the department (the lab runs 24×7; the front desk does not) — say which per runbook; a read on a holiday is not required and its absence is not a gap | `pilot.harvest_read` dates vs the department's calendar | note |
| G | **deploy-day** | `HMIS_DEPLOY_ALLOW_DIRTY=1` (rehearsal-only override, `deploy.sh:62-70`) is left exported in the owner's shell from a rehearsal, and Monday's deploy ships a dirty tree | the override is accepted only with `HMIS_DRILL_REHEARSAL=1` in the same environment; the §11 row records both variables' values | the guard's refusal text; the §11 row | note |
| H | **deploy-day on a shared box** | A UAT `docker build` (E1-M2, every green batch) runs beside production's postgres on the same 15 GB box; `test-lock.sh` serialises builds against jest, not against `hmis-prod-*` | UAT builds carry a memory limit; the build is refused when free memory < the last build's peak; production's containers have a reservation | `docker stats` peak recorded per build; the lock's holder line | note — RM Q3 says the box is at its ceiling with 5 GB of swap |
| I | **deploy window vs a 24-h bench** | "Window after 21:00 IST" (row 12) is the OPD's quiet hour; the lab pilot's bench runs the 02:00 potassium (drill A) | the lab's window is named by the pathologist per pilot week, not assumed | the window in the §11 row names the department that agreed it | note |

---

## 2. What held (looked for, not found)

- **The gate order.** G1→G7 per department with G7 hospital-wide = O6 matches RM §3:351-368 exactly; every milestone names its gate; no milestone claims G5 without a dated `## Executed` and no G6 without seven read rows.
- **Nothing on the track infers.** No Digest Writer, no Hermes, no DPIA on the path — verified against every "Build" paragraph; `pilot:harvest` is a deterministic script (edge row 10 makes reading an event with a reader — good).
- **Deploy-dark applied honestly.** 18c stays the named exception (RM rule 4:378-380; 11i D4:143-146); pharmacy's GST row is a `must-not-open-without` census row, not a label, exactly per RM rule 5:381-386 — and the design says why no `deploy-blocker` can exist (no fixing PR).
- **Numbering.** 11k (11i §6:411; HO:173), 11l, 11m are free letters in the deployment-and-operability block; no collision on `main` (`git grep -w 11l|11m` over docs = 0 hits outside the design's own references; the "11k" hits at `EXECUTE-METHOD.md:89` are "11k tokens").
- **The IPD gate is upstream, not downstream** — the track correctly treats O1/20/dues/11b as RM §4's critical path and builds nothing behind it.
- **Money/procurement/law are the only rulings asked** (NEW-P6-2, O6, L2, R-004, R-174, the server); the rest is DECIDED with a line each. NEW-P6-1 is a fact + a hand, correctly labelled.
- **G-B's instruments are real**: SHA tags and `HMIS_DEPLOY_ROLLBACK_TO` are on `main` (`deploy.sh:49-56,219-250`); `backup.drill_rehearsed` exists (`events.ts:126-129`).
- **One instrument is mis-aimed, though (parameter rule):** G-A's `grep -l '^## Executed' docs/runbooks/*-go-live.md` misses the only executed-section header that exists — PH's is `## 6. Executed on UAT — **NOT YET RUN**` (`pharmacy-go-live.md:257`), numbered, so `^## Executed` never matches it; and a looser pattern would count NOT YET RUN as executed. Pin the header shape in SCT (a dated `## Executed` line = G5) and make the grep match it. — AMEND (small, but it is the G5 instrument for every department).

---

## 3. Amendments, in the order a rewrite should apply them

1. Re-measure the header at `41a4be4` (or later): migrations 82, pending 4, open PRs #119–#122 + #73, nine runbooks, #117/#118 merged by the train 09-08 11:2x UTC; strike "#117 DIRTY", "12 open PRs", "five runbooks", "RAD only at origin/main".
2. **E2:** name `import:lab-catalogue` (#168) and `import:item-master` (#144) as what exists; E2-M1 becomes "dry-run the owner's file on UAT with `import:lab-catalogue`" (1 d); E2-M2 becomes the sale-registration/NPPA half over an already-loaded item master; adopt HO:173's 11k scope or say 11k is re-cut; move E2-M3 after E3-M3 or to Q1 2027 and state the RM §5 exception.
3. **E3:** insert E3-M0 "the front-desk lane lands" — owner's ask named as the act, serials `0074`/`0075` → `0082+` via `tools/renumber-lane-migrations.py`, the shared files listed; E3-M1 extends `opd-go-live.md` (no new file; SCT:133-151); the print-relay runbook moves under `docs/runbooks/`.
4. **E5-M3:** replace "O1 (a third account)" with "an `opd_admin` holder ≠ `admin` on production (grant at `/admin/users`; census row)"; prove the two-account ceremony on UAT in E5-M2; correct DECIDED 5 and edge row 18; cite `definitions.ts:154,208` and plan 15 :119.
5. **G5 on UAT by the lane:** write it as DECIDED (the S-gate is about the path — HO D8) with the department head's G5 on production as the human one; fix G-A's "wk 3" vs §7's "wk 2".
6. **E6:** record that OT's runbook + six rows exist (#157); cite SCT:89-125's `NOT_DEPARTMENTS` and state the board decision that flips materials/membership; move E6 out of "the LIMS lane after 11j" or name what it displaces from RM wk 3–6.
7. **Shared files:** name `standup-check.ts` + its test + `docs/runbooks/*` as belonging to everyone this quarter with one landing rule; give E7 a lane (commissioning, after #120) and the `deploy.sh` order #119/#120 → E7-M3 → E7-M1 → E5-M1; name `kernel/ops/mode.ts`, the billing template and the `pilot.*` event's home for E8, with a size and a week.
8. **E1-M1:** state that the landing may precede the owner's line and that NEW-P6-1 gates E1-M4 only; the artefact is the §11 row.
9. **Edge rows:** add A (seed re-run vs governed rows — #153), B (sole role holder absent), C (drill failed before deploy day), D (the owner misses Mondays — the label rule has no artefact for the human long pole), E (two lanes into the census pin); consider F–I.
10. **G-A instrument:** match the runbook header shape that exists (`pharmacy-go-live.md:257`) and pin it in SCT.
11. **E1-M2:** one line telling the owner which UAT URL replaces the `:8443`/`:8444` benches before they are retired.
