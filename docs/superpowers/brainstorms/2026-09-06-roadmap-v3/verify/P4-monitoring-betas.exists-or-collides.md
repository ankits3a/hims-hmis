# VERIFY — P4-monitoring-betas · lens EXISTS-OR-COLLIDES

Target: `wf/design/P4-monitoring-betas.md` (45 KB). Checked against the read-only checkout at `ff07cbc` (= `b04cbd9` + #155 … #154) and, where the CONTEXT override demands, `origin/main` @ `91c34fc` (2026-09-08 11:32 UTC) via `git show origin/main:<path>`. Paths relative to `main-ro`. Every citation in the design was grepped; the ones not listed under a finding held exactly (list at the end).

**Verdict: NEEDS-AMENDMENT.** Two kills, both on WHERE things go (a plan home already taken; a milestone already merged), not on the reconciliation or the epics' content. Seven amends, mostly "true at b04cbd9, false at 91c34fc" — the design measured on the 6th and three merges on the 7th/8th put runbooks and census rows exactly where it planned to create them.

---

## KILL

### K1 · E1's plan home "VD-3" is already a named, scoped plan — the serial seam
Design (§2 E1): *"Plan home: VD-3, the next letter of the vitals-desk series … Letter-in-block."* and DECIDED 22.

Repo:
- `docs/superpowers/plans/2026-08-31-phase1-vd1-vitals-rails.md:14` — *"VD-1 is the rails. VD-2 builds Bay One over them; **VD-3 adds the serial seam**."*
- `docs/superpowers/plans/reports/2026-08-31-vitals-desk-recon.md:168-170` — *"**VD-3 · The serial seam.** The lane toggle as config, the device-driver interface stub, keystrokes-vs-device-reads telemetry, and the procurement stop. Small; folds into VD-2 if VD-2 comes in light."*; `:173` *"E is VD-3."*
- `docs/superpowers/plans/2026-09-02-phase2-vd2-bay-one.md:167` — VD-2's CLOSE parks an unfixed defect on it: *"the cancelled-entry predicate suppresses a LATER new danger on the same entry (D4 says `cancelled` is terminal; the state table or the code is wrong — **RC-6/VD-3**)"*; `:221` M9 (held first take is one tab's `sessionStorage`) **NOT FIXED**, `:271`.

So VD-3 is not "the next free letter": it has a defined scope (device seam + a procurement stop — the owner's serial-toggle ruling, memory `vitals-desk-design-series`) and two owed deferrals from VD-2 pass 2 that touch the SAME escalation state machine E1-M2 builds on. A lane told "author VD-3 = baselines" would either overwrite the recon's VD-3 or ship baselines on top of a known-wrong `cancelled` predicate without knowing it. **Move E1 to VD-4** (or explicitly absorb VD-3's four items + the two VD-2 deferrals into E1 and say so in §2 and DECIDED 22). Either way E1-M2 must list the `cancelled`-terminal contradiction (`vd2:167`) as a precondition or a first task — it is the board decision the order consults.

### K2 · E2-M1's first census row is already merged (#162) under another name, on another block
Design (§0.2, G3(a), E2-M1, edge rows 9 and 23): *"no census row reads it (`grep formulary scripts/standup-check.ts` → empty)"* → build `formulary_interaction_pairs_present` on `standup:check pharmacy`.

Repo (origin/main): PR **#162** `4eb2739` *"feat(standup): an interaction checker with an empty book answers 'no interactions' to everything"* — `apps/core/scripts/standup-check.ts:183-226` on origin/main: `gate: "G2", code: "formulary_interactions_loaded"`, in the **`hospital`** block (`:128`), check `(await listInteractions(db)).length >= SEED_CENSUS.pairs` (`:224`, imports `SEED_CENSUS` from `./seed-formulary-interactions`, `:29`), fix text *"run: pnpm --filter @hmis/core seed:formulary — an empty interaction book answers 'no interactions' to every prescription"* (`:226`); its comment (`:198`) cites `deploy.sh:588`. The row's rationale (`:205-206`) explicitly rejects the design's own `> 0`/"present" shape: *"A `> 0` row would go green on ONE pair and certify 'somebody ran the seed'"*. The census test grew by 44 lines in the same PR.

The design's grep was true at `b04cbd9`; #162 landed 09-07. As written E2-M1 would author a second, weaker row on a different block for the same fact. **Kill the row as new work**; re-point G3(a), E2-M1, the pharmacy-runbook precondition, edge rows 9 and 23 and DECIDED 10 at `formulary_interactions_loaded` (hospital block, G2). What remains of E2-M1 is the curator row (see A1) and the owner's one `count(*)` — half a day, not a day.

---

## AMEND

### A1 · `formulary_curator_held` duplicates `pharmacy_role_held` unless it is defined on the permission
`formulary.manage` is granted to exactly one role, `pharmacy` — `apps/core/scripts/seed-roles.ts:321-334` (*"THIS GRANTS LIVE AUTHORITY TO NOBODY TODAY … `pharmacy` is one of the three roles this script creates with grants and no holders"*; the design's "a role with no humans in it", `formulary/manifest.ts:32`, is the same fact). origin/main already has `{ gate: "G4", code: "pharmacy_role_held", check: heldAtHospitalScope("pharmacy") }` (`standup-check.ts:423`). A `formulary_curator_held` row that checks the role is that row. If E2-M3's DTC chair (R-243: "DTC = senior physician", REG:255) is meant to hold `formulary.manage` without being a pharmacist, the row must check the PERMISSION at hospital scope, and seed-roles must grant it to a second role (pins at `seed-roles.test.ts:1102,1191` move) — say which.

### A2 · "No OT census rows, no OT runbook exist" — false at 91c34fc (#157)
Design E3 Gates: *"the mini-OT's own opening — no OT census rows, no OT runbook exist (`standup-check.ts:128,210,322,361`; `docs/runbooks`)"*; E3-M1 *"a new day-care runbook §walk"*; §6 *"No 15-ii this quarter unless the mini-OT opens … no OT census, no runbook"*; §7 Q1 *"OT census rows + runbook first — a precondition this pillar cannot write alone"*.

Repo (origin/main): PR **#157** *"fix(ot): §OT-UNROSTERED — the page `seed-ot.ts` has been pointing at, and five census rows"* → `docs/runbooks/ot-go-live.md` (*"OT & day-care go-live runbook"*, §0 the three Class-A/publish acts no seed can perform) and rows `ot_approval_types_registered` (`standup-check.ts:460`), `ot_theatre_present` (`:477`), `ot_workflow_definitions_active` (`:493`), `ot_definitions_published` (`:509`), `ot_surgeon_held` / `ot_anaesthetist_held` (`:520-522`). The cited lines `128/210/322/361` were the `hospital:`/`lab:`/`pharmacy:`/`radiology:` block openers at b04cbd9 — correct then. The runbook has no SSI/recall/follow-up section (grep empty) — E3's content is still new; its **home** is `ot-go-live.md` (append a §"after the door"), and the §6/§7 precondition sentence must be re-measured: written, not executed.

### A3 · "No OPD runbook exists today" — false at ff07cbc/91c34fc (#155)
Design §0 *"the vitals bay is deployed with no runbook … `docs/runbooks` = four files"*; E1-M1 *"a new `docs/runbooks/opd-vitals-go-live.md` §harvest names the row (no OPD runbook exists today)"*; DECIDED 24.

Repo: `docs/runbooks/opd-go-live.md` merged in **#155** (`ff07cbc`, in the checkout itself), plus `opd_visit_definition_active` (`standup-check.ts:244`). Nine runbooks on origin/main (`git show origin/main:docs/runbooks`). The four-file count was right at b04cbd9 (`lab`, `pharmacy`, `radiation-safety`, `radiology-pacs`). `opd-go-live.md` mentions `vitals_desk` once (`:154`, the role) and nothing about the danger protocol, bands or escalation — so E1-M1's harvest section is new, but a second OPD runbook is two homes. Amend E1-M1 to *"a §vitals / §harvest section in `opd-go-live.md`"*.

### A4 · Two homes for the SSI register and the day-30 survey: the series put them in 28b
Design E3 homes `ssi_register`, `ssi.followup_recorded`, `ot.ssi.read/write`, `ot_icn_held`, `ot_ssi_window_overdue` and the day-30 survey in **15-ii**, citing OTD:156 and OTD:47.

Repo — the same OT document hands them away: `15-ot-anaesthesia-cssd.md:551` *"**Plan 22 — Quality pack** (incident/near-miss/RCA, NABH indicators) **absorbs the OT-local registers**."* (`:550` is Plan 15's T1 list and does say "SSI register", one line before the hand-off). The index reconciles it: `00-INDEX-AND-SYNTHESIS.md:74` Plan **28** *"absorbs … 15 (22 quality pack), 06 (26 IC registers → 28b + 49)"*, **28b = infection control**; collisions table `:117` (doc 15 "22 quality pack" → 28). QUA's own plan sketch scopes the survey: `22-quality…md:535` **22b — Infection control** *"… sterilisation-monitoring audit + BI-fail recall trigger (needs 15/CSSD), **SSI post-discharge survey via Recall**"*; Q-4 (`:350`) and O-15 (`:526`) sit in that document. The authored Plan 15 phase doc never mentions SSI (grep empty) — it did not defer it, it never took it.

Not a kill: the index note on 28 (*"28a can run in parallel with Track A today under single-approver honesty mode (R-247)"*) shows a pull-forward is thinkable, and the OT runbook now exists. But E3-M2/M3 must **name 28b as the designed home and 15-ii as a deliberate pull-forward of 28b's SSI slice** (permissions as `quality.ssi.*` or a stated reason for `ot.ssi.*`; the ICN as 28b's actor, OTD:47 notwithstanding), or move M2/M3 to a 28b phase doc. E3-M1 stays in 15-ii on its own evidence: `2026-08-28-phase1-15-mini-ot-daycare.md:339` *"`dischargeDaycare` (DD10 + follow-up recall task via the notifications/scheduler seam + `daycare.discharged`"* — Plan 15's own promise, unbuilt (`recovery.ts:549` comment vs `:575-579`, verified).

### A5 · `whatsapp_templates_registered` is not a census row; `not_submitted` is an owner-action status
Design E3-M3: *"census row `whatsapp_templates_registered`; `not_submitted` today, 17c `:105`"*.

Repo: no such code on HEAD or origin/main (`git grep` empty). `17c-lims-five-seats.md:105` reads *"Owner ACTIONS … WhatsApp template submission (`not_submitted`) before any patient message"* — a status word in a prose list. The row that exists about the channel is `lab_report_ready_notice` (G3, `NotModelledRow`, origin/main `standup-check.ts:396-399`: *"kernel/notify has console adapters only: no provider, no TRAI DLT sender-id or template, no WABA template. NO PATIENT MESSAGE LEAVES THE BUILDING"*). Either cite that row (and widen it from lab to hospital) or mark the new row NEW.

### A6 · Wrong line: the lab's own-transaction audit rule
Design E2-M2 and DECIDED 11 cite `lab/events.ts:124`. That line is the `/* ─── Results (T6) ─── */` banner. The rule is `apps/core/src/modules/lab/events.ts:136` (*"17d T1 / D3 — THE SWAP, SUSPECTED, appended on its OWN transaction"*), with the same rule stated at `lab/ingest.ts:32`, `lab-collection.controller.ts:21`, `lab-instruments.controller.ts:131`.

### A7 · Citations that drifted between b04cbd9 and the override tip 91c34fc
CONTEXT NIGHT UPDATE 3 requires *"measured at `91c34fc` on 2026-09-08 11:32 UTC"*. Lines that moved: `deploy.sh:467` → **`:588`** (seed-formulary; `:441` the `set -euo pipefail` comment and `:432-438` the seed rule were correct at b04cbd9); `standup-check.ts:322-361` pharmacy block → `:409-440`; `:435-445` is now `radiology_devices_licensed` (the "shape" claim still works); `:183` is still `opd_config_present` on both tips but #162's row sits at `:183` on origin/main only by coincidence of the same number — cite the code, not the line; `pharmacy-go-live.md:41` no longer the precondition table row 1. Off-by-one at b04cbd9 itself: adult `pulse: { min: 50 }` is `config.ts:138` (`:137` is the comment above it). Header line 3 must change.

---

## NOTE

- N1 · ICU:163 (cited with ICU:99 for the intensivist limit order) is `icu_data_gaps`; the order is ICU:99 only (also ICU:460 Alarm Governor, ICU:538 O-12).
- N2 · NUR:583 says *"ship recorded-only for **30 days per ward**"*; E1-M4 and DECIDED 6 relay it as "the first pilot week". Keep the week if that is the decision, but do not attribute it to NUR:583.
- N3 · Edge row 17's proving artefact `patient.recall_initiated` ×2 exists only in `04-physio…md:88`; no such event in code (`git grep` empty). Mark NEW (it is Plan 25's).
- N4 · `voice-flag.ts` is `apps/web/src/lib/voice-flag.ts` (web); the server-side 503-until-flagged pattern E3-M4 wants is `apps/core/src/kernel/inference/speech.controller.ts`. COP:129 names "the voice-flag pattern", so the reference is right; the path is missing.
- N5 · Naming shapes: `16a-ii` follows a real precedent (`18a-ii` = deferred statutory output, `2026-09-04-HANDOFF-radiology-lane-18c-T6.md:179`, RM:423) and collides with nothing. `15-ii` has no precedent — the series uses letters for siblings (14b, 22a, 25b, 28a–e) and roman numerals only inside 18a; the index would read `15b`. Not a collision; pick one shape and say why.
- N6 · E4-M1 "pre-op ROM on the OT case **at PAC**": Plan 15's schema has no PAC record — `schema/ot.ts` carries `pacu_thresholds` as a definition kind (`:96`) and `anaesthesia_type` (`:261`); `pac_assessments` is design only (OTD:144). `ot_cases.pre_op_scores jsonb` is fine; say there is no PAC row to hang it on.
- N7 · E1-M1's `(day, band, vital)` rollup table is a KPI compute; the index names Plan **21** ("KPI formula registry & compute … every later plan's §8", `00-INDEX:70`) as the home for exactly that. E4-M4 acknowledges 21 for G5; E1-M1 does not. Name the table an interim harvest and 21 the eventual home, as E4-M4 already does.
- N8 · `user_day_facts` (`schema/desk.ts:35`: `userId, day, facts jsonb, computedAt`) is per-user and does not already count danger flags — E1-M1 does not duplicate it. `vitals.danger_flagged` consumers outside `modules/opd`: only tests (`test/opd-lifecycle.e2e.test.ts:352,373`) — the "only consumer is the realtime fan-out" claim holds for runtime code.

---

## Verified — no collision, citation exact (so the rest of the design can be trusted on these)

- **Free identifiers on origin/main** (git grep empty): `patient_vitals_baselines`, `vitals_baseline*`, `rollupVitalsFlags`, `opd_vitals_flag_days`, `prescription.check_refused`, `rx.check_changed`, `formulary_curator_held`, `ssi_register` (code), `ssi.followup_recorded` (code), `ot_icn_held`, `off_trajectory`, `therapy_plan` (code), `TRAJECTORY_WATCHER`, `WOUND_PHOTO_ENABLED`, `pre_op_scores`/`preOpScores`, `outcome_score`, order kind `therapy`. No `16a-ii`, no `15-ii`, no Plan 25 phase doc (`ls docs/superpowers/plans`), and the Plan 20 doc (#150) claims nothing about vitals, physio or baselines (grep empty).
- **Exists-as-stated, exact lines:** `config.ts:116-143` bands as data (owner 2026-08-15), `:178-192`, `:244-249`; `vitals-rules.ts:122-126` `bandFor`, `:175-213` `evaluateVitals(v, band, cfg)` no patient arg; `escalation.ts:21-33` the three-step ruling, `:147-148` `dangerOf` filters notices; `vitals.ts:262-266` the F1 split; `history.ts:125-146` + `chainFor` at `:128`; `prestage.ts:55-63,141-146,161-162`; `realtime.ts:9-20` (`vitals.danger_flagged` at `:11`); `opd/events.ts:198,230,245,255` the four events, `:341` `interactionOverrideCount`; `schema/opd.ts:459` `dangerFlags`, `:573` `interactionOverrides`, `:428` `opd_queue_entries.escalation`, `:333` `opd_encounters.danger_flagged`; `opd/manifest.ts:26,40,47,49`; `router.tsx:661` (FD-5 ruling: Bay One IS `/opd/vitals`); `opd-consult.tsx:170` (`apps/web/src/screens/`, the `vitals` history view).
- **DDI:** `rx-checks.ts:7-12,169-186` (`if (byKey.size === 0) return []` at `:186`); `prescriptions.ts:117,255-261,282,373-382`; `pharmacy/verify.ts:185-205`; `formulary.controller.ts:267` `@Patch("interactions/:id")` under `formulary.manage`; `masters.ts:284-288,316-335` (no `reason`); `formulary/events.ts:54-62` (`interaction.updated {interactionId, changed}` at `:60-62`); `schema/formulary.ts:30,131,148`; `seed-formulary-interactions.ts:14-19,81`; `test/seed-formulary-interactions.test.ts:89`; `formulary-admin.tsx` has **zero** "interaction" matches; `16a-formulary…md:377-389` names F22 as *"a small phase of its own"* (supports 16a-ii).
- **OT/kernel:** `recovery.ts:443-446` `dischargeDaycare`, `:549` the "recall task" comment, `:575-579` incident + event and no task; `ot/events.ts:84-86` payload `{encounterId, patientId, bayResourceId, at}` — no procedure code; `daycare.discharged` has no consumer outside `modules/ot` (grep); `schema/ot.ts:255-256,276`; `schema/patients.ts:200,264` the 512 KB staff photo; `multipart` in `apps/core/src` = two comments (`membership.controller.ts:95`, `partners.controller.ts:95`); `jobs.ts:339` `rollupUserDayFacts`; `notify/adapters.ts:33-36,60` console adapters; `alerts/alerts.ts:9-14`; `workflow/roles.ts:29-32`; `orders/kinds.ts:19-39`; `membership/index.ts:65-66`; `phi/audit.ts:244` `recordPhiAccess`; `phi_access_log` (`drizzle/0038`); `alerts.yml:52` job list, `:69` `HmisSchedulerJobMissing`; `seed-roles.test.ts:886` (161), `:1102` (308), `:1191` (147); `check-config-present.ts` exists; events `alert.read`, `escalation.triggered`, `appointment.no_show`, `patient.merged`, `prescription.issued` exist.
- **Docs:** SPEC `:281` (map 11 day-care), `:778-789` (tiers, roster at `:782-787`, Recall & Follow-up T1 at `:784`, guardrails `:789`); COP `:27,28,61,125-129,199`; DPIA `:11,20,31-33`; RM `:37,291,337,378-386,391-410,402,414,418,419,450` and §7 (`:463+`) "what is added, what does not move"; index `:71` Plan 25 row, `:198` risk 4; REG `:21,32,37,56,57,70,82,181,255,265`; NUR `:177,319,459,464,587`; OTD `:47,156`; QUA `:92,350,526`; PHY `:38,63` (four active alerts — verified the list), `:88,90,156,268-269,317,330,373,395,416,417,485,489`; ICU `:99,468,545`; `03-home…md:380` H-8; `patient-self-service/06-RULINGS-LOCKED.md:44` R-17; `22cA:3`; `22cD:90,110,153-165` (A16–A20 = `:161-165`); `vd1:52`; `vd2:210,215`; P15 `:155,190,339`.

## What the two kills cost the design
Nothing in §0's reconciliation, §1's parameters, §4's edge rows or §5's DECIDED lines is refuted on repo grounds. K1 moves a label and adds two owed deferrals to E1's preconditions; K2 deletes one row and re-points five references at `formulary_interactions_loaded`. A2/A3 turn two "create a runbook" lines into "append to the runbook that now exists"; A4 is the one structural question (15-ii vs 28b) the design must answer in prose rather than by citation.
