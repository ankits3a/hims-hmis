# P5-discharge-audit — verifier: EXISTS-OR-COLLIDES

Target: `wf/design/P5-discharge-audit.md`. Measured at `91c34fc` on 2026-09-08 11:32 UTC (`origin/main`; the `main-ro` checkout sits at `ff07cbc`, so anything newer was read with `git show origin/main:<path>`). PR states from `gh pr list --state all --limit 120`. Every design citation below was re-read at the line it names; a citation not listed here was checked and holds.

**Verdict: needs-amendment.** The five plan homes are correctly *free on the repo* (`15e`, `11l` grep-empty in `docs/`; `44`, `28d`, kernel-D match IDX:86, IDX:74, the plans dir). The refutations are of two kinds: (1) the design was written at `b04cbd9` and four of its "nothing exists" claims about the mini-OT were overtaken by #157/#158/#164 (merged 2026-09-07 04:02–05:09 UTC) before this pass; (2) three of its five epics are ALSO claimed by sibling pillars in this same brainstorm under different letters — the letter `11l` twice, the mini-OT opening three times, the `daycare.discharged` recall + procedure code twice. A reader of the six designs would meet two `11l`s and three OT runbooks. Those are kills on the *home*, not on the content.

---

## 1. KILL — `11l` is claimed by two pillars for two different things

- **P5-E2** — "`11l`: the breach ledger and the daily fact sheet" (design §2, D7).
- **P6-E7** — "`11l`: the milestone instrument and the session layer" (`wf/design/P6-absorption-track.md:66-67`, `:131`: `tools/milestones.sh → docs/STATUS.md`, heartbeat age, CI pin).
- **P6-E6** already takes the *next* letter, `11m` ("the silent three … census, a runbook, and a UAT rehearsal", P6:59-60: "next free letter after 11k/11l").

On the repo both are right that `11l` is free (`git grep -w 11l origin/main -- docs/` = 0 hits; `11k` is taken at `plans/2026-09-06-phase1-11i-the-stand-up-path.md:411`; `11j` at `ROADMAP-v2.md:469`). Inside the brainstorm the letter is double-booked and D7 ("Letters `15e` and `11l`… numbering never moves") is therefore false as written. One of the two must move; since P6-E6 holds `11m`, P5-E2 becomes `11n` unless the critic folds the two.

## 2. KILL — the mini-OT opening (census · runbook · UAT rehearsal · pilot) has THREE homes, and part of it is already merged

P5-E1 (`15e`) milestones M3/M4/M5 are the same work as:

| capability | P5-E1 | P3-E6 (`Plan 15`, `wf/design/P3-diagnostic-surgical.md:88-95`) | P6-E6 (`11m`, `P6-absorption-track.md:59-63,128`) |
|---|---|---|---|
| OT census rows | E1-M3: "module keys 4 → 5 · ≥ 8 rows" | E6-M1: "`standup:check ot` ≥ 6 rows; `deploy.sh` runs it" | E6: "`ot_definitions_published`, `daycare_package_services_priced`, `anaesthetist_role_held`" |
| the runbook | E1-M3: **`docs/runbooks/mini-ot-go-live.md`** | E6-M1: **`docs/runbooks/ot-go-live.md`** | E6-M2: "dated `## Executed` in **`ot-go-live.md`**" |
| UAT rehearsal | E1-M4: one case `booked → discharged` in a browser | E6-M3: seat drill on UAT with event ids | E6-M2: one case wheel-in → discharge on UAT |
| production pilot | E1-M5: first 20 discharges, p90 ≤ 120 min | E6-M4: 7 days empty harvest | (next quarter, P6 §7) |

And the repo has moved under all three:
- **`docs/runbooks/ot-go-live.md` EXISTS on main** — #157 "fix(ot): §OT-UNROSTERED — the page `seed-ot.ts` has been pointing at, and five census rows", merged 2026-09-07T04:02Z (184 lines; sections §0–§9 at `ot-go-live.md:15-173`, incl. §6 "The people" naming `ot_incharge`, `recovery_nurse` at `:46,148,150`, §7 "Money" — the package tariff at `:158`). The design's E1 "Exists" line says "no runbook (`docs/runbooks/` = four files, none OT)" — true at `b04cbd9` (`git ls-tree b04cbd9 docs/runbooks/` = 4), false now (9 files). Proposing a *second* file named `mini-ot-go-live.md` (E1-M3, D18, edge 7) creates two OT runbooks — the exact "runbook pointed at a document that does not exist" failure CONTEXT.md:42 warns about, inverted.
- **`standup:check` HAS an `ot` row set** — `apps/core/scripts/standup-check.ts:458-523` on `origin/main`: `ot_approval_types_registered` (:460), `ot_theatre_present` (:477), `ot_workflow_definitions_active` (:493), `ot_definitions_published` (:509), `ot_surgeon_held` (:520), `ot_anaesthetist_held` (:522). Module keys are now SIX (`hospital:138, lab:295, pharmacy:407, ot:458, pcpndt:544, radiology:595`), and #158 made the population `ALL_MANIFESTS` with compulsory classification. The design's "no `ot` rows … (`standup-check.ts:128,210,322,361` — hospital, lab, pharmacy, radiology)" and E1-M3's "module keys 4 → 5" describe `b04cbd9`.

What is genuinely still absent after #157 and belongs to *some* home: `ot_incharge`/`recovery_nurse` held-rows (the runbook names them at `:148,150` but the census checks only `surgeon`/`anaesthetist`), a package-tariff row (§7 prose only), a relay-reachable row, a `§Harvest` and a `§Executed` section (grep of `ot-go-live.md` for `harvest|executed` = 0). That delta is one milestone, and P3-E6-M1/P6-E6 already own its home. **P5-E1 should keep only what no sibling claims — E1-M2 (the two papers) and the discharge-specific harvest rows/decision→door line — and adopt P3-E6 / P6-E6 for M3–M5, citing #157 as the base.**

## 3. KILL — E1-M1 (procedure code + recall on `daycare.discharged`) is P4-E3-M1 under another letter

- **P5-E1-M1 (`15e`)**: "`daycare.discharged` has `procedureClass`+`procedureCode` … one `notifications` row key `daycare_followup` per discharge and per abscond".
- **P4-E3-M1 (`15-ii`, `wf/design/P4-monitoring-betas.md:54-58,92`)**: "M1 recall task at discharge and abscond + `procedureCode, procedureClass, woundClass, preOpScores` on the event (additive)"; acceptance "`dischargeDaycare` and `markAbsconded` each enqueue one follow-up task … `recovery.ts:549`'s comment becomes true".
- P2-E2-M3 (`P2-opd.md:57,104,132`) *consumes* the same seam (`post_procedure` anchored on `daycare.discharged` + review date) without claiming it — fine, but it is a third reader waiting on whichever of the two builds it.

Same event, same additive fields, same two write sites, two letters inside block 15 (`15e` vs `15-ii`). One must adopt the other; the block must carry one letter style (the index uses hyphen-letters only for lab extensions `17-E/17-M/17-H`, IDX:57; plain letters `14b/16c/18a` elsewhere — `15e` is the index's own style, so P4's `15-ii` is the odd one, but that is the critic's call, not this pass's).

## 4. AMEND — the recall on ABSCOND was refuted by a merged PR; the citation is stale

Design §0.5 and edge 4 cite "`markAbsconded`'s comment promises one (`recovery.ts:549`)". At `b04cbd9` `recovery.ts:549` read "A terminal with the bill issued as-is and a recall task". **#164** (merged 2026-09-07T05:09Z, `recovery.ts` +17/−3) rewrote it; `origin/main` `recovery.ts:549` now reads "A terminal state with the bill issued as-is; the cause recorded is…", and the PR body (`gh pr view 164`, body line 23) rules: "`markAbsconded`'s docstring promised *'a recall task'*. **No document asks for one.** Plan 15's DD2 lists `absconded` as a status and says nothing more … there is no task primitive anywhere in the kernel." CONTEXT.md:46 records the orchestrator upheld that refutation.

The *discharge* recall keeps its source — `plans/2026-08-28-phase1-15-mini-ot-daycare.md:339`: "`dischargeDaycare` (DD10 + follow-up recall task via the notifications/scheduler seam + `daycare.discharged`)" — and the design's mechanism (an `enqueueNotification` row, not a task) sidesteps #164's "no task primitive" objection. But "per abscond" (E1-M1 acceptance, edge 4 "**and** a recall is enqueued", D10) now needs a document that asks for it, or the abscond half goes. Re-cite: drop `recovery.ts:549`; cite P15:339 for discharge only. (P4-E3-M1 carries the same stale citation.)

## 5. AMEND — the fact sheet is composed in two places: P5's `kernel/ops` and P1's `kernel/copilot`

- **P5-E2**: `kernel/ops/fact-sheet.ts` `factSheetFor(day)` → `ops_fact_sheets` + `ops.fact_sheet_produced`; "the Digest Writer (12a) and Hermes (`digest.fact_sheet`) narrate it later".
- **P1-E3 (`12a-0`, `wf/design/P1-fabric-multilingual.md:49-53`)**: builds "`kernel/copilot` MCP controller … ten Class-0 tools with seeded-day fixtures; **`digest.fact_sheet` (H:60)**". The Hermes brainstorm it cites places the composer there too: `/opt/hmis/docs/superpowers/brainstorms/2026-09-01-hermes-ops-copilot/00-BRAINSTORM.md:113` — "`digest.fact_sheet` | the DPIA fact sheet for one hospital day, **composed from the above** | new, in `kernel/copilot`".
- DPIA:11 (the shape both cite) assigns the sheet to the Digest Writer, not to a module.

Neither design names the other. Two composers of "the DPIA fact sheet" is the two-homes case exactly. The seam that resolves it is one sentence P5 can own: `digest.fact_sheet(day)` is a *reader* of `ops_fact_sheets` (`SELECT facts WHERE day = ?`), never a composer; P1-E3 depends on P5-E2-M1. Related: **P6-E3** builds `pilot:harvest <module>` "a deterministic script printing the runbook's harvest rows with dates" (`P6-absorption-track.md:42,106`), while P5-E2-M4/D18 put "lab, pharmacy, OT harvest rows" on the sheet as sections — two instruments for the same six rows; one function, two printers, and the design should say which owns the function.

## 6. AMEND — `documents` is already a table name in the register, for a different table

E3 builds kernel-D's `documents` (KD DD1, `plans/2026-08-28-phase1-kernelD-document-chrome.md:92`: `documents(id, doc_no, doc_type, version, supersedes_id, status, patient_id FK, …)` — an issued clinical/money document). **R-260** (`00-OWNER-RULINGS-REGISTER.md:272`): "Expertise store: one **`documents`** table shared by human SOPs (28c) and agent playbooks (12a) — decide table sharing at 28c authoring; recommend shared with `audience` column". KD does not mention the Expertise store or R-260 (grep = 0), and E5 leans on that store (R-260) for its clinical rules. Two tables cannot both be `documents`; E3-M1's "migration +1 additive" must name the collision and reserve the name (or say the store's table is `expertise_documents`). Also under-cited in E3: **22cE** (authored, NOT APPROVED) already rules "the visit summary is a document" (`22cE-records-and-account.md:79` DD3, `doc_type='visit_summary'`), "the public page is kernel-D's" (:83 DD5) and `dsr_export` (:89 DD8), and is "gated on kernel-D (documents exist)" (:7) — a third and fourth adopter already written; E3's "two adopters" and its renderer contract should list them.

## 7. AMEND — the print-kind baseline is wrong (3 renderer cases, not 4)

E1 "Exists": "print outbox, four OPD kinds"; E1-M2: "renderer kinds · `render.ts` switch + parity test · **4 → 6**"; edge 32 "parity 6, not 5". The outbox's *kind union* has four members (`kernel/printing/enqueue.ts:56` includes `vitals_slip`; schema comment `printing.ts:42-43`), but `render.ts` has **three** `case` arms at both tips (`render.ts:660-662`: `opd_token_slip`, `opd_payment_receipt`, `opd_prescription`); `vitals_slip` "has no artboard" (`printing.controller.ts:190`) and a destination only (`enqueue.ts:39,84`). So the switch goes 3 → 5 and the union 4 → 6; the parity number in E1-M2 and edge 32 must say which it counts. (FD-27 "printing v2" is on the front-desk lane with no PR, CONTEXT.md:54 — the number may move again before E1-M2 rebases.)

## 8. NOTE — recall's other homes, and the 7-day default already in config

- The index gives "recalls" to **Plan 27** (IDX:73: "Programmes, renewal & patient CRM (consent gate, **recalls**, …)"); SPEC:784 names a T1 "Recall & Follow-up Agent"; PHY:485 puts a "Recall extension" in Plan 25 T7; #164's merged docstring calls the abscond record "the recall register's input on the day somebody builds one". E1-M1/D10 should say in one line that `daycare_followup` is a one-shot `enqueueNotification` with a new template key (keys today: `patient_welcome … imaging_report_ready`, `notify/templates.ts:52-165`; no follow-up key) and *not* the Plan 27 recall register, so 27 adopts rather than re-implements.
- D10 "else day 7" hard-codes what OPD already holds as config: `opdConfig.followUpDefaultDays` default 7 and `followUpExtensionDays` (`kernel/db/schema/opd.ts:24-25`), stamped per consultation as `followUpDays` (`:331`). Read the config; a second "7" is a second home for the hospital's follow-up interval.

## 9. NOTE — small citation slips

- §2 E4 "the registry `onRelease` cascade 15 already uses (`recovery.ts:486`)": `:486` is `await tx.update(daycareEncounters)`; the `onRelease: "cleaning"` comment and `releaseResource(...)` are `:483-484` (same at `b04cbd9` and `origin/main`).
- E2-M2 `pnpm ops:fact-sheet <day>`: no `ops:*` script namespace exists in `apps/core/package.json` (`standup:check` :30, `validate:config` :41, `check:config-present` :42); not a collision, just name it in the existing style or say it is new.
- Position line "`docs/runbooks/` = four files" and "no `ot` rows" — re-measure at `91c34fc` per CONTEXT.md:49 (nine runbooks; six census keys).

---

## What was checked and HOLDS (so the critic need not re-grep)

- Plan homes on the repo: `15e`, `11l` free (grep 0); `15b/15c/15d` claimed (P15:44,184; `schema/ot.ts:87-88`); `11j` RM:469, `11k` 11i:411; `44` IDX:86 (needs "41 live 30 d; 16 returns; 43b; R-190, R-192–R-195"); `28d` IDX:74 = Q22:537's "22d"; kernel-D authored 2026-08-28 and never built (no `pgTable("documents")`, no `kernel/documents/`, `git log --all --grep kernel-D` = docs only); no `…44…` or `…28…` phase doc in `docs/superpowers/plans/` (94 files listed).
- Existence claims: `fit_declared` 0 hits in `apps/core`+`packages`; `report.signed` not an event (only `signedAt` reads, e.g. `radiology/read.ts:427`); `audit.control_tested` 0 hits (Q22:125's "(exists)" is indeed false); `sla.breached`/`slaBreached` referenced in exactly five files (`workflow/events.ts`, `timers.ts`, `approvals/events.ts` comment, two tests) — no reader; `daycare.discharged` has zero consumers outside `modules/ot` at `origin/main`; `PROTOCOL_GATE_EVENTS`, `protocol_rules`, `fact_sheet`, `daycare_followup` 0 hits; `kernel/ops/` holds `mode|downtime-kit|interfaces|validate` only; `aerb/events.ts` defines `aerb.licence_filed`/`aerb.licence_status_changed` and no refusal event while `device_not_licensed` is thrown at `aerb/licences.ts:403` — the E2-M3 twin is a real gap; `pharmacy` has no returns file; `handoffDocumentId = newId()` still at `recovery.ts:525` after #164; `procedureClass` is on the list event (`ot/events.ts:65`), not on `daycare.discharged` (`:84-86`).
- Every gate-event name E2-M3 lists exists as a `defineEvent`: `gate.overridden` (`ot/events.ts:168`), `timeout.halted` (:154), `count.mismatch` (:162), `sod.violation_blocked`/`break_glass.used`/`emergency_elevation.used` (`kernel/auth/events.ts:16,5,28`), `lab.sod_violation_blocked` (:243), `lab.report_print_blocked` (:255), `lab.tube_mismatch_flagged` (:80), `imaging.critical_overdue` (`kernel/alerts/manifest.ts:48`), `prescription.issued` with the three `…OverrideCount` fields (`opd/events.ts:330,333,341-342`), `charge.orphan_flagged` (`billing/events.ts:157`), `escalation.triggered.fallbackExhausted` (`workflow/events.ts:45`).
- Doc lines: SPEC:125,196-199,201-204,256,460,778,784-785; RM:34,183-191,334-342,375,405,415-420,428,461-470; REG formula `12+nnn` holds for R-111(:123), R-126(:138), R-190(:202), R-192(:204), R-260(:272), R-262(:274); IPD:46,97-113,163,241,438-442,458-463,470,596; Q22:125,167,460,537,541; A12:393; PHY:90 (`daycare.discharged` with procedure code); K11:459 (`discharge_ready_to_discharged`); DPIA:3,11,38; plan-series:431,463; P15:172,201,339; KD:40-46,92,104; CLAUDE.md:38; hermes:60,113; `lab-go-live.md:207-218`; `cfba8d5` is the 17b snapshots commit; `0035_mute_vision.sql` is the OT migration (89 `ot_|daycare` hits).
- Code lines: `recovery.ts:277-282,443-457,462-480,483-487,509-545,552-582` (at `b04cbd9`; #164 shifted nothing the design cites except the `:549` comment text); `ot/events.ts:84-86`; `workflow-def.ts:29-31,88`; `timers.ts:13,86,122,150-165`; `workflow/events.ts:20-21`; `approvals/events.ts:6`; `aerb/errors.ts:19`; `config.ts:53-54`; `jobs.ts:199-207`; `schema/workflow.ts:56,67-80`; `schema/events.ts:28-29`; `printing.ts:38-46`; `enqueue.ts:66-73`; `notify/events.ts` (sent :17, failed :30, suppressed :43, expired :54); `schema/ot.ts:526,543`; `router.tsx:233,931-942`; `daily-close.ts:295-306` (OPD consult fee only); `desk/rollup.ts:9-25` + `user_day_facts` (`schema/desk.ts:36`); `validate-config.ts` report row + event (:30-38 at `b04cbd9`); `schema/ops.ts:79`; `lab/definitions.ts:22-24`; `radiology/chasers.ts:87-91`; `rx-checks.ts:178-186`; `kernel/approvals/{flow,worklist}.ts`; `seed-ot.ts:64-73`; `standup-check.ts:23-75`; `seed-roles.ts` holds `ot_incharge` (:871) and `recovery_nurse` (:915).
