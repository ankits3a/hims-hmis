# VERIFY — P2-opd, lens EXISTS-OR-COLLIDES

Target: `wf/design/P2-opd.md`. Checked against `main-ro` (HEAD `ff07cbc`, 28 commits past the design's stated base `b04cbd9`; `b04cbd9` is an ancestor). Four cited code files drifted since `b04cbd9` (`kernel/db/schema/lab.ts` +20, `modules/lab/events.ts` +52, `modules/lab/manifest.ts` +21, `modules/pharmacy/handover.ts` +41); every citation into those was read at `git show b04cbd9:…` and holds there. Everything else was read at HEAD. Plan list: `ls docs/superpowers/plans`; PR list: `gh pr list --state all --limit 120` (#51–#170).

**Verdict: needs-amendment.** Nothing has to die. Seven claims are wrong on repo grounds (four "what exists" citations, two plan-home collisions the design does not name, one milestone that restates a reader that already exists). Every "absent by grep" claim was re-grepped and is absent. The plan letters `07e`, `07f`, `16b-ii`, `17c-ii`, `16c-ii` do not appear anywhere in `docs/` and collide with no authored plan or PR title.

---

## A. Refutations (amend)

### A1 — E5 cites 18a-iv as a "mirror"; 18a-iv is on `main` and DECIDED the opposite door
- Design (E5, §2): *"Mirrors 18a-iv's 'ordering door' (`origin/lane/radiology-18a-iv-plan`, FOR APPROVAL)"*; E5-M1 *"The doctor can order … `placedBy` = the consult doctor"*.
- Repo: the doc is on `main` as `docs/superpowers/plans/2026-09-06-phase1-18a-iv-radiology-ordering-door.md` (merged #143, DOC ONLY, code held — CONTEXT.md:37); `git branch -r | grep 18a-iv` returns nothing. Its decisions: **D1** *"The door is RADIOLOGY RECEPTION's, not a new screen"* (`:60-62`); **D2** *"The advised line is a SUGGESTION, never an automatic order. The receptionist confirms it … the patient may decline, defer, or go elsewhere. Auto-placing on consultation close would bill people for scans they never had"* (`:63-66`); T2 *"The reception screen … calls `POST /radiology/orders`"* (`:92-95`). And 07d **DD4** is DECIDED: investigations *"print as advice … creates no order, books no sample"* (`2026-08-28-phase1-07d-doctor-cockpit.md:131-135`). The design itself lists this as a *blocker* in §0 (07d:133-134) and then makes the doctor the placer in E5-M1 without naming the reversal.
- So the lab order already has ONE door (17a `advisedTestItems` → `lab/desk.ts:408-421` places and bills in one tx; 17c seat 1), the sibling department just ruled its second door belongs to reception for a money reason (the bill), and the design's E5-M1 opens a second lab door at the doctor's seat citing the sibling as support. **Amend:** drop the "mirror"; cite the file on `main`; either argue the reversal of 07d DD4 / 18a-iv D2 explicitly (who consents to the invoice the seat creates — NEW-P2-1 covers *when* money moves, not *whether* the patient agreed to the item) or reshape M1 as a floor/desk door keyed on `advised_tests` (18a-iv's shape, at the room).

### A2 — E1/E2 absorb Plan 22's items 2 and 7 without saying so (two homes)
- Design: 07e = wait predictor + `doctor.delay_declared` + template; 07f = `slot_minutes_by_type` on `opd_config` + overbooking band per R-216; DECIDED 22 *"Plan 22 stays procurement; nothing is renumbered"*.
- Repo: index §3 gives **22** *"Front office at scale (appointments v2 …)"* (`00-INDEX-AND-SYNTHESIS.md:68`); doc 20 puts in Plan 22: *"Appointment book v2 (overbooking, waitlist, confirmations, reminders, **delay declarations**, leave cascade execution)"* with tables **`appointment_policies`** (`doctor_id | department_id, slot_minutes, overbook_pct, overbook_cap_pct, …`, versioned config — `20-front-office…md:219`) and **`doctor_delays`** (`:222`), events `doctor.delay_declared`, `doctor.delay_cleared`, `overbooking.applied` (`:131`), the delay semantics (`:133`), and item 7 *"Wait-Time Predictor (T0)"* (`:706`). R-216/R-217/R-218 carry plan column **22** (`00-OWNER-RULINGS-REGISTER.md:228-230`). ROADMAP §5 excludes 22 because *"PBX, kiosks, gateway are procurement"* (`ROADMAP-v2.md:414-416`) — item 2 and item 7 need no procurement.
- The design names item 7 and 22c-D T5 (`22cD:149-151`, `delay_note` on the session — not built: grep `delay` in `modules/opd` finds only the 07c T6 comments at `events.ts:208`, `sessions.ts:169`), but not item 2, `appointment_policies` or `doctor_delays`. Index rule 3: *"A component two documents both claimed gets one home and the other adopts it."* **Amend:** state that 07e/07f absorb Plan 22 items 2 and 7 (name the two tables and three events), that Plan 22 adopts, and that R-216/R-217/R-218's plan reference becomes 07f. Otherwise a future 22 author builds `appointment_policies.slot_minutes` beside `opd_config.slot_minutes_by_type`.

### A3 — "`sla.breached` write-only … its first reader" overstates: the waiting breach already reaches a human
- Design §0: *"nothing reads them (Plan 21 absent; `sla.breached` write-only)"*; E1 goal *"give `sla.breached` its first reader"*; E1-M4 *"Someone reads the breach"*.
- Repo: `kernel/workflow/timers.ts:119-140` appends `sla.breached` and, for `alerting: "active"` states with a ladder, schedules escalation timers → `escalation.triggered` → `kernel/alerts/consumer.ts:8,175-179` parses it and resolves `usersHoldingRole` recipients. The OPD `waiting` state IS such a state: `sla: { minutes: 45, alerting: "active", escalation: [{ afterMinutes: 15, toRole: "front_office_supervisor" }, { afterMinutes: 30, toRole: "duty_manager" }] }` (`modules/opd/workflow-def.ts:21`). Strictly, no code consumes the `sla.breached` *event*; but the breach on `waiting` is already read by the FO supervisor and duty manager through the alert ladder. What is absent is the aggregate (the S10 `1 − breaches ÷ visits` number, the tile) and any reader of the event as data. **Amend:** rename/rescope E1-M4 to the aggregate and cite the existing ladder as the reader that exists; Plan 21 is the KPI registry (`00-INDEX:64`), and the tile's home vs 21 should be named (the design's `user_day_facts` precedent at `kernel/desk/rollup.ts` exists — fine).

### A4 — E3-M1 repeats 16a's stale "12 manifests" as the number to re-base to
- Design E3-M1: *"§0 lists the re-based seams with SHAs (census 12 manifests, migration head at rebase, `RxOverride` shape, `16a:383-386`)"*.
- Repo: 16a's CLOSE said twelve on 2026-08-26 (`16a:387`). `ALL_MANIFESTS` at `ff07cbc` has **22** entries (`kernel/modules/manifests.ts:62-179`: auth … pharmacy). **Amend:** "census N manifests, measured at rebase" (22 today), not 12.

### A5 — E3 places the snapshot card in 16b without naming 12b's claim on it (two homes)
- Brainstorm 12 §14: *"Plan 12b — … formulary + Context Assembler + snapshot card (copilot Phase A, likely two plans …). Gate: 12a live 30 days"* (`12-agentic-copilot-layer.md:622`); repeated in `PROJECT-BRIEF-2026-08-30.md:1442`. Index §3: *"12a/12b/12c … unchanged"* (`:57`). The authored 16b plan (`2026-08-25-phase1-16b-…md:1-4`, PROVISIONAL) and the copilot spec (*"zero inference, zero DPIA gate"*, `copilot-design.md:172`) put it in 16b with no 12a gate. 16a (the formulary half of the same 12b line) is already built and deployed, so 12b's claim is half-moot by fact — but the card half is still claimed twice. **Amend:** one DECIDED line — the card's home is 16b; 12b adopts; 12b's "12a live 30 days" gate does not apply (spec §172).

### A6 — E5-M1's instrument names an event and a field that do not exist
- Design E5-M1: *"`lab.order_placed` rows with `placedBy` = the consult doctor, `orderingClinicianId` = self"*.
- Repo: no `lab.order_placed`. The kernel event is `order.placed` (`kernel/orders/events.ts:47-59`) with payload `{orderId, orderNo, kind, patientId, encounterNo, groupId, itemIds}` — no placer field (the envelope's `actor` carries it). Lab's own is `lab.order_desked` (`modules/lab/events.ts:57-61`) — no placer either. `orders` has `ordering_clinician_id` and `authority` (`kernel/db/schema/orders.ts:124,134`); no `placed_by` column (grep = 0). **Amend:** instrument = `order.placed` envelope actor (or `orders.created_by` if the seat writes one) + `orders.ordering_clinician_id`.

### A7 — E6-M3 cites 22a1 for payment links and invents a `channel` value 22a1 forbids
- Design E6-M3: *"`payment.received` (`billing/events.ts:34`) with `channel='upi_link'` … zero `tender='cash'` … 22a1 adapter (authored, not approved)"*.
- Repo: `payment.received` payload is `{receiptId, invoiceId, patientId, amountPaise}` (`modules/billing/events.ts:33-36`) — no channel, no tender. 22a1 **DD1**: *"`channel ∈ counter | online | kiosk` — three values from the start"*; tender `mode ∈ cash | upi | card` (`2026-08-28-phase1-22a1-payment-money-in.md:71,96`). **Payment links are 22a-2's scope**, not 22a-1's: *"next | 22a-2 | … payment links, degraded mode"* (`22a1:55`). **Amend:** depend on 22a-2 (or say 22a-1 + the link task pulled forward), use `channel='online'` + `mode='upi'` on the receipt-tender row (`receipt_tenders`, `22a1:69`) as the instrument, not the event.

---

## B. Notes (minor; fix in passing)

- **N1 — 07e/07f beside the FD-n series.** Free by grep, and index §3 does not govern 07's letters. But the OPD desk has been numbered FD-1, FD-7, FD-24…FD-28 since 07d (FD-26/27/28 are 17 unpushed commits on the front-desk lane, CONTEXT.md:23). "07e" restarts a series the lane left; say why (07x = the encounter engine, FD-n = the desk) or use the live series.
- **N2 — E2-M1's desk half exists.** FD-17 already says new/revisit/renewal *before the patient is seated* from `GET /opd/continuity`'s `wouldBe` (`modules/opd/continuity.ts:31,147`; `apps/web/src/screens/desk-one/stages.tsx:1035-1066`; `lib/walk-in-routing.ts:22-26`); FD-18 is the billing override that corrects the type (`desk-one.tsx:642`). The design credits `continuity.ts:147` but not that the desk renders it. New = the appointment column + booking-time call + self-booking (22c-C T5, `22cC:139-146`, NOT APPROVED).
- **N3 — E6-M1 "Rx ahead of the patient" is largely 16c's existing behaviour.** The queue row is created on `prescription.issued` (`pharmacy/queue.ts:21-30`; `events.ts:13-16` `source: "prescription_issued"`), `GET /pharmacy/queue` lists it and `POST /pharmacy/dispenses {dispenseId, door}` claims it with no scan (`pharmacy-counter.controller.ts:71-118`; `claim.ts:118-122`) — so verify-before-arrival is possible today. The 3-day build is unnamed, and the instrument's "first counter scan" has no event (`findAtCounter` → `ensureQueued` returns the existing row silently). Name the scan event as the build.
- **N4 — E1-M1's grep.** `waitingCount *` has four hits in `apps/web/src`: the two pace sites (`desk-one/model.ts:239`, `lib/walk-in-routing.ts:56`) plus two bar-width multipliers (`desk-one/overlays.tsx:203` ×10, `desk-one/stages.tsx:875` ×11). Pin the grep to `* d.avgConsultMinutes` / `* s.avgConsultMinutes` or the acceptance fails on the bars.
- **N5 — Runbook not named.** E1-M1/E2-M3 want a dated `## Executed on UAT` line; the runbook exists — `docs/runbooks/opd-go-live.md` (six runbooks on `main`). Name it (CONTEXT.md:42 lesson: a pointer to a runbook that does not exist stranded a module).
- **N6 — "no relay is installed anywhere (`position-state.md:59,69-70`)".** Those lines say 16c is deployed-inert and FD-24/FD-25 are deployed; the relay fact is at `position-state.md:98` (FD-27's "relay runbook") and memory `plan-fd27-printing-and-duplicate-bills`. Re-aim.
- **N7 — `16a:388`.** "Production still holds ONE prescription" is `16a:390`; and it is a 2026-08-26 fact about a production that has since run `399f92c` — say "at 16a's close".
- **N8 — E4 12c is a P2-shaped subset of 12c.** 12c per `12-agentic-copilot-layer.md:624` = Conversational Work Surface + Lane-2 worklists + Lane-3 copilot + Lens gates 1–5 + Honcho + override mining + *"voice carve-out closed or kept"*, gate *"Plan 13 live; Plan 19 live for the cohort; DPIA L1 signed"*. The design's 12c keeps the voice carve-out and the Lens and drops the rest — fine if another pillar owns the rest; say so. E4-M2 (voice search switch-on) is 11h's carried item (`11h:686-689`: F6's DPIA hard gate; three Hinglish recordings), not a 12c build — the design's E4-M1 does say "11h's open item"; make M2 say it too. DD11 control 5 ("Off at open counters", `11h:350-352`) is correctly cited.
- **N9 — 16b-ii vs 16e.** Doc 16:569 already assigns one 16b extension (a dispense-history section on the card) to **16e**. 16b-ii's scope (F22 event, scribe events, `rx_templates`) does not overlap, and no doc places prescription templates anywhere (07d, 16a, 16b, doc 05, doc 12: grep `template|favourite|order set` = 0 in the Rx sense) — 16b-ii is genuinely unclaimed. Name 16e's card extension so two letters extending 16b are visibly disjoint.
- **N10 — 17c-ii is a lab letter for an OPD screen.** 17c/17d/17-E/17-M/17-H are the 17 family (`00-INDEX:57`); the sixth seat is `opd-consult.tsx`. No collision (grep `sixth seat|consult room` in 17c/17d plans and the LIMS handoff = 0), but the module that owns the screen is `opd`. Either name the split (server in `lab`, screen in `opd`) or home it in 07.
- **N11 — Plan 15's follow-up recall is prose only.** Plan 15 DD10 says `dischargeDaycare` *"enqueues the follow-up recall task"* (`2026-08-28-phase1-15-mini-ot-daycare.md:156,339`); `modules/ot` has only the N9 absconded recall (`events.ts:99`, `recovery.ts:549`), no follow-up task. E2-M3's `post_procedure` anchor is therefore unclaimed — worth saying under the "what already occupies that name" rule.
- **N12 — Base SHA.** The design says "read against `main-ro` @ `b04cbd9`"; the checkout is at `ff07cbc`. Cited lines in the four drifted files are correct at `b04cbd9`; a reader at HEAD will find `lab/events.ts:77`, `:209`, `lab/manifest.ts:104`, `schema/lab.ts:292-294/316-319/383`, `pharmacy/handover.ts:31-35` shifted. Keep the SHA pin; add "re-measure at kickoff".

---

## C. Verified as stated (no finding)

- Plan homes free by grep: `07e`, `07f`, `16b-ii`, `17c-ii`, `16c-ii` (0 hits in `docs/`; no plan file, no PR title); `12c` reserved and unbuilt (`00-INDEX:57`; `12:624`); Plan 21 absent from code except comments (`partners/sources.ts:54`, `billing/charge-rules.ts:80`).
- Absent by grep, as claimed (0 hits in `apps/core/src`, `apps/web/src`, `packages`): `doctor.delay_declared`, `visit_type_expected`, `slot_minutes_by_type`, `post_procedure`, `rx_templates`, `rx.template_applied`, `rx.warning_heeded`, `scribe.suggestion_*`, `lab_label`, `dispatched_to_room`, `carrier_user_id`, `agent_runs`, `pilot.harvest_read`, `desk.opd.wait_sla`, `appointment_delayed`, `appointment.overbooked`, `no_known_allergies`, PHI surface `briefing`, `opd:wait-backtest` (package.json has only `seed:opd`).
- Exists, as claimed: `opd_config.slot_minutes` default 10 (`schema/opd.ts:23`); per-template `slot_minutes` (`:103`); `avg_consult_minutes` default 6 + "THIS COLUMN'S READ" (`:48-50`); no type on appointments (`:138-158`); `opd_appointments_slot_ux` partial unique (`:171-173`); `consult_started_at/completed_at` (`:334-335`); `eligible_at/called_at` (`:380-381`); `classifyVisit` (`visit-type.ts:9-13`) called at `encounters.ts:110`; `reclassifyVisit` (`:652`); `visitType` enum `new|revisit|renewal` (`opd/events.ts:321`); `admissionAdvised` (`:324`); three override counts (`:333-342`); `queue_session.opened{scheduledStart}` (`:294-296`); `appointment.no_show` swept 23:55 IST (`kernel/worker/jobs.ts:31,169`); `onLeaveToday` (`queue.ts:344`); the 20-minute rule (`walk-in-routing.ts:30-38`); the two pace multiplications; three history tabs (`opd-consult.tsx:170`); `runRxChecks` engine and its pharmacy call site (`prescriptions.ts:257-260`; `pharmacy/verify.ts:185-205`); `speech.controller.ts:33` defaults `en`, `:40-44` Class-2 note; `consult-scribe.tsx:30-44` controls, `:91` posts `{audio}` only (FD-25 screen 5, header `:7`); `voice-flag.ts:6-27`; `SpeechClient.transcribe` only (`kernel/inference/types.ts:9-11,32-33`) and no non-speech importer (`radiology/drafter.ts:9` mentions it in a comment, imports none); `place.ts:256-262` agent refusal, `:294-298` both-permissions rule; `lab/manifest.ts:104` `lab.orders.place`; `collect()` site-generic, chair-free (`collection.ts:308-333`); five sites (`schema/lab.ts:316-319`; `lab-desk.controller.ts:38`); consent pair (`schema/lab.ts:292-294`); `label_source` CHECK (`:383`); `lab.label_printed` (`lab/events.ts:70`); `lab.report_print_blocked` (`:209`); `lab_specimen.labelled` SLA 120 min active (`lab/workflow-def.ts:159`); `RESOURCE_KIND_VALUES` has no chair/printer (`schema/resources.ts:86-88`); renderer's three OPD cases (`kernel/printing/render.ts:659-661`; `vitals_slip` is named at `schema/printing.ts:42` but has no case); outbox + lease (`schema/printing.ts:13-15,37-42,97-103`); `pharmacy_dispense` states (`workflow-def.ts:33-39`); D7 hand-over rule (`handover.ts:31-35`); `dispense.line_declined` (`events.ts:36`), `dispense.handed_over` (`:57`); `daycare.discharged` (`ot/events.ts:84`); `tariff_item_missing` (`tariff/pricing.test.ts:146`); workflow definitions are versioned and instances pin one (`schema/workflow.ts:11,23,48`); `check:config-present` script (`apps/core/package.json:42`; ROADMAP G2 `:361`); `kernel/desk/rollup.ts` + `user_day_facts`; `escalation` roles static (`kernel/workflow/roles.ts:30-33`); templates render `hi|en` (`kernel/notify/templates.ts:20`); `opd-display.test.tsx:262`.
- Docs, as cited: 22c-D DD4 (`:84`), T5 (`:149-151`), CLOSE empty (`:181`); 22c-C S1/DD5 (`:62,81`), NOT APPROVED (`:3`); 16b PROVISIONAL (`:3`), CLOSE empty (`:166`); 16a F22 (`:378-381`), seams moved (`:383-389`); plan-series note 12 (`:463`); 07d DD4 (`:131-135`); 16c D6/D7/D8/D9 (`:43-46`), R-1..R-5 (`:95-101`); fd7 leave lesson (`:344-352`); 11i §2b (`:81`); doc 20 L1 (`:26`), predictor row (`:584`), one-tap (`:615`), item 7 (`:706`); index 22/22b (`:68`), theme 4 (`:144`); R-059 (`:71`), R-002/R-020/R-128/R-131/R-216/R-217/R-218/R-244/R-250/R-255/R-261/R-262 rows; doc 02 payment branch (`:95-98,112`), label at chair (`:482`); doc 16 Rx-ahead (`:459`), 8-min TAT (`:469`), 16d/16e/16f (`:535-537`); copilot laws 4 (`:29`), citations (`:114`), five gates (`:125-129`), Phase A (`:172`), SaMD (`:192`); spec §16 T2 cap (`:778`), T2 roster without an Rx drafter (`:785`); KPI S10 (`:40-42`); ROADMAP 498 events (`:129`), sequence (`:326-339`), §5 (`:414-416`), no 12a (`:428`), S-gate (`:482-486`); DPIA sign-off table (`:66-70`); lab runbook §9 harvest (`:209-218`); 12 §9.6 on-prem ASR for clinical narratives (`:513-515`), §9.12 ≥ 85 % (`:545`); doc 10 card 42 scribe post (`:65`); 22a1 NOT APPROVED (`:3`); `rulings-gates.md:15,147` as quoted.
