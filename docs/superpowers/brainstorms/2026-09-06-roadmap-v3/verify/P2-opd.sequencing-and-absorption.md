# VERIFY · P2-opd · lens: sequencing-and-absorption

Target: `wf/design/P2-opd.md` (181 lines). Read against `main-ro`, whose HEAD is **`ff07cbc`** (`git rev-parse` in this pass) — not `b04cbd9` as CONTEXT.md:4 says. `ff07cbc` = #155 (2026-09-07 03:31 UTC), which adds `docs/runbooks/opd-go-live.md`; it does **not** contain #157 (the OT runbook, merged 04:02 UTC). The design was written at `b04cbd9` (21:46 UTC 09-06), before the OPD runbook existed, so the design could not have cited it — but the runbook is now on `main` and it changes the design's premise about the OPD's G5. Also read: ROADMAP v2 §0c–§8, `00-INDEX-AND-SYNTHESIS.md` §3–§4, P1/P3/P6 designs, `wf/understand/{position-state,rulings-gates}.md`, and `gh pr view 143/157` (read-only). Every claim below re-grepped in this pass. Paths repo-relative to `main-ro`.

**Verdict: needs-amendment.** One premise is refuted outright — the design's definition of the OPD's G5, and the claim that "production OPD data" arrives at wk 4–6 "with no build, no human act", when the first production patient sits behind a three-person Class-A ceremony, a doctor of record, and an active tariff version that no one has activated. Around that: the design breaks its own "absorb before build" rule in §7(b); mislabels three human-held gates; calls E5 a "mirror" of a sibling decision it contradicts; sizes E6-M3 and E4-M4 on top of two unscheduled plans (22a1, 12a); touches ≥ 12 CLAUDE.md "files that belong to everyone" with no coordination line; and is missing the deploy-day / role-unheld / config-before-guard rows in five places, while one row (#20) and one milestone (E5-M3) pin a workflow state that does not exist.

## Checked and sound (do not re-check)

- ROADMAP citations resolve as claimed: `:129` (498 events), `:337` (front desk's six screens open on production wk 4–6), `:414-415` (19a not this quarter), `:428` (no 12a; scribe inert at 503), `:482-486` (S-gate).
- Kernel workflow versioning is real: instances are pinned to their definition version (`kernel/workflow/instances.ts:34-36,88`), activation retires the prior version (`definitions.ts:216-217`; `events.ts:16 retiredVersion`). E6-M2's "in-flight instances finish on their version" is supported as written.
- `room` is a registry kind (`kernel/db/schema/resources.ts:86-88`); E5-M2's FK target exists. Adding a kind would be a kernel edit (`:82-84`) — the design correctly does not add one.
- The `doctor` role already holds the placer pair `lab.orders.place` + `orders.place` (Plan 17 T2 DD16, `apps/core/scripts/seed-roles.ts:232-253`: "this is the doctor placing the order themselves"). E5-M1's grant exists; only the seat route does not.
- Sequencing of E5 M1/M2/M4 after the lab's G6 closes (§7 Q1 2027) matches ROADMAP §0c.5's rule (`:176-181`), and 17c-ii/16c-ii/12c are placed outside this quarter as §5 requires (`:412-436`).
- Print relay: `print_jobs.destination` is a logical destination the relay maps (`kernel/db/schema/printing.ts:49-56`), visibility timeout at `:97-103` — row #23's mechanism is real.
- The 22c-D DD4 rule ("position, not minutes", `plans/2026-08-28-phase1-22cD-appointment-journey.md:84`) and T5 ("running late", `:149`) are as cited; E1-M2 pulling T5 out of a NOT-APPROVED plan (`:3`) is stated.

## Refutations

### R1 · KILL — "the OPD's G5 is FD-25's six screens opening behind the census, wk 4–6" (§6 last bullet; §7(a) "baselines accrue … no build, the events exist"; every G5/G6 row in E1/E2/E3)

**Claim in the design:** the OPD passes G5 when the six screens open on production (wk 4–6, `ROADMAP-v2.md:337`), production OPD data then accrues by itself, and 07e/07f/16b can start in Q1 2027 "once 30 production OPD days exist" with "Human act at each start: 07e — none" (§7).

**Refuted on four facts:**
1. **G5 is a rehearsal on UAT with a dated section, not an opening on production.** ROADMAP §3 (`:369-370`): "G5 rehearsed on UAT — the runbook's seat walk-through and drills, done by a real person at each seat … a dated `## Executed` section appended to the runbook." `:337` is the front desk *opening* (G1–G4); it is not G5. The design conflates the two.
2. **The OPD runbook that now exists has no walk-through, drill, pilot or harvest.** `docs/runbooks/opd-go-live.md` (#155, on `ff07cbc`): sections are §0 activation, §1 preconditions, §2 the ceremony, §3 department + doctor of record, §4 the people, §5 charges, §6 not covered (`grep -n "^## "`); a grep for `executed|walk-through|seat|drill|pilot|harvest|shadow` returns **nothing**. §6 (`:174-177`) says explicitly: "Appointments, the queue and the display board — [not covered] — none of them can be exercised until §2 is done." So every P2 acceptance that reads "dated `## Executed on UAT` line" (E1-M1, E2-M3, E3-M2, E5-M1) names a section no runbook has a place for, and every P2 G6 row for the OPD (E1-M3, E1-M4, E2-M4, E3-M4) names a harvest nobody has defined. P6 defines G6 closes for the lab (wk 9) and pharmacy (wk 13) only (`wf/design/P6-absorption-track.md:17`); no design defines the OPD's harvest.
3. **The first production patient is behind a three-person Class-A ceremony the design never names.** `opd-go-live.md:9-12`: "Activate the `opd_visit` workflow definition. Until you do, the hospital cannot register a single patient." `:46,57-58`: three people at hospital scope — `opd_admin` drafts (not the owner: `:61-65` "the trap that has no way out"), `medical_superintendent` approves, `owner` approves and activates; two **distinct** approvers (`:105-106` `duplicate_approval`). The census confirms "No seed can do this: it is Class A" (`apps/core/scripts/standup-check.ts:214`). Production has one full administrator (CONTEXT.md:21). This is the "establish-once" family CONTEXT.md:38 names as the reason seven departments were uncommissionable.
4. **A consult cannot be priced without an active tariff version, and none is active.** `opd-go-live.md:167-170`: `chargeRules.opdConsult` services "must be priced in the active tariff version"; otherwise `tariff_item_missing` "at the counter, in front of a patient". `apps/core/scripts/check-config-present.ts:19-22,43`: an active tariff is "false in production today" and deliberately not checked by the deploy gate; `wf/understand/rulings-gates.md:14`: O6 "OPEN, unchanged since 24 Aug". Activating any tariff on production — placeholder GST rows (`seed-tariff.ts:33-100`) or CA-signed — is a human act with money on it. The design's own ruling table lists O6 as unblocking "every priced seat order (E5-M1) and pharmacy invoice" but **not** the consult fee that every 07e/07f baseline depends on.

**Consequence:** "30 production OPD days" is not a calendar wait; it is (a) two named humans distinct from the owner (`opd_admin`, `medical_superintendent`), (b) the Class-A ceremony, (c) a doctor of record (`active_doctor_in_a_department`, `standup-check.ts` hospital rows), (d) `front_office` + `cashier` held (`opd-go-live.md:150-160`), (e) a tariff version activated, (f) a walk-through that does not exist written and executed on UAT. Every Q1 2027 date in §7 derives from (a)–(f) and none is named. **Replace** the §6/§7(a) sentence with the six acts and their owners; add the OPD walk + harvest as a P2 (or P6-E3) deliverable that every P2 G5/G6 row depends on.

### R2 · AMEND — §7(b) builds E3-M1 + E3-M3 in wk 1–4, against the design's own rule and on a lane two designs already claim

§6's last bullet: "Building any P2 epic before its department has opened (G5) on UAT — absorption before building." §7(b): E3-M3 (3 d) + E3-M1 (1 d) "by the front-desk lane before it joins S at wk 4". The OPD has no G5 artefact (R1) and at best opens wk 4–6. ROADMAP §2 gives F: wk 1 "FD-25 backlog, printing v2" (`:334`), wk 2 "FD-25 close review" (`:335`), wk 4–6 "F closes its lane after the FD-25 close; F's capacity joins S" (`:337`). P6-E3-M1 also puts the front-desk runbook (2 d) on that lane in wk 1 "after it pushes FD-26/27/28 (renumber `0075`)" (`P6:103`), and CONTEXT.md:41 says the lane has NO PR, two serials to regenerate, and "will push only on the owner's ask". Three claims on one lane in the same weeks, unreconciled. Either say why E3-M3 is exempt from §6 (deterministic events, 3 d, no screen) and name the lane collision, or move it to Q1 2027 with 16b-ii.

### R3 · AMEND — E3-M1 "the orchestrator approves (owner-authorised)" is the owner's line in practice

CONTEXT.md:39: the orchestrator HOLDS #117–#122, 18a-iv code and "Plan 20 BUILD also waits on the owner's approval line (authoring is done)"; #143 merged "as a DOC ONLY". The orchestrator's practice is that a phase doc's approval is the owner's one line, whatever the mission brief's blanket authorisation says. 16b is PROVISIONAL (`plans/2026-08-25-phase1-16b-context-assembler-snapshot-card.md:3`). Label E3-M1's human act "owner: one approval line (as Plan 20's)" and stop treating it as 1 d of lane work.

### R4 · AMEND — E5 "mirrors 18a-iv's ordering door"; it is the opposite of what 18a-iv decided

`docs/superpowers/plans/2026-09-06-phase1-18a-iv-radiology-ordering-door.md:63`: "**D2 — The advised line is a SUGGESTION, never an automatic order.** The receptionist confirms it." `:115` (out of scope): "Any change to the doctor's advise picker." 18a-iv's door is the *reception* reading the doctor's advised lines; E5-M1 has the *doctor* place the order from the seat (`placedBy` = the consult doctor). That may be right for the lab — Plan 17 T2 DD16 already grants the doctor the placer pair (`seed-roles.ts:232-253`) — but it is a divergence from a sibling decision that is FOR APPROVAL and held by the orchestrator (CONTEXT.md:37,39), not a mirror. Say "diverges from 18a-iv D2 because the lab's role model already makes the doctor a placer and no indication is required," and note that if 18a-iv is approved as written, two departments will answer "who orders?" differently from the same consult screen.

### R5 · AMEND — E2-M3 "Plan 15 opening" hides O6, six names and a second Class-A ceremony, and no calendar holds it

ROADMAP §2 has no row that opens the mini-OT; §5 `:418` treats "15 opens for real" as a future fact. `opd-go-live.md:178-180`: "`daycare_case` and the OT's second definition are **also Class A and also have no runbook and no census row**" (the runbook arrived after `ff07cbc` as #157). P3-E6 is the only design that schedules it and gates it on "owner names the six (F4); prices are O6's, production only after" (`wf/design/P3-diagnostic-surgical.md:92`, `:174`). E2-M3's `depends_on` "Plan 15 opening" and §7's "07f-M3 — Plan 15 opened" must name P3-E6-M1..M4, the six named OT humans, the `daycare_case` ceremony, and O6 — or drop `daycare.discharged` as the anchor and keep `admissionAdvised` only until the OT opens.

### R6 · AMEND — E1-M2 / E2-M4: "patients are told by template" on a system whose only notify provider is `console`

`kernel/notify/adapters.ts:55-66`: `adaptersFor` switches on `NOTIFY_PROVIDER` and the only case is `"console"` — `consoleWhatsappAdapter` / `consoleSmsAdapter` log a line and return `providerMessageId: null` (`:33-46`). A `hi`/`en` template rendering (`templates.ts:1-4`) reaches no patient on production. `rulings-gates.md` P4: "WhatsApp/WABA + DLT registration — procurement — every patient message — Q4". E1-M2's acceptance (template renders) is satisfiable; the DECIDED line 4 ("patients are told by template (R-020)") and E2-M4's "unconfirmed slots released" are not, until P4. Name P4 as the dependency for the patient half; keep the desk half.

### R7 · AMEND — E2-M3 changes `modules/billing` (a CLAUDE.md hot module) and ships a counter-side throw if the enum lands first

`apps/core/src/modules/billing/charge-rules.ts:30-41`: a `switch (encounter.visitType)` with cases `new` and `renewal`; the default throws `no charge rule for visit type "…"`. `billing/config.ts:30` and `billing.controller.ts:413` pin `opdConsult: { new, renewal }`. A `post_procedure` visit therefore fails at the counter with the exact shape `opd-go-live.md:167-170` warns about. E2-M3 "3 d; none" names neither the billing edit (CLAUDE.md: `billing` is "imported by nearly every other module: a signature change there breaks every lane") nor the deploy-day row (the mapping must land in the same tip as, or before, the enum). Add both.

### R8 · AMEND — E4-M2 treats a compile-time constant as an environment flag; "UAT then prod" cannot happen on one image

`apps/web/src/lib/voice-flag.ts:28`: `export const VOICE_SEARCH_ENABLED = false;` — a TypeScript constant baked into the web bundle, whose header says "this flips to `true` in one line" (`:26`). UAT and production run "the same `deploy.sh`, same images" (ROADMAP Q3 `:243-244`). Flipping it is a code change on a tip: the next weekly deploy carries the mic to production whether or not `SPEECH_*` is set there (the route then 503s, which is safe, but the UI shows a control that fails). E4-M2's acceptance "`VOICE_SEARCH_ENABLED=true`, `SPEECH_*` set on UAT then prod" needs either an env-read flag (a build change, named) or a deploy-day row: "the flip lands on prod in the same tip; prod's `SPEECH_*` stay unset until the DPIA line; open-counter desks refused by `resources.attributes`".

### R9 · AMEND — 12c M4 (15 d) sits on 12a, which nobody schedules, and the §7 placement is self-contradictory

§7 lists "12c M4 only after 12a and the second server — per §5, the quarter after" under the **Q2 2027** heading. P1 `:210`: "No in-house 12a runtime … this quarter or next (RM:428)" — so 12a is Q2 2027 at the earliest and unauthored. The copilot spec's Phase B is "post-12a; each item behind its own gate" (`specs/2026-08-25-clinical-copilot-design.md:173`) with five ordered gates (`:123-129`), `agent_runs` provenance (absent), eval + adversarial fixtures, retention (`:184-186`) — 15 d for one lane is optimistic even after 12a exists. Move M4 to "after 12a — unscheduled" and drop the 15 d.

### R10 · AMEND — E6-M3 "5 d" assumes the 22a1 adapter is built; 22a1 is a nine-task plan nobody schedules

`plans/2026-08-28-phase1-22a1-payment-money-in.md:124-209`: T1–T9, seven CRITICAL, a stop-loss of 730,000 tokens (`:19`), migration serial `0039` (stale). ROADMAP §5 `:414-415`: "not 22/22a (… gateway are procurement)". §7 Q2 2027 says 16c-ii runs "once … the gateway adapter (22a1) is **approved**" — approved is not built. E6-M3's `depends_on` must read "22a1 built and closed (≈ 3–4 lane-weeks, unscheduled) + Razorpay agreement (R-261) + NEW-P2-2", or E6-M3 leaves P2 for 22a.

### R11 · AMEND — E1-M4 renders a KPI on a surface before Plan 21, against the index's standing rule, without saying so

`00-INDEX-AND-SYNTHESIS.md:148` (theme 8): "**Plan 21** lands before any surface renders KPIs; every later plan's §8 registers metric ids there; S10 v1.3 is the book of record until then. Standing rule." ROADMAP Q4 `:294-296`: 21 "follows 20 in Q1 2027". E1-M4 (Q1 2027) prints `1 − breaches ÷ visits` on a `desk.opd.wait_sla` tile. Either sequence E1-M4 after 21 (and register the metric id), or state that the tile is a count in the `user_day_facts` style (`kernel/desk/rollup.ts:16`) that 21 later re-homes — ROADMAP admits surfaces already render counts, but a new one needs the line.

### R12 · AMEND — E5-M3 and edge row #20 pin a lab state that does not exist

`apps/core/src/modules/lab/workflow-def.ts:43`: the states are `ordered, awaiting_collection, collected, accessioned, in_analysis, …`; `workflow-def.test.ts:70-72`: "`ordered` reaches only `awaiting_collection` and `cancelled`". There is no `awaiting_payment`. Pay-before-collect is enforced by the desk placing and billing in one transaction (design §0, `modules/lab/desk.ts:178-182,410-420`) and doc 02:112's rule, not by a state. E5-M3's "skips `awaiting_payment`" and row #20's "no `awaiting_payment` skip in the diff" are unprovable as written. Re-state the NEW-P2-1 mechanism on the transition `ordered → awaiting_collection` (roles at `workflow-def.ts:105`) and the desk's invoice-first transaction.

### R13 · AMEND — ≥ 12 "files that belong to everyone" with no coordination line anywhere in the design

CLAUDE.md "Files that belong to everyone — coordinate before editing". The design's builds touch, with nothing said:
- `kernel/notify/templates.ts` (E1-M2 `appointment_delayed`); `kernel/desk/*` (E1-M4 tile); `kernel/phi/audit.ts:14-16` — `PhiSurface` is a TS union, the `briefing` surface is a kernel edit (E3-M2); `kernel/printing/render.ts:659-661` + `kernel/db/schema/printing.ts:42` document kinds (E5-M4 `lab_label`); `kernel/workflow/roles.ts:30-33` (E5-M2 floor page).
- `kernel/modules/manifests.ts` + `manifests.test.ts:39` ("TWELVE manifests, by key, IN ORDER"): 16b T1 is a **new `briefing` module** (`16b:77`) → a thirteenth manifest and the pin (E3-M2 names "census 12 manifests" as a seam but no coordination).
- `kernel/db/schema/*` + `apps/core/drizzle/**`: at least six additive migrations (E2-M1, E2-M2, E3-M4 `rx_templates`, E5-M2, E6-M2, E4-M4 `agent_runs`) — "one per PR, numbered at rebase"; the front-desk lane already lost `0074/0075` (CONTEXT.md:41).
- `scripts/seed-roles.ts` + `test/seed-roles.test.ts`: E3-M4's "department-head permission" is a new string — pinned counts at `seed-roles.test.ts:886` (161), `:1102` (308), `:1120` (141), `:1191` (147) all move.
- `test/caddyfile-parity.test.ts` (route counts) for E1-M2's delay route, E3-M2's endpoint, E5-M1's seat door, E6-M2's hand-over path; `apps/web/src/router.tsx` (E6-M2 tablet path); `apps/web/src/locales/*.json` (every new string incl. row 18's "ordering not enabled").
- `modules/billing/charge-rules.ts` (R7); `scripts/check-config-present.ts` — E2-M2 and row #19 call it "the deploy's hard gate" for `slot_minutes_by_type`, but it reads no `opd_config` today (grep `opd` = empty); the OPD's G2 row lives in `standup:check` (`standup-check.ts:183`, non-fatal in `deploy.sh`, ROADMAP `:353-356`). Say which script gates it, or give the column a DB default and drop the gate.
Add a "shared files / coordination" column or paragraph per epic, and name the orchestrator as arbiter.

### R14 · AMEND — missing edge rows in the three required shapes

Present: #18 (role-unheld, but for a grant the role model already carries — the real gap is a *doctor named*, `active_doctor_in_a_department`), #19 (config-before-guard, E2-M2 only), #21 (deploy-day, measurement SHA). Missing:
1. **Deploy-day · E2-M3:** `post_procedure` enum lands; `charge-rules.ts:38-41` throws at the counter → the mapping ships in the same tip; test: every enum value has a charge rule (R7).
2. **Role-unheld · the OPD's first day:** no `opd_admin` distinct from `owner`, or no `medical_superintendent` → `opd_visit` cannot be activated (`opd-go-live.md:61-65`); the census row `opd_visit_definition_active` RED (`standup-check.ts:188`) is the artefact; the Desk shows "registration not enabled", not a 500.
3. **Role-unheld · E5-M2:** `phlebotomist` held by nobody at hospital scope → the role-broadcast page reaches an empty set and the specimen waits silently; artefact: a census row on `front-desk` (`STANDUP_ROWS["front-desk"]`, `standup-check.ts:181` — not `lab`, and there is no `standup:check opd`) plus the seat refusing to promise a draw. Same shape for `front_office_supervisor` (E1-M4: nobody to read the breach) — row #22 covers an unread harvest, not an unheld role.
4. **Config-before-guard · E5-M4:** `lab_label` ships; no relay claims an OPD-floor destination (`printing.ts:49-56`; P6:42 `print_destination_registered` NOT MODELLED) → `print_jobs` sit unclaimed, the tube is never `labelled`, every room draw is refused (row #13 says "refused" as if that were fine). State rule 4's choice (`ROADMAP-v2.md:383-386`): the label kind ships one deploy after the relay is registered, or the seat falls back to the desk path until `print_destination_registered` is green.
5. **Deploy-day · E4-M2:** the constant flip reaches production in the weekly tip (R8).
6. **Deploy-day · E6-M2:** the `pharmacy_dispense` version bump is Class C and the seed activates it (ROADMAP §3 rule 2); whether `seed-pharmacy` on re-run retires v1 (`definitions.ts:216-217`) or skips because one is active is a fact to measure before the row is written — no row today.
7. **Role-unheld · notify:** `NOTIFY_PROVIDER=console` on production → every patient template "sends" to a log line (R6); the desk must tell the patient by mouth; artefact: the console line count vs the template's producer count.

### R15 · NOTE — Q1 2027 lane arithmetic

P2 asks Q1 2027 for 07e 13 d + 07f 12 d + 16b 10 d + 16b-ii 6 d + 17c-ii 12 d = 53 lane-days ("one lane, or a second"), while ROADMAP §2 has three build lanes with R on Plan 20 → 41 and L joined to S (`:336-347`), and P3/P4/P5/P6 also claim Q1 2027. E2-M4 additionally needs 30 production days *after* E2-M1..M3 deploy, and E2-M1's own "mismatch < 5 % over 30 production days" needs 30 days after it lands: 07e (wk 14–16) → 07f build (wk 17–19) → data (wk 20–24) → M4 closes wk 25–26 only if nothing slips and R1's six acts are all done by wk 6. Name the lane and the critical chain; "Q1 2027" as a block hides that 07f's last milestone is a Q2 2027 close under any slip.

### R16 · NOTE — the design's "read against b04cbd9" header is already stale for its own subject

`opd-go-live.md` (#155) landed 5 h 45 m after the design was written, and it is the single document that decides what the OPD's G5 is. Per CONTEXT.md:42 ("any document a roadmap item depends on must be checked to exist before it is named"), v3 must re-read `docs/runbooks/opd-go-live.md` on the tip it cites and state the tip.

## Amendments (the list a reviser applies)

1. **§6 last bullet + §7(a) + every E1/E2/E3 G5/G6 row (R1):** define the OPD's G5 as "a seat walk-through for the six screens + queue + display, written into `docs/runbooks/opd-go-live.md` (which today has none: `:174-177`), executed on UAT and dated"; define the OPD's G6 harvest rows; and list the six acts before the first production OPD day with owners: `opd_admin` ≠ owner + `medical_superintendent` named; the Class-A `opd_visit` ceremony (`:46-65`); a doctor of record; `front_office` + `cashier` held; a tariff version activated (owner/CA — add to the ruling table beside O6 as unblocking "every consult fee, hence every 07e/07f baseline"); the walk executed. Re-derive every §7 date from those.
2. **§7(b) (R2):** either justify E3-M3's exemption from §6 and name the front-desk lane's wk 1–4 load (ROADMAP F rows; P6-E3-M1; FD-26/27/28 unpushed), or move it to Q1 2027.
3. **E3-M1 (R3):** "human act: the owner's approval line" — not the orchestrator's.
4. **E5 preamble (R4):** replace "mirrors 18a-iv" with "diverges from 18a-iv D2 (`:63`) because …", and add the cross-department consequence.
5. **E2-M3 + §7 (R5):** depends_on → P3-E6-M1..M4 (OT runbook, `daycare_case` Class-A ceremony, six names, O6 for package prices); or anchor on `admissionAdvised` only until then.
6. **E1-M2 DECIDED 4, E2-M4 (R6):** name P4 (WABA + DLT) for the patient half; `NOTIFY_PROVIDER` has one case, `console` (`adapters.ts:55-66`).
7. **E2-M3 (R7):** add `modules/billing/charge-rules.ts:30-41` to the build, mark `billing` as a hot module, and ship the mapping with the enum.
8. **E4-M2 (R8):** `VOICE_SEARCH_ENABLED` is a compile-time constant (`voice-flag.ts:28`); rewrite the acceptance as a tip-level flip with prod's `SPEECH_*` unset until the DPIA line, or add an env-read flag to the build.
9. **E4-M4 + §7 (R9):** "after 12a — unscheduled; no size"; remove from Q2 2027.
10. **E6-M3 + §7 (R10):** depends_on "22a1 built and closed (9 tasks, unscheduled, `:124-209`) + R-261 signed + NEW-P2-2"; or hand E6-M3 to 22a.
11. **E1-M4 (R11):** sequence after Plan 21 or state the count-not-KPI exemption with the index rule cited (`00-INDEX:148`).
12. **E5-M3 + row #20 (R12):** drop `awaiting_payment`; state the mechanism on `ordered → awaiting_collection` (`workflow-def.ts:105`) and the desk's invoice-first transaction.
13. **All epics (R13):** add a "shared files" line per milestone naming the kernel/schema/seed-roles/parity/router/locales/billing touches and the migration count; name the orchestrator as the arbiter and "numbered at rebase".
14. **§4 (R14):** add the seven rows above; fix row #18 to the real gap (a doctor named; census on `front-desk`, `standup-check.ts:181`, not `lab`); state rule 4's choice for E5-M4.
15. **§7 (R15):** name the lane per Q1 2027 item and the 07f critical chain; admit 07f-M4 closes in Q2 2027 under any slip.
16. **Header (R16):** re-read `docs/runbooks/opd-go-live.md` on the tip cited; state the tip (`ff07cbc` or later).
