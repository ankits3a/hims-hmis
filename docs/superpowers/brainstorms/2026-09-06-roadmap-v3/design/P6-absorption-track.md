# P6 — Track S, the absorption track: what is BUILT but not in the hospital's hands

Read-only, 2026-09-06 21:40 UTC. Paths relative to `main-ro` (checkout `b04cbd9`; its `origin/main` ref is already `fbe8fc4`, **11 commits further** — #123/#125–#131/#133/#136/#140 merged while this brainstorm ran; still 80 migrations `0000`–`0079`). **RM** = `docs/superpowers/2026-09-06-ROADMAP-v2.md`; **11i** = `docs/superpowers/plans/2026-09-06-phase1-11i-the-stand-up-path.md`; **HO** = `docs/superpowers/2026-09-06-HANDOFF-commissioning-lane-11i.md`; **SC** = `apps/core/scripts/standup-check.ts`; **DS** = `docker/prod/deploy.sh`; **LAB/PH/RS/PACS/RAD** = `docs/runbooks/{lab,pharmacy,radiation-safety,radiology-pacs,radiology}-go-live.md` (RAD only at `origin/main`, #140); notes = `wf/understand/*.md`. Position (position-state §0; `gh` re-checked 21:38 UTC): **production runs `hmis-prod/server:399f92c`, a commissioning-lane merge commit, since 12:35 UTC** — 78 migrations applied, 2 behind `main`, deployed by a jest mutation run (`memory:deploy-sh-executed-by-a-test:13-25`), no image of the previous base on the daemon (`memory:prod-deploy-state:105-107`). **12 open PRs**: #117 (T8, DIRTY) → #118–#122 CLEAN, #134/#137 (LIMS), #139/#142/#143 (radiology), #73. The `deploy-blocker` label exists.

## 0. The mission sentence(s) this pillar serves, and the honest reconciliation

**Sentences served.** "…assigns tasks to both AI agents and **human teams** in real time" and "…integrated into radiology and pathology workflows" (MISSION-BRIEF :3), plus the owner's ask: "make sure every new session could get the milestones easily even though I am not awake" (:29). Absorption is the human-team half of the first sentence and the word *integrated* in the second: a module is integrated when a named person at a seat uses it on production, not when it merges.

**Reconciliation.** (1) *Tiers.* Nothing in this pillar infers. Its automations — the census (SC), the config gate, the restore drill, the milestone file (E7) — are T0 deterministic scripts. The "digest" RM §3 names as G6's reader (RM:365) is Plan 12a's Digest Writer, which does not exist and sits behind DPIA v0.2 (RM:428; quality notes A5); this pillar substitutes a deterministic `pilot:harvest` printout (E3-M4), so **no DPIA is on its path**. (2) *Buy-not-build.* One purchase shapes it without blocking it: a second server for UAT (RM:440; 11i D1 :126). (3) *The IPD gate.* This pillar is the gate's precondition, not its subject: O1, the dues numbers and Plan 20 are RM §4's critical path (RM:407); absorbing the modules already merged is what the quarter does instead of building 41 (RM §0c.7 :193-207). (4) *Where the mission's wording exceeds what is designable:* it has no absorption sentence — it names capabilities as if present. The reconciled goal is RM Q1's: **"open" is the seven-gate checklist per department (RM:351-368), the hospital-wide exit is G7 = O6**, and nothing counts as in the hospital's hands until a runbook carries a dated `## Executed` section (RM rule 3 :376) — five runbooks on `origin/main`, **zero executed** (position-state §7; RAD has no such section either).

**What changed today and binds every epic.** RM §2 wk 1's catch-up deploy "by the owner's hand" (RM:334) has already happened, ungoverned: production is *ahead* of `main` and runs 11i T3/T7/T8/T9 before their PRs merged (position-state §2, §5). So the first *governed* weekly deploy (E1-M4) is a **reconciliation** from a `main` tip, and the backout defect T8 exists to close (11i §2 row 17 :63) recurred on the deploy that shipped T8. Pharmacy and 18c's licence gate are live with an empty shelf and an empty `aerb_licences` — RM rule 4 ("deploy-dark or config one deploy earlier", :378-380) broken by accident, not by decision. And the state moved again inside this brainstorm's hour (11 merges) — G-C's argument.

## 1. Goals — four, each with a north-star parameter

| # | goal | parameter | instrument | baseline today | target | who reads it |
|---|---|---|---|---|---|---|
| G-A | **Departments open on production behind the seven gates** | per module: G1–G4 RED count; G5 = a dated `## Executed` section; G6 = the harvest's last three rows empty seven days | `standup:check <module>` (SC:402-445; on prod the owner runs it as the seeds run, HO G1 :275) · `grep -l '^## Executed' docs/runbooks/*-go-live.md` · the runbook's harvest table (LAB:207-218) | G1–G4 printed once by the 12:35 deploy into `/opt/hmis-prod/log/` (DS:542-546), unreadable by a lane; lab RED on department/doctor/catalogue/roles; pharmacy `pharmacy_item_present`/`pharmacy_batch_in_stock` RED (SC:341-346); radiology `radiology_definitions_active` RED (origin/main SC:369); G5 **0 of 5**; G6 0 | wk 3: lab G5 dated on UAT; wk 6: front desk + lab G5 on production, pharmacy G5 on UAT; wk 9: lab G6 closed, radiology G5; wk 13: pharmacy G6 closed | owner (morning), orchestrator, every lane via `docs/STATUS.md` (E7) |
| G-B | **Production tracks `main` weekly, with a way back** | days since the last governed deploy; pending migrations (journal at `origin/main` − applied); the previous base's `<sha>` image present | `docker image ls hmis-prod/server` (T8 SHA tags, live) · the owner's applied-count query · the runbook's §11 row (`origin/lane/commissioning-t7:docs/runbooks/catch-up-deploy-2026-09.md:361`) · `backup.drill_rehearsed` (T8; `main` has only `drill_passed/failed`, `kernel/retention/events.ts:120-121`) | governed deploys **0** (four hand runs to 56, then the accident); pending 2; previous-base image **absent**; no deploy record in the repo (session-method G-c) | every Monday: lag ≤ 7 d, pending ≤ 5, previous `<sha>` present, a rehearsal event for the SHA ≤ 48 h before, a dated §11 row — or an open `deploy-blocker` saying why not | owner (he deploys), orchestrator |
| G-C | **A fresh session reads the milestone state from one measured file** | age of `docs/STATUS.md`'s `measured-at` header; count of hand-typed state facts in THE PROMPT blocks | the header (E7-M1); the CI pin (E7-M2); `grep -c` of SHAs/pending counts in `docs/superpowers/*HANDOFF*.md` prompts | file absent; three answers to "what is deployed" (session-method F6); seven of ten handoffs restate SHA + pending count by hand (R2); this file's own §0 was stale within an hour | STATUS.md ≤ 2 h old whenever a lane runs; every new THE PROMPT ≤ 3 state lines; board rows carry an age and list every worktree | every session at turn one; the owner asleep |
| G-D | **The hospital leaves `commissioning` once, honestly** | `validate:config` ok=true within 24 h and an `operating_mode_changes` row `commissioning → ramp` | `apps/core/scripts/validate-config.ts:1-11,28-29` (exit code; `ops.config_validated`); `kernel/ops/mode.ts:61-68` (zero rows = commissioning), `:169-191` (the gate); `POST /ops/mode` (`ops.controller.ts:213`) | never green: no CA signature, no active tariff (RM row 5 :33; 11f O6 :275-277) | wk 10–13 if O6 lands; otherwise the row stays red and STATUS.md says why | owner |

## 2. Epics

### P6-absorption-track-E1 — 11i finishes: the train, UAT, the S-gate, the first governed deploy, the accident written down
- **Plan home:** 11i (existing).
- **Goal:** close 11i by its own rule (RM §8 :482): the lab's stand-up executed on UAT and dated; production reconciled with `main` by a governed weekly deploy that has a backout; every status line that says `c11833d`/NOT DEPLOYED corrected once, in the artefact the census reads.
- **Exists:** T0/T1/T2/T4 merged (#113–#116) and running; T3/T5/T6a/T7/T8/T9 open (#117 DIRTY, #118–#122 CLEAN — measured 21:38 UTC), yet T3/T7/T8/T9 *run on production* as lane commits (position-state §5). `HMIS_TARGET=uat` (`origin/lane/commissioning-t3:docker/prod/deploy.sh:46-86`), `HMIS_DEPLOY_ROLLBACK_TO` + SHA tags kept 3 deep (`…-t8:…deploy.sh:49-56,81-89,219-233`), the catch-up runbook with step (2b) hand-tagging and §11 "NOT YET RUN" (`…-t7:docs/runbooks/catch-up-deploy-2026-09.md:7,158-168,361`), LAB §14 "NOT YET RUN" (`…-t6:docs/runbooks/lab-go-live.md:425`) — none on `main` (DS grep for those four names: empty). UAT env file exists, no container; drill PASSED 09-05 (RM §0c.1); LIMS #125–#131, pharmacy #123/#128/#136, radiology #133/#140 merged tonight (`fbe8fc4`).
- **Build:** rebase #117 onto the squash twins, chase #117→#122; stand UAT up with T3+T5; execute T6b (11i T6 :291-308) and append `## Executed on UAT — <date>` to LAB; write §8 CLOSE (11i:449); rewrite T7's runbook as **the weekly deploy runbook** whose §1 measured row is `399f92c/78` and whose step (2b) tags what runs *now* (already `:399f92c`); the first governed deploy is from a `main` tip ≥ `fbe8fc4`, carrying `0078` (re-landed above the watermark, `memory:radiology-commissioning-walk:17-18`) and `0079`; one docs PR sweeps every `c11833d`/NOT DEPLOYED status line (LAB:3, PACS:3, RM §0c :96-106, 11i §2 :43, HO §1 :34-44; PH:3 already corrected by #128) to point at STATUS.md (E7).
- **Gates:** G1 (drill + rehearsal), G5 for the lab on UAT; the S-gate. **Tier:** T0 scripts only. **Depends on:** the orchestrator's train; the owner's hand for the production deploy (11i D8 :160).

### P6-absorption-track-E2 — 11k: the loaders for the owner's files
- **Plan home:** 11k (letter-in-block; named by 11i §6 :411-412 and RM wk 4–6 :337 — "per-file scripts for the owner's spreadsheets with a row-by-row rejection report and a dry run, never a generic import surface").
- **Goal:** turn the chief pharmacist's and the pathologist's weeks into days (RM row 8 :36) without inventing a row: three loaders that take the owner's files, refuse by row, and run on production only with his data.
- **Exists:** `POST /lab/catalogue/analytes|orderables` (LAB:120-123) and the golden fixture that must never reach production (LAB:106-108); `seed-lab-catalogue.ts` behind the synthetic door (11i D5 :146); `import-holder-book.ts` as the one precedent of an operator loader kept out of `deploy.sh` (`apps/core/scripts/import-holder-book.ts:2-9`; `import:holder-book` in `package.json:33`); pharmacy master data is five screens per item (PH:37-52); `patients.legacy_uhid` for the back-book (`kernel/db/schema/patients.ts:110`; 11i §2b row 2 :92) and the `0057_uhid_floor_11000` floor. Refusals are already named — `foetal_sex_refused`, `catalogue_invalid`, `unknown_analyte` (LAB:122-129); `unknown_sale_item`, `price_unknown` (PH:96-98).
- **Build:** `load:lab-catalogue`, `load:pharmacy-items` (medicine → drug item → sale registration → NPPA ceiling; GRN stays human at `/materials/grn`, PH:41-46), `load:back-book` (legacy UHID + demographics; duplicates to `patient_merge`, SC:161); each takes `--dry-run`, prints accepted/rejected by row with the module's refusal code, and is **not** in `deploy.sh`. Census rows `lab_orderables_priced` (SC:249) and `lab_range_sources_present` (SC:261) exist; add `pharmacy_items_priced_within_ceiling`.
- **Gates:** G3 for lab and pharmacy. **Tier:** T0. **Depends on:** E1-M2 (UAT to dry-run against); the two spreadsheets (F3, §5).

### P6-absorption-track-E3 — Front desk + billing + the laboratory open on production
- **Plan home:** 17 (existing; LAB is the runbook) — with the front desk's runbook **moved from README into `docs/runbooks/front-desk-go-live.md`** so the census pin sees it (`README.md:62` patients, `:361` OPD, `:645` billing; `apps/core/test/standup-check.test.ts:46-51,94-100` maps only `docs/runbooks/*-go-live.md`).
- **Goal:** RM row 5's first opening — "front desk + billing + one department, never a department alone" (RM:33) — with the lab's G3→G6 on production and the six front-desk screens open behind the same census (RM:337).
- **Exists:** FD-25 six screens deployed (#92, `0067`); FD-26/27/28 authored, unpushed, 19 dirty files, serial `0075` already taken (position-state §4). Lab census rows (SC:210-320) incl. three NOT MODELLED — `lab_critical_call_list`, `lab_report_ready_notice`, `lab_printer_destinations` (SC:306-316; 11i §2b rows 6, 7, 14); harvest + close criterion (LAB:207-218); drills A–C (LAB:221-253); rollback keeps the tables (LAB:274-284). Hospital rows `second_administrator` (SC:173), `cashier_held`, `front_office_held` (SC:201-206). R-247 adopted — O1 is not the lab's gate (RM §0c.6 :183-191); LAB:35-41 still says it is (corrected by #122). **O2 — the burned 11e roster's rotation — is NOT DONE** (11f :263-265, :550-551) and appears on no roadmap row (rulings notes §1); `must_change_password` (`kernel/db/schema/auth.ts:30`) is its instrument.
- **Build:** the front-desk runbook file (preconditions, six-screen walk, the panel/TPA line, one test print per device, the three README runbooks folded); `pilot:harvest <module>` — a deterministic script printing the runbook's harvest rows with dates (LAB:207-216 are prose; quality notes B row 30; no harvest script exists in `apps/core/scripts`); hospital census gains `roster_credentials_rotated` (G4, reads `must_change_password` on the 11e roster) and `print_destination_registered` NOT MODELLED → runbook. No new module code.
- **Gates:** G3 (catalogue via E2; department + doctor of record through the OPD masters screen, HO D1 :242), G4 (four lab keys + `billing_manager` + `cashier`, HO C6 :235; O2), G5 (dated), G6 (harvest). **Depends on:** E1-M3 (S-gate), E2-M1, the names (F4).

### P6-absorption-track-E4 — The pharmacy opens: UAT first, production behind the money ruling
- **Plan home:** 16c (existing; PH).
- **Goal:** the counter that is live and inert passes G5 on UAT in weeks 4–6 and G3/G6 on production only after the GST treatment is ruled and the item master is his.
- **Exists:** `seed-pharmacy` establishes `PHARM-OPD` and activates `pharmacy_dispense` (DS:480-483; PH:31); PH's status line now true (#128, `origin/main` PH:3-8 "IS IN PRODUCTION and CANNOT DISPENSE ANYTHING"); census rows `pharmacy_store_present … pharmacy_batch_in_stock`, `pharmacist_council_number` NOT MODELLED (SC:324-358); the 20-minute seat drill (PH:54-90); eleven named refusals (PH:92-106); `seed:pharmacy-demo` merged (#136, `origin/main:apps/core/package.json:38`), **not deployed**; precondition 5 = CA-signed slabs (PH:35); `priceForBatch` adds CGST/SGST on top of the base (`tariff/pricing.ts:101-106`) — right today only because `pharmacy_exempt` is `exempt: true` as a DEV PLACEHOLDER (`seed-tariff.ts:90-96`); 16c R-2 ruled *which slab*, never inclusive-vs-exclusive (`plans/2026-09-02-phase1-16c-opd-dispense-counter.md:98`).
- **Build:** nothing new in the module. Census row `pharmacy_gst_treatment_ruled`, RED until the CA session writes the tax-inclusive rule into `gst_config` (RM rule 5 :381-386 would label a fixing PR; none exists, so a RED row named by PH §1.5). `pharmacy_retail_licence_on_file` NOT MODELLED → PH §1 (R-174). Item loader from E2.
- **Gates:** G3 blocked on the money ruling + the item master; G4 on the names (chief + three, RM:455). **Depends on:** E2-M2, NEW-P6-2 and O6.

### P6-absorption-track-E5 — Radiology + AERB open on production
- **Plan home:** 18c + 18a-iii (existing; RS, PACS, RAD).
- **Goal:** the licence gate live against an empty table (RS:12-17) becomes a register with the real certificates; the RSO and physicist appointed; the two Class-A definitions activated by three named humans; the clinical flow walked on UAT with `DEMO` licences, then production in a declared window.
- **Exists:** census rows `radiology_study_types_active` (SC:363; the seed is *not* run by `deploy.sh`, SC:364-369 — still true at `origin/main`), `radiology_device_present`, `radiology_devices_licensed`, `radiology_rso_appointed` (SC:372-392), and **`radiology_definitions_active`** (#140, `origin/main` SC:369-373: `imaging_study`/`imaging_gate` "activated by no seed and no script"); RAD §3 :73-106 — the definitions are **change-class A**: a drafter, the owner and the MS approve per *person*, a non-drafter activates, "the smallest lawful set is three"; `dev-radiology-standup.ts` does it on UAT and refuses production (RAD:101-104); #133 fixed `seed-radiology` on an empty DB; `seed:aerb-demo` + `file-demo.sh` (11i T5, #121); rollback "application first, migrations second, or neither" (RS:241-249); chasers dead-end at the duty manager until Plan 20 (RM row 11 :39).
- **Build:** `seed-radiology` joins `deploy.sh`'s seed list (study types + device resources are Class C rows with no prices, RAD:59-61; `deploy-parity` census +1, `apps/core/test/deploy-parity.test.ts:354-381`); the UAT walk of the three runbooks with `DEMO` licences (G5), recorded in each; the production Class-A ceremony as a dated runbook step performed by three named accounts; the licence filing as the RSO's step; 18a-iv's ordering door only if #143 is approved.
- **Gates:** G2 (study types), G3 (definitions — three humans; devices + licences = law), G4 (RSO), G5, G6. **Depends on:** a third distinct account on production (O1 gives it), certificates by wk 7 (RM:338), names (F4), R-004 for the release desk.

### P6-absorption-track-E6 — The silent three: mini-OT/day-care, materials, memberships & self-service get a census, a runbook, and a UAT rehearsal
- **Plan home:** 11m (letter-in-block; next free letter after 11k/11l in the deployment-and-operability series, RM §7 :461-470).
- **Goal:** three deployed modules with schema in production since `0034`–`0037`, "behaviour unverified" (position-state §1), become *stand-uppable*: census rows, a runbook each, one G5 on UAT this quarter; production opening is **next quarter** (§7).
- **Exists:** `seed-ot` drafts the whitelist, PACU scales and deposit policy and **activates none** (`apps/core/scripts/seed-ot.ts:56-71`); publishing is `ot_definition_publish` through the MS, and two distinct humans already exist on production (`plans/2026-08-28-phase1-15-mini-ot-daycare.md:119,201`); the mini-OT's runbook lives in README (`README.md:1354`); `seed-materials` registers two approval types, no item/vendor/store (`seed-materials.ts:2-25`); `seed-membership` registers one approval type and **no catalog by rule** — the owner's files arrive at commissioning (`seed-membership.ts:7-12`; `plans/2026-08-25-phase1-09-…:1577-1585`). **Absent:** any of the three in SC (`hospital|lab|pharmacy|radiology`, SC:128,210,322,361) and under `docs/runbooks/`.
- **Build:** census rows — OT `ot_definitions_published` (`status='active'`, never the seed's return), `daycare_package_services_priced`, `anaesthetist_role_held`; materials `store_present_per_department`, `items_loaded`, `vendor_present`; membership `plan_catalog_loaded`, `partner_book_loaded`, `grace_honor_approver_held`; three runbooks (OT's from README:1354 + Plan 15 T9 :387-392); one UAT walk of the mini-OT on `DEMO` definitions.
- **Gates:** G2–G5 on UAT only. **Depends on:** E1-M2; the MS's publish act; membership files (fact).

### P6-absorption-track-E7 — 11l: the milestone instrument and the session layer
- **Plan home:** 11l (letter-in-block; deployment/operability series).
- **Goal:** the owner's sentence, mechanised: one script re-derives the milestone state, one file carries it with its age, one writer commits it, and the failures measured tonight (F1–F12, session-method §4) each get a mechanical guard instead of a practice.
- **Exists:** every instrument already prints a verdict (session-method §3 lists nine, from `tools/lane.sh status` to `git ls-tree`); `O/bin/{board,test-lock,lane-report}.sh` outside git, `board.sh:13` hard-codes four lanes; `deploy-parity.test.ts` as the shape of a pinned census; the `deploy-blocker` label; the memory symlink shared by every lane (`tools/lane.sh:18-23`); `drop` refuses over unpushed work (`tools/lane.sh:79-84`).
- **Build:** `tools/milestones.sh` → `docs/STATUS.md` (session-method §6): G1–G7 per module; deployed base from `docker image inspect` *or* the `log/deploy-<sha>.json` that **one new `deploy.sh` line** writes (sha, utc, applied count, census transcript); heartbeat *age* from `O/state/*.status` mtime; lanes from `git worktree list`. A CI pin: STATUS.md's header hash equals its body hash (a hand edit fails CI). `CLAUDE.md` gains one line: read STATUS.md; if `measured-at` > 2 h, run the script. `O/bin` moves under `tools/`. THE PROMPT shrinks to lane + handoff path + "state is in STATUS.md". Session-layer guards: `deploy.sh` refuses under `JEST_WORKER_ID`/`NODE_ENV=test`, independent of the guard the mutation deleted; `lane.sh stale` lists lanes with heartbeat age > 12 h and unpushed commits.
- **Gates:** none (no owner ruling; 11i §7 :430 precedent). **Tier:** T0. **Depends on:** nothing; can start tonight in the roadmap lane.

### P6-absorption-track-E8 — The exit from commissioning (G7)
- **Plan home:** 11f (existing; O6 at `plans/2026-08-24-phase1-11f-operability-gate.md:275-277`; the gate is 11c's `validate:config`).
- **Goal:** the one hospital-wide act — `commissioning → ramp` from `/ops/mode` after `validate:config` is green — performed once, between sessions, with the pilots' harvests as the fix list (RM:339).
- **Exists:** the gate (`kernel/ops/mode.ts:169-191`), the report row (`kernel/db/schema/ops.ts:79`), the ten CA rows as one agenda (R-255, register :267), every GST row a DEV PLACEHOLDER (`seed-tariff.ts:33-36`), `getOperatingMode` with no module caller (RM row 5 :33) — the exit is a ledger fact, not a switch. `in_consult` is an OPD bench status (`modules/opd/bench.ts:68`); no transition guard reads it (grep of `kernel/ops` empty).
- **Build:** nothing in modules. Two hospital census rows: `config_validated_within_24h` (HO F1 :267) and `config_hash_matches_mode` (edge-corpus AB5: a tariff edit after the green run); a transition guard + runbook line "between sessions, no visit `in_consult`" (AB4); a `commissioning` invoice prints COMMISSIONING on its face (AB3) — a template test, front-desk lane.
- **Gates:** G7 = O6. **Depends on:** the CA session, E3-M4 (lab G6 closed) and E4-M3 as the precondition for calling the hospital open.

## 3. Milestones

Sizes are days of one lane; the human act names who does what, never "approval".

### E1 — 11i finishes
| id | name | gate | acceptance: parameter · instrument · threshold | depends_on | size · human act |
|---|---|---|---|---|---|
| E1-M1 | The train lands #117–#122 and the accident is written down | — | `gh pr list --state open` shows none of #117–#122 · 0 · the catch-up runbook §11 carries a dated row "executed 2026-09-06 12:35 UTC by a mutation run; no backout" | — | 2 d · owner: one line acknowledging the base (NEW-P6-1) |
| E1-M2 | UAT is up and clean | G1 (UAT) | `docker ps` shows `hmis-uat-{db,api,worker,caddy}` and no `hmis-preview-caddy` · 4/0 · `curl -k https://<ip>:8443/api/health` → `{status:"ok"}` · the shell renders the UAT banner (`HMIS_ENVIRONMENT_LABEL`, 11i §2b row 22 :112) · `deploy-parity` UAT census green (0 `hmis-prod` strings in the rendered script) | E1-M1 | 2 d · none |
| E1-M3 | The lab opens on UAT — the S-gate | G5 | `## Executed on UAT — 2026-09-…` present in LAB with drills A (after 21:00 IST or "not performed"), B, C, D and the three §2b registrations · section exists, 4 drill rows, 3 patients · `standup:check lab` on UAT exit 0 · 11i §8 CLOSE non-empty with the `wc -c` line | E1-M2 | 3 d · the commissioning lane at five seats in a browser (HO D8 :249 allows one person) |
| E1-M4 | The first governed production deploy (reconciliation) | G1 | `backup.drill_rehearsed{sha}` for the tip ≤ 48 h before · 1 · applied count = journal length at the tip (80 at `fbe8fc4`) · `hmis-prod/server:<newsha>` and `:399f92c` both present · §11 dated · census before/after attached · `gst_config.updated_at` unchanged | E1-M1 | 1 d · **the owner runs `deploy.sh` in a declared window** (11i D8) |
| E1-M5 | Four consecutive weekly deploys | G1 | Mondays with lag ≤ 7 d · 4/4 · pending at each ≤ 5 · every deploy has its predecessor `<sha>` image · 0 deploys without a §11 row | E1-M4 | recurring · owner weekly |

### E2 — 11k loaders
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E2-M1 | Lab catalogue loader dry-runs the owner's file on UAT | G3 | `load:lab-catalogue --dry-run` row report · 0 uncaught throws, every rejection carries a module refusal code · after the real run `standup:check lab` rows `lab_orderables_priced`, `lab_range_sources_present` ok on UAT | E1-M2; the spreadsheet | 3 d · owner hands over the file (F3) |
| E2-M2 | Pharmacy item loader | G3 | same shape · `pharmacy_item_present` ok on UAT after load; `pharmacy_items_priced_within_ceiling` ok · GRN still refused by the loader (0 `stock_batches` written) | E2-M1; the item master | 3 d · chief pharmacist's file |
| E2-M3 | Back-book loader | G3 | `load:back-book` report · duplicates land in the `patient_merge` queue, count printed · 0 UHIDs minted below the `0057` floor | E2-M1 | 2 d · owner's legacy register export |

### E3 — front desk + billing + lab on production
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E3-M1 | Front-desk runbook exists and the census sees it | G2/G4 | `docs/runbooks/front-desk-go-live.md` present; `standup-check.test.ts` "every runbook has rows" green with 6 files · `front_desk` rows ≥ 4 in SC | E1-M1 | 2 d · front-desk lane pushes FD-26/27/28 first (renumber `0075`) |
| E3-M2 | Lab G3 + G4 on production; O2 done | G3, G4 | `standup:check lab` on prod: RED = 0, NOT MODELLED = 3 · `LAB` department with `registration_no` set · four distinct users hold the four keys (`seed:staff` transcript kept) · **O2:** every 11e roster account shows `must_change_password = true` at hand-over (`auth.ts:30`), `roster_credentials_rotated` ok | E2-M1; names | 2 d · owner: F4 names + the rotation at `/admin/users` (11f O2); pathologist: the catalogue through E2 |
| E3-M3 | Front desk + lab G5 on production, then pilot opens | G5 | dated `## Executed` in both runbooks · 2 · one test print per device recorded · the `V` walk-in priced (0 `tariff_item_missing` in the walk) | E3-M1, E3-M2 | 2 d · department head performs |
| E3-M4 | Lab G6 closes | G6 | `pilot:harvest lab` daily for ≥ 14 d · last three rows empty 7 consecutive days (LAB:218) · `paper_vs_system` gap ≤ 10 % · `pilot.harvest_read` events ≥ 7 | E3-M3 | 3–5 wk · department head reads daily |

### E4 — pharmacy
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E4-M1 | Pharmacy G5 on UAT | G5 | `## Executed on UAT` in PH with the seat drill (PH:54-90) on `seed:pharmacy-demo`'s shelf · 11 refusals each seen once and quoted · `standup:check pharmacy` UAT exit 0 | E1-M2 (a UAT deploy carrying #136) | 2 d · lane + one pharmacist if available |
| E4-M2 | GST treatment ruled and the row green | G3 | `pharmacy_gst_treatment_ruled` ok · the slab test: an invoice for a ₹100 MRP pack totals ₹100.00 (tax backed out) | NEW-P6-2, O6 | 1 d · owner + CA |
| E4-M3 | Pharmacy G3/G4/G5 on production and pilot | G3–G6 | `standup:check pharmacy` prod RED = 0 · `pharmacy` held by a registered pharmacist (council no. NOT MODELLED → PH §1) · dated `## Executed` · harvest `dispense.handed_over` ≥ 1/day, `invoice_not_settled` refusals quoted | E4-M1, E4-M2, E2-M2, names | 2 d + pilot · chief pharmacist |

### E5 — radiology + AERB
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E5-M1 | `seed-radiology` in the deploy; study types + devices on production | G2 | `radiology_study_types_active`, `radiology_device_present` ok after the next deploy · `deploy-parity` seed census +1 | E1-M4 | 1 d · none |
| E5-M2 | Radiology G5 on UAT with `DEMO` licences | G5 | `## Executed on UAT` in RS, PACS, RAD · 3 · `dev-radiology-standup.ts` transcript · `/aerb/licences/gaps` empty after `file-demo.sh` · a CT refused `device_not_licensed` **before** filing, recorded | E1-M2, #121 | 3 d · lane |
| E5-M3 | The Class-A ceremony on production: `imaging_study`/`imaging_gate` active | G3 | `radiology_definitions_active` ok · three distinct `approver_id`/activator accounts on the two definitions (RAD:75-91) · one order → an `X` accession on `/radiology/reception` (RAD:98-100) | O1 (a third account), E5-M1 | 0.5 d · **drafter, owner, MS at the approvals screen** |
| E5-M4 | Certificates filed, RSO appointed, window closed | G3, G4 | `radiology_devices_licensed`, `radiology_rso_appointed` ok on prod · `aerb_licences` rows contain no `DEMO` · the `degraded` window row < 60 min naming radiology (or skipped, recorded — HO E1 :256) | certificates (L2), names | 1 d · **RSO files at `/radiology/radiation-safety`** |
| E5-M5 | Radiology G5 on production | G5 | dated `## Executed` in the three runbooks · a real study acquired, read, released with R-004 prices on the release desk | E5-M3, E5-M4, R-004 | 2 d · radiographer + radiologist |

### E6 — the silent three
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E6-M1 | Census rows + runbooks for OT, materials, membership | G2–G4 | `standup:check all` prints `ot.*`, `materials.*`, `membership.*` rows ≥ 3 each · 8 runbook files under `docs/runbooks/`, the mapping test green | E1-M1 | 3 d · none |
| E6-M2 | Mini-OT G5 on UAT | G5 | drafts published by the MS on UAT (`ot_definitions.status='active'` ≥ 3) · dated `## Executed` in `ot-go-live.md` · one day-care case wheel-in → discharge on UAT | E6-M1 | 2 d · a MS-role user on UAT |
| E6-M3 | Membership catalog loaded on UAT from a sample file | G3 | `plan_catalog_loaded` ok on UAT; the "no catalog in `apps/`" rule still holds (`seed-membership.ts:7-12`) | E6-M1 | 2 d · owner's sample file |

### E7 — 11l
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E7-M1 | `tools/milestones.sh` → `docs/STATUS.md` | — | header `measured-at <sha> <utc>` · G1–G7 cells per module · deployed base read from the daemon or `log/deploy-<sha>.json`, never typed · heartbeat age shown; rows = `git worktree list` count | — | 3 d · none |
| E7-M2 | Single writer + the CI pin | — | a hand edit to STATUS.md fails CI (mutant run, RED with the predicted message) · the orchestrator loop commits it under `flock` · `CLAUDE.md` has the one-line rule | E7-M1 | 1 d · none |
| E7-M3 | Session-layer guards | — | `deploy.sh` under `JEST_WORKER_ID` exits non-zero with the guard deleted (fail-first, then the mutant) · `lane.sh stale` lists the front-desk lane today · `board.sh` shows the commissioning lane | E7-M1 | 2 d · none |
| E7-M4 | Handoffs stop restating state | — | every HANDOFF written after this date: ≤ 3 state lines in THE PROMPT; `docs/handoffs/<lane>/` is the one directory; STATUS.md names the current file per lane | E7-M2 | 1 d · orchestrator |

### E8 — G7
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E8-M1 | The CA session happens; tariff active | — | `gst_config` rows carry signed rates, `updated_at` after the session · active tariff version ≥ 1 · `validate:config` prints `caSigned=true` for every scope | O6 | 0 d lane · **owner + CA, one session (R-255)** |
| E8-M2 | `commissioning → ramp` | G7 | `ops.config_validated` ok within 24 h · one `operating_mode_changes` row `to=ramp` · 0 visits `in_consult` at the instant (AB4) · STATUS.md G7 = done | E8-M1, E3-M4 | 0 d · owner at `/ops/mode`, between sessions |

## 4. Edge cases — the Indian day the absorption track must survive

| # | scenario | expected behaviour | artefact that proves it | milestone |
|---|---|---|---|---|
| 1 | A jest mutation run executes `deploy.sh` again (it did, 12:35 UTC today) | refuses under `JEST_WORKER_ID`/`NODE_ENV=test`, independent of any deletable guard | fail-first test in `deploy-parity`; the mutant (guard deleted) still refuses | E7-M3 |
| 2 | Production moved and no repo artefact changed; the next session reads "c11833d" in five documents | STATUS.md is re-derived from the daemon/deploy record; prose SHAs are gone | `grep -c c11833d docs/superpowers/*.md` after E1's sweep = citations only, no status lines | E1-M1, E7-M1 |
| 3 | The deploy that shipped the rollback has no rollback (c11833d image gone) | the runbook says so in §11; every later deploy keeps its predecessor `<sha>`; SHA tags pruned to the last three, never to zero (`…-t8:deploy.sh:87-89,120-127`) | `docker image ls` two tags per deploy; runbook row | E1-M1, E1-M4 |
| 4 | The guard lands before the config: 18c refuses every CT on an empty licence table, on production, tonight | RED row `radiology_devices_licensed`, not a blocker while radiology is unopened (HO E5 :260); the window is declared only when a device exists (HO E1 :256) | census transcript before/after; `operating_mode_changes` note | E5-M4 |
| 5 | The feature is merged and nobody holds the role: pharmacy live with 13 routes, `pharmacy` held by nobody | `seed-roles` NOT READY is non-fatal (DS:505-510); the census names the screen; the counter refuses `store_missing`/`unknown_sale_item` rather than selling | `standup:check pharmacy` RED rows quoted in STATUS.md | E4-M3 |
| 6 | The owner is asleep and a money ruling is pending (GST on MRP) while the pharmacy item loader is ready | UAT load only; prod row `pharmacy_gst_treatment_ruled` stays RED; STATUS.md reads "waiting: NEW-P6-2" with the default written; the session continues on another milestone (edge-corpus SL7) | STATUS.md row + phase doc §7 line | E4-M2 |
| 7 | The census is green on UAT and the seat still fails on production (a role at department scope) | the census checks hospital scope by name (HO C2 :231); the walk files the missing row (HO G5 :279) | new SC row + `## Executed` defect line | E3-M2 |
| 8 | Someone restores a UAT dump into production "for the demo catalogue" | UAT has no pgBackRest stanza, a different DB name; the `DEMO` licence row and the synthetic-patient check go RED (HO B2/B3 :216-217) | census rows; `deploy-parity` asserts the prod env lacks the door key | E1-M2 |
| 9 | Pilot day 3: paper register 41 lab orders, system 33 | the harvest prints the gap; > 10 % is a seat defect, never "training" | `pilot:harvest lab` row `paper_vs_system` dated | E3-M4 |
| 10 | "Seven empty harvest days" declared; nobody opened the harvest | reading is an event with a reader; G6 closes on seven *read* rows | `pilot.harvest_read` count ≥ 7 | E3-M4 |
| 11 | A real patient pays cash in the shadow window under placeholder GST | paper receipt authoritative; the system receipt prints COMMISSIONING on its face (AB3) | template test while `ops.mode = commissioning` | E8-M2 |
| 12 | The second weekly deploy falls in the lab pilot | window after 21:00 IST; the harvest marks the deploy day; an open `deploy-blocker` holds the tip (HO A6 :199) | runbook step; label check in the §11 row | E1-M5 |
| 13 | The Saturday 22:00 UTC drill and a UAT build collide for memory | UAT deploys avoid 22:00–23:00 UTC Saturday (11i T8); builds go through `test-lock.sh` (HO A2 :195) | drill log PASSED on Sunday; `test.holder` empty at 22:00 | E1-M2 |
| 14 | G7 exit at 10:00 with three consults open | `commissioning → ramp` refused while any visit is `in_consult` (`opd/bench.ts:68`); the runbook says "between sessions" | transition-guard test + runbook line | E8-M2 |
| 15 | A tariff edit lands the day after `validate:config` was green | the mode row carries the config hash; mismatch is a RED hospital row | `config_hash_matches_mode` row | E8-M2 |
| 16 | Two departments open beside a piloting one (pharmacy on UAT while the lab pilots on production) | each has its own runbook, harvest and census rows; the pharmacy drill quotes a real MRP/NPPA row and a placeholder slab refuses its invoice (AB8) | two `## Executed` sections; `standup:check all` per-module verdicts | E4-M1 |
| 17 | The MS never publishes the OT drafts; the census reads green because the seed ran | `ot_definitions_published` checks `status='active'`, not the seed's return | SC row + mutant (draft-only reads RED) | E6-M1 |
| 18 | Radiology's two definitions need three distinct people and production has two accounts | `radiology_definitions_active` stays RED with the fix text naming the third person; `duplicate_approval` per person is the guard (RAD:84-86); nobody makes a second account for one human | census row; `approver_id` distinct count = 2 + activator ≠ drafter | E5-M3 |
| 19 | The first real pharmacist logs in with a credential from the burned 11e roster (O2 never done) | no department human's first login happens before the roster rotation; `roster_credentials_rotated` RED blocks G4 in STATUS.md | `must_change_password` count on the roster; 11f O2 transcript | E3-M2 |
| 20 | A lane holds three phases unpushed with a migration serial already taken (front-desk tonight) | `lane.sh stale` names it; the serial is renumbered at rebase; `drop` refuses over unpushed work (`tools/lane.sh:79-84`) | `lane.sh stale` output | E7-M3, E3-M1 |
| 21 | A lane writes a wrong "FIXED" into the shared memory at 02:00 (edge-corpus SL9) | entries carry SHA + date; STATUS.md, not memory, answers "is it deployed"; re-measure before acting | SHA on the entry; CLAUDE.md line | E7-M2 |
| 22 | The orchestrator session dies mid-train (SL8) | `MERGE-TRAIN.md` + `train.lock` on disk; any session resumes; STATUS.md shows the train's last SHA | file dated; `flock` refused by name | E7-M2 |
| 23 | `board.sh` cannot see the lane that deployed production | lanes come from `git worktree list`, never a name list | board row count = worktree count | E7-M1 |
| 24 | The owner asks "is it safe to deploy?" on Sunday with a peer's stack half-merged | the answer is a procedure (label clear, rehearsal event, §11 template), never a judgement (HO H2 :286) | STATUS.md "deploy-ready: yes/no + why" line | E7-M1 |
| 25 | A session is asked, at 03:00, to "just deploy it" (SL14) | never an agent's act; it writes the runbook step, opens the `deploy-blocker` check, and stops; STATUS.md shows "awaiting: the owner's hand" | the refusal in the lane report; no `deploy-<sha>.json` written by a session | E1-M4 |
| 26 | The position drifts inside one hour (11 merges landed while this document was written) | STATUS.md's `measured-at` is the only state a prompt may cite; a reader older than 2 h re-runs the script before acting | header age; the CLAUDE.md line | E7-M1 |
| 27 | T9's redirects are removed while a desk still bookmarks `/counter/seat` (AB7) | the removing PR reads Caddy 404 counts on the three paths first, in the PR body | counts in the PR body | E1-M5 |

## 5. DECIDED, and RULINGS

**DECIDED (Indian-corporate-hospital standard; none is money, procurement or law):**
1. The accidental deploy is the base; nobody rebuilds `c11833d` (`deploy.sh` builds only `origin/main`, DS:47-61); the runbook records "no backout for this one" and the rule holds from the next deploy.
2. The first governed deploy is a reconciliation from a `main` tip, not a rollback to the lane's ancestor — same content plus two additive migrations; the watermark guard (T4, in prod) is the proof.
3. The digest that reads the harvest is a deterministic script, not the Digest Writer — G6 must not wait on DPIA v0.2.
4. The front-desk runbook moves from README to `docs/runbooks/` — the census pin sees only that directory.
5. `seed-radiology` joins `deploy.sh` for the Class-C rows (study types without prices, device resources — RM rule 2 :372-374); the two Class-A definitions are **never** seeded — RAD §3's three-person ceremony stands, and its third person is what O1 supplies.
6. OT/materials/membership open on production **next quarter**; this quarter they get rows, runbooks and one UAT rehearsal — absorption before building; no consumer opens for them before the IPD gate (RM §5).
7. GST-on-MRP is a RED census row now; a `deploy-blocker` label needs a fixing PR (RM rule 5 :381-386) and none exists.
8. Loaders are per-file scripts outside `deploy.sh` with `--dry-run` (11i §6 :411-412; the `import-holder-book` precedent); never a generic import surface.
9. STATUS.md has one writer (the orchestrator loop under `flock`) and a CI pin; handoffs stop typing SHAs.
10. No fake clock for drill A; no synthetic person on production; UAT never restores a production backup (11i §2b rows 18, 23 :108,113).
11. The retail drug licence (R-174) is a NOT MODELLED census row → PH §1, not a schema change.
12. The panel/TPA line in the front-desk runbook is the corporate-standard one — "the panel rate is the price; ₹X is payable" — with a front-desk briefing before it reaches a live counter; the owner's 09-05 answer was oblique (`memory:fd25-owner-rulings-2026-09-05:69-75`) and the standing rule closes it, not a new ruling.
13. O2 (rotate the burned 11e roster, 11f :263-265) is a G4 precondition for every department's first real login — hygiene, performed by the owner at `/admin/users`, evidenced by `must_change_password`; it needs no ruling and gets a census row so it cannot vanish again.
14. Radiology's G5 on UAT uses `dev-radiology-standup.ts` (RAD:101-104); production's ceremony names real people and is recorded in RAD as a dated step.

**RULINGS — money / procurement / law / facts only the owner holds:**
| id | question | category | recommended default | unblocks |
|---|---|---|---|---|
| NEW-P6-1 | Acknowledge `399f92c` (12:35 UTC) as the deployed base with no backout, and run the first governed deploy from a `main` tip in a declared window | fact (+ his hand) | yes; Monday of wk 2, after 21:00 IST | E1-M1, E1-M4 |
| NEW-P6-2 | Medicine MRP is tax-inclusive: the counter backs GST out of the printed MRP and never adds to it | money (CA) | inclusive (Legal Metrology + DPCO practice); the CA states the rates in the O6 session | E4-M2, pharmacy G3 |
| O6 / R-255 | The CA session: ten CA rows + the real tariff | money/law | one session, wk 6–10 | E8-M1, G7 |
| F3 (RM §6.4 :447-449) | The lab catalogue spreadsheet; the pharmacy item master (schedule, HSN, GST, MRP) | fact/data | wk 3 / wk 4; the loaders take his files | E2, E3-M2, E4-M3 |
| F4 (RM §0c.4, :455) | The names: pathologist of record + 4 lab keys; chief pharmacist + 3; RSO + physicist; the radiology drafter | fact/staffing | a row without a name reads *unstaffed* | E3-M2, E4-M3, E5-M3/M4 |
| L2 / R-256 / R-237 | AERB certificates per ionising device; e-LORA; the real PCPNDT §19 registration | law | file by wk 7; devices stay refused until then | E5-M4 |
| R-004 | Film/CD prices | money | film-free; CD ₹150, film ₹250, MLC free (register :16) | E5-M5 |
| R-174 | Confirm the retail drug licence (Form 20/21) is on file | law/fact | it exists; census row NOT MODELLED | E4-M3 |
| RM §6.1 (:440) | A second server for UAT (₹1.5–3k/mo VPS) | money | buy when convenient; nothing waits | E1-M2 relocation |
| O1 | Second administrator (a name) | staffing fact | wk 3; not the lab's gate (R-247); **is** radiology's third account | IPD gate, E5-M3, OT two-key re-ratification |

## 6. Not now

- **No Digest Writer, Hermes bridge or any inference on the absorption path** — DPIA v0.2 is counsel's; the harvest is a script. Hermes is P1's, behind the Class-0 signature and a second server.
- **No production opening for mini-OT/day-care, materials or memberships this quarter** — rows, runbooks and one UAT walk only; production needs the MS's publish, an item master and the owner's membership files, and no consumer opens for them before the IPD gate.
- **No generic import framework, per-module flags, automatic production deploy or release-train tool** (11i §6; RM Q3 :252-258).
- **No third demo stack, no per-lane bench** — UAT is the bench (RM:431).
- **No KPI registry (21), no 28d audit pack** — every parameter above is a query over the spine; the registry follows Plan 20 (RM:414-418).
- **No 17-M/17-H, 18b-ii, 16d/16e/16f, 41** — behind G6, PACS rulings, the dataset licence and the IPD gate (RM §5 :412-436).
- **No method rewrite** — `O/bin` moves into `tools/`; the S-gate is the only method change (RM §8).
- **Next quarter:** OT/materials/membership G5→G6 on production; the back-book bulk load on production; STATUS.md gains the department heads' harvest rows; UAT off-box if the server is bought.

## 7. Sequencing note

Weeks count from Monday 2026-09-07 (RM §2). The calendar's week-1 act ("the owner deploys from the runbook", RM:334) has been overtaken: production already carries the batch. **Wk 1:** E7-M1 starts tonight in the roadmap lane (no human act); E1-M1 in the commissioning lane (owner: NEW-P6-1, one line); E3-M1 in the front-desk lane after it pushes FD-26/27/28 (renumber `0075`). **Wk 2:** E1-M2 UAT up (a deploy carrying #136's shelf); E1-M3 the S-gate; **E1-M4 the first governed deploy — the owner's hand, a declared window**; E7-M2/M3. **Wk 3:** 11i closes; E3-M2 needs the names (F4), the catalogue (F3) → E2-M1, and O2 at `/admin/users`; E5-M1 rides the second weekly deploy; E5-M3 the moment O1 gives radiology its third account; E6-M1 by the LIMS lane after 11j (RM §0c.5). **Wk 4–6:** E2-M2/M3; E3-M3 front desk + lab G5 on production and the pilot opens; E4-M1 pharmacy on UAT; E4-M2 needs NEW-P6-2 before any prod item is registered for sale; E6-M2/M3 on UAT; E7-M4. **Wk 7–9:** E5-M2/M4/M5 need the certificates (L2), the names and R-004; E3-M4 the lab's G6 closes; E4-M3 needs the chief pharmacist's file and the four names. **Wk 10–13:** E8-M1 the CA session (O6) → E8-M2 the exit, between sessions, if E3-M4 has closed; E1-M5 continues weekly. **Q1 2027:** the silent three on production; the back-book on production; UAT off-box. **Q2 2027:** STATUS.md feeds Plan 21 once Plan 20 gives denominators. Where a named act is missing, STATUS.md reads *unstaffed* / *waiting: <ruling id>*, never "late".
