# P1 — Unified intelligence fabric (tasks to agents AND humans) + multilingual speech — REVISED

Re-read against `main-ro` @ **`ff07cbc`** (2026-09-07 09:01 IST, #155; the design was written at `b04cbd9`, the verifiers at `3f596e4`) — every line below re-grepped at `ff07cbc`. Paths repo-relative; `apps/core/src` and `apps/web/src` prefixes dropped. `H` = the **untracked** Hermes brainstorm `/opt/hmis/docs/superpowers/brainstorms/2026-09-01-hermes-ops-copilot/00-BRAINSTORM.md` (`git status` → `??`; absent from `main-ro`) · `P20` = `docs/superpowers/plans/2026-09-06-phase1-20-workforce-roster.md` (#150, FOR APPROVAL, NOT EXECUTED — P20:3) · `RM` = `docs/superpowers/2026-09-06-ROADMAP-v2.md` · `REG` = `…department-series/00-OWNER-RULINGS-REGISTER.md` (R-nnn at line 12+nnn) · `12`/`20`/`22q` = department docs 12, 20, 22-quality · `DPIA` = `docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md` · `spec` = `docs/superpowers/specs/2026-08-10-hmis-architecture-design.md` · `11h`/`11i` = those phase docs · `index` = `…department-series/00-INDEX-AND-SYNTHESIS.md`. Production = `399f92c` since 12:35 UTC 09-06 (CONTEXT.md:20); "deployed" = in that image. Goal ids are **NS1–NS5** (north-star), never G1–G7, which are the roadmap's gates (RM:351-367).

## 0. The mission sentence(s) this pillar serves, and the honest reconciliation with the spec (tiers, buy-not-build, DPIA, the IPD gate)

Mission (MISSION-BRIEF.md:3,6,7): *"…assigns tasks to both AI agents and human teams in real time"*; §1 *"connect every hospital department into a single intelligence layer … nurses, phlebotomists, radiology technicians, care coordinators"*; *"understand and process speech across 15 Indian languages natively."*

**The one place this pillar reframes an owner sentence, said first:** "15 Indian languages" is not scheduled as fifteen flips. The OS becomes language-neutral (the language is an attribute of the person and the document, not of the deployment), the registry holds any number of tags, and N is *measured* from `patients.spoken_language` over 90 days (D17). Today no document names fifteen languages and a Bhojpuri speaker (36 mentions across the series) cannot be recorded: `patients.controller.ts:80` `z.enum(["hi","en"])` refuses `bho` at the API.

| mission phrase | what the spec / law allows | what exists (cite) | reconciled goal |
|---|---|---|---|
| "a single intelligence layer" | agents "consume the event stream and act through the same permission-enforced APIs … never the database" (spec:776); no agent-to-agent transport (12:158); rule beats model (spec:780) | one spine, `Actor = user\|agent\|system\|patient` (`packages/contracts/src/envelope.ts:35`; `kernel/db/schema/events.ts:30`); guarded transitions + SLA timers + ladders (`kernel/workflow/timers.ts:146-170`); alerts (`kernel/alerts/consumer.ts:173-188`) | **The fabric exists and is deterministic.** Missing: a *destination that is a person* (Plan 20) and a *task entity* (19a). A model-driven planner is in no design and refused by construction (D1). |
| "assigns tasks to AI agents in real time" | T0–T4; clinical caps at T2–T3 (spec:778); PRE-12a gate = DPIA + inference locus (spec:839) | the actor half only: `agents` + key + kill switch (`kernel/db/schema/auth.ts:91-100`; `kernel/auth/guards.ts:47-52`); **kill switch is a bare column update, NOT evented** (`kernel/auth/agents.ts:26` `setKillSwitch(db, agentId, on)` vs spec:788 "itself evented"); "agents hold no permissions yet" (`guards.ts:99-101`); `complete()` undeclared (`kernel/inference/types.ts:9-11`); no `agent_permissions` (`kernel/printing/printing.controller.ts:29-35`), no `agent_ledger` (two prose owners: RC-4 at `modules/opd/events.ts:224`, RC-6 at `…SCOPE-registration-counter-remainder.md:40,56`), no `kernel/copilot`; DPIA unsigned (DPIA:3,66-72); RM:428 | This quarter: automations only (danger protocol `modules/opd/escalation.ts:29-33`, ladders, sweeps, chasers) and the *documents* for one T0 external agent. Q-next: the Hermes Ops Copilot (pull-only, Class-0, no model call inside HMIS — H:9-13) behind a **signed** Class-0 addendum + base DPIA §8 row. Clinical drafters are P2/P3/P5's, never above T2. |
| "assigns tasks to human teams … in real time" | Plan 20 (index:66; RM Q4:278-300; **AUTHORED** as #150, P20 T1–T7); kernel `tasks` = 19a (index:153; R-094 REG:106); RM:414 "not 19a" this quarter | `usersHoldingRole(tx: Tx, roleKey: string): Promise<string[]>` (`kernel/workflow/roles.ts:71`) — **three runtime files**: `alerts/consumer.ts` (5 calls :179,229,266,300,322), `workflow/timers.ts` (:148,153), `notify/consumer.ts` (:230); two seeds that **stay static** (`scripts/seed-ops.ts:95,161-162`, `seed-roles.ts:1628`; P20 T3:100-102); `modules/ot/lists.ts:26` is a comment; ladder writes `resolvedUserIds/fallback/fallbackExhausted` (`timers.ts:148-168`; `workflow/events.ts:34-46`); the chasers' destination is a **code constant** `DUTY_MANAGER_ROLE` (`alerts/consumer.ts:300,322`; P20:32,40-44); the lab's critical ladder is a phone log with a rung enum and free-text contact — **it resolves no role** (`modules/lab/criticals.ts:49-62`); no roster table (P20:33); no `tasks` (grep) | Q-this: Plan 20 approved, built, rehearsed, live — every escalation resolves to one on-duty holder or emits a visible fallback (E1). Q-next: the task entity with claim discipline; phlebotomy and the radiology technician as first consumers (E2). "Real time" is the ladder's timer, which exists. |
| "speech across 15 Indian languages natively" | spec §9 buys commodity infra (:161-166); ASR bought — cloud for search, on-prem before dictation (12:541); DPIA §3-A pending owner accept (DPIA:31-33); R-059 blocks transcript-to-note (REG:71); RM:428 "the voice scribe stays inert at 503" | `POST /speech/transcribe` body `{audio, language: hi\|en}` — **no caller discriminator** (`kernel/inference/speech.controller.ts:30-34`); user actors only (:80); 503 before the egress row (:87-98); **the OPD consult scribe posts `{ audio }` to the same route, mounted unconditionally in the note tab, no `voice-flag` import** (`components/consult-scribe.tsx:1-3,91`; `screens/opd-consult.tsx:1080-1091`; `VOICE_SEARCH_ENABLED` read only by `voice-button.tsx:16`) — so the "one environment change" (`consult-scribe.tsx:28`) would switch on dictation into the chief complaint, not just search; literal `hi\|en` at **14 lines** (§1 NS4); browser TTS on the OPD display (`screens/opd-display.tsx:44-52` `speak(token, room, t)`) | "Natively" = language-neutral surfaces + bought ASR behind `SpeechClient` (`inference/types.ts:32`) + a flip per language on measured WER. The scribe stays **refused server-side** (D24) until a dictation addendum is ruled under R-059 — P2's question. |

**Where the mission exceeds what is designable or lawful now:** (i) a central model that assigns tasks — no design; (ii) ambient listening — the opposite of every authorised control (11h:341-342; push-to-hold, 15 s, audio discarded, `consult-scribe.tsx:30-38`); (iii) 15 languages of speech — not lawful for anything clinical under the current DPIA, not measurable until a third language is recordable; (iv) T4 for anything clinical — never (spec:778). **DPIA:** the standing rule binds every gate here — nothing with inference runs on production before DPIA v0.2 (counsel) (CONTEXT.md:28; RM:405). "v0.2" is read as the base file's §8 counsel row filled (DPIA:66-72) **and** the addendum for that activation signed; addenda are a *recommendation to counsel* on structure (D4), not a way round the base. **IPD gate:** RM:400 reads "Plan 20 roster live" — E1-M4a is on the IPD critical path; nothing else here is behind the gate. **Buy-not-build:** Hermes (OSS), the pinned model provider (DPA), the ASR vendor; nothing here trains a model.

## 1. Goals — 2 to 5 goals, each with a NORTH-STAR PARAMETER

| # | goal | parameter | instrument | baseline today | target | who reads it |
|---|---|---|---|---|---|---|
| NS1 | **One named human.** Every escalation and chaser reaches one on-duty holder | (a) share of `escalation.triggered` rows with `payload.resolvedBy='roster'` and `resolvedUserIds.length=1` (discriminator added to P20 T2/T3 by the approval rider, E1-M1); (b) `alerts` rows per `source_event_id` for `imaging.critical_overdue`/`imaging.report_unread` (`modules/radiology/events.ts:162,167`; `kernel/db/schema/alerts.ts:17,23` `user_id`, `source_event_id`) = 1 recipient; (c) `fallbackExhausted=true` per day | two SQLs over `events.payload` and `alerts`; floor **≥ 20 escalations in the window, else NOT MEASURED with N** | **undefined** — no discriminator exists; length-1 is common with one holder per role on a one-administrator production (CONTEXT.md:21); the lab ladder resolves no role (`criticals.ts:49`) so P20 T5 adds a mechanism, not a re-point | (a) ≥ 90 %, (b) = 1, (c) = 0 on any day a period is published; counted from the first production `escalation.triggered` after the flag flips | Q-this: the SQL + P20 T7's census row (`standup:check hospital`); the digest only once E3-M4 exists |
| NS2 | **The first agent acts under governance** — observed, killable, on production | `agent_ledger` rows `action='digest.fact_sheet' outcome='ok'` one per IST day; `events.actor_type='agent'` filtered by the Hermes `agents.id` (the print relay is also an agent, `printing.controller.ts:29-35`); refusals `outcome='refused'` per day (fail-closed count) + a fixture; a dated drill | `agent_ledger` (minted by E3); `events`; `docs/runbooks/ops-copilot-go-live.md` (written by E3-M0) | 0 — no agent identity on production; six runbooks on `main` (lab, opd, pharmacy, radiation-safety, radiology, radiology-pacs; `ls docs/runbooks`), none for the copilot; kill switch column only, not evented (`agents.ts:26`) | ≥ 27/30 digest days by ledger (Telegram = human cross-check); refusal count reported daily (never "0 leaks" — an absence claim); one dated kill-switch + global-halt drill per quarter | owner |
| NS3 | **Speech exposure is measured before it is widened** | WER per clip and language from `apps/core/scripts/speech-bench.ts` (≥ 3 clips to consider a UAT flip; ≥ 50 per language before any production flip, D14); `search_audit.source='voice'` rows/day **per actor** (`kernel/db/schema/search.ts:35,49`; per terminal only after `terminal_id` lands, 11h-ii) | the 11h-ii phase doc's dated table; SQL over `search_audit` | 0 rows (503 precedes egress, `speech.controller.ts:87-98`); WER NOT MEASURED (11h:135,688) | WER recorded per clip; the flag flips only under D5 **and** the signatures (E4-M4); voice rows visible per actor | front-desk lane; the owner at accept/refuse |
| NS4 | **Language-neutral surfaces** — a third language is recordable; every patient send names its language or logs a fallback | literal-language sites on the CI pattern `"hi"\s*\|\s*"en"` / `"en"\s*\|\s*"hi"` / `z.enum\(\["hi","en"\]\)` (both orders, single or double quotes, non-test, `apps`+`packages`) — stated once in the 22-L doc; distinct `patients.spoken_language` over 90 d; `notification.language_fallback` ÷ patient sends, per language | CI grep; `patients`; `events` | **14 lines (12 code + 2 comments):** `patient-detail.tsx:213`, `lib/i18n.ts:34`, `desk-one/session.ts:109`, `lab-report-print.tsx:142`, `lib/patients-api.ts:155`, `patients.controller.ts:80`, `patients/events.ts:26` (the `patient.registered` **event contract** — a versioned change), `patients/types.ts:5`, `notify/templates.ts:19`, `inference/types.ts:17`, `speech.controller.ts:33`, `notify/pump.ts:378`; comments `templates.ts:2`, `schema/patients.ts:95`; `spoken_language` NOT RECORDABLE; `notification.sent` carries no language (`kernel/notify/events.ts:16-28`) | 0 matches outside `kernel/languages/`; `bho` recordable; fallback share per language reported (the "100 % of patient sends carry `language`" is a pinned test, not a north-star — it is true by construction once the field exists, `pump.ts:378`) | front office; owner digest |
| NS5 | **A Hindi seat with zero defects** | three countable classes in 11i T6's Executed section: (a) an English string on a Hindi screen whose key is outside the 67-leaf allowlist; (b) a refusal rendered as raw zod `issues[]` instead of a locale string; (c) a printed page with tofu boxes | `docs/runbooks/lab-go-live.md` Executed section (0 `## Executed` headings in any runbook today); the allowlist test (E5-M4 build — `lib/i18n.test.ts:11-13` pins parity only); the wire assertion of E5-M3c | T6 NOT RUN (held, §5 facts); **3,073** leaves, 67 non-Devanagari, 66 identical to English (re-measured); `modules/lab/lab-http.ts:72-76` `parsed<T>(schema, body)` throws `r.error.issues`; #131's `lib/error-strings.test.ts` is the precedent, `en.json` has `materialsErrors`/`otErrors`/`pharmacyErrors` (:1425/:1635/:3038) and no `labErrors` | 0 in each class, or fixed in the same PR; allowlist green at 67 (pinned on 67, never on the total); lab refusals coded on the wire (`RuleCode` pattern, `modules/materials/qc.ts:40`) | commissioning lane; the pathologist at the seat |

## 2. Epics

### E1 · `P1-fabric-multilingual-E1` — One named human: Plan 20 approved, built, live
- **Plan home:** **Plan 20** (existing; index:66; RM:278-300; **authored** — P20, #150 `d6c27ed`; build waits on the owner's approval line, CONTEXT.md:39).
- **Goal:** NS1.
- **Exists:** the phase doc (P20 T1–T7, D1–D7, one §7 ruling); the three runtime call sites and two static seeds (§0); ladder → `duty_manager` → owner SMS (`timers.ts:151-154`; `notify/consumer.ts:228-230`); `escalation.triggered{resolvedUserIds,fallback,fallbackExhausted}` (`workflow/events.ts:34-46`, :42 "roster substrate is the Plan 11 seam"); census `second_administrator` (`scripts/standup-check.ts:173-177`); 18a-iii's chaser names nobody (P20 T5:110). **Not** existing: a chaser destination row (a constant, P20:32 — T5 makes it a row).
- **Build (P20 as authored, in its names):** T1 `roster_periods` (draft/published/superseded) + `roster_assignments`, publication in one transaction; T2 `whoIsOn(tx, roleKey, at)` behind `ROSTER_RESOLVER_ENABLED`, parity test over three call sites; T3 the three runtime files move, seeds stay; T4 CSV import validated whole; T5 the destination row for the two chaser branches + the lab ladder; T6 the coverage proposal + approval screen; T7 **one** census row RED only when the resolver is enabled AND no period is published, and the runbook — proposed name `docs/runbooks/roster-go-live.md` (no collision; six runbooks exist, none for a roster). The `roster` module is kernel-adjacent, **app and worker** (P20 D1:64-67). **Riders this design adds to P20 §5 before build (E1-M1):** `resolvedBy: 'roster'|'static'` (+ `rosterPeriodId`) on `escalation.triggered`; event names `roster.unpublished` (once/day, flag on + no period), `coverage.proposed`/`coverage.applied`; the census row's code; `coverage.proposed` mode-gated (off in `commissioning`, 12:175; T1 in `disaster`, 12:174) with a mode-matrix test.
- **Gates:** G1–G5 for 20; IPD condition = "20 live" (RM:400). No DPIA — no inference; R-262's "before the first Track-C plan deploys" (REG:274) is reconciled by RM:405 "no for 41 (no inference in it)": the same reading applies to 20, and 20 is absent from R-262's Blocks column.
- **Tier:** `whoIsOn` is a **flag-gated function, no harness** (no 12a runtime this quarter, RM:428; not "T0" — that word implies spec:780's harness); proposals are T3 (P20 D4), T1 in `disaster`, off in `commissioning`.
- **Deps:** Plan 13 (deployed); D8. **Files everyone owns:** `kernel/modules/manifests.ts`, `app.module.ts`, `worker.module.ts`, `kernel/alerts/consumer.ts` (P20:36-38 "the real coupling cost"), `kernel/workflow/timers.ts`, `kernel/notify/consumer.ts`, `kernel/db/schema/index.ts`, `scripts/standup-check.ts` (T7; `usersHoldingRoleAtScope` at :5,124,176 is out of scope, P20:126), `seed-roles` counts. The `fallbackExhausted` digest line is **E3's** fact-sheet field, not P20's (D26). Competency tags (index:66) are not in P20 and are recorded as out of scope by rider (D27).
- **Fallback / manual path:** flag off or no period → today's broadcast (P20 D2, "not an error path"); `roster.unpublished` once a day; the duty manager's phone list stays the paper path.

### E2 · `P1-fabric-multilingual-E2` — Tasks to human teams: the kernel task entity and the pools (Q-next)
- **Plan home:** **19a** (existing letter; index:46,153; R-094 REG:106). RM:414 keeps it out of this quarter.
- **Goal:** phlebotomists and technicians claim work from a pool; "nobody took it" is an event.
- **Exists:** nothing task-shaped — alerts are bounded pages (`kernel/alerts/alerts.ts:9-14`), approvals have a worklist (`kernel/approvals/worklist.ts`), lab `awaiting_collection` carries a state SLA (`modules/lab/sweeps.ts:260` `slaByState`, :290), `fallbackExhausted` routes to the owner (`timers.ts:154`).
- **Build:** `tasks` (pool, claimed_by, claim TTL, due_at, `pool_empty`), events `task.offered/claimed/released/completed/pool_empty`, one worklist per pool; **first consumers changed from R-094's ruled housekeeping to the phlebotomy round and the radiology technician worklist (D22)** — those departments will have passed G5/G6 by Q-next (RM:337-338) while Plan 19 has no runbook and no open date; housekeeping is the third consumer when 19 opens; **porters (Plan 31) only when 15 passes G5** (index:77; RM:414-418). SLA Chaser T1 on `due_at`; Turnover Dispatcher T4 is Plan 19 proper.
- **Gates:** E1-M4a; G1–G5; no DPIA — but the phone worklist puts patient identity on personal phones: 19a's doc carries a BYOD/consent line (DPIA:38 staff data), listed as a ruling below.
- **Tier:** claim discipline = function; chaser T1; dispatcher T4 operational only. A task points at the order, never carries clinical content (D10).
- **Deps:** E1-M4a; UAT; eligibility = the role key at hospital scope until a competency table exists (D27). R-082 (purchase, REG:94) and R-089 (scope, REG:101) sit on 19a authoring and are named in §5. **Fallback:** `pool_empty` → duty manager (index:153); manual path = today's broadcast.

### E3 · `P1-fabric-multilingual-E3` — The bridge: Hermes Ops Copilot as 12a-0
- **Plan home:** **12a-0** (letter inside block 12; H:184 R4; grep `12a-0` on `main` = 0; D2). **The two prior homes, retired here:** doc 12:16 put the Ops Copilot in **12b** "only after Plan 13 and only if reopened" — Plan 13 is deployed, and 12a-0 **is** that reopening; doc 12:374's **12c** "Ops Copilot for housekeeping (Lane 3 pilot), T2, staff ask in Hinglish" is a different, staff-facing agent and stays in 12c; spec:791 "Ops Copilot … out of Phase 1 (no state to query yet — revisit after the resource registry)" is superseded by the registry being live. **The plan home rests on an untracked file until E3-M0 commits H to `main`** (CONTEXT.md:42).
- **Goal:** NS2. The owner asks in Telegram and gets live Class-0 figures; the Digest Writer's 08:00 digest (spec:783, the T0 line) — no model call inside HMIS, no patient leaving HMIS.
- **Exists:** actor half (`auth.ts:91-100`; `guards.ts:47-52`; `createAgent(db, name)` mints an **unprefixed** `randomToken()` — `kernel/auth/agents.ts:7-11`); the seam (`guards.ts:99-101`); desk tiles (`kernel/desk/brief.ts`); H's T1–T6 (H:192-199), edges E1–E13 (H:205-220). Absent: `agent_permissions`, `agent_ledger`, `kernel/copilot`, any Class-0 read shaped for an agent, the runbook.
- **Build (H §7 + this design):** `agent_permissions` + guard discharge (user ∩ agent, `clinical-copilot-design.md:13`); `kernel/copilot` MCP controller: Class-0 schema test, fail-closed scrubber, rate limit, `agent_ledger` (H:147 columns incl. `surface`), `copilot.tool_called`, `agent.kill_switch_set`; **global halt as a DB row on `/admin/agents`, events `agent.global_halt_set/cleared` with actor, R-127's clearing rule enforced** (REG:139) — `COPILOT_GATEWAY_ENABLED` (H:152) stays the deploy-time master only; key prefixes `stg_`/`prod_`; ten Class-0 tools with seeded-day fixtures; `digest.fact_sheet` (H:60, carries `fallbackExhausted` count); the Hermes pack (H:136-144) whose skill says **"do not type names"**; `/admin/agents`; **`docs/runbooks/ops-copilot-go-live.md`**; staging smoke on UAT. Stop-loss H:201: ten tools or the plan splits. Everyone-files: `guards.ts`, `schema/index.ts`, `app.module.ts`, `kernel/modules/manifests.ts`, `test/caddyfile-parity.test.ts` (`/mcp`), `seed-roles`, `router.tsx` + `locales/*.json` (`/admin/agents`).
- **Gates:** DPIA v0.2 for this activation = base §8 counsel row filled **and** the Class-0/MCP addendum signed, with the base `[COUNSEL]` items E3 rests on closed — DPIA:27 (Class-0 payloads and transfer analysis) and DPIA:29 (the router) — before any production key (H:181 R1); pinned provider under a no-training DPA (H:183); second server (H:182; RM:440-441); UAT as staging; G4 for the delegate user (H E8). Landing E3-M2 **revises RM:428** ("No 12a agent runtime") — the 12a-0 doc says so in one line rather than reading round it.
- **Tier:** T0 pull-only (H:130); no writes, no push in v0 (H:62-71).
- **Data flow, stated honestly:** the owner's prompt reaches Telegram and the pinned provider *before* HMIS is called (H:134 argues residency on the outbound payload only). "Zero egress" means **zero egress from HMIS**; the addendum names Telegram and the provider as processors of the owner's own prompts and records the delivery-channel change from DPIA:11 ("via the notification gateway") to H:30,132.
- **Deps:** the addendum signed (E3-M2); UAT (held, §5 facts); E3 mints `agent_ledger`, RC-4/RC-6 consume (D3; H:172 C1; `opd/events.ts:224`). **Fallback:** Hermes down → nothing in HMIS depends on it (H:90); the owner reads the desk tiles; kill switch (`guards.ts:51`).

### E4 · `P1-fabric-multilingual-E4` — 11h-ii: voice search, the switch-on, measured — and the scribe kept refused
- **Plan home:** **11h-ii** (roman sub-phase of the closed 11h, the 18a-iii pattern; grep = 0; scope = 11h §6's four routed items :543-562 + the scribe finding).
- **Goal:** NS3. The preconditions in `lib/voice-flag.ts:6-24` become artefacts; the flag flips or the phase closes with the number that says why not; **the scribe cannot ride the flip.**
- **Exists:** the route (`speech.controller.ts:33` hi|en default `en`; :65-68 `client()`; :80; :87-90 coded 503; :98 `recordVoiceEgress(db, {actor, audioBytes})` before the call, `kernel/search/audit.ts:146-163`; :103 the one `transcribe`); `vad_filter` forced (`inference/workers-ai.ts:9-17`); the scribe wired to the same route with no discriminator (§0 row 4); `search_audit` has `actor_id` and no terminal column (`search.ts:35`) though the session knows one (`kernel/auth/sessions.ts:12` `terminalId`; `decorators.ts:6` `hmisSession`); config keys (`kernel/config.ts:170-172`); spec §19 carries no speech amendment (grep = 0); no `open_counter`/desk setting anywhere (grep = 0); no DPDP-posture artefact on disk (`docs/compliance/` = one file; spec:839's PRE-PILOT gate).
- **Build:** (a) the §19 amendment as a spec PR; the §3-A carve-out as its own addendum with a sign-off table, **the clip protocol** (scripted utterances, fictitious names, consenting staff, stored off-repo, 90-day destroy line per R-009 REG:21, staff notice per DPIA:38) and **the stage-2 DPDP posture for the voice class** stated (spec:839); (b) `apps/core/scripts/speech-bench.ts`: clips + reference transcripts → WER per clip/language; refuses to run unless the addendum with the owner's row filled is on disk; exits 2 without `SPEECH_*`; runs in its own shell — **no stack sets `SPEECH_*` this quarter** (RM:428); (c) **the scribe refused, pinned:** `surface: 'search'|'scribe'` in the body, `scribe` or absent → coded 403 `speech_surface_refused`; `voice-button.tsx` sends `search`; the edge-20 CI pin covers flag=false AND the refusal; any future dictation carve-out must stamp spec:789's provenance (model id, prompt version, input/output hash) on `scribe.suggestion_*` and mark inserted text in the note — P2's, under R-059; (d) Q-next seams: `terminal_id` + `outcome` (`ok|empty|provider_failed`) on `search_audit`; the open-counter setting **palette-side, browser-local** (11h:350 asks for a desk-level setting; the desk is a browser) — RM rule 4 (:378-381) choice: **config-first; the server refuses nothing until a desk identity exists**, which would be its own kernel task; (e) the flip is one line, Q-next.
- **Gates:** owner accepts §3-A (law); counsel signs the addendum **and** the base §8 row; the DPDP posture on file; WER ≤ D5.
- **Tier:** a processor, no tier — **for search only**; the transcript never chains into a model (`speech.controller.ts:53`).
- **Deps:** E4-M1's accept line before any clip leaves; the front-desk lane owns `consult-scribe.tsx` and `voice-button.tsx`. **Fallback:** 503 → typing, always (`voice-button.tsx:16`).

### E5 · `P1-fabric-multilingual-E5` — 22-L: the language pack — the OS becomes language-neutral (Q-next)
- **Plan home:** **22-L** (letter in block 22, 17-E/17-M style; not `22d` — 22q:537 uses it, remapped to 28d; D20). Source rows: doc 20 LA-1/LA-5 (20:427,431), `announcements(display_id, token_no, room, language, tts_ref, …)` (20:233), EQ-2 (20:375,641), open item 6 (20:752). **22-L inherits none of block 22's prerequisites** (index:68 PBX, Plan 10 public surface, R-216–R-227) — languages need no PBX.
- **Goal:** NS4 + NS5. A Bhojpuri-only patient is recordable; every patient-facing paper, message and announcement renders in the read language or logs a fallback; the UI stays hi/en (D6).
- **Exists:** 14 literal sites (§1); parity tests (`i18n.test.ts:11-13`; `i18n-keys.test.ts`); #131's `error-strings.test.ts` (every server code has en/hi); template parity by type (`notify/templates.ts:2,19`), `waApprovalStatus` (:17); pump reads `patients.language` at send (`pump.ts:376-379`); token slip bilingual (`kernel/printing/render.ts:402,469-471`), `<html lang="en">` (:163), relay needs a Devanagari face (:89-91); lab report per-copy `lang` (`lab-report-print.tsx:132-144`); pharmacy label no language (`modules/pharmacy/label.ts`, grep = 0); picker hi/en (`desk-one/stages.tsx:788-791`); `printJobs` (`kernel/db/schema/printing.ts:37`, no `language`); display TTS in the browser (`opd-display.tsx:44-52`); P20 §6:128-130 hands **alert `title`/`body` English prose** to "whichever phase takes the refusal-localisation work" — **22-L claims it**.
- **Build:** `kernel/languages` registry (BCP-47 tag, script, spoken/read); additive migration `patients.spoken_language`, `patients.read_language` (`language` kept as the read alias, D7); `patient.registered` contract widened (a versioned event change, `patients/events.ts:26`); `users.ui_language` optional; `notification.sent.language` + `notification.language_fallback`; `print_jobs.language` with per-document policy; Hindi sig + pictograms on the pharmacy label (16:300 K1); lab refusals → codes + `labErrors` namespace (the #131 pattern) + the alerts bell; **display audio = EQ-2's pre-rendered numeral clips per language (0–999), token + room only, no live TTS** (20:375,641; `tts_ref` is that clip's reference) — `announcements` has **one** home, here; the 67-leaf allowlist test; the CI grep pattern stated once.
- **Gates:** G1–G5 on the front desk (`docs/runbooks/opd-go-live.md` exists on `ff07cbc`, #155 — preconditions and ceremonies, **no seat walk section**: `standup:check front-desk` :29,123; §6 "what this runbook does NOT cover"); the relay font as a runbook line + test page; WhatsApp template approval per language (procurement, NEW-P1-3).
- **Tier:** none — templates, Class 0; "a model talking to patients … refused, not deferred" (12:511).
- **Deps:** FD-24 printing (`1a752e0`, ancestor of `f211075`, in production); 11i T6 executed (held); `patients`, `locales/*.json`, `kernel/notify`, `kernel/printing`, `modules/lab`, `modules/pharmacy`, `modules/opd`, `schema/index.ts` — coordinate; serial at rebase. **Fallback:** a missing template/paper renders `hi` and logs `language_fallback`; never drops the send; an **empty registry is not a fallback** (edge f).

### E6 · `P1-fabric-multilingual-E6` — 11h-iii: Indic ASR behind the choke module, bought and measured per language (Q+2)
- **Plan home:** **11h-iii** (the second roman sub-phase of 11h — the ASR lineage), **not 12c** (D23): 12c carries three gates — Plan 13, Plan 19 live for the cohort, DPIA L1 (12:624) — and a number carries its gate (index §3 rule 1); E6 needs none of the last two. 12c proper (Conversational Work Surface, Lens narration, the Lane-3 staff copilot) is P2's. The display-audio half left E6 for 22-L (E5).
- **Goal:** any registry language can be spoken to the palette, switched on by its own measured WER; the §3-A carve-out closes when in-region ASR arrives (11h:557-560; DPIA:33).
- **Exists:** `SpeechClient` seam + offline stand-in (`inference/types.ts:32`, `offline.ts`); Cloudflare Whisper (`workers-ai.ts`); 12:541; GPU box ₹3–6 L (12:603); ASR ₹0–2 k/month (12:598).
- **Build:** a second `SpeechClient` (in-region Indic ASR, or on-prem whisper.cpp/IndicConformer) selected per language by config; `TranscribeInput.language` widened to the registry (`types.ts:17`); E4's bench at ≥ 50 clips per language; paired eval fixtures (12:193 I5).
- **Gates:** vendor + DPA (NEW-P1-1); GPU box only if chosen (NEW-P1-2); R-059 still binds anything beyond search; a DPIA addendum per provider; **the vendor bench runs only under an evaluation DPA/NDA, or on-prem, or on the no-PHI scripted set** — E6-M1 says which.
- **Tier:** processor. **Deps:** E4-M3; E5-M2. **Fallback:** provider down → typing.

## 3. Milestones

Size = days of one lane. Acceptance cells read `parameter · instrument · threshold`. "held" = the owner's line on #118–#122 (11i T3/T5/T6/T7/T9 — measured OPEN on 2026-09-07; T8 landed via #146/#117), CONTEXT.md:39.

**E1**
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E1-M1 | Plan 20 approved, with riders | approval | the approval line · #150 / P20 §8 · present; riders recorded in P20 §5 by one docs commit · the doc · `resolvedBy`, `roster.unpublished`, `coverage.proposed/applied`, the census code, the mode gate named; P20 §7's one ruling · §5 below · listed, non-blocking for T1–T3 (P20:136) | — | 0 d · **the owner: one line on #150** |
| E1-M2 | T1–T7 merged, flag off | G1 merged | migration · `apps/core/drizzle` · additive, numbered at rebase; parity · P20 T2's fixture over **three** call sites · identical ids and order with flag off; seeds · `seed-ops.ts`/`seed-roles.ts` · unchanged; census · `standup:check hospital` · one row, RED only when enabled AND unpublished; mode matrix · test · no `coverage.proposed` in `commissioning`; payload · `escalation.triggered` · `resolvedBy` present | E1-M1 | 10 d (R, RM:337) · none |
| E1-M3 | Rehearsed on UAT (G5) | G5 | a week's roster · `roster_periods.status='published'` · 1; induced critical, empty on-duty role · `escalation.triggered` · `fallback=true, resolvedBy='static'`; with a holder · same · `resolvedBy='roster'`, length 1; chaser · `alerts` per imaging source event · 1; dated `## Executed on UAT` · `roster-go-live.md` · present | E1-M2, 11i T3 (held) | 3 d · the duty manager (F4) walks it |
| E1-M4a | Live on production (the IPD gate reads this, RM:400) | G6 | flag on after a published period · config + `roster_periods` · true; census row · green; destination row · set to the on-call radiologist | E1-M3, the governed weekly deploy | 0 d · the owner's weekly deploy; department heads publish |
| E1-M4b | 30-day read (Q-next) | NS1 | NS1(a) ≥ 90 % over ≥ 20 escalations counted from the first production `escalation.triggered` after the flip · SQL; `fallbackExhausted` on published days · 0; NS1(b) = 1 | E1-M4a | 0 d · none |

**E2** (Q-next)
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E2-M1 | 19a phase doc authored | authoring | consumers · the doc · phlebotomy, radiology tech; housekeeping third; porters deferred to 15's G5; R-082 re-confirmed and the BYOD/consent line present · the doc · yes | E1-M4a | 3 d · the owner's approval line |
| E2-M2 | `tasks` kernel merged | G1 merged | two concurrent claimers · test · exactly one `task.claimed`; TTL · `task.released{reason:'ttl'}` · at 15 min; empty pool · `task.pool_empty` → duty-manager alert · 1; no pool configured · fixture · today's broadcast unchanged | E2-M1 | 8 d · none |
| E2-M3 | Phlebotomy round on UAT (G5) | G5 | `awaiting_collection` items become tasks · count = items; a phlebotomist claims from a phone · `task.claimed` actor = that user; `claimed→completed` p50 · events · ≤ the `awaiting_collection` **state SLA** (`sweeps.ts:260`), not the orderable's order-to-report TAT (`schema/lab.ts:70`); dated section · `lab-go-live.md` · present | E2-M2 | 4 d · a real phlebotomist |
| E2-M4 | Production pilot 14 days | G6 | items claimed via tasks · SQL · ≥ 80 %; `pool_empty`/day · SQL (digest when E3-M4 exists) · reported | E2-M3 | 0 d · weekly deploy |

**E3**
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E3-M0 | 12a-0 phase doc authored; H committed | authoring | H on `main` · `git ls-files docs/superpowers/brainstorms` · 1 dir; the doc names the two retired homes (12:16, 12:374), spec:791, the RM:428 revision, the global-halt row, the everyone-files, H:201's stop-loss, `ops-copilot-go-live.md` as a T4 deliverable · the doc · present | — | 2 d (parked pharmacy worktree or wk 3+, F10) · the owner's approval line |
| E3-M1 | DPIA addenda drafted | docs | Class-0/MCP addendum · `docs/compliance/` · assertion site = MCP egress (H:126, H's own §3.4 numbering — the DPIA has no §3.4), Telegram + provider named as processors of the owner's prompts, the DPIA:11 delivery change recorded, sign-off table; the §3-A carve-out moved to its own addendum · same · §3-A no longer in the base; base `[COUNSEL]` items E3 needs · DPIA:27, :29 · listed | — | 1 d · **the owner books counsel** — R-262's one session (RM:405) is expected to produce the base §8 row, the Class-0 addendum and the §3-A ruling |
| E3-M2 | T1–T3 merged behind the seam (Q-next) | G1 merged, DPIA | agent without grant · test · 403; kill switch · test · 403 + `agent.kill_switch_set`; global halt · DB row + `agent.global_halt_set/cleared` with actor · R-127 clearing rule enforced; key prefix · `createAgent` · `stg_`/`prod_`; every tool field · Class-0 schema test · enum/label/number only; ledger · `agent_ledger` · 1 row per call; ten tools vs a seeded day · fixtures · match | **the Class-0 addendum SIGNED + base §8 row filled**, E3-M0 | 10 d · none |
| E3-M3 | Staging smoke on UAT (G5) | G5 | H §4's four steps · dated `## Executed on UAT` in `ops-copilot-go-live.md` · present; 7 digests · `agent_ledger` `digest.fact_sheet` ok · 7/7 (Telegram = cross-check); "show me patient X" · ledger · refused, no `copilot.tool_called`; token audit · the doc · tokens per digest and per question, one week | E3-M2, 11i T3 (held) | 4 d · **second server bought** (H R2); provider DPA signed (H R3) |
| E3-M4 | Production connection | DPIA v0.2 | production key · `agents` row · only after base §8 counsel row AND addendum row filled; census `agent_halt_authority_held` · green; digest days · ledger · ≥ 27/30; drill · runbook · dated; `events.actor_type='agent'` by Hermes id · SQL · > 0 | E3-M3, counsel | 1 d · counsel signs; the owner pairs Telegram |

**E4**
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E4-M0 | 11h-ii phase doc authored | authoring | scope · the doc · 11h §6's four items + the scribe finding + the rule-4 line (config-first) + the clip protocol | — | 1 d · the owner's approval line |
| E4-M1 | §19 amendment + §3-A addendum + posture | docs | spec §19 · grep `speech-to-text` in §19 · ≥ 1; addendum · `docs/compliance/` · sign-off table, clip protocol, the stage-2 DPDP posture for the voice class (spec:839) stated; `voice-flag.ts` comment · the file · cites the addendum | E4-M0 | 1 d · **the owner accepts or refuses §3-A** (one line); counsel |
| E4-M2a | The scribe refused, pinned | G1 merged | `surface` absent or `scribe` · test · 403 `speech_surface_refused`; palette · `voice-button.tsx` · sends `search`; CI pin · test · flag `false` AND refusal present while no signed addendum on disk | — | 1 d, any lane, before any stack sets `SPEECH_*` · none |
| E4-M2b | Seams (Q-next) | G1 merged | `terminal_id`, `outcome` · `search_audit` · written from `req.hmisSession.terminalId`; open-counter · palette-side, browser-local · mic not rendered; bench in CI · exit · 2 | E4-M2a | 3 d · none |
| E4-M3 | Measured, off-stack | measurement | clips · the doc's dated table · ≥ 3 scripted Hinglish counter clips, WER per clip/language; the bench · refuses without the filled addendum on disk · verified; `SPEECH_*` · no running stack · unset on production and UAT | E4-M1 **with the owner's accept line filled**, the bench script, the clips (F4) | 2 d · **the desk records three scripted clips** (fictitious names, consenting staff) |
| E4-M4 | Flip, or close as "stays off" (Q-next) | law + D5 | `VOICE_SEARCH_ENABLED` · the constant · `true` only if WER ≤ D5 AND base §8 row + §3-A addendum signed AND the posture on file AND E4-M2a merged; else `false` with the number in §8 CLOSE; voice rows/day per actor · SQL · reported (digest when E3-M4 exists) | E4-M2b, E4-M3 | 1 d · none beyond the signatures |

**E5** (Q-next)
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E5-M1 | 22-L phase doc authored | authoring | the CI grep pattern stated · the doc · once; coordination list · the doc · `patients`, `locales`, `printing`, `notify`, `lab`, `pharmacy`, `opd`; a seat-walk section for the front desk · `opd-go-live.md` · added and executed on UAT (dated) | 11i T6 executed (held); the front-desk seat walk | 3 d · the owner's approval line; a front-desk seat walked |
| E5-M2 | Registry + patient languages merged | G1 merged | literal sites · CI grep · 0 outside `kernel/languages/`; migration · additive, serial at rebase; `patient.registered` · contract version bumped; picker · UAT · `bho` stored | E5-M1 | 6 d · none |
| E5-M3a | Notify + print (kernel lane) | G1 merged | patient sends · `notification.sent.language` · pinned test 100 %; missing template · `language_fallback` emitted, send not dropped; patient copies · `print_jobs.language` = read language | E5-M2 | 3 d · WABA per-language approval (procurement) |
| E5-M3b | Pharmacy label (pharmacy lane) | G1 merged | Hindi sig + pictograms · fixture · renders | E5-M2 | 2 d · none |
| E5-M3c | Lab refusals + alerts bell (LIMS lane) | G1 merged | POST both `encounterNo` and `walkIn` · wire · 400 with a `code`, no `issues[]` (today `lab-desk.controller.ts:81`); `labErrors` · `error-strings.test.ts` · every code has en/hi; alert `title`/`body` · a key, not prose | E5-M2 | 3 d · none |
| E5-M3d | Announcements + display clips (OPD lane) | G1 merged | `announcements(language)` · order from the catchment; numeral clips · harness plays 0–999 per language · every token maps (EQ-2); no name reaches audio · lint · `token_no` only (20:233) | E5-M2 | 4 d · a native speaker records the numerals (NEW-P1-4 → D21) |
| E5-M4 | Hindi seat + relay font on UAT (G5) | G5 | NS5's three classes · `lab-go-live.md` Executed · 0 or fixed in the same PR; relay · printed test page · Devanagari renders; allowlist test · `i18n` · green at 67 | E5-M3a–d, 11i T6 (held) | 2 d · a Hindi-reading staff member runs the seat |
| E5-M5 | Production, 30 days | G6 | sends by language vs `read_language` distribution · SQL · reported; `language_fallback` ÷ patient sends per language · SQL · < 5 % in the last 7 of 30 days | E5-M4 | 0 d · weekly deploy |

**E6** (Q+2)
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E6-M0 | 11h-iii phase doc authored | authoring | vendor-bench protocol · the doc · evaluation DPA/NDA, on-prem, or the no-PHI set — one named | E4-M3, E5-M2 | 1 d · the owner's approval line |
| E6-M1 | Vendor bench | procurement input | ≥ 2 candidates on the same clip set · the doc · WER per language, ≥ 50 clips each; residency · the DPA · India-region or on-prem; **no clip leaves without the named protocol** · the doc · true | E6-M0 | 3 d · **the owner picks the vendor** (NEW-P1-1) |
| E6-M2 | Second `SpeechClient` merged | G1 merged | provider per language · config · selected; provider down · test · 503, typing unaffected; `language` · `TranscribeInput` · registry tag | E6-M1 | 6 d · none |
| E6-M3 | Languages flipped one at a time | law + D14 | each flip · the doc · own WER ≤ D5 on ≥ 50 own clips; §3-A · addendum · closed if in-region | E6-M2 | 1 d each · counsel per provider |

## 4. Edge cases — the Indian day each must survive (11i §2b shape)

| # | scenario | expected behaviour | artefact that proves it | milestone |
|---|---|---|---|---|
| 1 | 02:00, no on-call radiologist on the roster; a CT shows a bleed | `whoIsOn` falls to `duty_manager`; `escalation.triggered{fallback:true, resolvedBy:'static'}`; the chaser reads P20 T5's destination row (DECIDED by D6 — a constant today, `alerts/consumer.ts:300`) | event row + the row | E1-M3 |
| 2 | The resolver deploys before anyone loads a roster (deploy-dark) | flag off by default: nothing changes; flag on + no period: static holders + `roster.unpublished` once/day; **T7's census row RED only when enabled AND unpublished** (P20:117), with the screen that fixes it | `standup:check hospital` line | E1-M2 |
| 3 | A nurse is on the roster but her account is deactivated | `whoIsOn` ignores inactive users; the parity fixture holds a deactivated holder; the roster screen names her (no second census row — P20 has one) | fixture + screen | E1-M3 |
| 4 | Doctors' strike: six consultants absent in one upload | Coverage Resolver emits `coverage.proposed` (T3) — **only outside `commissioning`**; nothing is assigned until the duty manager approves; in `commissioning` no proposal is emitted | `coverage.proposed` without `coverage.applied`; the mode-matrix test | E1-M3 |
| 5 | A phlebotomist claims a round and her phone dies | TTL 15 min → `task.released{reason:'ttl'}`; the item is back in the pool, SLA clock untouched | event pair | E2-M2 |
| 6 | `pool_empty` at 03:00, the duty manager asleep | the owner rung fires (`fallbackExhausted`); the task stays visible; the fact sheet carries it | alert + ledger | E2-M2 |
| 7 | The owner types "show me Ram Kumar's bill" into Telegram | the name has already reached Telegram and the provider — the addendum names both as processors of the owner's prompts and the skill says not to type names; **zero egress from HMIS**: no tool answers, scripted refusal, ledger row | `agent_ledger` refusal, no `copilot.tool_called` | E3-M2 |
| 8 | A ward name in a master carries a phone number | scrubber fails the call closed; alert to the admin; the digest says "one figure withheld" | ledger `outcome='refused'` + the fixture | E3-M2 |
| 9 | The second server is unreachable at 08:00 | Hermes reports "the hospital did not answer", retries 08:15; nothing in HMIS notices | Hermes log; no ledger row | E3-M3 |
| 10 | A staging Hermes is pointed at production | keys are prefixed (`stg_`/`prod_`, a build item — `createAgent` mints unprefixed today, `agents.ts:9`); production refuses; no production key before the signatures | test + `agents` row absent | E3-M4 |
| 11 | A ceiling fan hums into the counter mic | `vad_filter` returns ""; no phantom query; the egress row exists with `outcome='empty'` (distinguishable from a provider throw, `speech.controller.ts:100-104`) | `search_audit` row | E4-M2b |
| 12 | Cloudflare is down mid-shift | 503 `speech_provider_failed`; mic greys; typing unaffected; no retry buffer | test + component | E4-M2b |
| 13 | A doctor holds the scribe key on a stack where `SPEECH_*` is set | **403 `speech_surface_refused`** — the route refuses any caller without `surface:'search'`; the component shows its "not switched on" state; nothing leaves | test; the CI pin | E4-M2a |
| 14 | The registrar opens voice at the public counter | the palette's browser-local open-counter setting renders no mic; the server refuses nothing (rule-4: config-first; no desk identity exists) | test + the doc's rule-4 line | E4-M2b |
| 15 | A Bhojpuri-only grandmother who reads Hindi registers | `spoken=bho, read=hi`; WhatsApp and the slip in Hindi; the ASR hint stays `hi` until 11h-iii | patient row + `notification.sent.language='hi'` | E5-M2 |
| 16 | A language has no approved WhatsApp template | send in `hi` + `notification.language_fallback`; never dropped | event pair | E5-M3a |
| 17 | The relay PC has no Devanagari face | the test page prints boxes; the seat drill stops there; the runbook line is named (NOT MODELLED → runbook §n) | printed page + runbook line | E5-M4 |
| 18 | The lab refuses "exactly one of encounterNo or walkIn" to a Hindi seat | a code on the wire; the screen renders its locale string; digits read the same in both scripts | the wire test + locale key | E5-M3c |
| 19 | **Session:** E3-M3 is green, the DPIA is unsigned, the owner is asleep | "production connection waits on the signatures" written into §8 CLOSE; no key created | §8 line; `agents` row absent | E3-M4 |
| 20 | **Session:** a lane flips `VOICE_SEARCH_ENABLED` or drops the scribe refusal to make a test pass | a CI pin asserts flag `false` AND the `surface` refusal on `main` while no signed addendum exists in `docs/compliance/` | the pin test | E4-M2a |
| 21 | **Absorption:** the roster is deployed but nobody on production holds `duty_manager` | seed/census RED; `deploy.sh` prints it and continues (`docker/prod/deploy.sh:663-668`); the S-gate cannot close; the owner names the holder (F4) | census + runbook | E1-M3 |
| 22 | **Session:** 22-L's migration took `0080` at authoring; by rebase it is taken | numbered at rebase (CLAUDE.md drizzle rule; the five-step procedure) | serial = next free at rebase | E5-M2 |
| 23 | **Absorption:** the three clips never arrive | E4 closes "stays off — NOT MEASURED" with the date; no synthetic clip substitutes (11h:688) | §8 CLOSE line | E4-M3 |
| 23b | **Absorption:** the owner refuses §3-A; three clips exist | the bench never runs (it refuses without the filled row); §8 CLOSE records the refusal; the clips are destroyed under the protocol | §8 line + the destroy line ticked | E4-M3 |
| 24 | **Session:** RC-4/RC-6 and 12a-0 both mint `agent_ledger` | whichever reaches `main` first owns the table with H:147's columns incl. `surface`; the second rebases (H:172; `opd/events.ts:224`; SCOPE:40) | one migration; `surface` present | E3-M2 |
| 25 | **Absorption:** the delegate user loses hospital scope in a reshuffle | user ∩ agent = ∅ → every call 403s; `/admin/agents` shows why; the digest stops and says so (H E8) | 403 ledger rows; admin page | E3-M4 |
| 26 | **Config-before-guard, E1:** T5's destination row lands and nobody set it | the ladder resolves to `duty_manager` exactly as today (P20 D2, D6); census `escalation_destination_set` RED with the fixing screen | census row + `escalation.triggered{fallback:true}` | E1-M2 |
| 27 | **Role-unheld, E1:** nobody holds the publish permission (P20 D3) | no period can be published; with the flag on every escalation falls back and T7's row is the signal; `roster_publisher_held` (G4) beside it | census rows | E1-M2 |
| 28 | **Deploy-day, E3:** `/mcp` reaches production with no `agents` row, no allowlist | 401/403 for every caller; the Caddyfile does not expose it until the runbook step that does | `caddyfile-parity.test.ts` + a 401 test | E3-M2 |
| 29 | **Role-unheld, E3:** nobody on production holds R-127's halt authority | census `agent_halt_authority_held` RED; no production key (row 10's rule extended) | census row | E3-M4 |
| 30 | **Deploy-day, E5:** the migration lands; `languages` empty; `read_language` NULL on every row while `language` stays `notNull().default("hi")` (`schema/patients.ts:95`) | the pump reads `patients.language` exactly as today (`pump.ts:376-379`); **zero** `language_fallback` rows — an empty registry is not a fallback; census `languages_registry_populated` | fallback count = 0 on deploy day; census row | E5-M2 |
| 31 | **Ops act, E5-M4:** the relay (`render.ts:19-32`) is code on `main`, installed on no UAT desk | the print step records "relay not installed"; the runbook names the install as an ops act, not a defect | runbook line | E5-M4 |
| 32 | **Law, E6-M1:** a lane sends the clip set to two vendors before any DPA | the bench refuses without the named protocol (DPA/NDA, on-prem, or the no-PHI set); the doc records which | the doc + refusal | E6-M1 |

## 5. DECIDED — judgement calls — and RULINGS (money / procurement / law / facts only)

**DECIDED** (Indian-corporate-hospital default; one line each)
- D1 The fabric is the event spine + workflow + ladders; no central planner (12:158; spec:776,780).
- D2 The Hermes bridge is **12a-0**; 12b's "only if reopened" slot (12:16) is this reopening; 12c's Lane-3 copilot stays 12c; spec:791 superseded.
- D3 E3 mints `agent_ledger`; RC-4/RC-6 consume — whichever reaches `main` first (H:172).
- D4 **A recommendation to counsel, not a rule:** addenda (Class-0/MCP; §3-A; later L1) beside the base so one session can sign the base §8 row and the Class-0 addendum independently of §3-A. No activation runs on the addendum alone (CONTEXT.md:28).
- D5 Search-flip threshold **WER ≤ 25 %** on the scripted Hinglish set; revised only by measurement.
- D6 UI stays hi/en; a third language enters through paper, messages and audio (20:427); 3,073 leaves × N is a translation job.
- D7 Registry = BCP-47 tag + script + spoken/read; `patients.language` survives as the read alias.
- D8 Roster policy rows R-071/R-072/R-067 at register defaults (REG:83-84,79). *R-110 dropped — it is Plan 43's (REG:122).*
- D9 Coverage proposals T3; T1 in `disaster` (12:174); **off in `commissioning`** (12:175); the resolver is a flag-gated function, no harness.
- D10 Claim TTL 15 min; `pool_empty` → duty manager; a task points at the order.
- D11 Plan 20 ships flag + static fallback (P20 D2); no consumer changes behaviour with the flag off.
- D12 Hermes v0: Telegram, pull-only, ten Class-0 tools, no staff names, skill creation off, **"do not type names"** in the skill.
- D13 The bench never runs in CI; CI never contacts a provider (`voice-flag.ts:21-23`); no running stack sets `SPEECH_*` this quarter.
- D14 A language flips one at a time on ≥ 50 own clips.
- D15 **Displays get pre-rendered numeral clips per language (doc 20 EQ-2 adopted, 20:375,641) — no live TTS, server or browser, ever; token + room only.** *(Reverses the design's server-TTS line.)*
- D16 Steward = the quality manager (R-133); global-halt authority per R-127, **enforced by a DB row with actor events**, not an env var; dictation-to-note is P2's.
- D17 No "15 languages" milestone: N is measured (§0 says so first).
- D18 Prescription drafting and ambient listening are P2's.
- D19 `users.ui_language` optional; browser-local choice stays for shared desks (`i18n.ts:8-13`).
- D20 The language pack is **22-L**, not `22d` (22q:537 → 28d).
- D21 Who translates ~8 templates + 4 papers and records the numerals: the hospital's own bilingual staff, reviewed by the department head — `staffing`, a register category (R-133, REG:145), not money.
- D22 19a's first consumers are the phlebotomy round and the radiology technician worklist, **changing R-094's ruled housekeeping-first** (`scope`, REG:106): those departments will have passed G5/G6; Plan 19 has no runbook or date. Housekeeping third; porters when 15 passes G5.
- D23 E6 is **11h-iii**, not 12c: 12c's gates (Plan 19 for the cohort, DPIA L1; 12:624) are not E6's.
- D24 **The scribe stays refused server-side** by a `surface` discriminator until a dictation addendum is ruled under R-059; any carve-out stamps spec:789's provenance.
- D25 `whoIsOn` and the chaser are "flag-gated functions, no harness" — the harness is 12a's (spec:780; RM:428).
- D26 The `fallbackExhausted` digest line is E3's `digest.fact_sheet` field, not a Plan 20 task.
- D27 Pool eligibility = the role key at hospital scope until a competency table exists; P20 dropped index:66's tags without naming them — the rider records them out of scope.

**RULINGS** (money / procurement / law / facts only)
| id | question | category | recommended default | unblocks |
|---|---|---|---|---|
| R-262 + H R1 | DPIA v0.2 for the first activation: base §8 counsel row **and** the Class-0/MCP addendum; closes DPIA:27 (Class-0 transfer analysis) and DPIA:29 (the router) | law | one counsel session produces the base row, the Class-0 addendum and the §3-A ruling (RM:405) | E3-M2, E3-M4 |
| DPIA §3-A (DPIA:31-33) | accept or refuse the speech-to-text carve-out for read-only search, with the clip protocol | law (owner's accept) | accept on the stated terms; closes when in-region ASR arrives | E4-M3, E4-M4 |
| R-059 (REG:71) | the §19 audio inference-locus amendment | law | search-only carve-out; transcript-to-note stays blocked (D24) | E4; E6's ceiling |
| spec:839 | the stage-2 DPDP posture for the voice class (no artefact on disk) | law (counsel) | stated inside E4-M1's addendum | E4-M4 |
| P20 §7 (P20:136-139) | does an on-call person's contact detail live in this system (DPDP, retention) | law | no number in T1–T7; ruled before any telephone rung | E1 — non-blocking for T1–T3 |
| Second server (RM:440-441; H:182) | an Indian-region VPS for Hermes | money | ₹1,500–3,000/month, 2 vCPU / 4 GB | E3-M3 |
| H R3 (H:183) | pinned model provider under a no-training DPA | money + law | Anthropic direct, pinned model | E3-M3 |
| R-131 (REG:143) | inference cost cap | money | ₹5,000/day in commissioning; class halt at 150 % of the 7-day median | E3-M4 |
| R-082 (REG:94) | housekeeping in-house vs outsourced — sits on 19a authoring | purchase | already defaulted (outsourced CLRA vendor); re-confirm at E2-M1; does not block the clinical pools | E2-M1 |
| 19a BYOD (DPIA:38) | patient identity on staff phones for the pool worklist | law | hospital-managed devices or a consent + notice line in 19a's doc | E2-M3 |
| NEW-P1-1 | the Indic ASR vendor with an India-region DPA (no register row) | procurement + law | in-region provider under DPA; on-prem only if the GPU box is bought | E6-M1/M2 |
| NEW-P1-2 | the on-prem GPU box, ₹3–6 L (12:603) | money | **not now**; only on E6-M1's evidence *and* P2's dictation ruling | on-prem ASR |
| NEW-P1-3 | WABA + DLT registration and per-language template approval (`templates.ts:17`) | procurement | register now; console adapters (`notify/adapters.ts:11`) until then | E5-M3a |
| **Facts (F1)** | the owner's approval line on #150 (Plan 20) — held by the orchestrator, CONTEXT.md:39 | fact | one line | E1-M2 |
| **Facts (F2)** | the owner's line on #118–#122 (11i T3/T5/T6/T7/T9; landing them ratifies the 12:35 deploy) | fact | one line | every "(held)" dependency: E1-M3, E3-M3, E5-M1, E5-M4 |
| Facts (F4) | the names: duty manager and on-call holders per role; the desk's three scripted clips; the catchment's language mix | fact | a row without a name reads *unstaffed* | E1-M3, E4-M3, E5 |

## 6. Not now — what this pillar does NOT build this quarter and the next, and why

- **No in-house 12a runtime, no `InferenceClient.complete()`** (RM:428; `types.ts:9-11`). **No `kernel/copilot`, no `agent_permissions` this quarter either** — E3-M2 waits for the signed addendum (index:57 "slot when the DPIA is signed"; DPIA:5 defines grants + catalog as the runtime); it lands Q-next and revises RM:428 when it does.
- **No tasks engine this quarter** (RM:414); porters not until 15 passes G5.
- **No ambient scribe, no dictation-to-note, no prescription drafter** — and **no stack where the scribe answers**: the route refuses `scribe` by construction (D24) and no stack sets `SPEECH_*` (RM:428).
- **No 11h-ii seams this quarter** beyond the scribe refusal and the bench script (F's lane is not free — RM:337; CONTEXT.md:41).
- **No UI in a third language** (D6). **No live TTS for displays, ever** (D15). **No cloud ASR for clinical narratives** (12:514); **no model talking to patients** (12:511).
- **No IVR rung** until a telephony provider exists; **no model translation of signed documents** (R-006, REG:18); **no agent writes from Hermes** (H:62-71); **no Workflow Tuner** (spec:786).
- **Why:** absorption before building. Production has ≈ 500 events and no real patient; every agent parameter is 0 because no department is open. **Q-this is: one substrate (E1, approval → build → UAT → live), four documents (E3-M0, E3-M1, E4-M0, E4-M1), one 1-day guard (E4-M2a), one script (the bench) and one measurement (E4-M3).**

## 7. Sequencing note — against ROADMAP v2 §2's 13 weeks and the two quarters after

Against RM §2 (Monday 2026-09-07). Production runs a lane build (`399f92c`); every "weekly deploy" is the governed one T7/T8 define. Each start names its human act, so a slip reads as *that act's* slip.

- **Wk 1–2:** nothing from this pillar on S — S's fortnight is re-landing the 11i stack (RM:334-335). *Human acts:* the owner's two lines — #150 (E1-M1) and #118–#122 (F2). The second server whenever he likes (nothing waits on it).
- **Wk 3:** E1-M1's riders committed to P20 §5 on approval; E3-M0, E4-M0, E3-M1, E4-M1 — documents, in the parked pharmacy worktree or by R before its build starts. *Human acts:* the owner books counsel; accepts or refuses §3-A in one line; approval lines on the two new phase docs.
- **Wk 4–6:** E1-M2 by R (10 d, RM:337); E4-M2a (1 d) and the bench script (1 d) by any lane. *Human acts:* F4 names the duty manager and on-call holders; the desk records three scripted clips once §3-A is accepted.
- **Wk 7–9:** E1-M3 on UAT (needs T3 landed — F2); E4-M3 off-stack if the accept line and the clips exist. *Human acts:* the duty manager walks the roster drill; 20's close review (RM:338).
- **Wk 10–13:** E1-M4a live through the weekly deploy — the IPD gate's "20 live" (RM:339,400). *Human acts:* the weekly deploy; department heads publish the first period.
- **Q-next (Dec–Feb):** E1-M4b (the 30-day read from the first production escalation); E3-M2 → M3 → M4 on the signatures, second server and DPA; E4-M2b, E4-M4 flip or close; E2 M1–M4 after E1-M4a; E5 M1–M5 after T6 executed and a front-desk seat walked. *Human acts:* counsel signs; NEW-P1-3; the seat walks.
- **Q+2 (Mar–May):** E6 (11h-iii) M0–M3 after E4-M3 and E5-M2. *Human acts:* NEW-P1-1 vendor pick; NEW-P1-2 only on E6-M1's evidence. 12c proper stays P2's, behind DPIA L1.

## 8. Changelog — each refutation → accepted/rejected → what changed

**exists-or-collides**
- R1 (KILL E1-M1) → **accepted.** P20 exists (#150, P20:3). E1-M1 is no longer an authoring milestone: it is the approval line + riders (0 d, the owner); the doc moved to *Exists*; §7 wk 3 says "riders on approval", not "authored by R". Kept as a row rather than deleted so a waking session sees the gate (measurability and sequencing asked for exactly that shape).
- R2 → **accepted.** E1 rewritten in P20's names: three runtime files (:28), seeds static (T3:100-102), `whoIsOn(tx, roleKey, at)` (:96), `roster_periods`/`roster_assignments` (:92), the `roster` module app+worker (D1:64-67), one census row RED = enabled AND unpublished (T7:117); edge 2 inverted; P20 §7's ruling in the table; §6's alert-prose handoff claimed by E5-M3c.
- R3 → **accepted.** E3 names and retires 12b (12:16) and distinguishes 12c's Lane-3 copilot (12:374); spec:791 superseded; E3-M0 commits H.
- R4 → **accepted, option (i).** E6 → 11h-iii (D23); display audio → 22-L as EQ-2's clips (D15 reversed); `announcements` has one home.
- R5 → **accepted.** D22 states the change to R-094's first consumer and why; R-082/R-089 named in §5/E2.
- R6 → sound, no change. R7 → **accepted** (`docker/prod/deploy.sh:663-668`, verified). R8 → full path used. R9 → spec:783. R10 → `schema/printing.ts:37`. R11 → #131's `error-strings.test.ts` + `labErrors` named; alert prose claimed. R12 → 3,073 (re-measured: 3,073 / 67 / 66). R13 → **corrected further:** six runbooks on `ff07cbc` (opd added by #155; no OT runbook landed despite CONTEXT.md:37). R14 → R-110 dropped from D8. R15 → RC-4 (`opd/events.ts:224`) named in edge 24 and D3. R16 → header now `ff07cbc`.

**measurability**
- G1-a/b/c → **accepted.** `resolvedBy` rider; second SQL over `alerts` (`alerts.ts:17,23`; `radiology/events.ts:162,167`); lab ladder resolves no role (`criticals.ts:49-62`, verified); floor ≥ 20; reader = SQL + census. Baseline "undefined".
- G2-a/b/c/d → **accepted.** Ledger rows per IST day; "0 leaks" dropped for the refusal count + fixture; Hermes `agents.id` filter (relay is an agent, `printing.controller.ts:29-35`); kill switch "column only, not evented" (`agents.ts:26`, verified).
- G3-a/b/c → **accepted.** Per actor until `terminal_id` (11h-ii, Q-next); ≥ 3 vs ≥ 50 stated; `outcome` column.
- G4-a/b/c → **accepted.** Pattern stated; re-measured **14 lines** (my double-quote grep gives 13; the 14th is the single-quoted comment at `schema/patients.ts:95`, included by the stated pattern); target "0 outside `kernel/languages/`"; the 100 % is a pin; NOT RECORDABLE.
- G5-a/b/c → **accepted.** Three defect classes; allowlist test = E5-M4 build; `qc.ts:40`.
- E1-M1, E1 Exists (chaser row), E1-M2/M3 names, E1-M2 three → **accepted** (see R1/R2). E2-M3 → **accepted** (`sweeps.ts:260` `slaByState`; the cite `:18-19` was wrong — imports — corrected). E5-M3 wire assertion → **accepted** (E5-M3c). E5-M5 < 5 % → **accepted.** E4-M4, E4-M2, E3-M1 (H §3.4), E3-M3 (what is counted), edge 4, edge 10 (prefix is a build item, `agents.ts:9` verified), edge 24 → **accepted.**
- **Rejected (one note):** "`second_administrator` row at `:168-172`, not `:173`" — measured at `ff07cbc`: `:168` is `registration_config_present`; `second_administrator` starts at `:173`. The design's cite stood; kept as `:173-177`.

**authority-and-safety**
- §1 (KILL E4's flip) → **accepted.** Verified: `consult-scribe.tsx:1-3,91` (no `voice-flag`, `{ audio }` only), `opd-consult.tsx:1080-1091` (unconditional mount), `speech.controller.ts:30-34` (no discriminator). D24; E4-M2a "the scribe refused, pinned" (1 d, Q-this, before any stack sets `SPEECH_*`); edge 13 rewritten as the refusal; edge 20 covers it; E4 (c) scribe-language/`scribe.suggestion_*` removed from E4 and named as P2's under R-059 with spec:789 stamps; "processor, no tier" restricted to search.
- §2 → **accepted, and strengthened:** E4-M3 depends on E4-M1 with the accept line filled; the bench refuses without the filled addendum; clip protocol in E4-M1; **the bench runs off-stack** so E4-M3 no longer depends on UAT at all (D13).
- §3 → **accepted** (E6-M1 protocol; edge 32). §4 → **accepted** (edge 7; E3-M1 deliverables; D12). §5 → **accepted** (D4 as a recommendation; E3-M4/E4-M4 gates = base §8 row + addendum; DPIA:27/:29 named). §6 → **accepted** (D9; E1-M2 mode-matrix test; edge 4). §7 → **accepted** (global halt DB row, evented, R-127; env var = deploy-time master). §8 → **accepted** (D21; `staffing` is a register category — R-133 REG:145). §9 → **accepted** (spec:839 posture row; E4-M1 states it; E4-M4 gate).
- N1 → R-110 dropped. N2 → the R-262 reconciliation line in E1 Gates. N3 → moot after D15 (clips, `token_no` only, E5-M3d). N4 → the BYOD ruling row. N5 → D25. N6 → §0's first paragraph. N7 → E3 Gates says E3-M2 revises RM:428. N8 → no change.

**sequencing-and-absorption**
- F1, F2 → **accepted** (as R1/R2; everyone-files completed incl. `manifests.ts`, `app.module.ts`, `worker.module.ts`, `alerts/consumer.ts`, `timers.ts`, `notify/consumer.ts`, `standup-check.ts`; digest line → E3 (D26); competency tags → D27 rider).
- F3 → **accepted, corrected:** the hold is **#118–#122** (gh: #117 shows MERGED — T8 via #146); "(held)" on E1-M3, E3-M3, E5-M1, E5-M4; the fact row F2 in §5. E4-M3 no longer carries it (off-stack bench).
- F4 → **accepted.** E3-M2 depends on the addendum SIGNED + base row; E3-M2/M3 Q-next; §7 wk 7–9 drops them; §6's summary now true. The alternative (override RM:428/index:57) not taken.
- F5 → **accepted** (with §2 above; edge 23b).
- F6 → **accepted in part.** E4-M2b (seams, open-counter, `terminal_id`) Q-next; Build (d) palette-side with the rule-4 line (edge 14); "per desk" → "per actor". **Kept Q-this:** E4-M2a, the scribe refusal (1 d) — the authority KILL requires it before any stack sets `SPEECH_*`, and it is a guard on a route that is 503 anyway; the bench script stays Q-this as F6 asks.
- F7 → **accepted.** `ops-copilot-go-live.md` in E3's Build; E3-M0, E4-M0, E6-M0 with lanes and the approval-line act.
- F8 → **accepted, corrected:** `docs/runbooks/opd-go-live.md` now **exists** (#155, `ff07cbc`) but has no seat walk — E5-M1's precondition is a seat-walk section added and executed, not a new runbook; E5-M3 split into M3a–d with lanes; porters deferred to 15's G5 (D22).
- F9 → **accepted** (edges 26–31; 32 from authority §3).
- F10 → **accepted** (§7 wk 1–2 empty for this pillar; documents wk 3). F11 → **accepted** (E1-M4a/M4b; the window starts at the first production escalation). F12 → **accepted** (H:201; everyone-files in E3). F13 → **accepted** (NS1–NS5; 22-L inheritance line). F14 → **accepted** (E3-M4 gate named "DPIA v0.2 = base row + addendum"; R-262's one session produces all three).
