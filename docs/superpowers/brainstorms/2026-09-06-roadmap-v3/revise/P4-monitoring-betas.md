# P4 — Patient care & monitoring: personalised baselines, DDI (beta), Vision-AI wound assessment (beta), physio ROM trajectory (beta) — REVISED

Read-only design, **measured at `91c34fc` on 2026-09-08 11:32 UTC** (`git show 91c34fc:<path>`; `main-ro` checkout = `ff07cbc`). At write time `origin/main` = `41a4be4` (+1: #118, 11i T9 — web router/redirect test + `caddyfile-parity.test.ts`; no core module). Production = `399f92c` (78 migrations; `0078`–`0081` pending). Paths relative to `main-ro`. Series = `docs/superpowers/brainstorms/2026-08-27-department-series/`; REG = its `00-OWNER-RULINGS-REGISTER.md` (R-nnn at line 12+nnn); INDEX = its `00-INDEX-AND-SYNTHESIS.md`; RM = `docs/superpowers/2026-09-06-ROADMAP-v2.md`; SPEC = `docs/superpowers/specs/2026-08-10-hmis-architecture-design.md`; COP = `docs/superpowers/specs/2026-08-25-clinical-copilot-design.md`; DPIA = `docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md` (self-labelled "0.2 DRAFT … not yet reviewed" `:3`, §8 sign-off empty `:66-68` — every gate below says **"counsel-signed DPIA"**); PHY/NUR/ICU/OTD/QUA = series docs 04/07/06/15/22; SC = `apps/core/scripts/standup-check.ts`; vd1/vd2 = `docs/superpowers/plans/2026-08-31-phase1-vd1-vitals-rails.md` / `2026-09-02-phase2-vd2-bay-one.md`; P15 = `plans/2026-08-28-phase1-15-mini-ot-daycare.md`; 22cD = `plans/2026-08-28-phase1-22cD-appointment-journey.md`.

## 0. The mission sentences this pillar serves, and the honest reconciliation

**Mission (verbatim):** "patient monitoring capabilities that adjust alerts based on individual clinical histories rather than fixed thresholds" · "automated drug–drug interaction checks" (beta) · "AI-based post-surgical wound assessment through remote image scans" (beta) · "track pre-/post-surgery range of motion for joint replacements; alert clinical teams if recovery strays from the expected trajectory" (beta).

**What is true today.**
1. *Monitoring.* One surface in production: the OPD vitals bay (VD-2 `451458b` ⊂ `399f92c`; `/opd/vitals`, `apps/web/src/router.tsx:661` — #118 adds 56 lines there, re-measure). Thresholds are four age bands as `opd_config.danger_ranges` DATA (`modules/opd/config.ts:116-143`; one typed row, `dangerRangesSchema` `:74`, loader hard-fails on invalid jsonb `:177-183`); `evaluateVitals(v, band, cfg)` takes no patient (`vitals-rules.ts:175-213`); adult `pulse.min: 50` is `danger` (`config.ts:138`); a double-confirmed danger bumps the board alone with a 10-s cancel (`escalation.ts:21-33`). **The mission's runner is a live false positive.** The board decision is made at FOUR sites: `dangerOf` (`escalation.ts:131`; called `:203`, `:250`; its comment `:143-144` warns "a fourth will be added"), the record path (`vitals.ts:267` → `danger: true` `:312-318`), the amend path (`vitals.ts:438` → `:469-478`). No per-patient baseline exists anywhere (code, plans, SPEC:782-787 roster, REG); nearest: the ICU limit order (ICU:99; R-070 REG:82). **VD-3 is taken** — the serial seam (vd1:14; `plans/reports/2026-08-31-vitals-desk-recon.md:168-173`) — and VD-2's close owes it two defects on the state machine an order would consult: the `cancelled`-terminal contradiction (vd2:167; `ne(escalation,"cancelled")` at `vitals.ts:312-318`) and the held first take (vd2:221, `:271`, a migration).
2. *DDI.* Plan 16a is deployed: checks at issue time (`opd/rx-checks.ts:169-186`, `[]` on an empty book `:186`), the refusal thrown at `prescriptions.ts:373-382` BEFORE `withTx` opens at `:409`; the dispense re-check (`pharmacy/verify.ts:197-205`). The 26-pair seed runs at `docker/prod/deploy.sh:588` under `set -euo pipefail` (`:40`), and **the census row reading it EXISTS since #162 (09-07): `hospital.formulary_interactions_loaded`, G2, predicate `salts ≥ SEED_CENSUS.salts && pairs ≥ SEED_CENSUS.pairs` (`SC:183-227`; ok/RED test `test/standup-check.test.ts:477-490`), printed by every deploy (`deploy.sh:663-667`; `GIT_SHA` `:86,:250`).** It sits on `hospital` because the consumer is prescribing (`SC:194-197`); `runCensus(db,"pharmacy")` never prints it (`SC:667-673`). Override rate: numerator only (`opd/events.ts:341`; 16a plan `:377-381` F22). Curation half-built: `PATCH /formulary/interactions/:id` (`formulary.controller.ts:266-268`) → `updateInteraction` (`masters.ts:316`) → `interaction.updated{interactionId, changed}` (`formulary/events.ts:60-62`), no `reason`, no screen (`formulary-admin.tsx`: 0 "interaction"); `formulary.manage` granted to one role, `pharmacy` (`scripts/seed-roles.ts:321-334`, "live authority to nobody today"), read by `pharmacy.pharmacy_role_held` (`SC:423`). Curated rows are distinguishable: `source` NOT NULL (`schema/formulary.ts:139`), `SEED_SOURCE = "seed-2026-08"` (`seed-formulary-interactions.ts:79`).
3. *Wound images.* No patient can upload: no credential, no multipart (comments only, `membership.controller.ts:95`, `partners.controller.ts:95`), 22c-A…F NOT APPROVED (22cD:3); the only image write is the 512 KB staff photo (`schema/patients.ts:200,264`). NUR:464 *rejected for now* an assessor ("no DPIA basis … not before stage 4"); NUR:587 defers photography on "DPDP basis and storage cost". DPIA:20 (Class 2 never enters inference) and the speech carve-out (DPIA:31-33) are the only templates; R-169 (REG:181, legal) wants a photography policy first; R-008 (REG:20) makes image-AI "bought never built". The series' deterministic surveillance — `ssi_register` (OTD:156), Q-4 day-30 survey (QUA:350) / O-15 = R-249 (REG:261), `ssi.followup_recorded` (QUA:92) — is **not built and its designed home is Plan 28b**: OTD:554 ("Plan 22 — Quality pack … absorbs the OT-local registers"), INDEX:74 (28 absorbs "15 (22 quality pack)"; 28b = infection control), INDEX:124, QUA:535 (22b: "SSI post-discharge survey via Recall"). P15 never took SSI (grep empty) but promised "`dischargeDaycare` (DD10 + follow-up recall task via the notifications/scheduler seam)" (P15:339) — unbuilt, and the code's comment was **corrected 2026-09-07: "no document asks for a recall task … there is no task primitive anywhere in the kernel — building it would be inventing a subsystem"** (`ot/recovery.ts:553-566`). `daycare.discharged` carries `{encounterId, patientId, bayResourceId, at}` (`ot/events.ts:84-86`), no consumer outside `modules/ot`. The OT now HAS `docs/runbooks/ot-go-live.md` (#157) and six census rows (`SC:460-522`); `daycare_case` is Class A (`ot/workflow-def.ts:72`; `SC:487-490`) with two `alerting:"active"` states (`:80,:88`).
4. *ROM.* Plan 25 is Track C, needs "13, 09, 10; 15's `daycare.discharged`" (INDEX:71; PHY:485), no phase doc. Designed: `outcome_scores` with `ROM` (PHY:156), goniometer widget (`:417`), packs (`:416`), N8 clearance (`:330`), four go-live active alerts (`:63`), events incl. `patient.recall_initiated`, `task.*` (`:88` — designed names, none in code). PHY:489's must-be-true list gates 25's AUTHORING; R-036/R-038 are **money, pre-plan-authoring** (REG:48,50), R-043 the **CA** line (REG:55). No pre-op step; no PAC row (`schema/ot.ts:96` is a definition kind).

**Reconciliation.**
- *Tiers.* Clinical actions cap at T2–T3 (SPEC:778); "the rule wins" (SPEC:780). Everything below is a sweep, a definition SLA, or a human order; nothing calls a model. **Honest line on §16's harness:** `main` has ONE kill switch — `agents.kill_switch` (`schema/auth.ts:97`; `kernel/auth/agents.ts:25-27`) enforced only in the HTTP guard for `x-agent-key` callers (`kernel/auth/guards.ts:47-53`) — and **no global halt** (grep `global_halt|AGENTS_ENABLED` → empty). Sweeps are in-process registrations with a heartbeat and `sweep.failed` (`kernel/worker/scheduler.ts:9-16`), no runtime disable (`jobs.ts:308,351` are config-time). So every automation here gets a **DB-held switch whose flip appends an event**: E1's in `opd_config` behind `PATCH /opd/config` plus a new `opd.config_updated` (`updateOpdConfig` `config.ts:244-252` appends none today); E3's and E4's ARE their definition versions (activated/retired through the approvals engine, evented, SPEC:206-208).
- *"Individual histories rather than fixed thresholds"* → **a doctor's dated per-patient limit order consulted by the BOARD decision only, never the chart** (VD-1 D4 vd1:52), inside the ONE helper all four sites call; plus a proposal that is an **EVENT, never a row** (only the ordering API writes the table). No inference, no DPIA delta, no IPD dependency. A learned suppression is refused (COP law 3 `:28`; ICU:468 "no per-user tuning").
- *DDI beta* → **evidence**: the row that exists, cited; a denominator event; a curation screen with a `reason`. The licensed dataset (₹8–12 L/yr, RM:37,450) stays 16e's.
- *"Vision AI wound assessment"* exceeds what is lawful or designed (no patient actor, no rail, no DPIA basis, no policy, R-008). **Reconciled goal: every day-care surgical patient has a wound outcome by day 30, text-first**, built with the primitives the kernel HAS — a workflow definition with active SLAs (`sla.breached` → `escalation.triggered` → `alerts` row → `alert.read`; `kernel/workflow/events.ts:20-45`, `timers.ts:119-160`, `kernel/alerts/consumer.ts`) and a scheduled `notifications` row (`scheduled_for`, `schema/notifications.ts:43`). A consented photo may attach to a NURSE's escalation as a human-read artefact (H-8 `03-home…md:380`; N-P5 NUR:319) after R-169 and the DPDP gates (§5). Inference over images is PARKED (§6).
- *ROM* → Plan 25's design plus a procedure code on `daycare.discharged` (15-ii), the pre-op ROM captured INSIDE 25 (with its reader), and a **T1 Trajectory Watcher as an active-SLA state** of 25's session definition — ack is `alert.read` for free.
- *IPD gate.* Nothing here needs 41/42a/49/50 (RM §5:419).
- *Absorption first, re-measured.* Nine runbooks; only pharmacy's has `## Executed` and it reads **NOT YET RUN** (`pharmacy-go-live.md:257`); `opd-go-live.md` (#155) names the bay's seat once (`:154`), nothing of bands/escalation; `front-desk` census has `opd_config_present`, `opd_visit_definition_active` (`SC:239-244`). UAT: 11i T3 "UAT as a deploy target" is **#120, still OPEN** (#118/T9 merged 09-08 11:59 UTC; #119/#121/#122 open). Every artefact below that reads UAT or production waits on two owner acts: the 11i ruling and "the weekly deploy by the owner's hand" (RM:249).

## 1. Goals — north-star parameters

| # | goal | parameter | instrument | baseline today | target | who reads it |
|---|---|---|---|---|---|---|
| G1 | The hospital knows its own alarm rate (INDEX:198 risk 4) | danger flags **per 100 charts** by band × vital; the chain `recheck_demanded → escalated → cancelled` per day; cancel timing | numerator `vitals.danger_flagged` (`opd/events.ts:198`; vital axis `:24-29`), **denominator `vitals.recorded{band}` (`:190-196`)**, `vitals.recheck_demanded` (`:230`), `queue.escalated` (`:245`), `queue.escalation_cancelled{withinMs}` (`:255-258`); one nightly row per (day, band, vital) in `opd_vitals_flag_days` | computable; **no reader** (`realtime.ts:11` only); prod ≈ 500 events at the last reading (09-06, pre-deploy), unmeasured since | a dated row every night from the first day the bay records on UAT/production; `withinMs` p50/p90; cancels ÷ escalations < 20 % over 7 d **read only when escalations ≥ 20** | OPD in-charge over the table (no Digest Writer exists in code) |
| G2 | A known baseline never bumps the board; nothing else changes | (a) `queue.escalated` per 1,000 charts on readings INSIDE an order in force (join `ordered_at ≤ recordedAt < coalesce(cancelled_at, valid_until)`) vs outside; (b) the chart invariant | `patient_vitals_baselines` + `vitals.baseline_ordered/.superseded/.cancelled/.lapsed`; the rollup's `baselined` column | not measurable — no table; the runner is flagged and bumped | (a) 0 in the first pilot month at ≥ 1 order per 200 charts; (b) un-ordered rows follow band rules byte-for-byte (the `vitals.ts:262` mutant); baselined rows `flags ≥ escalations` nightly | OPD in-charge; the doctor on the tile |
| G3 | Every prescription is checked against a non-empty, curated pair set; the override rate has a denominator | (a) book loaded and curated; (b) share of the last 30 days' Rx lines resolved; (c) weekly override rate | (a) `hospital.formulary_interactions_loaded` ok in the deploy transcript + `count(*) FROM formulary_interactions WHERE source <> 'seed-2026-08'`; (b) nightly query over `opd_prescriptions.lines` (E2-M2); (c) `interactionOverrideCount` ÷ (overrides + `prescription.check_refused` then `rx.check_changed`) | (a) inferred ≥ 26 (deploy.sh:588), **printed by the next owner-hand deploy**; curated 0; (b) unmeasured until E2-M2's query; (c) numerator only | (a) ok on every transcript; curated > 0 within a month of the pharmacist holding `pharmacy`; (b) ≥ 80 % (COP:61) by pharmacy G6; (c) weekly **only at denominator ≥ 20** | chief pharmacist; DTC chair (R-243) as reviewer |
| G4 | Every day-care surgical patient has a wound outcome by day 30, text-first | share of `daycare.discharged` with `ssi_register.followup_recorded_at ≤ window_end + 5 d`; positive answer → escalation ≤ 24 h → read ≤ 24 h | `daycare.discharged`; `ssi_register` + `ssi.followup_recorded`; `sla.breached{alerting:'active'}` → `escalation.triggered{role:'icn'}` → `alerts.createdAt/readAt` → `alert.read` on the `ssi_followup` definition | 0 — no consumer, no register | ≥ 90 % by day 35 in the pilot (episodes ≥ 10); 100 % of positive answers raise `escalation.triggered` (mutant) | ICN (OTD:47); HICC when 28b exists |
| G5 | Post-arthroplasty ROM tracked pre- and post-op; a straying course noticed within a day | share of ortho episodes with pre-op ROM AND ≥ 3 post-op scores by day 14; `off_trajectory` acknowledged ≤ 24 h; re-referral ≤ 30 d | `ot_cases.pre_op_scores` (25 T1) → `outcome_score.recorded` (PHY:88); the `off_trajectory` state's SLA → `escalation.triggered` → `alert.read`; `referral.issued` (`opd/events.ts:346`) | not measurable — no module, no doc | ≥ 80 % by day 14 in 25's first 90 days (episodes ≥ 10); ≥ 95 % acknowledged ≤ 24 h; re-referral diagnostic only (PHY:373) | rehab head (PHY:38); the surgeon's feed |

## 2. Epics

### P4-monitoring-betas-E1 — VD-4: the patient's own numbers
- **Plan home:** **VD-4** — VD-3 is the serial seam (§0.1); its two owed VD-2 items are preconditions here, not scope. Letter-in-block (OPD 07).
- **Goal:** G1 + G2.
- **Exists:** §0.1; pre-stage carries limits to the bench (`prestage.ts:55-63,161-162`); prior readings across the merge chain (`history.ts:125-128 chainFor`; `opd.vitals.history.read`, `opd/manifest.ts:47`); sealed → none (`prestage.ts:141-146`; vd2:215); `opd_vitals.dangerFlags` (`schema/opd.ts:459`); `user_day_facts` = job cadence only, store is per-user (`schema/desk.ts:35-40`; `jobs.ts:338-344`); `test/alerts-parity.test.ts:96-101` is the guard that goes red when a job is added (five censuses, `jobs.ts:318-330`).
- **Build:** (1) nightly rollup `opd_vitals_flag_days` + job; (2) migration `patient_vitals_baselines(patient_id, param, low, high, reason, ordered_by NOT NULL, ordered_at, valid_until, proposal_event_id NULL, superseded_by, cancelled_by/at)` — no `source` column, one writer `orderBaseline`; (3) `boardFlags(flags, order)` inside the ONE helper all four sites call; (4) permissions `opd.vitals.baseline.order` (→ `doctor`), `.read` (→ `vitals_desk`); (5) pre-stage carries the order; tile shows author + date; (6) proposal = `vitals.baseline_proposed` EVENT from a nightly sweep, rendered in the cockpit on open; (7) lapse = `vitals.baseline_lapsed` + tile text; (8) chart-only "far from usual" notice; (9) `baseline` OPTIONAL key in `dangerRangesSchema` (`k`, `min_readings`, `order_max_days`, `proposals`) + census `vitals_baseline_config_present` + `opd.config_updated`.
- **Gates:** none legal. Deploy-dark, **RM rule 4 choice = inert-until-configured** (`:378-381`): absent key → band behaviour; the must-not-open-without is the census row (rule 5 `:382-386`), never a `check-config-present` refusal (its header `check-config-present.ts:15-27` says why; `seed:opd` is `onConflictDoNothing`, `seed-opd.ts:17-21`, and cannot write a new key into production's row).
- **Tier:** the order is a human act; the sweeps are module sweeps with a DB-held evented switch; the consult is a rule. Fail-open toward the alarm.
- **Dependencies:** VD-3 (rebase over `held_reading` if landed); OPD G5 (`opd-go-live.md` dated `## Executed`); 11i ruled + #120 live, or the weekly deploy.

### P4-monitoring-betas-E2 — 16a-ii: the interaction check on production, measured
- **Plan home:** **16a-ii** — roman numeral = the parent phase doc's own unclosed promise, the 18a-ii precedent (`2026-09-04-HANDOFF-radiology-lane-18c-T6.md:179`; RM:423); letters remain sibling scope (14b, 22a). 16e stays.
- **Goal:** G3. **Exists:** §0.2; the seed's downgrade-survives test (`test/seed-formulary-interactions.test.ts:89`).
- **Build:** (1) the pharmacy-runbook precondition row citing the two rows that exist; (2) `prescription.check_refused{kind, hits}` + `rx.check_changed`; (3) the nightly coverage query; (4) the curation screen over the PATCH that exists + `reason` on `interaction.updated`.
- **Gates:** pharmacy G2–G4, then its dated `## Executed`; 16e is procurement (RM §6.4). **Tier:** a rule in the transaction; curation is human. Manual path: the paper interaction chart.
- **Dependencies:** the chief pharmacist holding `pharmacy` (RM:337; the curator — DECIDED 10); the DTC chair (R-243) as reviewer, not editor.

### P4-monitoring-betas-E3 — 15-ii: after the door — the event widened, the register, the day-30 follow-up (text-first)
- **Plan home:** **15-ii** (P15:339's own unbuilt promise). **M2/M3 are a DELIBERATE pull-forward of 28b's SSI slice** (OTD:554; INDEX:74,124; QUA:535): tables in `modules/ot` under `ot.ssi.*` because no `quality` module exists and RM §5:414-418 forbids a new one; OTD:157 is the precedent for OT-local registers that migrate; the ICN is 28b's actor; 28b absorbs when authored. The image half rides 22c-D T6 (unapproved); inference has no home (§6).
- **Goal:** G4, and the seam G5 needs.
- **Exists:** §0.3; `EscortVerification.notifyOk` = the ESCORT's consent (`recovery.ts:326-328`); notify kernel: console adapters return `providerMessageId: null` and the row still settles `sent` (`notify/adapters.ts:33-38`; `pump.ts:221-251`), `NOTIFY_PROVIDER` enum has one member (`kernel/config.ts:31,61`), templates render `hi|en` only (`templates.ts:19`), quiet hours defer `patient`+`routine` only (`pump.ts:104-109`), no inbound rail (grep empty); `lab_report_ready_notice` is the NotModelled row about the channel (`SC:396-399`); alerts kernel: closed subscription list (`kernel/alerts/manifest.ts`), `fallback` → `duty_manager`, exhausted → owner (`timers.ts:148-154`; `consumer.ts:19,178-179`); `promotionalOptIn` is the only consent column and the gateway refuses the promotional class (`schema/patients.ts:96-101`); a new consumer's cursor seeds at the head (`scripts/seed-cursors.ts:5-9`); no `icn` role key (`seed-roles.ts` grep empty).
- **Build:** M1 event widened + `ssi_register` + consumer + `icn` role; M2 the `ssi_followup` definition (the "recall" as an active-SLA state); M3 the day-30 survey — call first, one-way message when the channel exists; M4 the nurse's photo; M5 the patient upload keyed to the episode.
- **Gates:** the OT runbook's dated `## Executed` (G5) — written, not executed; R-169 before M4; **SPEC §19 pre-pilot DPDP posture (SPEC:33-35, :839-840), R-254 (REG:266) for an identified image at rest on the stage-1 cloud host, and a DPIA addendum row (R-028/R-262 precedent, REG:40,274) before M4/M5**; R-249 for M3; R-058 (money); 22c-A + 22c-D + 11b for M5; counsel-signed image addendum + shadow ≥ 90 d before any model reads an image.
- **Tier:** the follow-up and survey are definition SLAs + a scheduled notification — T1 by effect, switched by the definition version; the photo is a stored artefact a human reads. Manual path: the ICN's printed day-30 call list; paper in downtime, backfilled with `occurred_at`.
- **Dependencies:** OT G5; the ICN named; the Class A activation of `ssi_followup` (DECIDED 16).

### P4-monitoring-betas-E4 — Plan 25: ROM trajectory as the fifth active alert
- **Plan home:** **Plan 25** (INDEX:71; PHY numbers itself "Plan 20" `:485,:489` — cite the index; the real Plan 20 doc is `plans/2026-09-06-phase1-20-workforce-roster.md`).
- **Goal:** G5. **Exists (design):** §0.4; in code `consumeEntitlements` (`membership/index.ts:65-66`); orders `kind` open, `therapy` unclaimed (`kernel/orders/kinds.ts:19-39`).
- **Build (inside 25):** T1 `ot_cases.pre_op_scores` (moved here from 15-ii — a writer needs its reader) + `procedure_class → pack` map + the `daycare.discharged` subscriber (cursor at head); T6 packs with `expected_rom[]` as Class-B data; **T7 Trajectory Watcher = an `off_trajectory` state with an active SLA on the episode instance** (ladder therapist → rehab head → surgeon) — `escalation.triggered` → `alerts` → `alert.read` come free, `kernel/alerts` untouched; T9 KPI formulas.
- **Gates:** 25 authoring — RM §5:414; **R-036, R-038 (money, pre-plan-authoring), R-043 (CA)** + PHY:489; R-044/R-045 (purchase); a physiotherapist and rehab head named. RM §5:416 gives "consumes 22a" as 25's reason; INDEX:71 and PHY:485 list no 22a — this doc follows the index and says so. Nothing orders 25 after 21 (RM:291 orders 21 after 20 only).
- **Tier:** T1 by effect, switched by the definition version. Fail-open: inactive → the widget still graphs deltas (PHY:417). Missed sessions are recalls (SPEC:281 map 11).
- **Dependencies:** 15-ii M1 (procedure code); Plan 20; 25 authored and approved.

## 3. Milestones

### E1 — VD-4
| id | name | gate | acceptance parameters (parameter · instrument · threshold) | depends_on | size / human act |
|---|---|---|---|---|---|
| **E1-M1** | The alarm ledger — measure before changing | G1 | nightly row per (day, band, vital): `charts` (from `vitals.recorded{band}`), `flags, notices, rechecks, escalations, cancels, cancel_within_ms_p50/p90` · job `rollupVitalsFlags` (`dailyIst`) — **five censuses move** (`jobs.ts:318-330`: `jobs.test.ts` count; `alerts-parity` names + count; `scheduler.test.ts` `THE_EIGHTEEN`; `worker-runtime.e2e`; `alerts.yml` `:52` regex AND `:69` `absent()` chain); `kernel/worker/jobs.ts` is `kernel/**` — coordinate · replay test over a fixture day reproduces the log (mutant: skip `queue.escalation_cancelled` → red) · `opd-go-live.md` gains `## 8. The vitals bay — the harvest` (interim harvest; Plan 21 the eventual home, INDEX:70) · one row per day from the first day the bay records | 11i ruled → #120 live, or the weekly deploy | 3 d; **the weekly deploy by the owner's hand** |
| **E1-M2** | The order — table, events, one board helper, tile | G2→G5 | **T0:** resolve vd2:167 (state table vs code on `cancelled`-terminal); `held_reading` (vd2:221) stays VD-3's — rebase over it · additive migration numbered at rebase · `baseline` OPTIONAL in `dangerRangesSchema` (`config.ts:74`); absent → inert (test "absent config ⇒ band behaviour") · census `vitals_baseline_config_present` (G2, `front-desk` set beside `opd_config_present` `SC:239`), fix text `PATCH /opd/config {dangerRanges.baseline: …}` — `updateOpdConfig` (`config.ts:244-252`) is the only writer production has · `opd.config_updated` appended by that PATCH · events `vitals.baseline_ordered/.superseded/.cancelled` · `boardFlags` inside the ONE helper called at `escalation.ts:203,250`, `vitals.ts:267→312`, `:438→469` · **mutants:** an order widens `dangerFlags` → red (`vitals.ts:262`); order suppresses below its own low → red; a fixture row with `ordered_by` null suppresses → red; an AMENDMENT on a baselined patient inside the order bumps → red · **regression:** VD-1/VD-2 danger suites unchanged, counts pasted · `seed-roles.test.ts` **six pins** move (`:886` 161, `:1102` 308, `:1120` 141, `:1131`, `:1191` 147, `:1195`), measured at merge · shared files in the PR: `seed-roles.ts`+test, `packages/contracts` (param enum), `locales/en.json`/`hi.json` (11 `vitals` keys each), drizzle serial at rebase, `SC` classification map (#158) · UAT walk = §4 row 1, dated in `opd-go-live.md` | E1-M1 (≥ 7 daily rows from production); `opd-go-live.md` dated `## Executed` with the vitals seat walked (G5); VD-3 if landed | 5 d; **a doctor account on UAT holding `doctor`; the OPD admin's `PATCH /opd/config` (the switch); the weekly deploy** |
| **E1-M3** | The proposal and the lapse — events, rendered on open | G5 | sweep `proposeVitalsBaselines` (`dailyIst`; censuses move again) appends `vitals.baseline_proposed{patientId, param, low, high, readingIds, rule_version, constants}` only from ≥ 4 unflagged readings across `chainFor` (`history.ts:128`); **`k = 3` (median ± 3·MAD) shipped as `baseline.k` and pinned** · sealed → no proposal (`prestage.ts:141`) · the cockpit history tab (`opd-consult.tsx:170`) renders the latest proposal and any lapse ON OPEN — no push, no page, `realtime.ts:9-20` unchanged · a proposal becomes an order only via `orderBaseline` with `proposal_event_id` (mutant: any other writer → red) · `vitals.baseline_lapsed` when `valid_until` passes; lapsed → band behaviour (mutant: lapsed order consulted → red) · `baseline.proposals = off` (PATCH, evented) → zero proposals, orders still work · UAT: 6 readings, 2 flagged → proposal from 4 | E1-M2 | 3 d; none |
| **E1-M4** | "Far from usual" — chart-only notice | G5→G6 | reading > 3·MAD from the median → `notice` flag `usual`; `dangerOf` still filters notices (`escalation.ts:148`) — mutant: notice buys an escalation → red · renders on the cockpit vitals tab with median and n · G1 `notices` moves, escalations do not · **record-only for 30 days (NUR:583)**; keep/drop read beside the harvest numbers (notices/day, n) | E1-M3 | 2 d; the OPD in-charge reads 30 days and says keep/drop |

### E2 — 16a-ii
| id | name | gate | acceptance parameters | depends_on | size / human act |
|---|---|---|---|---|---|
| **E2-M1** | Prove the pairs exist — the rows that exist, cited | G2/G4 | `pharmacy-go-live.md` §1 table (`:71`) gains a row: proof = `hospital.formulary_interactions_loaded` ok AND `pharmacy.pharmacy_role_held` ok in the deploy transcript (`deploy.sh:663-667`; the census block prints no SHA — the `GIT_SHA` line `:86,:250` does), noting `standup:check pharmacy` never prints a `hospital` row (`SC:667-673`) — run `all` · G3(a)'s baseline = that line after the next owner-hand deploy; no owner query | 11i ruled; the weekly deploy | 0.5 d; **the weekly deploy (prints the row)** |
| **E2-M2** | The denominator — F22 closed (a planned instrument, not a pilot fix) | pharmacy `## Executed` | `prescription.check_refused{kind: allergy\|interaction\|duplicate, hits}` appended on `db` in the refusal path — the throw at `prescriptions.ts:373-382` precedes `withTx` (`:409`), so no transaction to outlive (17d's deferred-write rule `lab/events.ts:136`, `lab/results.ts:314` is not needed) · `rx.check_changed` on re-issue without the hit · nightly coverage row = share of the last 30 days' `opd_prescriptions.lines` the formulary resolves (COP:61) · a UAT week yields numerator AND denominator · mutant: refusal without event → red | E2-M1; `pharmacy-go-live.md:257` dated | 2 d; none |
| **E2-M3** | The curation seat — a screen and a reason on the path that exists | G4→G6 | every change through `updateInteraction` (`masters.ts:316`); `interaction.updated` gains `reason` (additive; mutant: a downgrade without a reason → red) · the interaction table on `/formulary/admin` (0 today) · seed re-run after a downgrade leaves it (`seed-formulary-interactions.test.ts:89`, run, count pasted) · curated pairs carry `source <> 'seed-2026-08'` · until O1 a downgrade is recorded on the event as `approvers: 1`; R-247's `governance.single_approver_used` (not in code) is 28-G's to mint · coverage ≥ 80 % is a harvest target, never a gate (COP law 1) | E2-M1 | 2 d; **the chief pharmacist holding `pharmacy`; the DTC chair named (R-243) as reader** |

### E3 — 15-ii
| id | name | gate | acceptance parameters | depends_on | size / human act |
|---|---|---|---|---|---|
| **E3-M1** | After the door — the event widened, the register opened | G1→G4 (OT) | `daycare.discharged` gains `procedureCode, procedureClass, woundClass` (additive; the zod payload `ot/events.ts:84-86` widens — no contracts-parity test exists; the consumer test pins it) · additive `ssi_register(case_id, wound_class, window_end, followup_recorded_at, classification, contact_opt_out_at, …)` + consumer: one row per `daycare.discharged` (mutant: consumer skips → red); **cursor at head (`seed-cursors.ts:5-9`): prior discharges get no row — the runbook section names the first covered discharge** · `ssi.followup_recorded` · `ot.ssi.read/write` (+2 pins) · **new role key `icn`** in `seed-roles.ts` (role + grant pins move) · census `ot_icn_held` (G4, `heldAtHospitalScope("icn")`) · `ot-go-live.md` gains `## 10. After the door` recording one UAT discharge → register row | OT `## Executed` (G5) | 4 d; **the ICN named and assigned `icn` at `/admin/users`** |
| **E3-M2** | The follow-up as a workflow state — the "recall" the kernel can record | G5 | new definition `ssi_followup` (**Class A** — pages on a clinical-safety signal, SPEC:623; `changeClass` `kernel/workflow/definition.ts:3,30`): one instance per register row; state `day30_due`, `sla: {minutes: 35 d, alerting: "active", escalation: [{toRole: "icn"}]}` → `sla.breached` → `escalation.triggered{role:'icn'}` → `alerts` → `alert.read` (`workflow/events.ts:20-45`; `timers.ts:119-160`; first build check: the timer sweep's horizon handles a 50,400-minute SLA) · ICN unheld → `fallback: true` to `duty_manager`, exhausted → owner, asserted · switch = the definition version · `daycare_case` stays at two active alerts (`workflow-def.ts:80,88`) · manual path: `GET /ot/ssi/register?due=today` prints the call list | E3-M1 | 3 d; **owner + MS activate `ssi_followup` (Class A ceremony as `ot-go-live.md` §2; R-247 until O1)** |
| **E3-M3** | Day-30 survey — a call first, a one-way message when the channel exists | G5→G6 | the ICN records six structured answers → `ssi.followup_recorded{answers}`; positive → state `positive_answer` (24-h active SLA → `icn`) — that escalation IS the "ICN task" · **one-way by construction:** template `ssi_day30_survey` (`hi|en`) ends "a nurse will call you"; answers are taken on the call; an inbound rail is its own gated item (R-254 + DPIA addendum) · enqueued at discharge as a `notifications` row, `scheduled_for = occurred_at + 30 d` (`schema/notifications.ts:43`), audience `patient`, urgency `routine` (quiet hours apply); discharges older than 30 d at deploy get none — stated in the runbook · **no message leaves until `NOTIFY_PROVIDER ≠ console`; the measure is `notification.sent.providerMessageId IS NOT NULL`, never `status`** (`adapters.ts:33-38`; `pump.ts:221-251`); `lab_report_ready_notice` (`SC:396-399`) gets a scope note, no new census row · sent only to the PATIENT's own consented number; escort's phone → call only · opt-out = `contact_opt_out_at` on the register row, honoured at enqueue (R-249) | E3-M2 | 3 d; **WABA/DLT for the message path (NEW-P4-2); none for the call** |
| **E3-M4** | The nurse's photo — human-read attachment | G5→G6; **R-169; SPEC §19 posture; R-254; DPIA addendum row** | `WOUND_PHOTO_ENABLED` as a `z.enum(["true","false"])` server flag (`kernel/config.ts:213-228` pattern; the web `voice-flag.ts` is a client constant, not this) default false, coded refusal · when on: in-app capture only, ≤ 2 MB `image/jpeg`, `bytea`, `photo_ref` on the escalation row, sealed-class inherits the patient, no gallery persistence (mutant: file on device FS → red, NUR:319) · every read PHI-logged (`kernel/phi/audit.ts:244`) · purge at 3 y (R-009) asserted by the sweep test — **inert on production until `RETENTION_ENABLED=true`** (`config.ts:114-123`) · lint: no import path from the bytes to `kernel/inference` (root `eslint.config.mjs:26-36` — shared) AND a caller-contract test that no `InferenceClient` call site can receive `bytea`/`photo_ref` (DPIA:22) · runbook names the consent enumeration | E3-M3 | 3 d; **R-169 policy; the DPDP posture on file or the host in-region; the flag is the owner's** |
| **E3-M5** | Patient upload keyed to the episode (22c-D T6 widened) | 22c-A + 22c-D; 11b; **R-009** | 22c-D A16–A20 (`22cD:161-165`) hold with `episode_id` · ICN worklist under the `patient_supplied` band, never the register's classification (R-17, `…patient-self-service/06-RULINGS-LOCKED.md:44`) · "a nurse will call" the only patient-facing string (R-020) · **retention to counsel under R-009:** DD7's 90-day purge rests on "not the medical record" (22cD:90); an image that opened an ICN escalation or a register classification HAS entered a clinical decision — default: it follows the clinical photo period, the rest DD7 | E3-M4; 22c-A/D | inside 22c-D T6; **22c-A approval; 11b if bytes exceed DD7's budget; counsel's R-009 line** |

### E4 — Plan 25
| id | name | gate | acceptance parameters | depends_on | size / human act |
|---|---|---|---|---|---|
| **E4-M1** | Pre-op ROM on the surgical case — inside 25 T1 | 25 authored; G1 | `ot_cases.pre_op_scores jsonb` `[{scale:'ROM', joint, side, movement, degrees, instrument_version, recorded_by, at}]` at the surgeon's pre-op visit (no PAC row exists, `schema/ot.ts:96,261`); H1/H2 validation as a shared zod schema in `packages/contracts` · a TKR case without one is a list `notice`, never a gate · lands WITH its reader (score #0) | 25 T1; E3-M1 | inside 25, 1 d; none |
| **E4-M2** | Packs with expected-ROM bands (25 T6) | 25; G2 | Class-B `rehab_pack:ortho_tkr` with `expected_rom[]` per day + `rule_version`; draft until surgeon + rehab head sign (R-247 until O1) · a pack without bands cannot activate (definition lint) · `therapy_plan.opened` count = ortho `daycare.discharged` count on UAT within one dispatcher cycle | E4-M1 | inside 25; **the surgeon's band numbers at UAT (default: published normal ranges)** |
| **E4-M3** | Trajectory Watcher — the fifth active alert, as a state | 25 T7; G5 | the session definition gains state `off_trajectory`, `sla: {alerting: "active", escalation: therapist → rehab_head → surgeon}` (PHY:63's four become five, asserted by the definition test) — a NEW VERSION of 25's definition; Class A if it pages on a clinical deviation (SPEC:623) · entered on two consecutive below-band scores or no score by a milestone day; never on a missed session · `escalation.triggered` → `alerts` → `alert.read` ≤ 24 h · rehab head unheld → `fallback` to `duty_manager`, asserted · switch = the definition version · fixtures: a 45° day-7 pair on 70–90 flags; a single 68° does not | E4-M2 | inside 25, 3 d; **the rehab head named; activation through the approvals engine (SPEC:206-208; R-247 until O1, two-key after)** |
| **E4-M4** | G5 registered and harvested | 25 T9; G6 | the three G5 parameters as formulas in 25's KPI task or harvest queries until Plan 21 (the doc says which); floors (episodes ≥ 10); `referral.issued` for re-referral, diagnostic only (PHY:373) | E4-M3 | 1 d; none |

## 4. Edge cases — the Indian day it must survive

| # | the day | expected behaviour | artefact that proves it | milestone |
|---|---|---|---|---|
| 1 | A 42-year-old marathoner, pulse 44, active order `pulse 40–70` from Dr X | tile shows the order; no recheck; the chart still records `pulse<50` | `opd_vitals.dangerFlags` has the flag; `opd_queue_entries.escalation` = `none`; test + UAT walk | E1-M2 |
| 2 | Same patient, pulse 34 | below the order's own low → full protocol: other arm, second take bumps | `vitals.recheck_demanded` then `queue.escalated`; mutant "order suppresses all" red | E1-M2 |
| 3 | Same patient; the nurse AMENDS yesterday's 46 to 44 | the amend path consults the same helper — no bump | fixture on the `vitals.ts:438` path; mutant red | E1-M2 |
| 4 | COPD patient; the doctor asks for an SpO₂ baseline at 86 | refused — SpO₂ and SBP floors are absolute in OPD | 409 `baseline_param_not_customisable`; contract enum excludes `spo2` | E1-M2 |
| 5 | The order expired yesterday; pulse 46 today | band rule applies; the tile says "lapsed 12 Jun — renew?"; the bay gets nothing | `vitals.baseline_lapsed{orderId}`; bay topic unchanged | E1-M3 |
| 6 | Two UHIDs merged last week; the order was on the old one | the order follows the canonical id through the chain as `patientVitalsHistory` does | one order after `patient.merged`; chain test | E1-M2 |
| 7 | A sealed patient at the bench | band answered, order NOT answered; the cockpit reads it under PHI log | pre-stage `sealed:true`, no `baseline`; `phi_access_log` row | E1-M2 |
| 8 | The bench nurse tries to set a baseline | 403 — only `opd.vitals.baseline.order` holders; the nurse reads the proposal | permission census; `seed-roles.test` pins named | E1-M2 |
| 9 | The nightly sweep proposes `pulse 40–70`; nobody signs | the proposal is an event; the board is untouched; the bump still fires at 44 | `vitals.baseline_proposed` with no `baseline_ordered`; `queue.escalated` present | E1-M3 |
| 10 | A beta-blocker started at the last consult; the old order is now wrong | the cockpit shows "prescription after order" on open; only a person cancels | `prescription.issued.at > ordered_at` rendered; no machine cancel | E1-M3 |
| 11 | Pharmacist verifies warfarin + aspirin on production; the seed failed | `hospital.formulary_interactions_loaded` RED in the transcript; the runbook precondition fails BEFORE the counter opens; `[]` is never "checked" | the row; `deploy.sh` stops on a non-zero seed (`:40`, `:562-567`) | E2-M1 |
| 12 | Doctor sees the severe warning, changes the drug, re-issues | refusal and change are both events; the weekly rate has a denominator | `prescription.check_refused` then `rx.check_changed` on one encounter | E2-M2 |
| 13 | Pharmacist downgrades a seed pair; the weekly deploy re-runs the seed | the downgrade survives; the seed leaves an existing pair alone | `seed-formulary-interactions.test.ts:89`; `interaction.updated{changed:['severity'], reason}` | E2-M3 |
| 14 | Day-care TKR discharged 19:40; the patient's phone is the escort's | register row opens; the day-30 message is NOT enqueued (not the patient's number); the follow-up state's SLA still calls the ICN | `ssi_register` row; no `notifications` row; `sla.breached` at day 35 | E3-M1/M3 |
| 15 | Day 30 falls on Diwali; the message is due at 23:00 | quiet hours hold a `routine` patient message to morning; nothing pages | `pump.ts:104-109` path; `scheduled_for`; no `escalation.triggered` | E3-M3 |
| 16 | Patient answers "pus and fever" on the call at 23:00 | `positive_answer` state with a 24-h active SLA; the patient hears "a nurse will call you", never a diagnosis | `ssi.followup_recorded{answers}`; `escalation.triggered{role:'icn'}` ≤ 24 h; string test (R-020) | E3-M3 |
| 17 | ICN not yet named; the first positive answer | the rung resolves to nobody → `duty_manager`; if empty, the owner's alert | `escalation.triggered{fallback:true}`; `ot_icn_held` RED with the `/admin/users` fix | E3-M2 |
| 18 | `ssi_followup` drafted; nobody activates it | no instance opens; the register still fills; the printed call list is the path | a `…_definitions_active`-shaped row RED; register rows present | E3-M2 |
| 19 | E3-M1 lands on a base with 12 prior discharges | no rows for them (cursor at head); the runbook names the first covered discharge | `seed-cursors` log line; `ot-go-live.md` `## 10` | E3-M1 |
| 20 | Nurse photographs the wrong wound, or a relative's | attaches only to HER escalation for THIS episode; wristband/episode id in frame; a mismatch is an `unverified` flag a human resolves | `photo_ref` FK; flag column; no free-floating image row | E3-M4 |
| 21 | The photo carries a face and a tattoo | sealed-class inherits the patient; PHI-logged on every read; never leaves the host; the at-rest question is on counsel's list before the flag flips | `phi_access_log`; lint + caller-contract test; §5 RULINGS row | E3-M4 |
| 22 | Post-TKR patient misses two sessions, then scores 45° on a 70–90 day-7 band | the misses are recalls (map 11); the first low score does not flag; the second enters `off_trajectory` | 25's recall event (`patient.recall_initiated` is a designed name, PHY:88 — 25 names what it appends); state entered after the second score | E4-M3 |
| 23 | Two therapists measure 62° and 78° on one knee the same day | inter-rater spread is a plan `notice`; the watcher uses the signed score | notice row; fixture | E4-M3 |
| 24 | Surgeon has not cleared weight-bearing; the day-2 milestone is due | N8: the milestone waits on the clearance event; a gated milestone never flags | no `off_trajectory` while `surgeon_clearance` absent | E4-M3 |
| 25 | *Session layer:* VD-4 merges Tuesday night; production's `opd_config` has no `baseline` key | census RED (`vitals_baseline_config_present`), consult inert, **deploy proceeds, api up**; the fix text names `PATCH /opd/config` | the transcript's census block; test "absent config ⇒ band behaviour" | E1-M2 |
| 26 | *Session layer:* the restore-drill rehearsal of that tip (RM:264-265) | seeds and gate run against last night's copy; a tip that refuses at the configuration gate is NOT deployed — fix, re-rehearse | drill log; `check-config-present` exit | E1-M2 |
| 27 | *Session layer:* a lane builds E3-M4 at 02:00; R-169 is unruled | stop at the ruling: ship behind the flag, record R-169 + the DPDP posture in §7 with the corporate default, never enable, continue on M1–M3 | phase doc §7 row; `WOUND_PHOTO_ENABLED=false` asserted | E3-M4 |
| 28 | *Session layer:* a session reads "16a deployed, DDI live" and reports the beta working on production | a claim without the row is not a measurement; cite `hospital.formulary_interactions_loaded` from a dated transcript or say "unmeasured" | the transcript line + `GIT_SHA` (`deploy.sh:86`) | E2-M1 |
| 29 | *Session layer:* VD-4 and 15-ii move `seed-roles` pins the same week | the second to rebase re-measures at merge (`seed-roles.test.ts:1098-1101`); the PR pastes both counts | pinned counts in the PR | E1-M2 / E3-M1 |
| 30 | *Session layer:* a lane names E1's home "VD-3" | the recon's VD-3 is the serial seam; the lane rebases its doc to VD-4 and reads vd2:167/:221 first | this doc §2; `vitals-desk-recon.md:168` | E1 |
| 31 | *Session layer:* a lane greps "DPIA v0.2" and finds the header says 0.2 | the file is an unsigned draft (`:3`, `:66-68`); the gate is "counsel-signed, §8 rows filled" | DPIA §8 | E3-M4/M5 |

## 5. DECIDED, RULINGS, and the facts the owner supplies

**DECIDED** (Indian-corporate-hospital standard; one line each):
1. A baseline is a doctor's dated ORDER, never a learned suppression — R-070's shape (ICU:99) at an OPD horizon; COP law 3; the chart flag never weakened (vd1:52).
2. Orders widen only toward the patient's own history; SpO₂ and SBP floors stay absolute in OPD.
3. Order life: next consult or 12 months, whichever first (`order_max_days` in config).
4. Proposal arithmetic: median ± **3**·MAD (the Hampel default) over ≥ 4 unflagged readings; constants as `opd_config.danger_ranges.baseline` data (the 2026-08-15 precedent).
5. The proposal is an EVENT with `readingIds, rule_version, constants`; only `orderBaseline` writes the table; the cockpit renders proposals and lapses on open — no push, no batch, no page (no batching primitive exists; digests are 12a's, `pump.ts:109`).
6. "Far from usual" is a chart-only `notice`, record-only for 30 days (NUR:583); `dangerOf` keeps filtering notices.
7. Sealed patients: the order is not answered at the bench (vd2:215); the cockpit reads it under PHI logging.
8. A medication change after the order is shown, never auto-cancelled ("only a person un-bumps", `vitals.ts:302-304`).
9. Fail direction: unreadable table or absent config → band behaviour; RM rule 4 choice = inert-until-configured; the must-not-open-without is a census row, never a deploy refusal.
10. DDI beta = evidence: cite `hospital.formulary_interactions_loaded` (#162), one denominator event, a screen and a `reason`. **The curator is the chief pharmacist** (`pharmacy` already holds `formulary.manage`, `seed-roles.ts:321-334`); the DTC chair (R-243, a senior physician) reviews with `formulary.read`. `formulary_curator_held` is dropped as a duplicate of `pharmacy_role_held`; no new helper, no new grant.
11. The refusal event is appended on `db` — the interaction refusal is thrown before the transaction opens (`prescriptions.ts:373-382` vs `:409`).
12. Coverage 80 % (COP:61) is a harvest target, never a gate (COP law 1).
13. **PARKED, not rejected:** Vision-AI wound assessment waits on NEW-P4-1 (law), R-126/R-254 (in-region path) and R-008 (image-AI "bought never built"); NUR:464's "for now" is honoured; the text-first SSI chain plus a human-read photo is this year's substitute.
14. The day-30 survey is a call first and a one-way WhatsApp message only with a real adapter and a registered template — R-249 (templates only, patient language, opt-out honoured); R-020 stays the standing prohibition on model text to patients.
15. A positive answer enters `ssi_followup`'s `positive_answer` state (24-h active SLA to `icn`); `daycare_case` keeps its two active alerts.
16. `ssi_followup` is **Class A** (pages on a clinical-safety signal, SPEC:623): owner + MS activate, distinct drafter, R-247 single-approver honesty until O1.
17. E3-M2/M3 are a deliberate pull-forward of **28b**'s SSI slice into `modules/ot` (`ot.ssi.*`) — a `quality` module would be a new module (RM §5); OTD:157 precedent; 28b absorbs.
18. The wound photo is staff-side, in-app, no gallery persistence (NUR:319), `bytea` under 22c-D's cap, sealed-aware (NUR:177), default-off; lint + caller-contract test keep the bytes from `kernel/inference`.
19. Opt-out for the follow-up is recorded on the register row by the ICN ("stop" on the call), not a new `patients` column (kernel schema).
20. Pre-op ROM is captured inside Plan 25 T1 with its reader; 15-ii widens the event with procedure code/class and wound class only.
21. Trajectory band = pack's day-N expected ROM ± tolerance; flag on two consecutive below-band scores or no score by a milestone day; missed sessions are recalls (SPEC:281).
22. The Watcher is an active-SLA STATE of 25's session definition (its fifth active alert), so ack = `alert.read` without a `kernel/alerts` edit.
23. Packs' numbers ship as published normal ranges, Class-B drafts revised by the surgeon at UAT.
24. Inter-rater spread on one joint in one day is a `notice`; the watcher uses the signed score.
25. Plan homes: **VD-4**, 16a-ii, 15-ii, Plan 25 — no new block, no renumbering (INDEX §3 `:59-61`; RM §7 `:461`); roman numerals = the parent's own unclosed promise (18a-ii precedent), letters = sibling scope.
26. Ward/ICU monitoring stays 42a/49/50 behind the IPD gate; VD-4's table shape and chart-never-weakened rule are their precedent.
27. The bay's harvest and walk are SECTIONS of `opd-go-live.md`; 15-ii's walk is a section of `ot-go-live.md` — one runbook per module; the S-gate dates one file.
28. Every new sweep or definition carries a DB-held, evented switch (§0); none claims §16's kill switch, which reaches only `x-agent-key` callers today.
29. Week numbers in §7 are forecasts; the gate is the trigger (RM:341).

**RULINGS** (money / procurement / law only):

| id | question | category | recommended default | unblocks |
|---|---|---|---|---|
| R-169 (REG:181) | Written photography/consent policy before any image-capture feature ships | law | corporate template: consent per episode, purpose "clinical follow-up", no teaching use without separate consent; counsel reviews in the L1 session | E3-M4 |
| SPEC §19 + R-254 (REG:266) + R-262/R-028 (REG:274,40) | An identified wound photograph at rest on the stage-1 cloud host: the pre-pilot DPDP posture (SPEC:33-35, :839-840), India-region processing, a DPIA addendum row "wound photo at rest, no inference" | law | posture on file or the host in-region before the flag flips; the addendum row drafted with R-169 in the same counsel session | E3-M4, E3-M5 |
| R-262 + NEW-P4-1 | Counsel-signed DPIA: an IMAGE exception to the Class-2 law on the speech carve-out's terms (DPIA:31-33), or refuse until an in-region/on-prem vision path exists? | law | **refuse for now**; images stay human-read; revisit with R-126/R-254 and 22c-A/D; any product is bought and evaluated as a T1 nudge (R-008), never built | §6 |
| R-009 (REG:21) | One retention schedule — photos 3 y; and the patient-supplied image that entered a clinical decision | law | 3 y for staff photos; a patient upload that opened an ICN escalation or a register classification follows the clinical period, the rest DD7's 90 d; counsel confirms once | E3-M4 sweep; E3-M5 |
| R-058 (REG:70) | Post-discharge nurse touchpoints paid or free | money | free for 30 d post day-care | E3-M3 |
| NEW-P4-2 | WABA + DLT registration and one `ssi_day30_survey` template (hi/en) | procurement | register once; until then the survey is a call | E3-M3 message path |
| RM §6.4 / RM:37 | Interaction/dose dataset licence ₹8–12 L/yr | procurement + money | sign after the RFQ; seed + pharmacist curation until then | 16e |
| R-036, R-038 (REG:48,50); R-043 (REG:55) | Refunds of unused prepaid sessions; course tariff lock; GST on wellness physio packages | money; money; CA | the register's defaults; R-043 "clinical physio exempt, wellness taxable" pending the CA | Plan 25 AUTHORING (PHY:489) |
| R-044 / R-045 (REG:56-57) | Licensed outcome scales; physio equipment with 25 | purchase | free scales only (ROM is free); goniometers with 25 | E4-M2 |
| 11b (RM:402) | Storage beyond `bytea` under DD7's budget | procurement | `bytea` under cap until 11b | E3-M5 |

**Facts and human acts the owner supplies (not rulings — RM:452's distinction):** the 11i ruling on the accidental deploy → #120 (UAT) may land; the weekly deploy by his hand (prints G3(a)'s row); names — the chief pharmacist (curator), the DTC chair (R-243, reader), the ICN, the rehab head and a physiotherapist (R-253); the Class A activations (`ssi_followup`; 25's definition version) until O1; the `PATCH /opd/config` that turns the baseline consult on.

## 6. Not now

- **No Vision-AI wound assessor this quarter or next — PARKED** (DECIDED 13): NUR:464 "for now"; no counsel-signed image basis (DPIA:20; the carve-out `:31-33` itself pending); no patient actor (22cD:3); no photography policy (R-169); no 11b store; **R-008: image-AI is bought, never built**. Earliest sequence: R-169 → DPDP posture/R-254 → 22c-A/D → counsel's image addendum → a pinned in-region provider under DPA or on-prem (R-126) → **procurement** of a product → adversarial eval → shadow ≥ 90 d (COP:125-129) → T1 nudge to the ICN, T2 draft text at most, never a patient-facing verdict (R-020) — and only if E3's harvest shows a miss rate the text path cannot close.
- **No learned or per-doctor baselines, no ML trend model** — COP law 3; ICU:468; NUR:459 "only after 12 months data, T1 max".
- **No ward/ICU monitoring, EWS, Deterioration Watch, alarm classes or telemetry** — 42a/49/50/51 behind the IPD gate (RM §4:391-410; §5:419); 49/50 also need 11b and a CMS with HL7 export (ICU:545).
- **No Plan 25 this quarter** — RM §5:414; 25 follows 20; R-036/R-038/R-043 and PHY:489 gate its authoring. No ROM work precedes it except 15-ii's procedure code on the event.
- **No 15-ii this quarter** — the OT runbook exists (#157) and is not executed; the trigger is its dated `## Executed`, not "an opening list".
- **No E1-M2 this quarter by default** — building the order before a nurse has walked the bay on UAT is the 17-M pattern (RM:176-181); E1-M2 waits on OPD G5 and seven production rows of E1-M1.
- **No licensed interaction dataset before the RFQ closes** (16e); no dose-range/renal/paediatric checks (COP:199).
- **No inbound WhatsApp reply rail; no patient-facing "your usual numbers" card** (needs 22c-A); no message before WABA/DLT.
- **No `formulary_curator_held`, no `ot_ssi_window_overdue`, no `whatsapp_templates_registered`** — a duplicate of `pharmacy_role_held`; a row that fails commissioning on an operating lapse (`SC:709-711` exits 1 on any RED); a row that reads a source constant (`templates.ts:17`).

## 7. Sequencing note

Week numbers are RM §2's forecast from 2026-09-07 (RM:341); **the gate is the trigger, the week is not**. As of 09-08 no wk-1 owner act has landed. This quarter (wk 1–13): lab (wk 2–6), pharmacy (wk 4–6), radiology (wk 7–9), Plan 20 by R (wk 3–6), pilot fixes; F joins S after FD-25's close (RM:337). P4 fits only as a pilot instrument or a runbook edit:
- **On the 11i ruling → #120 (UAT) live, then the first owner-hand deploy:** E2-M1 (0.5 d — the transcript prints G3(a)); E1-M1 (3 d) so the bay's first production days are harvested. Human acts: the 11i line; the weekly deploy.
- **On pharmacy's dated `## Executed` (`pharmacy-go-live.md:257`):** E2-M2 (2 d) as a planned instrument during G6; E2-M3 (2 d) when the chief pharmacist is named; the DTC chair reads.
- **On `opd-go-live.md` gaining a dated `## Executed` with the vitals seat walked AND ≥ 7 daily E1-M1 rows from production:** E1-M2 (5 d). **Default placement Q1 2027**; earlier only if OPD G5 lands this quarter. Human acts: a doctor on UAT; the `PATCH /opd/config`; the weekly deploy; rehearse the tip on the restore drill (§4 row 26).
- **Rulings for the wk 4–8 counsel session (RM §6 L1):** R-169; SPEC §19 posture + R-254 + the DPIA addendum row; NEW-P4-1 (refuse for now); R-009 incl. the patient-upload clause — so 15-ii M4/M5 are lawful or explicitly parked before anyone builds them.

**Q1 2027 (wk 14–26):** 15-ii E3-M1→M3 on `ot-go-live.md`'s dated `## Executed` — the ICN named and assigned; owner + MS activate `ssi_followup` (R-247 until O1); WABA/DLT for the message path. E1-M3/M4 close VD-4 after E1-M2's own dated section. Plan 25 authored after 20 closes AND R-036/R-038/R-043 are ruled (PHY:489).

**Q2 2027 (wk 27–39):** Plan 25 built with E4-M1→M4 (its definition version activated through the approvals engine); E3-M4 if R-169, the DPDP posture and R-009 are on file and the owner flips the flag; E3-M5 only if 22c-A/22c-D are approved and executed. Vision inference: not scheduled (§6).

**What every start needs from a human:** ALL — the 11i ruling (UAT) and the weekly deploy by the owner's hand. E1 — a doctor on UAT; the OPD admin's `PATCH /opd/config`; the OPD's executed runbook section. E2 — the chief pharmacist holding `pharmacy`; the DTC chair named. E3 — the OT's executed section; the ICN named; the Class A activation; R-169 + DPDP posture for M4; WABA for messages. E4 — R-036/R-038/R-043 ruled; Plan 25 approved; the rehab head and a physiotherapist named; the surgeon's band numbers; the definition activation.

## 8. Changelog

Every refutation's evidence was re-read at `91c34fc`; **none rejected**. Four refuter line-drifts corrected silently in the body: OTD:551→**554**; INDEX:117→**124**; `workflow-def.ts:15`→**`:72`** (Class A) and the two active states are `:80,:88` (not `:23,:31`); locales carry **11** `vitals` keys each (not 36/24). Size: the source design was 45 KB; this revision is larger than the 30 KB guidance because the amendments add rows, not prose.

| id (lens) | verdict | what changed |
|---|---|---|
| K1 (exists) | accepted | E1 home → **VD-4** (VD-3 = serial seam, vd1:14; VD-4 free); vd2:167 is E1-M2's T0, vd2:221 stays VD-3's with a rebase note; DECIDED 25; §4 row 30. |
| K2 (exists) / F8 (meas.) / R4 (seq.) / N-1 (auth.) | accepted | `formulary_interaction_pairs_present` deleted; G3(a), E2-M1, §4 rows 11/28, DECIDED 10 re-pointed at `hospital.formulary_interactions_loaded` (`SC:183-227`); NEW-P4-3 retired (the deploy transcript prints the row); E2-M1 = a runbook row, 0.5 d. |
| A1 (exists) / F13, F19 (meas.) | accepted — resolved by dropping | The DTC chair is a physician (R-243); the standard curator is the chief pharmacist, whose `pharmacy` role already holds `formulary.manage` and is read by `pharmacy_role_held`. `formulary_curator_held` dropped as a duplicate (no permission-scoped helper exists — grep empty; no new grant, pins untouched). `vitals_baseline_orderer_held` likewise dropped: the grant lands on `doctor` by the deploy (Class C), so the row proved nothing; §4 row 25 names the config row instead. |
| A2 (exists) / R5 (seq.) | accepted | E3 gate re-measured (`ot-go-live.md` + six `ot_*` rows, written not executed); E3-M1's walk = `## 10` of that file; trigger = its dated `## Executed`. |
| A3 (exists) / F17 (meas.) | accepted | `opd-go-live.md` exists; E1-M1's harvest = `## 8` of it; "four files" → nine; old DECIDED 24 → 27. |
| A4 (exists) | accepted (cite → OTD:554, INDEX:124) | E3 states the 28b pull-forward with reasons (DECIDED 17); ICN as 28b's actor; E3-M1 stays on P15:339. |
| A5 (exists) / F25 (meas.) | accepted | `whatsapp_templates_registered` removed; instrument = `NOTIFY_PROVIDER` enum + `providerMessageId IS NOT NULL`; `lab_report_ready_notice` cited; `hi|en` stated. |
| A6 (exists) / F11 second half | accepted | `lab/events.ts:124` → `:136` (+ `results.ts:314`) and marked moot — the refusal throws before `withTx`; DECIDED 11 rewritten. |
| A7 (exists) / F29 / R14 / N-5 pointers | accepted | Header at `91c34fc`; `deploy.sh:40/:588/:562-567/:663-667/:86`; `config.ts:138`; REG:37 → RM:37; `voice-flag.ts` is a web constant, the server pattern is `config.ts:213-228`; "DPIA v0.2" → "counsel-signed DPIA"; `router.tsx:661` flagged as moved by #118. |
| N1–N8 (exists) | accepted | ICU:163 dropped; NUR:583 = 30 days adopted; edge 22 marks `patient.recall_initiated` designed-only; naming shape written (DECIDED 25); no PAC row (E4-M1); Plan 21 named as the rollup's eventual home; `user_day_facts` scoped to cadence. |
| F1–F5 (meas.) | accepted | G1: `vitals.recorded{band}` denominator, floor ≥ 20, `withinMs` p50/p90, the honest reader, pre-deploy baseline wording. |
| F6, F7 (meas.) | accepted | Events `vitals.baseline_ordered/.superseded/.cancelled/.lapsed` (+ `.proposed`); G2(a) states the join; G2(b) = the `vitals.ts:262` mutant + nightly invariant. |
| F9, F10, F11 (meas.) | accepted | Curation count at G3(a); G3(b) unmeasured until E2-M2; 16a:388 dropped; G3(c) weekly at denominator ≥ 20. |
| F12, F23, F24, F27 (meas.) / N-4 (auth.) / R7 (seq.) | accepted | Every "task" → the `ssi_followup` state chain (`sla.breached` → `escalation.triggered` → `alerts` → `alert.read`) + a `notifications` row; E3 restructured; `ot_ssi_window_overdue` dropped (RED fails commissioning); `recovery.ts:553-566` cited; "contracts parity moves by 4 fields" dropped; unheld-role rows (§4 17; E4-M3). |
| F14, F15 (meas.) | accepted | E4-M3 = route (i), an active-SLA state; `referral.issued` named; floors. |
| F16 (meas.) / R8 (seq.) | accepted | `alerts-parity.test.ts:96-101` the guard; two `alerts.yml` legs; five censuses; E1-M1 3 d; E1-M2 5 d, six pins, shared-file list; root `eslint.config.mjs` shared. |
| F18 (meas.) / R1 (seq., kill of the parameter) | accepted | `check-config-present` refusal deleted; `baseline` optional; `vitals_baseline_config_present` with `PATCH /opd/config` fix text; RM rule 4 choice stated; §4 row 25 "deploy proceeds, api up"; restore-drill row 26. |
| F20–F22, F26, F28 (meas.) | accepted | `k = 3` pinned; 30 days per NUR:583; the R-247 banner → `approvers: 1` on the event; purge inert until `RETENTION_ENABLED`; SHA from the transcript's `GIT_SHA` line. |
| A-1 (auth.) | accepted | §0 states the harness honestly (one column, one guard, no global halt); every sweep/definition gets a DB-held evented switch — E1 via `PATCH /opd/config` + new `opd.config_updated` (verified: none today), E3/E4 via the definition version; DECIDED 28. |
| A-2 (auth.) | accepted | Proposal = event; no `source` column, `ordered_by NOT NULL`; `orderBaseline` the only writer with `proposal_event_id`; mutant "a row with `ordered_by` null suppresses → red"; `rule_version` + `constants` on the event. |
| A-3 (auth.) | accepted | Four board sites listed; `boardFlags` in the one helper; amendment fixture (§4 row 3). |
| A-4, A-5 (auth.) | accepted | SPEC §19 posture, R-254, DPIA addendum on E3-M4/M5's gates, RULINGS and §7's counsel list; E3-M5's retention to counsel under R-009 with the "entered a clinical decision" default and DD7's premise cited. |
| A-6 (auth.) | accepted | DECIDED 13 = PARKED on NEW-P4-1 / R-126 / R-254 / R-008; §6 phrases the path as procurement evaluated as a T1 nudge. |
| A-7 (auth.) | accepted | E3-M3 one-way; opt-out on the register row (DECIDED 19); patient's own number only, escort's phone → call (§4 row 14); inbound rail = its own gated item. |
| A-8 (auth.) / R6 (seq.) | accepted (R6 cites corrected) | `ssi_followup` is a NEW definition, Class A (SPEC:623), owner + MS activate, R-247 until O1 — human-act cells, DECIDED 16, §4 row 18; `daycare_case` keeps two active alerts, so no re-version of it. |
| N-2, N-3, N-5 (auth.) | accepted | R-249 cited; lint + caller-contract test (DPIA:22); "counsel-signed DPIA"; §4 row 31. |
| R2 (seq.) | accepted | E1-M2 gated on OPD's dated `## Executed` + ≥ 7 production rows; default Q1 2027; "if F/S capacity" removed; E2-M2 = planned instrument gated on pharmacy's `## Executed`. |
| R3 (seq.) | accepted, sharpened | "11i ruled → UAT live" and "the weekly deploy by the owner's hand" added to E1-M1/E2-M1 and §7; measured: #118 (T9) merged 09-08 11:59 UTC, **#120 (T3, UAT) still OPEN**. |
| R9 (seq.) | accepted | "Batched" removed: proposals/lapses rendered on open; `off_trajectory` is an SLA-ladder alert; DECIDED 5/22. |
| R10 (seq.) | accepted | E4 gates add R-036, R-038, R-043 + PHY:489; the RM §5 "22a" reason flagged as unsupported by INDEX:71/PHY:485; "and 21" dropped. |
| R11 (seq.) | accepted | E4-M1 moved inside Plan 25 T1 with its reader; 15-ii widens the event with code/class/wound only (DECIDED 20). |
| R12, R13 (seq.) | accepted | §4 rows 19 (cursor at head), 26 (restore drill); old discharges get no message (E3-M3); "the gate is the trigger" (DECIDED 29). |
| exists-N5 "15-ii vs 15b" | kept roman | Neither refuter calls it a collision; the rule is now written (DECIDED 25) so no lane re-litigates it. |
| F12's 35-day SLA | accepted with a check | `slaSchema.minutes` is a positive int (`definition.ts:7-9`); 50,400 minutes is legal but the timer sweep's horizon is unverified — named as E3-M2's first build check. |
