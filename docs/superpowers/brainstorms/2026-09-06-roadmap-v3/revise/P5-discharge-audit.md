# P5 — Operational & quality: Magic Discharge and the 100 % real-time audit (REVISED)

Measured at `91c34fc` on 2026-09-08 11:32 UTC (CONTEXT.md:48-55), re-checked the same day at `41a4be4` (#118, one commit later); paths repo-relative, files newer than the `main-ro` checkout read with `git show origin/main:<path>`. Position: production still runs `399f92c` (78 migrations); `origin/main` has 82 (`0000`–`0081`), four pending; nine runbooks; only pharmacy's has an `## Executed` section and it reads NOT YET RUN (`pharmacy-go-live.md:257`); #119–#122 (11i T3/T5 among them) OPEN, #73 never-merge. Abbreviations: SPEC = `docs/superpowers/specs/2026-08-10-hmis-architecture-design.md`; RM = `docs/superpowers/2026-09-06-ROADMAP-v2.md`; IDX = `docs/superpowers/brainstorms/2026-08-27-department-series/00-INDEX-AND-SYNTHESIS.md`; REG = `…/00-OWNER-RULINGS-REGISTER.md` (R-nnn at line 12+nnn); IPD = `…/17-ipd-adt-bed-mrd.md`; Q22 = `…/22-quality-nabh-incidents-infection-control-governance.md`; A12 = `…/12-agentic-copilot-layer.md`; PHY = `…/04-physiotherapy-session-departments.md`; K11 = `…/11-staff-kpi-kra-performance.md`; P15 = `docs/superpowers/plans/2026-08-28-phase1-15-mini-ot-daycare.md`; KD = `docs/superpowers/plans/2026-08-28-phase1-kernelD-document-chrome.md`; 22cE = `docs/superpowers/plans/2026-08-28-phase1-22cE-records-and-account.md`; DPIA = `docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md`; OTRB = `docs/runbooks/ot-go-live.md`; H = `/opt/hmis/docs/superpowers/brainstorms/2026-09-01-hermes-ops-copilot/00-BRAINSTORM.md`; core = `apps/core/src`. Sibling designs: P1 `wf/design/P1-fabric-multilingual.md`, P3 `…/P3-diagnostic-surgical.md`, P4 `…/P4-monitoring-betas.md`, P6 `…/P6-absorption-track.md`.

## 0. The mission sentence(s) this pillar serves, and the honest reconciliation with the spec

**5a. "Magic Discharge: instantly generate the summary on doctor confirmation and process departmental clearances automatically in the background."**
**5b. "100 % Case Real-Time Auditing: monitor and audit 100 % of patient interactions against clinical protocols in real time."**

1. **The cascade is already the spec's law** — SPEC:256: eight SLA-timed steps, fit-declared → out < 3 h, "summary drafted by AI (T2), doctor edits and signs". It is **Plan 44** (IDX:86: needs "41 live 30 d; 16 returns; 43b; R-190, R-192–R-195"); 41 is only *authored* week 7–9 and starts only if O1, Plan 20, the dues numbers and 11b land (RM:338-339, 419-420). Nothing of it is in code: `fit_declared`, `report.signed`, `patient.discharged`, `pass.issued`, `undertaking.signed`, `task.verified` each 0 hits in `core` + `packages` + `scripts`.
2. **"Doctor confirmation" is lawfully an RMO here.** R-192 (REG:204): RMO signs, consultant countersigns ≤ 24 h. Two clocks; IPD:438 names `fit_to_summary_signed` but no countersign clock — added (D12). No `rmo` or `discharge_coordinator` role key exists in `scripts/seed-roles.ts` (roles present: `opd_admin`:277, `medical_superintendent`:600, `duty_manager`:678, `ot_incharge`:871, `surgeon`:894, `anaesthetist`:904, `ot_nurse`:908, `recovery_nurse`:915, `daycare_coordinator`:919); Plan 20 (authored, FOR APPROVAL, `…phase1-20-workforce-roster.md:3`) mints them.
3. **"Instantly" = pre-generation, and the template is the product.** IPD:596: provider latency 0.8–60 s, "pre-generated at fit-declared"; IPD:241 D5: timeout → editor opens with structured pre-fill, "never blocks signing; test: drafter kill switch on → summary flow completes". The model half is a T2 agent (SPEC:785; cap SPEC:778) behind 12a ("No 12a agent runtime", RM:428), an undeclared `complete()` (`core/kernel/inference/types.ts:9-11`), R-126 (REG:138), R-128 (REG:140) and a **Class-1 DPIA revision** — DPIA v0.2 as drafted does not cover it (DPIA:14 "Future stages (not activated by this DPIA …): T2 clinical drafters consuming Class-1"; DPIA:28 "L1 (future revision)"; DPIA:37 lawful basis `[COUNSEL]`). **Reconciled:** the fail-open path is the feature; the model draft is a layer behind five gates. Buy-not-build: the model is a provider behind one choke module; template, signature chain and fact sheet are ours.
4. **The design auto-*chases*; it never auto-*clears*.** IPD:99-112: each clearance "a task with 60-min SLA, silence auto-escalates"; IPD:470: only drafting, coding, readiness prediction and forecasting use inference "and none acts". **Reconciled:** "automatic" = opened, chased, escalated and *attributed* (hospital/payer/family/doctor, IPD:113,163) by rules; "cleared" = a named human within 60 min — except the billing clearance, which auto-satisfies on an **empty orphan scan** (SPEC:256 step 4 "no-pending-charges gate"; IPD:104 `billing_pending_charges (orphan check)`), never on a balance (D2), and an empty orders sweep.
5. **The only discharge in code is the day-care one, and it is G1 only — and the repo moved under it on 09-07.** `dischargeDaycare` (`core/modules/ot/recovery.ts:443-506`, inside `withTx` :447) refuses outside `discharge_ready` (:455-457), without a discharge-time escort verification (:462-475), with an unnamed ISBAR acknowledger (:478-480); releases the bay to cleaning (`releaseResource`, :483-484); emits `daycare.discharged{encounterId, patientId, bayResourceId, at}` (`ot/events.ts:84-86`). Deployed (0035 ≤ 78). **Now on main:** `standup:check` has an `ot` key with six rows (`standup-check.ts:458-527`: `ot_approval_types_registered`, `ot_theatre_present`, `ot_workflow_definitions_active`, `ot_definitions_published`, `ot_surgeon_held`, `ot_anaesthetist_held`; six module keys :138,295,407,458,544,595; #157/#158); `docs/runbooks/ot-go-live.md` EXISTS (#157, §0–§9 at OTRB:15-173) and is pinned by the runbook↔module map at `test/standup-check.test.ts:146` — it has no §Seat walk, §Harvest or §Executed (grep 0); Class-B seeds "stay drafts" (RM:373; `scripts/seed-ot.ts:64-73`, which deliberately never drafts `privileges`); `daycare.discharged` has zero consumers outside `modules/ot`; **no recall exists** — P15:339 promised "DD10 + follow-up recall task via the notifications/scheduler seam" for the *discharge*; `markAbsconded`'s comment was rewritten by #164 (merged 09-07T05:09Z) to say the opposite (`recovery.ts:548-565`: "no document asks for a recall task on a day-care abscond … there is no task primitive anywhere in the kernel"; the body writes an `ot_incidents` row it calls "the recall register's input on the day somebody builds one"); the conversion "handoff document" is a bare ULID (`recovery.ts:525`). **So this quarter's buildable Magic Discharge is the day-care discharge finished, measured and rehearsed — as a DELTA on #157 and on the siblings that already own the OT's opening (P3-E6, P6-E6)**; 44 inherits a measured seam.
6. **5b: the structural audit exists; the protocol audit does not.** SPEC:125 ("audit is structural"), SPEC:201-204 (§10.3: every state carries an SLA, every breach recorded), emitted by `runDueTimers` (`core/kernel/workflow/timers.ts:86,120-133`, payload carries `defKey, state, slaMinutes, alerting, dueAt`). Missing: any *reader* — `sla.breached` is referenced by its definer (`workflow/events.ts:20-31`), its emitter, a comment (`approvals/events.ts:6`) and test files; the four lab hits are the lab's own `lab.sla_breached` (`lab/events.ts:277`, routed by `lab/realtime.ts:78`), not a reader of the kernel event. Protocol-as-data, an evaluator and deviation tasks are **deferred design note 12** (`plans/2026-08-11-phase1-plan-series.md:463`; owner-ruled "log, don't schedule", :431) and Plan **28d** (Q22:537 as "22d", IDX:74) "only when machine-readable protocols exist", T2-capped review tasks (Q22:460; A12:393), not this quarter (RM:416). Q22:125's `audit.control_tested` "(exists)" is false: 0 hits.
7. **Reconciled 5b, three rungs:** (i) *read what is recorded* — a Class-0 daily fact sheet (DPIA:11 fixes the shape: "SLA breach counts, orphan/variance summaries"); (ii) *the gates already built are the first protocol set* — refusals and overrides per gate per day, `no_data` never rendered as compliance. A gate that leaves no event is not audited: the measured universe is **23 authority (403) codes in 7 modules** (aerb 3: `device_not_licensed`, `no_active_licence`, `not_appointed`, `aerb/errors.ts:80-82`, thrown at `aerb/licences.ts:403`; lab 7; ot 2: `privilege_refused`, `same_actor`, `ot/errors.ts:104,113`; pcpndt 5; pharmacy 2; radiology 4), of which only the lab's `sod_violation` has an event twin; `aerb/events.ts` defines three events, none a refusal — the first lint finding of E2-M3; (iii) *the evaluator* (28d) after an Expertise store (R-260), just-culture (R-240), a named reviewer via Plan 20, versioned rules. "Real time" = the dispatcher's cycle — 2 s dispatch, 20 s timers (`core/kernel/config.ts:53-54`; `worker/jobs.ts:199-207`) — never in-transaction; a block is the module's own gate (D4). T0 report; T2-capped review tasks; never T3+.
8. **DPIA, the IPD gate, and the off switch.** The only inference here is the Drafter's model half (E4-M5). Deviation tasks are staff behavioural data (DPIA:38 "flagged for review now"; R-118 REG:130 written staff notice, pre-pilot) → flag-inert on production until the notice and the DPIA staff line are signed, before E5-M2 runs there (A5). Every automation this design adds ships behind an inert `*_ENABLED` config flag whose toggle is evented — the `RETENTION_ENABLED` precedent (`config.ts:114-123`, an enum of two exact strings) — because the kernel's kill switch bites only `x-agent-key` callers (`auth/guards.ts:51`), worker jobs have none, and no global halt exists (grep 0) while SPEC:789 requires one (D23). The IPD gate binds E4 and nothing else.
9. **Seams, by signature (CONTEXT:45 rule 1).** `enqueueNotification(tx: Tx, input)` (`notify/enqueue.ts:68-100`) runs inside the caller's transaction and *throws* on an unregistered key, a promotional class, or a patient-audience template with no `patientId` — so the recall must never run inside `dischargeDaycare`'s `withTx` (D25). `adaptersFor(cfg)` (`notify/adapters.ts:56-66`) has one provider case, `console`; `NOTIFY_PROVIDER` is `z.enum(["console"])` (`config.ts:31,61`); channels are `whatsapp | sms` (`adapters.ts:11`; `notify/events.ts:24`) — `notification.sent` is "a statement about THIS GATEWAY, not about delivery" (`adapters.ts:4-8`). `releaseResource(tx, actor, kinds, id, {at, reason})` puts a bed in `cleaning` (`resources/kinds.ts:103` `onRelease: "cleaning"`), evented `resource.status_changed` (`resources/events.ts:94`).

## 1. Goals

| # | goal | north-star parameter | instrument | baseline today | target | reader |
|---|---|---|---|---|---|---|
| G1 | A discharge the hospital can perform, measured decision → door | `daycare_case` `discharge_ready → discharged` p50/p90 min (K11:459 `ops.discharge_ready_to_discharged_hours`, registered by 21 later — D17); `sla.breached{defKey: daycare_case, state: discharge_ready}` per week | `workflow_transitions{from_state,to_state,at}` (`schema/workflow.ts:67-80`); the 120-min ACTIVE SLA (`ot/workflow-def.ts:29-31`) | structurally 0: `ot_workflow_definitions_active` is a Class-A ceremony no seed performs (`standup-check.ts:487-500`), so no instance can exist on production | p90 ≤ 120 min over the first 20 real discharges; **0 breaches in a week with ≥ 5 `discharge_ready` entries** (`no_data` otherwise) | OT in-charge; the sheet |
| G2 | Every breach recorded is **read** | days in the last 7 whose sheet alert has `alerts.read_at` within 24 h; `sla.breached` rows > 24 h old absent from any sheet | `ops_fact_sheets` + `ops.fact_sheet_produced` → `kernel.alerts` row (`schema/alerts.ts:25` `read_at`, index :29) | 0 readers of `sla.breached` (§0.6) | 7/7 read; 0 unread breaches | owner 07:00 IST; the quality holder (no `quality_manager` role exists — the owner until Plan 20 mints one) |
| G3 | The gates are the protocol — refusals and overrides are numbers | refusals/overrides per gate kind per day; gate kinds reporting `no_data`; authority refusals with no event twin | `PROTOCOL_GATE_EVENTS` (E2-M3) over events that exist: `gate.overridden` (`ot/events.ts:168`), `timeout.halted` (:154), `count.mismatch` (:162), `sod.violation_blocked`/`break_glass.used`/`emergency_elevation.used` (`auth/events.ts`), `lab.sod_violation_blocked`, `lab.report_print_blocked`, `lab.tube_mismatch_flagged`, `imaging.critical_overdue`, `prescription.issued.{allergy,interaction,duplicate}OverrideCount` (`opd/events.ts:333,341-342`); plus the twins E2-M3 adds | events deployed, never counted; **23 403-codes, 1 twin** (§0.7) | every registered gate on the sheet daily; 0 un-evented authority refusals (generic denials exempt, D31) | owner; 28d |
| G4 | IPD Magic Discharge under two clocks | `fit_declared → discharged` p50 hospital-attributed; countersign ≤ 24 h share | 44's definition transitions; `sla.breached{summary_countersign_pending}` | not measurable — no IPD | p50 < 3 h (SPEC:256) over 50; countersign ≥ 95 %; before-11 *reported* at 60 % by month 6, never gated (R-190, REG:202) | discharge coordinator (IPD:46; role from Plan 20); RMO |
| G5 | Deviations are review tasks with a named reviewer | rules with a version id; deviation tasks opened/closed per day; rules auto-demoted; `fallbackExhausted` count | `protocol_rules`, `protocol.deviation_flagged`, approval instances (`schema/approvals.ts:36,48,50-52`); `escalation.triggered{fallbackExhausted}` (`timers.ts:150-165`) | 0 rules, 0 evaluator | 5 clinical rules in shadow 30 d; ≥ 1 holder of the rung role at open and `decided_by` non-null at close · 100 %; closure ≤ 72 h p90 | quality holder; DTC |

## 2. Epics

### P5-discharge-audit-E1 — 15e: the day-care discharge follow-through (a delta on #157, shared with P4-E3-M1)
- **Plan home:** `15e`, a free letter in Plan 15 (15b/c/d claimed: P15:44,184; `schema/ot.ts:87-88`; `15e` 0 hits in `docs/`; the index's block-15 style is a plain letter, IDX:57 — P4's `15-ii` is asked to adopt `15e`, D26). Letter-in-block. **What already occupies the name:** `docs/runbooks/ot-go-live.md` (#157), six `ot` census rows (#158), P3-E6 "the mini-OT opens" (P3:40,88-94), P6-E6-M1/M2 (P6:59-63,127-128), P4-E3-M1 (P4:54-58,92). This epic builds only what none of them claims (D27).
- **Goal:** the deployed day-care discharge becomes one the hospital can print, follow up and measure, so 44 adopts a measured seam; the OT's opening itself is P3-E6/P6-E6's.
- **Exists (re-measured):** §0.5; `evaluateDischargeReady` system move + late-cutoff *offer* (`recovery.ts:277-282`); `convertToAdmission`, `at` = billing boundary (:509-545); `markAbsconded` (:566-…) writing `ot_incidents` with the escort fact; `daycare.absconded{escortVerifiedAtDischarge}` (`ot/events.ts:100-103`); `escort.verified{at: checkin|discharge}` (:74-77); `list.resequenced` (:136, #164); `ot_cases.procedure_code/procedure_class/wound_class` (`schema/ot.ts:255-256,276`); `escort.notifyOk` captured "so the consent exists the day the channel does" (`schema/ot.ts:198-199`; `ot/gates.ts:88`; P15:190); `DocumentKind` = 4 (`printing/enqueue.ts:52-56`, incl. `vitals_slip` which renders null, `printing.controller.ts:190`) but `render.ts` has **three** `case` arms (:660-662) and no kinds↔switch parity test; `opdConfig.followUpDefaultDays` default 7 (`schema/opd.ts:24`); `ot_definitions_one_active_ux` (`schema/ot.ts:543`); `/ot/recovery` (`router.tsx:233`).
- **Build:** ONE additive payload on `daycare.discharged` — `procedureCode, procedureClass, woundClass, preOpScores` (this milestone and P4-E3-M1 are the same, D26; PHY:434 and P2-E2-M3 adopt); the recall **consumer** (own tx, `DAYCARE_RECALL_ENABLED` inert) writing a `notifications` row `daycare_followup` (a new template key, `notify/templates.ts:52-165` has none) — the primitive #164 said exists, never the task it refused (D25); `daycare_encounters.review_date` (additive, nullable); outbox kinds `daycare_discharge_slip` and `daycare_conversion_handoff` (the body behind `handoffDocumentId`) plus the parity test; `daycare.escort_refused{encounterId, reason}` (the 422 at `ot/errors.ts:127` leaves no event); §Seat walk / §Harvest / §Executed appended to `ot-go-live.md`; the census rows the runbook names and the census lacks (`ot_incharge_held`, `recovery_nurse_held`, `daycare_coordinator_held`, bays ≥ 2, package priced, relay reachable — OTRB:142-160 vs `standup-check.ts:458-527`); the decision→door line on the sheet (E2).
- **Gates:** G1 done. G2: the Class-A ceremony for `daycare_case`/`ot_gate` (OTRB:19-22, 59-63: `opd_admin` drafts, MS + `owner` approve, a non-drafter activates) and the Class-B `criteria` published by the MS **with a `department_head`** (R-170 REG:182 "owner + department heads approve before activation"; SPEC:623 Class B = department head + duty approval; P15:201 measured `department_head` at zero holders) — R-247 (REG:259) covers only the missing second administrator (D9); the MS drafts and publishes `privileges` (OTRB:116-130; `seed-ot.ts:68-71`). G3: day-care package + implant prices in the active tariff (OTRB:156-160 "fails at booking, in front of a patient"; placeholders on UAT; the owner's number on production before the first real booking — money, R-255; O6 for the CA signature and the exit). G4: seven names (NEW-P5-2). G5: the discharge leg of P3-E6-M3/P6-E6-M2's dated walk. No DPIA, no O1.
- **Tier:** recall = T1 automation (SPEC:784), behind its flag. No agent. **Deps:** #120 + #121 landed and `deploy.sh HMIS_TARGET=uat` (11i T3/T5, OPEN); FD-24 outbox (0069); printing v2 (front-desk, no PR, `lane/front-desk-fd25` local-only, heartbeat 09-05) touches `render.ts` — the second lane rebases; `packages/contracts` parity; `notify/templates.ts` (everyone's).

### P5-discharge-audit-E2 — 11n: the breach ledger and the daily fact sheet
- **Plan home:** `11n` — the 11 series is deployment and operability (RM:461-467); 11j (RM:469-470), 11k (`…11i….md:411`) are taken on the repo; `11l` and `11m` are P6-E7's and P6-E6's inside this brainstorm (P6:60,67); `11n` is 0 hits on the repo and in every sibling design. Letter-in-block (D7).
- **Goal:** 5b rungs (i)+(ii): every breach, escalation, orphan and gate refusal read within a day as one deterministic Class-0 object. **The seam:** `factSheetFor(day)` in `kernel/ops/fact-sheet.ts` is the ONLY composer; P1-E3's `digest.fact_sheet` (H:60,113 places it in `kernel/copilot`) is a *reader* of `ops_fact_sheets` and depends on E2-M1; harvest rows come from one function `harvestRowsFor(module, day)` that P6-E3's `pilot:harvest <module>` (P6:42,106) and the sheet both print (D24). HMIS writes no prose; Hermes narrates only after "DPIA v0.2 signed, H §6 R1" (H:33,126).
- **Exists:** §0.6; `escalation.triggered{rung, role, fallback, fallbackExhausted}` (`workflow/events.ts:34-46`; rung starts at 0, `timers.ts:139`); `charge.orphan_flagged{encounterId, patientId, reason}` from `orphanScan`, OPD consult fee only (`billing/events.ts:157-160`; `billing/daily-close.ts:295-306`, importing `listVisits` from `../opd`, :8); `workflow_instances.state_entered_at` + `workflow_transitions`; `occurred_at`/`recorded_at` (`schema/events.ts:28-29`) — for `sla.breached` the emitter passes no `occurredAt` and `make()` defaults it (`packages/contracts/src/envelope.ts:96`), so the true breach instant is `payload.dueAt` and a backlog drained after downtime (`timers.ts:83-85`) carries a later `occurred_at`; `user_day_facts` (`desk/rollup.ts:9-25`); `/ops/mode`, `/ops/downtime-kit` (`router.tsx:931-942`); `kernel/ops/manifest.ts` with three `ops.*` strings (:27-29); `kernel.alerts` consumer subscribing `escalation.triggered`, `notification.failed`, `ops.mode_changed`, `imaging.critical_overdue`, `imaging.report_unread` (`alerts/manifest.ts:20-49`); `alerts.read_at`; DPIA:11's sheet shape; the lab harvest as prose (`lab-go-live.md:209-218`); catalogue arrays the lint can parse (`OT_EVENTS` `ot/events.ts:236`, `BILLING_EVENTS` `billing/events.ts:169`; `auth/events.ts` defines multi-line, :3-6).
- **Build:** `factSheetFor(day)` → `ops_fact_sheets{day, facts, input_event_watermark, partial}` + `ops.fact_sheet_produced`; sections: breaches by defKey/state (bucketed by `dueAt`, D29), escalations + `fallbackExhausted`, orphans by `feeKind`, gate refusals/overrides, open critical calls at 07:00 (`lab.critical_*`, `imaging.critical_overdue`), late-entry share, agent share (`actor_type='agent'`, today 0), discharge decision→door, harvest sections via `harvestRowsFor`, the R-247 digest line (`governance.single_approver_used`, emitted by the approvals engine — a build item here, 0 hits today); `/ops/fact-sheet` + `pnpm ops:fact-sheet <day>` (a new script in `package.json`'s style; no `ops:*` exists); `kernel.alerts` subscribes `ops.fact_sheet_produced`; `PROTOCOL_GATE_EVENTS` + the two lints; `aerb.acquisition_refused`; the orphan scan widened through per-module `unbilledOrdersFor(day)` (D32); `FACT_SHEET_ENABLED` inert.
- **Gates:** G1/G2 — the read permission ships with `seed:roles` in `deploy.sh` (deploy-parity), RM rule 4 (RM:378-381) being the sequencing rule it is. Class 0: no patient identifier. No names, no DPIA. **Tier:** T0 automation. **Deps (everyone's files, CLAUDE.md:36-42):** `kernel/worker/jobs.ts` (+ the scheduler census `worker/jobs.test.ts:337-344` names: "FOUR censuses and `alerts.yml`", `docker/prod/prometheus/alerts.yml`); `kernel/db/schema/index.ts` + one drizzle serial at rebase; `kernel/ops/manifest.ts` + `scripts/seed-roles.ts` + five pins in `test/seed-roles.test.ts` (:886 161, :1102 308, :1120 141, :1191 147, :1195); `notify/templates.ts`; a CI lint over every module's `errors.ts`/`events.ts`; `router.tsx` + `caddyfile-parity`; `kernel/alerts/manifest.ts`. Rule: a rebase slot on the train; counts re-measured at rebase by landing and reading the failures. E2-M3's landing is announced on the train (D31).

### P5-discharge-audit-E3 — kernel-D `documents`: the signed clinical document, revived
- **Plan home:** KD (authored 2026-08-28, never built: no `pgTable("documents")`, no `kernel/documents/`). Existing. **Name collision:** R-260 (REG:272) reserves "one `documents` table" for the Expertise store (28c SOPs + 12a playbooks). Kernel-D keeps `documents` (authored first; 22cE:7 is gated on it); the Expertise store's table is `expertise_documents` — R-260 says decide at 28c authoring; this design reserves the names so they never collide (D28).
- **Goal:** one document face for 44's signed summary, 45's MRD file, 15e's handoff and 22cE's `visit_summary` / `dsr_export` / public page (22cE:79,83,89) — not a fifth bespoke face after FD-24's outbox and the lab's snapshots.
- **Exists:** DD1 identity snapshot + body at issue (KD:92); DD4 "the QR never carries the access code" (KD:98); the verify routes ruled `GET /verify/:docNo` (form) · `POST /verify/:docNo` with the code (KD:157); DD7 reissue grammar (KD:104); `print_jobs` (`schema/printing.ts:38-46`); the lab's versioned snapshots (`cfba8d5`).
- **Build:** `documents` table (retention class per R-009, REG:21: IPD documents 10 y), reissue grammar, verification per KD:157, a renderer that takes a `documents` row and nothing else; outbox gains nullable `document_id`; adopters. Files: `kernel/documents/` (new), `schema/index.ts` + one serial at rebase, `kernel/printing/render.ts`, a route (`caddyfile-parity`).
- **Gates:** none legal; starts after the lab's G6 closes — a dated `## Pilot closed` line in `lab-go-live.md` (pharmacy's `## 6. Executed on UAT` is the pattern; #122, OPEN, writes the lab runbook's gate section). **Tier:** none. **Deps:** printing v2; adopter = L back from S after the lab's G6; if L is still on S, the roadmap lane authors E3-M1 and the lab adopts at its next rebase.

### P5-discharge-audit-E4 — Plan 44: the IPD discharge cascade
- **Plan home:** `44` (IDX:86), IPD cluster. Existing.
- **Goal:** SPEC:256 in code: fit declared → sweep → nine parallel clearances → template summary signed under two clocks → final bill → gate pass → bed turnover; the model Drafter as a shadow layer last.
- **Exists:** the definition (IPD:97-113), sketched `ipd_discharges`/`ipd_clearances` (IPD:163), roster rows (IPD:458-470), KPI ids (IPD:438-442), D5 (IPD:241); the approvals engine (`kernel/approvals/flow.ts`, `worklist.ts`; role-addressed at open, person-addressed at decision, `schema/approvals.ts:36,50`); the registry cascade 15 uses (`recovery.ts:483-484`); timers and ladders (`timers.ts:86-175`); passes are 41's object (IDX:83 "wristbands/passes").
- **Build:** `ipd_discharge` definition with `discharge_type` (IPD:113); clearances as approval instances `discharge_clearance.<dept>`, 60-min SLA + ladder (D1); orders sweep; template summary + `summary_countersign_pending` (D12) on E3; final bill via R-252 → R-195's Plan 08 path; pass issue/scan on 41's object; bed release = `resource.status_changed` `cleaning → available` by a named housekeeping human (D33's manual path; IPD:458 "supervisor manual assign"); the Chaser as an SLA-ladder instance behind `DISCHARGE_CHASER_ENABLED`; sheet lines; Drafter shadow (T2) last behind `DISCHARGE_DRAFTER_ENABLED`.
- **Gates:** the IPD gate (O1; Plan 20 approved and live; the five money ladders under R-252; 41 live 30 d); 16 returns for `pharmacy_returns` (absent in `core/modules/pharmacy` — `NOT MODELLED → runbook`); `death` = `NOT MODELLED → runbook` (MCCD by the certifier, R-111 REG:123; no gate pass) until 43b (IDX:85; 43b needs 20 + 42a, which needs 16d — excluded, RM:424); for M5 only: the Class-1 DPIA revision, R-128, R-126, 12a, `complete()`.
- **Tier:** Chaser T1; Drafter T2 (SPEC:785); the T4 Turnover Dispatcher (SPEC:787; IPD:458 home "Plan 20 (w/ 19)") is a **later** line gated on 19/20 + 12a's harness — not in the first cut; Readiness Predictor T0 after 90 d (IPD:463). **Deps:** 41, 20, E3, 16 returns, 43b.

### P5-discharge-audit-E5 — 28d: protocol adherence as data, evaluator, deviation review
- **Plan home:** `28d` (IDX:74; Q22:537). Existing.
- **Goal:** note 12 exactly: protocol → versioned rules → event → evaluation → deviation task with a named reviewer → resolution, every step evented; sampling audits the fallback.
- **Exists:** note 12; roster rows (A12:393; Q22:460); four module-private books (`lab/definitions.ts:22-24`, `radiology/chasers.ts:87-91`, `schema/ot.ts:526-543`, `opd/rx-checks.ts:178-186`); versioned workflow definitions (SPEC:196-199); E2's `PROTOCOL_GATE_EVENTS`; the PHI log (`kernel/phi/audit.ts:15` `PhiSurface`).
- **Build:** `protocol_rules{id, module, version, kind: block|review, source, event_names, status: active|diagnostic}` (retention: KPI packs 8 y, R-009) seeded from the gates; evaluator consumer behind `PROTOCOL_EVALUATOR_ENABLED` → `protocol.deviation_flagged{ruleId, ruleVersion}` → review approval; demotion rule; `no_protocol` line; first five clinical rules from DTC-signed in-house SOPs via the Expertise store (D21); the inspector's read `quality.reconstruct.read` (D35).
- **Gates:** R-240 signed (law); Plan 20 live; O1 for 28-G; the doc-11 registry shape frozen before 28a (Q22:541,167 — 28d keeps its own register, D14); R-118 staff notice + DPIA staff-data line (DPIA:38) before E5-M2 on production; R-119 by construction (D22). No inference (28e separate).
- **Tier:** T0 evaluator; T2-capped review tasks; never a block, never a correction (Q22:460). **Deps:** E2-M3, Plan 20, the approvals engine.

## 3. Milestones

### E1 — 15e
| id | name | gate | acceptance (parameter · instrument · threshold) | depends_on | size · human act |
|---|---|---|---|---|---|
| E1-M1 | One payload, one writer (= P4-E3-M1) | G1 | `daycare.discharged` gains `procedureCode, procedureClass, woundClass, preOpScores` (additive v2) · contracts parity + consumer test · 100 %; the recall consumer writes one `notifications` row `daycare_followup` per `daycare.discharged` and per `daycare.absconded` · fail-first (0 rows today) · exactly 1, idempotent (dedupe key `enqueue.ts:66-67`); `notifyOk=false` or no phone → row `suppressed` (`schema/notifications.ts:44`) and the slip carries the date · test · 100 %; `enqueueNotification` throws → discharge still completes, row absent, sheet counts it · test · 100 %; recall date = `review_date` else discharge + `opdConfig.followUpDefaultDays` · unit test · 2 fixtures; `DAYCARE_RECALL_ENABLED` inert by default, toggle evented | — | 3 d · none |
| E1-M2 | Two papers in the outbox | G1 | `DocumentKind` 4 → 6 and `render.ts` cases 3 → 5 · a kinds↔switch parity test, a BUILD item (fail-first: 4 ≠ 3 today) · green; slip carries follow-up date, Hindi + English, digits unchanged · snapshot per language · 2; `handoffDocumentId` resolves to a `print_jobs` row · test on `convertToAdmission` · 100 % | — (coordinate printing v2) | 4 d · none |
| E1-M3 | The discharge delta on #157 — runbook sections and census rows | G2–G4 | `ot-go-live.md` gains §Seat walk, §Harvest (six rows: `discharge_ready` breaches, `daycare.escort_refused`, late-cutoff offers, `daycare.absconded`, conversions without a printed handoff, `tariff_item_missing` **as a prose row** — thrown at `tariff/pricing.ts:27`, no event), §Executed · grep · 3 headings in ONE file, `standup-check.test.ts:146` map unchanged; census `ot` rows 6 → ≥ 11 at rebase · `standup:check ot` · `rows=` line; `department_head` held · seed-roles READY · ≥ 1 before any Class-B publish; `daycare.escort_refused` · defined + emitted where the 422 is thrown · fail-first; `governance.single_approver_used` · emitted by the approvals engine on every single-approver publish · fail-first | — | 4 d · the Class-A ceremony on UAT (drafter `opd_admin` ≠ activator `owner`, MS approves); the MS drafts + publishes `privileges`; the owner names seven (NEW-P5-2) |
| E1-M4 | The discharge leg of the UAT walk (P3-E6-M3 / P6-E6-M2's section) | G5 | one synthetic case `booked → discharged` in a browser by the OT in-charge · `## Executed on UAT — <date>` in `ot-go-live.md` · dated, one section, never a second file; decision→door for the walk · `workflow_transitions` · a number in the section; `standup:check ot` · exit code (`standup-check.ts:712`) · 0 | E1-M2, E1-M3; #120 + #121 landed, `deploy.sh HMIS_TARGET=uat` | 2 d + one afternoon of the OT in-charge |
| E1-M5 | The discharge rows of the production pilot (P3-E6-M4's harvest) | G6 | first 20 real discharges · transitions · p90 ≤ 120 min; breaches · sheet · 0 in a week with ≥ 5 `discharge_ready` entries; one `notifications` row per discharge/abscond · SQL · 100 %; `notification.sent` ≥ 95 % **only once `NOTIFY_PROVIDER` has a non-console member** (SPEC:820) — until then the call is the recovery nurse's, a harvest line; harvest read · `alerts.read_at` on the sheet alert · 7/7; no system-generated message or invoice reaches a patient during G6 · paper authoritative (RM:365) | E1-M4; E2-M1; package + implant prices in production's active tariff (money) | 0 build · the owner's weekly deploy; OT in-charge reads |

### E2 — 11n
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E2-M1 | `factSheetFor(day)`, table, job, event | G1 | rows · `ops_fact_sheets` · 1 per day, idempotent on re-run; sections · typed object · ≥ 9; empty day · fixture · every section `no_data`, never `0 = compliant`; `sla.breached` bucketed by `payload.dueAt` · fixture: a breach due 23:59 IST drained by `runDueTimers` at 00:01 lands on the earlier day, late-entry = `occurred_at − dueAt` · test; human-keyed late entries (`late_entry.flagged`, `ot/events.ts:188`) by `occurred_at`/`recorded_at` · test; a deploy at 14:00 IST → first row flagged `partial` from the deploy-time watermark · test; patient identifiers · schema test · 0; `FACT_SHEET_ENABLED` inert, toggle evented | — | 4 d · none |
| E2-M2 | Read surfaces | G1–G2 | `/ops/fact-sheet` · `router.tsx` + `caddyfile-parity` · +1; `ops.fact_sheet.read` · `kernel/ops/manifest.ts` 3 → 4 strings + `seed-roles.test.ts` five pins re-measured at rebase · green; `seed:roles` in `deploy.sh` · deploy-parity · present; `pnpm ops:fact-sheet <day>` · one-arithmetic test · same object as the screen | E2-M1 | 3 d · none |
| E2-M3 | The gates are the protocol | G1 | `PROTOCOL_GATE_EVENTS` · an explicit allowlist checked against the parsed catalogues (`OT_EVENTS`, `BILLING_EVENTS`, each module's) with suffix rule `_blocked\|_refused\|\.blocked\|\.overridden\|\.halted` · ≥ 12 names, CI red on a matching name off the list (the design's `*_flagged` pattern matched 8 non-gates such as `lab.result_delta_flagged` and missed both `_blocked` names — dropped); authority refusals without an event twin · lint over the 23 403-codes in 7 modules, generic denials exempt (`permission_denied`, `forbidden`, `user_actor_required`) · 0; `aerb.acquisition_refused` · defined + emitted · fail-first — lands FIRST (radiology lane) or the lint lands with a dated allowlist emptied at close, announced on the train; per-gate line · sheet · daily | E2-M1 | 3 d · none |
| E2-M4 | Delivered and read | G5-equivalent | `kernel.alerts` subscribes `ops.fact_sheet_produced` → one `alerts` row per day for the owner and the quality holder · `created_at` ≤ 07:05 IST · 7/7; read · `alerts.read_at` ≤ 24 h · 7/7; the lab's harvest rows on the sheet by pilot day N · dated · present; a breach with no sheet line after 24 h · SQL · 0 | E2-M2 | 2 d · the owner reads; WhatsApp/SMS only after the SPEC §19 gateway |
| E2-M5 | The orphan scan widens | G1 | `unbilledOrdersFor(day)` exported from `lab`/`radiology`/`pharmacy` `index.ts` · lint (module imports only via `index.ts`) · green; the join in `kernel/ops/fact-sheet.ts`, billing gains no import edge · grep · 0; `charge.orphan_flagged` gains `feeKind` (additive v2: `opd_consult\|lab\|imaging\|pharmacy`) · one per encounter per kind, second run appends nothing; no orders · fixture · `no_data` | E2-M1 | 3 d · none |

### E3 — kernel-D
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E3-M1 | `documents` table and renderer contract | G1 | migration · `apps/core/drizzle` · +1 additive, serial at rebase; table named `documents`, `expertise_documents` reserved (D28) · schema · both names; reissue · test · version+1, `supersedes_id`, old row `superseded`; `GET /verify/:docNo` form + `POST /verify/:docNo` with the code (KD:157) · 100 % of issued, 0 % of revoked, code never in a path · route test; retention class · R-009 · set | lab G6 closed — dated `## Pilot closed` line in `lab-go-live.md` | 6 d · none |
| E3-M2 | Adopters | G1 | lab print jobs with `document_id` on UAT · SQL · 100 %; 15e's `handoffDocumentId` · FK `documents.id` · every conversion; 22cE's `visit_summary` / `dsr_export` / public page · listed in the renderer contract · 3 | E3-M1; E1-M2 | 5 d · none |
| E3-M3 | Reissue walk rehearsed | G5 | an amended lab report reissued AMENDED in a browser · runbook §Executed · dated; old code · verify · `superseded` | E3-M2 | 1 d · the pathologist of record |

### E4 — Plan 44
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E4-M1 | Authored, gate conditions written as conditions | IPD gate | phase doc `…44…md` · exists; §2b rows · ≥ 24; each clearance names a role and a `NOT MODELLED → runbook` fallback · 9/9, `death` and `pharmacy_returns` among them; money rows · §7 · each names R-nnn under R-252 + the corporate default | 41 live 30 d; O1; Plan 20's approval line | 5 d · O1 named; the five ladder numbers |
| E4-M2 | Cascade, clearances, sweep, template summary, two clocks (no inference) | G1–G4 on UAT | `fit_declared` opens 9 approval instances in one tx · test · 9; silence 60 min · `escalation.triggered` rung 0 (`timers.ts:139`) · timer test; empty required section · completeness check · unsigned; RMO signs → `documents` row · 100 %; countersign · `summary_countersign_pending` 1440 min · breach attributed `doctor`; the `record_only → active` flip is a definition version bump, in-flight admissions finish on the old version · test (SPEC:196-199); `DISCHARGE_CHASER_ENABLED` inert | E4-M1; E3; Plan 20 (mints `rmo`) | 15 d · RMO + consultant named |
| E4-M3 | Final bill, dues, pass, bed | G1–G4 on UAT | final bill = charges − deposit − returns · composer test with a return · exact; dues · R-252 → R-195's Plan 08 path · no discharge blocked on money; pass issue/scan on 41's pass object · 1 each; bed `cleaning → available` · `resource.status_changed` by a named housekeeping human · 100 %, no engine; `pharmacy_returns` without 16 returns · census · `NOT MODELLED` | E4-M2; 41's pass object; 16 returns | 10 d · money ladders ruled |
| E4-M4 | Rehearsed on UAT | G5 | one synthetic admission fit → exit in a browser, every clearance by its seat · runbook §Executed · dated, ≤ 3 h; hospital-attributed minutes · attributed durations · per hold | E4-M3 | 3 d · a morning of coordinator, RMO, nurse, billing, pharmacist |
| E4-M5 | The Drafter in shadow (T2) | Class-1 DPIA revision signed (DPIA:14,28,37) · R-128 opt-out honoured · R-126 ruled · `complete()` landed · 12a harness | draft at `fit_declared` · provenance (model id/prompt/hashes) on the event and the signed document · 30 consecutive, p90 < 60 s; kill switch on · D5 test · flow completes; request body carries no identifier field · call-site test (DPIA:22) · 0; uncited claims dropped · renderer test · 100 %; draft→signed edit distance · hashes · recorded (R-006's 90 d of data); English draft only, Hindi patient copy deterministic · test; `DISCHARGE_DRAFTER_ENABLED` inert | E4-M4; 12a; `complete()` | 10 d · counsel signs the revision; owner promotes the tier (R-130) |
| E4-M6 | Piloted | G6 | fit → exit p50 hospital-attributed · sheet · < 3 h over 50; countersign ≤ 24 h · ≥ 95 %, read daily (D12's dependency); clearance breaches by dept · read daily; before-11 share · reported, never gated | E4-M4 | 0 build · discharge coordinator named (role from Plan 20) |

### E5 — 28d
| id | name | gate | acceptance | depends_on | size · human act |
|---|---|---|---|---|---|
| E5-M1 | The register of the gates that exist | G1 | `protocol_rules` · seeded from `PROTOCOL_GATE_EVENTS` with a lint · ≥ 12, each with a version; each rule id on ≥ 1 sheet line in the window · SQL · 100 % | E2-M3 | 5 d · none |
| E5-M2 | Evaluator → deviation → named reviewer (shadow) | G5 on UAT; on production flag-inert until R-118 + the DPIA staff line | lag `recorded_at` → `protocol.deviation_flagged` · p90 < 5 min; ≥ 1 holder of the rung role at open AND `decided_by` non-null at close · 100 %, `fallbackExhausted` 0; one patient-day reconstructed · `quality.reconstruct.read` render with a `PhiSurface` row, the script exits 0 printing N events / M evaluations / K tasks · yes; clinical-record writes by the evaluator · test · 0; `PROTOCOL_EVALUATOR_ENABLED` inert | E5-M1; Plan 20; R-240 | 10 d · just-culture signed; staff notice signed |
| E5-M3 | Demotion and honesty | G1 | flag rate > 30 % over 7 d · demotion event + alert · automatic; conditions without a rule · `no_protocol` line · shown, never "compliant" | E5-M2 | 3 d · none |
| E5-M4 | First five clinical rules from a named source | G3 | rules with source citation, DTC signature · 5; shadow 30 d · share of deviations closed `accepted` (`approvals.status`/`decision_note`) · ≥ 70 % | E5-M3; R-260 | 8 d · DTC chairs named (R-243) |
| E5-M5 | Piloted | G6 | closure · ≤ 72 h p90; per-person aggregates · schema + test · none (D22) | E5-M4 | 0 build · quality holder reads daily |

## 4. Edge cases — the Indian day this pillar must survive

| # | the day | expected behaviour | artefact that proves it | milestone |
|---|---|---|---|---|
| 1 | Day-care patient scored ready 19:55 IST; the escort's phone is the patient's own | refuse `escort_required` (422) **and emit `daycare.escort_refused`**; the 20:00 cutoff *offers* conversion, never performs it | refusal + event + no conversion event (`recovery.ts:277-282,462-475`) | E1-M3 |
| 2 | Ready patient sits in bay 2 for 3 h; the bill is not composed | `sla.breached` at 120 min; discharge never blocked on money (P15:172) | breach on the next sheet, bucketed by `dueAt` | E1-M5 / E2-M1 |
| 3 | Discharge 21:30 Saturday; day 7 falls on a public holiday | one row dated day 7; never two; no holiday calendar exists (grep 0) — the nurse moves the call on paper; a calendar is Plan 27's config surface | `notifications` count = 1 per encounter | E1-M1 |
| 4 | Patient walks out while the nurse is at the counter | `absconded`; `ot_incidents` records the escort fact **and** the recall consumer writes a row (D25 — this design's decision; #164 refuted the *task*, not a notification) | `ot_incidents` + `notifications` (today: incident only) | E1-M1 |
| 5 | Conversion to the incumbent IPD at 20:10; the ward wants paper | the handoff prints with the bill cut at `at` (R-3.6); the ULID at `recovery.ts:525` has a body | `print_jobs` kind `daycare_conversion_handoff` | E1-M2 |
| 6 | The attendant reads only Hindi | the slip carries Hindi beside English; digits the same in both scripts | snapshot per language | E1-M2 |
| 7 | The `ot` module is deployed; the MS never published the drafts | `standup:check ot` prints `ot_definitions_published` RED with the fixing screen (`standup-check.ts:509`); the recovery screen refuses with the reason | census row + refusal | E1-M3 |
| 8 | 02:00, nobody holds `recovery_nurse`; the 120-min timer fires | ladder falls to `duty_manager`; `fallbackExhausted` recorded; `recovery_nurse_held` RED next morning | `escalation.triggered{fallbackExhausted:true}` + sheet | E1-M3 / E2-M1 |
| 9 | Holi: zero events all day | every section `no_data`; nothing reads as compliant | empty-day fixture | E2-M1 |
| 10 | A breach due 23:59:50 IST; the worker was down; `runDueTimers` drains it at 06:10 | lands on the day of `dueAt`; late-entry share counts `occurred_at − dueAt` | `dueAt` fixture | E2-M1 |
| 11 | A lane ships a new `*_refused` event for a new gate | CI fails until the name is on `PROTOCOL_GATE_EVENTS`; the fix is one name, never a weaker pattern | lint red → green | E2-M3 |
| 12 | The AERB gate refuses a night CT on the empty licence table; the technologist retries three times | each refusal is an event, not only a 403; the sheet shows 3 refusals against 0 licences | `aerb.acquisition_refused` × 3 (today: nothing written, `aerb/errors.ts:80`) | E2-M3 |
| 13 | Printer relay down at discharge | the slip queues; discharge proceeds; queue depth is a sheet line | `print_jobs` status + sheet | E1-M2 |
| 14 | IPD: summary drafted while a lab result is unpublished | the template lists pending results; `pending_results` cannot satisfy while one is unpublished | gate test | E4-M2 |
| 15 | IPD: meds "returned" that never were; billing clears on its own | clearances are approvals by a named human; billing auto-satisfies only on an empty orphan scan, never on balance | holder ≠ `system` for 8 of 9; scan fixture | E4-M3 |
| 16 | IPD: the consultant changes the plan after fit was declared | `fit_declared` versioned; a signed summary is reissued AMENDED (KD DD7) | `documents` version+1 with `supersedes_id` | E4-M2 / E3 |
| 17 | IPD: consultant in theatre till 13:00; six summaries wait | the RMO signs; the 24-h clock runs; a breach is attributed `doctor` | `sla.breached{summary_countersign_pending}` | E4-M2 |
| 18 | IPD: the drafter's provider times out / flag off | template editor opens at once; flow completes; flag state is a sheet line | D5 test (IPD:241) + sheet | E4-M5 |
| 19 | IPD: TPA final approval at 17:00 for a 10:00 fit | clock attributed `payer`; above the threshold the counsellor signs and the patient leaves (R-219) | attributed hold + the undertaking on 41's object | E4-M3 |
| 20 | IPD: the family wants the 06:00 muhurat | `family_hold` attributed `family`; no extra bed-day for a hospital-caused delay (R-186) | hold row + bed-day test | E4-M3 |
| 21 | IPD: death on the ward at 03:00 | `discharge_type: death` = `NOT MODELLED → runbook` (MCCD by the certifier, R-111); no gate pass; no template until the certifier acts; 43b later | condition row; no pass | E4-M1 |
| 22 | Audit: a condition with no codified protocol | `no_protocol` shown as coverage, never as compliance | coverage line | E5-M3 |
| 23 | Audit: a wrong rule flags 100 % of admissions | > 30 % over 7 d → `diagnostic`; the quality holder alerted; nobody tasked | demotion event | E5-M3 |
| 24 | Audit: a protocol version changes mid-admission | evaluation pins the version at encounter start; the deviation carries `ruleVersion` | payload test | E5-M2 |
| 25 | Audit: an MLC/sealed record | the evaluator reads through the caller's filter (SPEC:559, §11.19-C item 5, the sealed class); the task shows no sealed content | sealed-class test | E5-M2 |
| 26 | An inspector says "show me the 100 %" | one patient-day reconstructed through `quality.reconstruct.read` under the quality holder's identity; a `PhiSurface` row is written; never a shell transcript | the read's render + PHI log row | E5-M2 |
| 27 | HR asks for deviations per doctor for appraisal | refused by design — no per-person aggregate exists (D22) | schema + test | E5-M5 |
| 28 | **Session:** 15e merged; nobody holds `ot_incharge` on production | census reads *unstaffed*; the session writes the runbook line and stops — a user on production is a named human through `/admin/users` (RM:362, G4), never a session's act | RED row + refusal in the lane report | E1-M3 |
| 29 | **Session:** the deploy lands the sheet job before its read permission | the job stores the sheet; the screen refuses all but `admin`; `seed-roles.test.ts`'s pins and deploy-parity name the missing grant (RM rule 4 chose "config surface first") | `ops_fact_sheets` rows + the failing pin | E2-M2 |
| 30 | **Session:** the owner is asleep; 44's write-off number is pending | the amount is config; the session records the corporate default (R-148 under R-252) in §7, builds the approval type, stops at the number | §7 row naming R-148 | E4-M1 |
| 31 | **Session:** a grep finds `sla.breached` and the report says "the audit exists" | a claim is a reader with a row count; the report pastes the day's sheet count and the SHA; `lab.sla_breached` is not `sla.breached` | count in the report header | E2-M1 |
| 32 | **Session:** printing v2 and 15e both add kinds to `render.ts` in one week | the second lane rebases and the parity test proves both render (`DocumentKind` 6, cases 5 — not "6 and 6") | parity count in the PR | E1-M2 |
| 33 | **Session:** a lane proposes the Drafter "in shadow" on production before counsel signs the Class-1 revision | refused; E4-M5 stays *not started* with the gate named | milestone row unchanged + the refusal | E4-M5 |
| 34 | **Config-before-guard:** the MS published the three drafts but never drafted `privileges`; the first case is booked | `ot_gate` refuses every surgeon; `standup:check ot` → RED with OTRB §4 as the fix; the walk stops at booking, never at discharge | census row + refusal (OTRB:19-30,116-130) | E1-M3 |
| 35 | **Ceremony:** the owner drafted `daycare_case` himself | nobody can activate (`workflow_drafter_activator`, OTRB:59-63); discard and redraft by `opd_admin`; a runbook line, never a seed | refusal + OTRB §1 trap | E1-M3 |
| 36 | **Deploy-day / money:** production's first real day-care booking; the package is unpriced | refuses `tariff_item_missing` in front of the patient (OTRB:156-160); the fix is a tariff row by the owner (money, R-255); day-1 count is 0 bookings, never "0 breaches" | refusal + census `package priced` RED | E1-M5 |
| 37 | **Deploy-day:** the deploy lands `ops_fact_sheets` + the 07:00 job at 14:00 IST | the first sheet covers a partial day, flagged `partial` from the deploy-time watermark — never `no_data`, never a full-day number | first row's watermark | E2-M1 |
| 38 | **Role-unheld (reader):** no quality holder exists; `quality_manager` is not a role | the sheet is produced; the owner's alert row alone is written; G2 counts `read_at`, never `fact_sheet_produced` | alerts row + G2 line | E2-M4 |
| 39 | **Event-before-reader:** the sheet is deployed one deploy before `aerb.acquisition_refused` | the AERB line reads `no_data` (not 0) until the twin lands; the census row explains | sheet section | E2-M3 |
| 40 | **Lint landing:** lane X has an unmerged `*_refused` event when the lint merges | X's rebase goes red; the fix is one name on the list; the landing was announced on the train; a red `main` freezes merges (CLAUDE.md:19) so the twin lands first | red → green on X's PR | E2-M3 |
| 41 | **Session / train:** #120/#121 have not landed when E1-M4 is due | every G5 row reads "blocked: #120" with the PR number, never "rehearsal late" | milestone row text | E1-M4, E3-M3, E4-M4 |
| 42 | **Session / name collision:** a lane finds `ot-go-live.md` exists | it appends §Seat walk/§Harvest/§Executed; never creates `mini-ot-go-live.md`; the `standup-check.test.ts:146` map stays one entry | one OT file in `docs/runbooks/` | E1-M3 |
| 43 | **Deploy-day / version:** D12 flips `summary_countersign_pending` from `record_only` to `active` after the pilot | a definition version bump; in-flight admissions complete on the old version and never breach; the sheet shows the version split | `workflow_instances.definition_version` per breach line | E4-M2 |
| 44 | **Role-unheld:** 02:00 deviation, nobody holds the reviewer role | ladder to `duty_manager`; `fallbackExhausted` recorded; never auto-closed | `escalation.triggered{fallbackExhausted}` | E5-M2 |
| 45 | **Fail-open:** `enqueueNotification` throws at discharge (unregistered key, no `patientId`) | the discharge completed in its own tx; the recall consumer's failure is its own; the row is absent and the sheet's "discharges without a recall row" counts it | discharge event + 0 rows + sheet line | E1-M1 |

## 5. DECIDED and RULINGS

**DECIDED** (Indian-corporate-hospital standard; one line of reasoning each):
- **D1** Clearances are approval instances `discharge_clearance.<dept>` on the existing approvals engine, 60-min SLA + ladder — a clearance is a one-click confirm; no tasks engine before 44 (there is none in the kernel, #164).
- **D2** The billing clearance auto-satisfies on an **empty orphan scan** for the encounter (E2-M5's `charge.orphan_flagged{feeKind}` is the instrument), never on a zero balance — a zero balance with an un-posted charge is the leakage SPEC:783 exists for; balance/dues stay on R-252 → R-195's Plan 08 path; the rule is a Class-A definition (SPEC:623 "money rules") activated by two keys. The empty orders sweep auto-satisfies likewise.
- **D3** The template summary is the product and ships first; the model draft is a layer — the fail-open path must exist before the thing that fails.
- **D4** "Real time" is the dispatcher cycle (2 s / 20 s); anything in-transaction is a module gate.
- **D5** The first protocol set is the gates already built; a gate that refuses without an event gets its event twin first.
- **D6** The sheet is Class 0 (aggregates, no ids); its readers are **in-app `alerts` rows** now (`kernel.alerts` subscribes `ops.fact_sheet_produced`), WhatsApp/SMS after the SPEC §19 gateway (SPEC:820); email is nobody's build item (no channel exists, `adapters.ts:11`).
- **D7** Letters `15e` and `11n` (15b/c/d, 11j/11k on the repo; 11l/11m P6's); numbering never moves.
- **D8** Kernel-D is revived before 44 is **built** (E4-M2 depends on E3); authoring (E4-M1) needs no table; 44 and 45 land on it; the outbox stays the transport (KD:44-46).
- **D9** The OT's Class-B `criteria` are published by the MS **with a `department_head`** (R-170; SPEC:623) — R-247 honesty mode covers only the missing second administrator: Class-A re-ratification by two keys ≤ 30 d after O1 (RM:183-191). The Class-A ceremony itself is OTRB §2 (`opd_admin` drafts, MS + `owner` approve, non-drafter activates).
- **D10** Recall date = `daycare_encounters.review_date` (additive, set at discharge) else discharge + `opdConfig.followUpDefaultDays` (`schema/opd.ts:24`, default 7 — the hospital's one follow-up interval, never a second "7"); one message; missed = recall, never no-show (SPEC:460). The day-7 review is the package's included visit; anything beyond it is R-058 (REG:70, money). No holiday calendar exists (grep 0) — none is invented here. This is a one-shot `enqueueNotification` row, **not** Plan 27's recall register (IDX:73); 27 adopts.
- **D11** Slip in Hindi + English, digits unchanged — R-006's own default (REG:18 "deterministic templates").
- **D12** Countersign is a second SLA state `summary_countersign_pending` 1440 min — `record_only` in commissioning, `active` after the pilot, **and E4-M6 reads the countersign share daily** so the clock's silence is a read line, not a blind spot (R-192 is law).
- **D13** Deviation tasks carry no per-person aggregate — R-119 by construction.
- **D14** 28d keeps its own `protocol_rules`; Plan 21 consumes later.
- **D15** Rule versions pin at encounter start (SPEC:196-199); demotion at > 30 % over 7 d.
- **D16** Orphan widening adds **no import edge into billing** (billing imports opd/tariff/patients/membership only; lab imports billing in 13 files, ot 10, pharmacy 5): each module exports `unbilledOrdersFor(day)` on its `index.ts`; the join lives in `kernel/ops/fact-sheet.ts`; the event gains `feeKind` additively.
- **D17** Discharge KPIs are one arithmetic on the sheet (`user_day_facts` precedent) until 21 registers them under K11:459's ids.
- **D18** The OT harvest is six rows on the lab's model (`lab-go-live.md:209-218`), **appended to `ot-go-live.md` §Harvest**; `tariff_item_missing` is a prose row (no event); the window closes when the last three are empty for a week.
- **D19** 44 adopts `daycare.discharged` for `discharge_type: day_care`.
- **D20** Readiness Predictor and Demand Forecaster stay out of 44's first cut (IPD:463).
- **D21** Protocol source: the first five clinical rules are DTC-signed in-house SOPs via the Expertise store (R-260); licensed content is a purchase on the §19 knowledge budget line (R-044 pattern, REG:56), raised only when a service line names it — ₹0 by default is a decision, not a ruling.
- **D22** R-119 (REG:131, category `policy`) is adopted by construction (D13, edge 27); the owner signs the HR policy *text* at 21/28a authoring (IDX:207 risk 13), not as a gate on E5-M5.
- **D23** Every automation this design adds (recall consumer, fact-sheet job, refusal-twin lint's fix events, evaluator, Chaser ladder, Drafter) ships behind an inert `*_ENABLED` config flag, an enum of two exact strings whose toggle is evented — the `RETENTION_ENABLED` precedent (`config.ts:114-123`) — the kill switch until 12a's harness exists (SPEC:789).
- **D24** One fact-sheet composer: `factSheetFor(day)` in `kernel/ops/fact-sheet.ts`; harvest rows come from one function `harvestRowsFor(module, day)` in `kernel/ops/harvest.ts`, which `pilot:harvest <module>` (P6-E3) and the sheet both print — whichever lands first owns the function, the other imports it; `digest.fact_sheet` (P1-E3) reads `ops_fact_sheets`.
- **D25** The recall is a **consumer** of `daycare.discharged` / `daycare.absconded` in its own transaction — `enqueueNotification(tx, …)` throws (`enqueue.ts:68-100`) and must never sit inside `dischargeDaycare`'s `withTx` (SPEC:789 fail-open); it honours `escort.notifyOk` (P15:190), the patient's opt-out and language (SPEC:771; R-020 REG:32 templates only; R-249 REG:261; R-114 REG:126); `notifyOk=false` or no phone → the slip carries the date and the row is `suppressed`. The abscond half is this design's own decision (standard: a day-care surgical patient who walked out gets a call), with its input the `ot_incidents` row #164 named.
- **D26** E1-M1 and P4-E3-M1 are one milestone under `15e`: one additive payload (`procedureCode, procedureClass, woundClass, preOpScores`), one recall writer; whichever design lands first owns it; P4's SSI/wound halves stay P4's; P2-E2-M3 and Plan 25 (PHY:434) adopt.
- **D27** E1-M3/M4/M5 are deltas on #157 / P3-E6-M1..M4 / P6-E6-M1..M2: one runbook file (`ot-go-live.md`), one census, one dated walk, one harvest — this pillar contributes the discharge rows and never a second file or section.
- **D28** Kernel-D's table is `documents`; the Expertise store's is `expertise_documents` (R-260's "decide at 28c authoring" — the name is reserved now so the two never collide).
- **D29** `sla.breached` buckets by `payload.dueAt`; late-entry share for breaches = `occurred_at − dueAt`; human-keyed events keep `occurred_at`/`recorded_at`.
- **D30** `PROTOCOL_GATE_EVENTS` is an explicit allowlist checked against the parsed catalogues with a suffix rule; generic denials (`permission_denied`, `forbidden`, `user_actor_required`) are declared exempt from the twin lint; `aerb.acquisition_refused` lands before the lint or the lint carries a dated allowlist emptied at close.
- **D31** E2-M3's landing is announced on the train — a red `main` freezes merges.
- **D32** (= D16's mechanism) per-module `unbilledOrdersFor(day)`; the join in kernel/ops.
- **D33** `death` = `NOT MODELLED → runbook` (R-111) in 44's first cut until 43b; bed release is `resource.status_changed` by a named housekeeping human; the T4 Turnover Dispatcher is a later line gated on 19/20 + 12a.
- **D34** E5-M2 is flag-inert on production until the R-118 staff notice and the DPIA staff-data line are signed (R-122's shape, REG:134); its shadow runs on UAT with synthetic data until then.
- **D35** The inspector's reconstruction is a permissioned read `quality.reconstruct.read` under the quality holder's identity, writing a `PhiSurface` row (`kernel/phi/audit.ts:15`) — never a script against the database (SPEC:789 API-only).

**RULINGS** (money / procurement / law only):

| id | question | category | recommended default | unblocks |
|---|---|---|---|---|
| R-252 → R-148 / R-187 / R-195 / R-219 / R-220 | The Plan 08 dues/advance ruling, then the five IPD money ladders it parents (REG:264) | money | the register's defaults (≤ ₹2k auto write-off, ≤ ₹25k billing head; admit regardless; dues only via Plan 08; counsellor-signed undertaking; ≤ ₹10k / ≤ ₹50k) | E4-M1, E4-M3 |
| R-255 (package + implant prices) | The day-care package and implant prices in production's active tariff before the first real booking (OTRB:156-160) | money | the owner's number, entered through the tariff screen; placeholders on UAT only | E1-M5 |
| O6 | CA signature + real package tariff, GST categories | money / law | one CA session (R-255) | E1-M5's first *live* invoice; the exit from commissioning |
| R-192 + R-111 | RMO signs / consultant ≤ 24 h; MCCD chain | law | as REG:204,123 | E4-M2; the `death` runbook line |
| R-262 + the Class-1 revision | DPIA v0.2 signed by counsel, then the Class-1 revision (DPIA:14,28,37) | law | two counsel sessions; Class-0 signs alone first (H:126) | Hermes narration (v0.2); E4-M5 (revision) |
| R-126 | Class-1 inference locus | law | on-prem default; cloud only with in-region DPA | E4-M5 |
| R-128 | Consent posture for Class-1 runs | law | opt-out honoured before the first shadow draft | E4-M5 |
| R-240 / R-241 | Just-culture; open disclosure | law | adopt the register's text | E5-M2 |
| R-245 | Internal auditor appointment | money | appoint the §138 firm before the payouts pack | 28d control-testing |
| SPEC §19 gateway (was NEW-P5-3) | WhatsApp Business API / SMS / IVR provider (SPEC:820; R-020, R-249, R-114 bind the channel) | procurement | in-app + printed slip now; `NOTIFY_PROVIDER` widens when the provider lands | E1-M5's `sent` clause; E2-M4's WhatsApp line |
| Licensed protocol content (deferred) | Only if a service line names it (D21) | procurement | none this year | E5-M4 (later) |

**FACTS the owner states** (RM:452: "two facts, not rulings"):

| id | fact | needed by |
|---|---|---|
| O1 | The second full administrator's name (RM:336) | E4-M1; 28-G; Class-A re-ratification |
| NEW-P5-2 | Seven names: `surgeon`, `anaesthetist`, `ot_incharge`, `daycare_coordinator`, `ot_nurse`, `recovery_nurse` (OTRB:142-153) and a `department_head` (R-170; no holder, P15:201; no role key in `seed-roles.ts` — a grant and a name) | E1-M3, E1-M5 |
| Plan 20 | The approval line on `…phase1-20-workforce-roster.md` (FOR APPROVAL, :3) — mints `rmo`, `discharge_coordinator`, `quality_manager` | E4, E5 |
| Quality holder | Who reads the sheet beside the owner (no `quality_manager` role exists) | G2, E2-M4 |

## 6. Not now

- **No 12a runtime, no model Drafter** this quarter or next (RM:428); E4-M5 is the only inference here and it is last, behind the Class-1 DPIA revision, not v0.2.
- **44 not built** this quarter, not authored before 41 has been live 30 d (IDX:86; RM:419-420). 45, 54, the Coding Suggester, ABDM care-context, the WhatsApp summary PDF and the discharge lounge follow 44. **No T4 Turnover Dispatcher** (19/20 + 12a).
- **No pharmacy returns path**, **no `death` chain** (43b) here; both carried as `NOT MODELLED → runbook`.
- **No 28a/28e, no Expertise store module** this quarter (RM:416); E5-M1 is Q1 2027 at the earliest.
- **No Plan 21 registry** (RM:415); **no Grafana clinical panel**; **no holiday calendar**; **no email channel**; **no Plan 27 recall register** — the day-care recall is one row it will adopt.
- **No second OT runbook, no second OT census, no second fact-sheet composer** — the three collisions this revision removed.
- **No physio trajectory alert** (Plan 25) — E1-M1's payload is the seam it keys on (PHY:434).
- **Kernel-D waits for the lab's G6** (Q1 2027).
- **Absorption before building, as a number:** ≈ 28 lane-days **build-only** on E1+E2 this quarter (≈ 36 with the two close passes per phase, RM:433, and the S-gate afternoon, RM §8), 0 on E3–E5.

## 7. Sequencing note

Against RM §2 (weeks from Monday 2026-09-07; lanes L/R/S/F; rows RM:334-339). **Departures from RM §2, stated:** (a) RM gives **F** no build after week 2 ("F closes its lane after the FD-25 close; F's capacity joins S", RM:337) and F's lane is local-only with no PR (CONTEXT:54; heartbeat 09-05) — so E2 goes to **L**, who joins S from week 3 (RM:336), at the cost of one lane-fortnight of S's pilot capacity in weeks 3–5; (b) **RM §2 gains a Track S row for Plan 15** (the mini-OT is in no row) — E1-M3 by L beside S's pharmacy census in weeks 4–6.
- **Weeks 3–5 — E2-M1..M3 (11n)** by L. No human act to start. On production **week 6 at best** (12 lane-days + two close passes + the owner's weekly deploy, RM:342): the lab's harvest in weeks 4–5 runs on P6-E3's `pilot:harvest` and prose (`lab-go-live.md:209-218`), and E2-M4's dated acceptance is "the lab's harvest rows on the sheet by pilot day N". `aerb.acquisition_refused` lands from the radiology lane before the lint (D30), so the licence gate's refusals are counted from radiology's first real week (RM:338). **Weeks 6–7 — E2-M4/M5** by L.
- **Weeks 4–6 — E1-M3** (the #157 delta) by L; the Class-A ceremony and `privileges` draft on UAT by `opd_admin`/MS/`owner`; the seven names (NEW-P5-2) by week 6 or the rows read *unstaffed*. **Weeks 6–8 — E1-M2** (the papers) by whoever lands printing v2 (F if FD-25 has landed, else S), then **E1-M1** (= P4-E3-M1) in the same lane — a pilot fix if the walk finds it first (RM:236-237). **Week 9 — E1-M4**, the discharge leg of P3-E6-M3 / P6-E6-M2's UAT walk, gated on #120 + #121 landed and `deploy.sh HMIS_TARGET=uat`; the slip is the walk's prerequisite (an attendant leaves with paper), the recall is not.
- **Weeks 10–13 — E1-M5**, the discharge rows of P3-E6-M4's production pilot behind the owner's weekly deploy, paper authoritative, harvest on the sheet; gated on the package + implant prices in production's active tariff (money) before the first real booking. This makes 15 "open for real" — RM:418's precondition for 29/31, next quarter's call.
- **Q1 2027 — E3** weeks 1–4 after the lab's G6 closes (dated in `lab-go-live.md`), kernel files named in E2/E3's Deps, adopter L (or the roadmap lane authors and the lab adopts at rebase); **E5-M1** weeks 3–4. **E4-M1** only once 41 — authored week 7–9 (RM:338), started weeks 10–13 only if O1 + Plan 20 approved and live + the R-252 numbers + 11b land (RM:339,419-420) — has been live 30 d: end of Q1 2027 at best.
- **Q2 2027 — E4-M2..M4** (≈ 28 d, one lane) on E3 + Plan 20 + approvals; **E5-M2/M3** (≈ 13 d) once R-240 and the R-118 staff notice are signed and 20 is live. **E4-M5** and **E5-M4/M5** start only when their gates are dated — the Class-1 DPIA revision signed, R-128 and R-126 ruled, `complete()` landed; the DTC chairs named. Each start is a human act with a name, so a slip reads "unsigned / unnamed / blocked: #nnn", never "the discharge is late".

## 8. Changelog

Refutation ids: EC = exists-or-collides §n; M = measurability §2.n / §3; A/N = authority-and-safety; R = sequencing-and-absorption. Each line: id → accepted/rejected → change. Evidence re-checked at `41a4be4` for every line marked ✓.

- **EC1** (KILL, `11l` double-booked with P6-E7) → accepted ✓ (`11l`, `11n` 0 hits on repo; `11l`/`11m` at P6:60,67; `11n` absent from every sibling) → E2 re-lettered `11n`; D7 rewritten; not folded with P6-E7 (a milestone instrument and a fact sheet are different objects).
- **EC2** (KILL, mini-OT opening has three homes; #157 merged) → accepted ✓ (`ot-go-live.md` §0–§9, no harvest/executed/seat-walk; `standup-check.ts:458-527` six rows; `standup-check.test.ts:146` map) → E1-M3/M4/M5 rewritten as deltas on #157 / P3-E6 / P6-E6 (D27); `mini-ot-go-live.md` and "module keys 4 → 5" deleted; E1 keeps M1/M2 and the discharge rows; position lines re-measured (nine runbooks, six keys).
- **EC3** (KILL, E1-M1 = P4-E3-M1 under `15-ii`) → accepted ✓ (P4:54-58,92) → merged under `15e` (D26): one payload with P4's `woundClass`, `preOpScores`; one writer; P2-E2-M3 and Plan 25 adopt.
- **EC4** (AMEND, abscond recall's `recovery.ts:549` citation stale) → accepted in part ✓ (`recovery.ts:548-565` now denies the task) → citation dropped; discharge recall cited to P15:339 + SPEC:460; the abscond half **kept as this design's own DECIDED item** (D25, edge 4) — #164 refuted a *task* and a *comment*, and a `notifications` row is the primitive it said exists; the standard answer (a walked-out surgical patient gets a call) is not money/law.
- **EC5** (AMEND, two fact-sheet composers; `pilot:harvest` twin) → accepted ✓ (H:60,113; P6:42,106) → D24: one composer, `harvestRowsFor` shared, `digest.fact_sheet` a reader depending on E2-M1.
- **EC6** (AMEND, `documents` name collision with R-260; 22cE adopters) → accepted ✓ (REG:272; 22cE:7,79,83,89) → D28 reserves `expertise_documents`; E3-M2 lists 22cE's three adopters.
- **EC7** (AMEND, print-kind baseline 3 vs 4) → accepted ✓ (`render.ts:660-662`; `enqueue.ts:52-56`; no parity test) → E1-M2 "`DocumentKind` 4 → 6, cases 3 → 5, parity test a build item"; edge 32 fixed.
- **EC8** (NOTE, recall homes; `followUpDefaultDays`) → accepted ✓ (`schema/opd.ts:24`; IDX:73) → D10 reads the config, names Plan 27 as adopter.
- **EC9** (NOTE, `recovery.ts:483-484`; `ops:*` namespace; position lines) → accepted ✓ → cites fixed; `pnpm ops:fact-sheet` marked new in the existing style.
- **M2.1** (G1 vacuous on an empty week) → accepted → "0 breaches in a week with ≥ 5 `discharge_ready` entries; `no_data` otherwise" (G1, E1-M5).
- **M2.2** (G2 measures production not reading) → accepted ✓ (`schema/alerts.ts:25,29`; `alerts/manifest.ts:20-49`) → `kernel.alerts` subscribes `ops.fact_sheet_produced`; G2/E1-M5/E2-M4 read via `alerts.read_at`.
- **M2.3** (G3 "fails CI" is tests-pass; universe understated) → accepted ✓ (23 codes counted by exact `code: 403` lines: aerb 3, lab 7, ot 2, pcpndt 5, pharmacy 2, radiology 4) → "fails CI" moved to E2-M3; the 23-code universe pasted in §0.7/G3.
- **M2.4** (E1-M1 review date / holiday / stale cite) → accepted ✓ (no `review`/`follow` column in `schema/ot.ts`; `holiday` 0 hits) → `review_date` additive column; holiday clause dropped (edge 3 rewritten); SPEC:460 + P15:339 cited.
- **M2.5** (E1-M2 count) → accepted (= EC7).
- **M2.6** (E1-M3 overtaken; `escort_required` 422 no event; `tariff_item_missing` no event; R-247 event absent) → accepted ✓ (`ot/errors.ts:127`; `tariff/pricing.ts:27`; `governance.single_approver_used` 0 hits) → delta 6 → ≥ 11; `daycare.escort_refused` added; `tariff_item_missing` a prose row; `governance.single_approver_used` a build item of the approvals engine printed by 11n.
- **M2.7** (E1-M5 `notification.sent` measures a console adapter) → accepted ✓ (`config.ts:31,61`; `adapters.ts:4-8,56-66`) → split into rows 100 % / `sent` gated on the §19 gateway / the nurse's harvest line; read via `alerts.read_at`.
- **M2.8** (E2-M1 watermark unconstructible; bucket by `dueAt`) → accepted ✓ (`timers.ts:83-85,120-133`; `envelope.ts:96`) → D29; E2-M1 and edge 10 rewritten.
- **M2.9** (`check-config-present` does not check permissions; RM rule 4 is sequencing) → accepted ✓ (`check-config-present.ts:5-8,43-45`; RM:378-381) → instrument = `seed-roles.test.ts` pins + `seed:roles` in `deploy.sh`; edge 29 rewritten.
- **M2.10** (lint pattern: 8 false positives, misses `_blocked`) → accepted ✓ (my grep of the catalogues: 10 `_flagged` names of which 8 are not gates; `sod.violation_blocked`, `lab.sod_violation_blocked`, `retention.drop_blocked`, `payout.class_blocked`, `lab.reflex_refused`, `instrument.lookup_refused` unmatched by the original pattern) → D30: explicit allowlist over `OT_EVENTS`/`BILLING_EVENTS`-style catalogues (`ot/events.ts:236`; `billing/events.ts:169`), suffix rule, generic-denial exemption.
- **M2.11** (E2-M4 "email/in-app" channels do not exist) → accepted ✓ (`adapters.ts:11`) → D6 rewritten; email dropped; in-app = alerts rows.
- **M2.12** (`feeKind` does not exist) → accepted ✓ (`billing/events.ts:157-160`) → "gains `feeKind` (additive v2)".
- **M2.13** (E3-M1 "lab G6 closed (dated)" has no section) → accepted ✓ (no `Executed`/`G6` in `lab-go-live.md`; #122 OPEN writes the gate section) → gate = a dated `## Pilot closed` line, pharmacy's pattern.
- **M2.14** (E4 vocabulary: rung 0; `cleaning`; no `rmo`) → accepted ✓ (`timers.ts:139`; `kinds.ts:103`; `resources/events.ts:94`; `seed-roles.ts` grep) → E4-M2/M3 rewritten; roles from Plan 20.
- **M2.15** (E5 yes/no and committee clauses) → accepted ✓ (`schema/approvals.ts:36,48,50-52`) → E5-M1/M2/M4 rewritten as counts; `quality_manager` absence noted in FACTS.
- **M3** (stale baselines 1–6) → accepted → §0.5, §0.6 (the lab's hits are `lab.sla_breached`, `lab/events.ts:277` — verified; the "no reader" claim stands), header re-based.
- **A1** (D9 wrong authority for Class-B `criteria`) → accepted ✓ (REG:182 R-170; SPEC:623; P15:201) → D9 rewritten; `department_head` in NEW-P5-2 and E1-M3's acceptance; R-247 only for the second administrator.
- **A6 / N4** (D2 auto-satisfies on balance; R-252 parent) → accepted ✓ (SPEC:256 step 4; IPD:104; REG:264) → D2 rewritten; R-252 heads the money row; edge 15 rewritten.
- **A7** (R-119 is `policy`, not law) → accepted ✓ (REG:131) → D22; removed from RULINGS.
- **A8** (NEW-P5-1 ₹0 default is a decision) → accepted ✓ (REG:56 R-044; REG:272) → D21; one deferred procurement line kept.
- **N1** (facts are not rulings) → accepted ✓ (RM:452) → FACTS table split from RULINGS.
- **N2** (NEW-P5-3 duplicates SPEC §19; channel law uncited) → accepted ✓ (SPEC:820, not ~826; REG:32,261,126) → row renamed "SPEC §19 gateway (was NEW-P5-3)" with the three rulings cited.
- **N3 / N9 / N10** (R-058; D12's read dependency; no message to a patient in G6) → accepted → D10, D12, E1-M5 amended.
- **A2** (E4-M5 gate is not the Class-1 gate; R-128; identifier test; R-006) → accepted ✓ (DPIA:14 "Future stages", :28 "L1 (future revision)", :37 `[COUNSEL]`, :22 identifier tests — the verifier's :15/:24/:20 were off by one to four lines; REG:140; REG:18) → E4-M5 gate and acceptance rewritten; §0.3 and RULINGS updated.
- **A3** (no off switch; T4 Dispatcher; `task.verified`) → accepted ✓ (`guards.ts:51`; global halt 0 hits; SPEC:789 — the verifier's :786 is the T3 roster row; `config.ts:114-123`; IPD:458) → D23 flags on every automation; D33 manual path first, T4 later; `task.verified` gone.
- **A4** (recall can block a discharge; ignores `notifyOk`; console instrument) → accepted ✓ (`enqueue.ts:68-100`; `recovery.ts:447` `withTx`; `schema/ot.ts:198-199`; P15:190) → D25; E1-M1 rewritten; edge 45 added; E1-M5 instrument = rows by status.
- **A5** (E5-M2 processes staff data on production before the addendum) → accepted ✓ (DPIA:38; REG:130,134) → D34; E5-M2 gate rewritten; §0.8.
- **A10** (reconstruction script is a Class-2 read outside RBAC) → accepted ✓ (`kernel/phi/audit.ts:15`) → D35; E5-M2 and edge 26 rewritten.
- **N6** (Hermes narration is inference) → accepted ✓ (H:33,126) → E2 Goal names the gate.
- **N5 / N8** (edge 25 "spec fix 25"; edge 28 "RM rule 1") → accepted ✓ (SPEC:559 item 5; RM:362 G4, RM:370 rule 1 is about synthetic people) → cites fixed.
- **A9** (verify code in a GET path) → accepted ✓ (KD:98,157) → E3-M1 adopts KD:157.
- **N7** (R-009 retention unnamed) → accepted ✓ (REG:21) → retention classes on E3-M1, `protocol_rules`.
- **R1** (E1-M3 re-proposes #157) → accepted (= EC2) → delta; edge 42.
- **R2** (six roles; Class-A ceremony; `privileges` draft) → accepted ✓ (OTRB:19-30,46-63,116-130,142-153) → NEW-P5-2 = seven names (six + `department_head`); E1-M3 human act; edges 34, 35, 42.
- **R3** (E1-M5 depends on a money act the table waived) → accepted ✓ (OTRB:156-160; P15:172 is about discharge, not booking) → RULINGS row "R-255 (package + implant prices)"; E1-M5 gate; edge 36.
- **R4 / R15** (F is not free; Track S row for 15) → accepted ✓ (RM:334-337; CONTEXT:54; heartbeat 09-05 06:52) → §7 states both departures; E2 → L; E1-M3 → L; E1-M1/M2 → whoever lands printing v2.
- **R5** ("sheet live before the harvest week" false by arithmetic) → accepted → claim dropped; production week 6 at best; E2-M4's dated acceptance.
- **R6** (E2 "no hard deps" touches eight everyone's-files) → accepted ✓ (`worker/jobs.test.ts:337-344`, not `test/jobs.test.ts` as cited; `docker/prod/prometheus/alerts.yml`; `ops/manifest.ts:27-29`; the five pins) → E2 Deps row lists them and the rebase rule.
- **R7** (lint can redden `main`; ordering with radiology) → accepted → D30/D31; E2-M3; edges 39, 40.
- **R8** (E2-M5 creates an import cycle) → accepted ✓ (`daily-close.ts:8`; billing imports opd/tariff/patients/membership; lab→billing 13 files) → D16/D32; E2-M5 rewritten.
- **R9** (channel is procurement + kernel build; two thresholds measure a console) → accepted (= M2.7/M2.11) → gateway row gates E1-M5's `sent` clause and E2-M4's WhatsApp line; edges 37, 38.
- **R10 / R11** (`task.verified`; passes; `death` two plans behind) → accepted ✓ (0 hits; IDX:83,85; RM:424) → E4-M3; D33; edge 21.
- **R12** (roles not in `seed-roles.ts`) → accepted ✓ → §0.2, FACTS, E4/E5 rows.
- **R13** (`recovery.ts:549` moved; holiday calendar absent) → accepted (= EC4, M2.4).
- **R14** (UAT is a train fact; freeze unnamed) → accepted ✓ (#118 MERGED 09-08 11:59 UTC; #119–#122 OPEN; #73 OPEN never-merge) → E1 Deps and E1-M4 name "#120 + #121 landed, `deploy.sh HMIS_TARGET=uat`"; edge 41.
- **R16** (7 d of build before the walk) → accepted in part → §7 orders E1-M3 → E1-M2 → walk → E1-M1 as a pilot fix; the slip is declared the walk's prerequisite, the recall is not.
- **R17** (28 lane-days build-only) → accepted → §6 "build-only; ≈ 36 with the two close passes and the S-gate".
- **R18** (D8 vs table) → accepted → D8 "before 44 is built".
- **R19** (E3 is kernel work; L on S) → accepted → E3 names its files and the substitute adopter.
- **R20** (Plan 20 is FOR APPROVAL) → accepted ✓ (`…phase1-20-workforce-roster.md:3`) → FACTS row; E4-M1 gate.
- **Missing edge rows M1–M11** → accepted → edges 34–44; A4's enqueue-throws row → edge 45.
- **Citation corrections to the verdicts themselves** (not refutations, recorded so the next pass does not re-chase them): SPEC guardrails are at :789 (not :786); per-patient language at :771 (not :773); the §19 gateway line at :820 (not ~826); DPIA future stages :14, identifier tests :22, L1 :28 (not :15/:20/:24); PHY's `daycare.discharged` with procedure is :434 (not :90); IDX "wristbands/passes" is :83 (not :81); the jobs census comment is `src/kernel/worker/jobs.test.ts:337-344`.
