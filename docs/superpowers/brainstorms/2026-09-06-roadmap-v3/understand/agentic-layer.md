# Agentic layer — what the design says, what the code has, what is missing (pillars 1a, 2b, 4b, 5b)

Read against `main-ro` @ `b04cbd9` (2026-09-06). Paths are repo-relative unless prefixed `/opt/hmis`. Status vocabulary: **deployed** (on prod base `c11833d`, 56 migrations) / **merged** (on `main`) / **authored** (plan or spec written) / **brainstormed** / **absent**.

## 0. The frame every item below sits inside

- **Spec §16** (`docs/superpowers/specs/2026-08-10-hmis-architecture-design.md:774-791`): tiers T0 observe → T1 nudge → T2 draft for sign-off → T3 act behind approval → T4 autonomous; "**Clinical actions cap at T2–T3 permanently — agents draft, doctors decide**" (:778). Roster taxonomy: *automations* (rules/SQL/ladders on the harness) vs *agents* (call a model) (:780); "Where a rule is safer, cheaper and more auditable than a model, the rule wins." Roster of 15 (:782-787). Guardrails: first-class actor, API-only, fail-open, per-agent kill switch, global halt, draft provenance, lint-enforced (:789). Rollout: Plan 12a = runtime + Digest Writer + Leakage Auditor; Ops Copilot "out of Phase 1" (:791). PRE-12a gates: DPIA + inference-locus decision "before any agent activation" (:839).
- **Brainstorm 12** (`docs/superpowers/brainstorms/2026-08-27-department-series/12-agentic-copilot-layer.md`): runtime as a workflow `agent_run` propose → confirm → act → verify (:51-83); 15 runtime tables (:84-110); ~85-row roster, ~60 automations / ~25 agents (:301-484); copilot §9 (:485-575); maturity ladder shadow → recommend → propose (T2) → act-with-approval (T3) → autonomous (T4 non-clinical only) with numeric gates (:544-559); rulings O-1…O-10 (:607-619); plan sketch 12a/12b/12c (:621-631). "Built today" inventory as of 2026-08-27 (:21): `agents` table + `x-agent-key` + kill switch; `transition()` seam; `kernel/inference/` with `transcribe()` only, `complete()` deliberately not stubbed.
- **Clinical copilot spec** (`docs/superpowers/specs/2026-08-25-clinical-copilot-design.md`): D1 split — deterministic checks + snapshot card near-term; LLM briefing post-12a via the Class-1 lane; "clinical-roles-last conversational-copilot rollout ruling stands" (:20). Ten design laws (:26-35), the load-bearing ones for the mission: law 2 checks evaluate at issue time inside the transaction; law 3 checks never personalised; **law 4 "The LLM narrates, never originates. Hard safety alerts come only from deterministic checks"**; law 5 the model never sees more than the caller; law 9 inference is strictly additive. Phase A/B/C (:172-174); five activation gates (:125-129); counsel list incl. SaMD (:192).
- **DPIA** (`docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md`): file is labelled "Version 0.2 DRAFT … Not yet reviewed, not yet signed" (:3); scope = 12a runtime + Digest Writer + Leakage Auditor (:5); "Future stages … T2 clinical drafters consuming Class-1" each trigger a revision (:14); **Class 2 never enters an inference request, any stage, any provider, ever** (:20); router finding — owner's router picks a provider per request, Class-0-only with pinned model (:29); §3-A speech carve-out "pending owner accept/refuse and counsel review" (:31-33); sign-off table empty (:68-72). It is the only file in `docs/compliance/`.
- **Roadmap v2** (`docs/superpowers/2026-09-06-ROADMAP-v2.md`): "**No 12a agent runtime.** DPIA v0.2 is counsel's; the voice scribe stays inert at 503" (:428); DPIA v0.2 needed for "12a, 43c, 44's Drafter, the voice scribe" (:405); owner item 1 — a second server that "UAT and the Hermes ops copilot both want" (:440-441); the interaction/dose dataset licence ₹8–12 L a year, 16e waits on it (:37, :450).
- **Code, the whole agentic surface today:** `agents` table `id,name,api_key_hash,kill_switch,created_at` (`apps/core/src/kernel/db/schema/auth.ts:91-101`); `createAgent/findAgentByKey/setKillSwitch` (`kernel/auth/agents.ts:7-27`); `x-agent-key` → `{type:"agent"}` honouring the kill switch (`kernel/auth/guards.ts:47-52`); PermissionGuard: "agents hold no permissions yet" (`guards.ts:99-101`); `Actor` has four members (`packages/contracts/src/envelope.ts:35`); `kernel/inference/` = `types.ts` (`transcribe` only; "`complete` is NOT declared here as a placeholder: 12a owns that half" :9-11), `offline.ts`, `workers-ai.ts`, `speech.controller.ts` ("SHIPPED INERT" :37-44, user actors only :80); `SPEECH_PROVIDER` defaults `""` (`kernel/config.ts:170-172`); `VOICE_SEARCH_ENABLED = false` (`apps/web/src/lib/voice-flag.ts:27`); `VoiceButton` returns null. No `agent_permissions`, no `agent_ledger` (comments only: `guards.ts:100`, `kernel/printing/printing.controller.ts:31,43`, `modules/opd/events.ts:224`), no `agent_runs`, no tool catalog, no `kernel/agents/` or `kernel/copilot/`. `pnpm agent:create` exists (`apps/core/package.json:16`).

---

## 1a — Agentic task orchestration (one intelligence layer assigning real-time tasks to AI agents)

**Home:** Plan 12a/12b/12c (index §3 "slot when the DPIA is signed", `00-INDEX-AND-SYNTHESIS.md:57`); spec §16; brainstorm 12 §3 (runtime as workflow) and §8 (roster). First activation proposed as **12a-0 "the bridge"** — Hermes Ops Copilot (`/opt/hmis/docs/superpowers/brainstorms/2026-09-01-hermes-ops-copilot/00-BRAINSTORM.md:9-13`, R4 at :184), untracked, brainstorm v1.
**Status:** brainstormed. The runtime is absent in code; the harness pieces that exist are identity + key + kill switch (above).
**Evidence:** roadmap v2 §5 "No 12a agent runtime" (:428). Hermes §1.1 one-sentence finding: "the hospital OS already has the actor half … and none of the rest (no agent permissions, no tool surface, no ledger, no Class-0 reads shaped for an agent)" (:11). The radiology `ReportDrafter` seam exists and "IS NOT A MODEL" — offline drafter fills technique only, findings empty, lockout-checked (`apps/core/src/modules/radiology/drafter.ts:7-25`, seam at :173). Workflow engine reconstructs actors as users: "agent drafters are the Plan 12 seam" (`apps/core/src/kernel/workflow/definitions.ts:206`).
**What "AI agents" would be assigned today:** nothing model-backed. ~70 % of the roster is automations at ₹0 inference (brainstorm 12 §9.11 :538); "if it can be a rule, it is an automation" (:483). Live automations acting on events: OPD danger protocol — the one place "the agent acts alone", a double-confirmed danger reading sets queue class 0 with a 10-s human cancel (`modules/opd/escalation.ts:21-33`); the two radiology chasers (`kernel/alerts/consumer.ts` header); lab sweeps as `system` callers (`modules/lab/sweeps.ts:40`); workflow timers (`kernel/db/schema/workflow.ts:82`).
**Gaps:** (1) DPIA v0.2 unsigned — the PRE-12a gate (spec :839; R-262). (2) No second server (roadmap v2 :440). (3) Pinned provider under a no-training DPA (Hermes R1/R3 :181-183; DPIA :29). (4) `agent_permissions` + PermissionGuard seam (Hermes T1 :194). (5) Tool catalog + Class-0 egress law (Hermes :100-126). (6) `agent_ledger` shared with RC-6 (Hermes :172). (7) Runtime tables / `agent_run` workflow (brainstorm 12 §4).
**Rulings/gates:** R-127 global-halt authority, R-128 consent posture, R-129 liability footer, R-130 promotion numbers, R-131 cost caps ₹5,000/day (money), R-133 steward card, R-247 single-approver honesty (blocks 12a kill-switch drills), R-260 Expertise store sharing, R-262 DPIA v0.2 (`00-OWNER-RULINGS-REGISTER.md:139-146, 259, 272, 274`). Hermes R1–R4 (:181-184). Spec fix 42: DPIA re-run before each agentic phase (DPIA :5, :64).
**Tier:** operational automations may climb to T4; Hermes bridge is T0 (observe/report); clinical classes ≤ T3 asserted in CI (brainstorm 12 §4 `tier_max`).
**Where the mission exceeds the spec:** "one intelligence layer … in real time" reads as a central model-driven planner. The spec has no such thing and refuses it by construction: agents "communicate only through events and tasks the humans can also see", "No agent-to-agent transport" (brainstorm 12 §2 :47); every act is a workflow transition behind the same guards as a human. **Honest reconciliation:** the "intelligence layer" is the event log + workflow engine + escalation ladders, with automations subscribed to events and agents (model-backed) limited to T2 drafts and T0 reports. The first *AI agent* to be assigned anything is the T0 Hermes Ops Copilot, and only after DPIA signature + second server + pinned DPA.

## 1a-task-assignment-to-humans (nurses, phlebotomists, radiology technicians, coordinators)

**Home:** kernel `tasks` / P5 pool engine — index §4 theme 13 (`00-INDEX-AND-SYNTHESIS.md:153`), kernel ask "Accept in 19a (R-094)" (:175), register R-094 "one engine, five pools; housekeeping first consumer" (`00-OWNER-RULINGS-REGISTER.md:106`). Roster substrate — Plan 20 (index :63; roadmap v2 Q4 :276-300: Coverage Resolver as pure function, T3 only, duty manager approves).
**Status:** absent (tasks engine); authored/DECIDED (Plan 20 is the first Track C substrate this quarter, roadmap v2 gate table :401 "YES"). Roadmap v2 §5: "not 19a/19 (housekeeping and the P5 pool engine — no consumer opens this quarter)" (:414-415).
**What exists:** no `tasks` table in `apps/core/src/kernel/db/schema/` (grep `"tasks"` empty; tables present: `alerts`, `approvals`, `workflow_*`, `notifications`). Assignment today is **role-broadcast**: `usersHoldingRole` — "until [the roster] lands, escalation resolves to everyone currently holding the role" (`apps/core/src/kernel/workflow/roles.ts:29-32`); ladder rung → `duty_manager` → `owner` (`kernel/alerts/consumer.ts` `OWNER_ROLE`, `DUTY_MANAGER_ROLE`); alerts are bounded pages (`kernel/alerts/alerts.ts:9-14`). Approvals worklist exists (`kernel/approvals/worklist.ts`).
**Gaps:** a task entity with claim discipline and `pool_empty` fallback (theme 13); on-duty resolution (Plan 20); per-role nudge budgets (brainstorm 12 L1 :215); the chaser destination as a configuration row (roadmap v2 :298-300).
**Rulings:** R-094 (scope, pre-plan-authoring), R-110 escalation timers (:122), R-071/R-072/R-067 for Plan 20 (index :63). Fix 37 equity (payer-blind, VIP-blind prioritisation) binds any allocator (brainstorm 12 :14).
**Tier:** Turnover Dispatcher T4 (Plan 19), Coverage Resolver T3 (Plan 20), SLA Chaser T1 (exists as ladders).
**Reconciliation:** real-time human task assignment in the quarter = Plan 20 roster (DECIDED) re-pointing the existing ladders from "everyone holding the role" to the on-duty holder; the pool engine (19a) is the next quarter's first Track B build. Measurable parameter: `escalation` alerts whose recipient set is 1 named on-duty holder rather than N role holders.

## 2b-listen — ambient co-pilot "listens in real time"

**Home:** Plan 11h DD11 (voice search, push-to-talk); brainstorm 12 §9.6 (`12-agentic-copilot-layer.md:513-514`): "clinical dictation for drafters is a separate, later carve-out requiring in-region or on-prem ASR (whisper.cpp on the box) — **cloud ASR for clinical narratives is not proposed**"; 05 video-consult brainstorm: "any transcript-to-note or **ambient scribe** is blocked by the standing inference-locus law pending the §19 amendment" (`05-video-consultation-opd.md:459`); index theme 4: "**no transcript-to-note agent is scheduled in any plan**" (`00-INDEX-AND-SYNTHESIS.md:144`); 43c "inference (handover/duty-report drafters, dictation) after 12a + R-059" (:85).
**Status:** merged-and-inert (ASR plumbing only); the scribe itself is absent from every plan.
**Evidence:** `speech.controller.ts:37-44` ("voice audio is Class 2 until the DPIA rules otherwise"), :53-55 transcript "is never forwarded to a language model"; `types.ts:17` language is `"hi" | "en"` only; `workers-ai.ts:9-17` `vad_filter` measured hallucination defence; DPIA §3-A carve-out terms (:33): push-to-talk, 15-s cap, audio discarded, transcript into a deterministic parser, cross-border acknowledged; `voice-flag.ts:6-22` three preconditions (DPIA revision, measured Hinglish accuracy, server config).
**Gaps:** (1) R-059 — the §19 audio inference-locus amendment, legal, pre-plan-authoring (`00-OWNER-RULINGS-REGISTER.md:71`). (2) On-prem ASR box ₹3–6 L (brainstorm 12 §12 :595-606; R-126). (3) DPIA L1 revision for any transcript that reaches a text model. (4) Consent posture in the room (R-128), TPG/IMC exposure (brainstorm 12 §7 :294-297). (5) Hinglish eval fixtures (§9.7 :516). (6) 15 Indian languages promised vs two in code.
**Tier:** ASR is a processor, not a tier; a scribe that drafts a note is a T2 drafter (Handover/Progress Note Drafter rows, roster :340, :391) and the roster has no OPD consult-note drafter row at all.
**Where the mission exceeds the spec:** "listens in real time" = continuous ambient capture; the only authorised shape is push-to-talk ≤ 15 s, nothing persisted, transcript never chained into a model. Ambient capture is the opposite on every axis (continuous, persists a transcript, feeds a model). **Reconciliation, three rungs, each behind its own gate:** (i) push-to-talk structured intent at the desk (built, needs DPIA §3-A accept + Hinglish accuracy measured); (ii) clinical dictation-to-draft on on-prem ASR (needs R-059 + GPU box + DPIA L1); (iii) ambient — not in any design; requires a new DPIA class analysis (a third party in the room: attendant), and R-020's spirit (no model to patients). Say so in the roadmap rather than schedule a scribe by accident.

## 2b-surface-history — "surfaces relevant medical history"

**Home:** Plan 16b — Context Assembler & Snapshot Card (`docs/superpowers/plans/2026-08-25-phase1-16b-context-assembler-snapshot-card.md`), copilot spec §2.1 (:85-96). Seven tasks: pack contract, assembler with four states, medication review (the check engine's second call site), endpoint, card, enum preference surface, DPIA v0.3 L1 skeleton (:77-166).
**Status:** authored **PROVISIONAL — "Not approved for execution; does not start before 16a closes"** (:3). 16a closed 2026-08-26 and named 16b's drifted seams (census twelve not eleven, migration head `0027` not `0011`, `RxOverride` shape) (`…16a-formulary-prescribing-safety.md:377-389`). 16b's §6 CLOSE is an empty template (:166-168). No `briefing` module exists (`ls apps/core/src/modules` — 14 modules, none named briefing); no snapshot component in `apps/web/src` (grep `Snapshot|briefing|assembler` hits only `opd-display.test.tsx` and `lab-api.ts`).
**What exists instead (deployed):** consult screen history tab `visits | rx | vitals` (`apps/web/src/screens/opd-consult.tsx:170,177`), allergies query (:282-283), lab results for the encounter (:20 `resultsForEncounter`); readers `GET /opd/patients/:id/timeline|prescriptions|vitals` (`apps/core/src/modules/opd/opd-visits.controller.ts:613-650`) built by 07d T1 because before it "there is no way to read a prior prescription at all" (`modules/opd/history.ts:4-11`); every read logs PHI access with care-context (`kernel/phi/audit.ts:244`, `modules/opd/care-context.ts`).
**Gaps:** the assembled one-sheet with allowlist, four-state render ("blank is not a state", law 6), per-section permission filtering, "as of HH:MM" freshness, in-system-only honesty labels, and **current medications run through the check engine against each other** (16b T3 :104) — none built. The NKDA owner decision (16b §0.7 :22) is still unasked.
**Rulings/gates:** none legal — "zero inference, zero DPIA gate" (copilot spec :172). Owner decision only on NKDA record type. Sealed-class propagation (fix 25 / theme 7) inherits the caller's filter.
**Tier:** none (deterministic read; the card "is a complete product" — law 9).
**Reconciliation:** this is the cheapest and only un-gated 2b capability; the mission's wording is met exactly by Phase A. It is also the precondition for 2b-suggest (Phase B inherits the assembler invariants; "building the card ad-hoc … bequeaths Phase B a bypass", :96).

## 2b-suggest — "suggests clinical considerations"

**Home:** Clinical Context Lens narration, copilot spec §2.2–2.6; Plan 12c "Lens narration behind gates 1–5" (brainstorm 12 §14 :626); Honcho socket §3.
**Status:** authored; absent in code (`complete()` not declared, `types.ts:9-11`).
**Evidence:** output contract — typed claims `summary | observation | alert-restatement`, every claim cites fact-sheet line-ids, **renderer drops uncited claims**, alert-restatement must reference a real deterministic alert id (:114); residual mis-citation risk stated (:116); on-demand, streaming, never auto-fired, degrades to the card (:120); five ordered gates: DPIA L1, pinned provider under DPA (router excluded), eval suite with adversarial fixtures, shadow mode only after gates 1–3, `CLINICAL_LENS_LLM_ENABLED=false` (:125-129). Cost model: 30 % of consults invoke, ≈ ₹100/day at 100 OPD (brainstorm 12 §9.10 :525-535).
**Gaps:** everything in Phase B plus 16b as its floor; DPIA L1 (Class-1 re-identification analysis over the in-text scrubber, "the crux", :101); R-126 inference locus (on-prem small models default); eval lane that actually runs (two lanes, :178); retention of briefings as sealed artefacts (:186) — counsel item 5.
**Rulings:** R-126 (legal, pre-plan-authoring), R-254 residency, R-128 consent, R-129 footer, R-131 caps, R-132 clinical roles last; copilot counsel items 1, 3 (SaMD), 4 (patient notice), 5 (:190-194). Note 12 protocol-adherence is the only route by which a "consideration" could be *rule-originated* rather than narrated.
**Tier:** T2 (narration for a human reader); the Lens is "narrate-never-originate".
**Where the mission exceeds the spec:** "suggests clinical considerations" implies originating differentials or next steps. Design law 4 forbids origination: model text renders only as a cited restatement of what the fact sheet already holds, and "hard safety alerts come only from deterministic checks". **Reconciliation:** a *consideration* is one of three things, in order of arrival — (a) a deterministic check hit (allergy/DDI/duplicate — exists), (b) a cited restatement/trend across sheet lines (Phase B, e.g. three creatinine lines), (c) a protocol-rule prompt when machine-readable protocols exist (note 12). Free-form differential diagnosis is outside the cap and should be written into the roadmap as *not built*, with SaMD (Medical Device Rules 2017) as the reason counsel is asked.

## 2b-draft-rx — "auto-drafts prescriptions for physician approval"

**Home:** none. Spec §16's T2 drafters are Discharge Summary, Radiology Report, Claims, Workflow Tuner (:785); brainstorm 12's ~85-row roster (:301-484) has **no prescription drafter** — the nearest rows are Substitution Suggester (pharmacy, T2, "same-salt alternatives", :421) and the ICD Coding Suggester (:431). The copilot spec keeps prescribing human: lines free-text-capable, resolution re-verified at issue, checks inside the transaction (:59-68).
**Status:** absent — not brainstormed as a roster row.
**Evidence:** the pipeline is `issuePrescription` → `runRxChecks` → hard-warn/override/KPI (`modules/opd/prescriptions.ts:255-261, 373-393`); the DPIA names "T2 clinical drafters consuming Class-1" as a future stage triggering a revision (:14); "AI-drafted documents carry RMP signature; no auto-signing" (brainstorm 12 §7 :297).
**Gaps:** no roster row (trigger, output, sign-off, fixtures); its Class-1 payload would be the 16b fact sheet; SaMD (copilot counsel item 3 :192) is sharpest for an Rx draft; e-Rx signature R-050 (`register:62`); footer R-129.
**Tier:** T2 is the ceiling and is *permitted* in principle (draft for sign-off), but the spec's own posture is that where a rule is safer the rule wins (:780).
**Where the mission exceeds the spec:** the mission promises the highest-risk clinical drafter the design never rostered. **Reconciliation, two rungs:** (i) deterministic pre-fill — order sets / department templates keyed by diagnosis + the patient's current meds, checked by the same engine at issue (a "T2-equivalent (template)" like the death-certificate pre-fill, :433) — zero inference, no DPIA gate, buildable on 16a+16b; (ii) a model Rx drafter as a new roster row, shadow-first, behind the five Lens gates plus a counsel opinion on SaMD, activated per the §9.12 ladder (owner signs clinical promotions, R-130). Name it in the roadmap as parked, not as scheduled.

## 4b — Drug–drug interaction checks (beta)

**Home:** Plan 16a — Formulary & Prescribing Safety (`docs/superpowers/plans/2026-08-25-phase1-16a-formulary-prescribing-safety.md`), copilot spec §1.3 (:63-69), §1.4 calibration (:73).
**Status:** **merged and CLOSED 2026-08-26** (`cf9a9e4`, 212 suites/1847 tests, 14 mutants died, :366-375); **deployed by migration count** (16a's head was `0027` :383; prod runs 56) — **but inert on data**: with no pairs `checkInteractions` returns `[]` (`apps/core/src/modules/opd/rx-checks.ts:186`), and nothing I read records `seed-formulary-interactions` ever running on production; 16a itself: "Production still holds ONE prescription" (:388).
**Evidence in code:** `checkInteractions(lines, priors, pairs, now)` pairwise over moieties in-rx and against *current* prior prescriptions, same-line FDC pairs skipped (`rx-checks.ts:169-184`); `checkDuplicateSalt` (:262); one engine, two call sites (:7-12) — the OPD issue pipeline (`prescriptions.ts:259-260`) and the **pharmacy dispense re-check D9**, which refuses a severe pair the prescriber did not override, "back to the doctor" (`modules/pharmacy/verify.ts:185-205`); overrides persisted as jsonb (`kernel/db/schema/opd.ts:573`); KPI `interactionOverrideCount` (`modules/opd/events.ts:341`); starter seed of **26 severe pairs**, `source:'seed-2026-08'`, "a floor, not a formulary" (`apps/core/scripts/seed-formulary-interactions.ts:14-19, 81`).
**Gaps (named by the design itself):** (1) the interaction/dose dataset licence ₹8–12 L/yr — procurement, blocks 16e clinical pharmacy (roadmap v2 :37, :450); (2) DTC expansion of pairs (seed :17-18) — a clinical committee that needs a second approving actor (O1); (3) coverage: only *resolved* lines get the suite, unresolved lines get substring allergy only (copilot spec :59); the coverage-gated notice stays off below 80 % (:61); (4) F22 — override RATE has no denominator (16a :377-381); (5) deferred by name: dose-range/renal/hepatic/paediatric/pregnancy, class cross-sensitivity, retro-scan on composition correction (copilot spec :199); (6) pharmacy's D9 re-check is merged but pharmacy is live-and-inert (memory: `pharmacy-launch-2026-09-06`).
**Rulings:** none legal for the check itself (rule, not model); copilot counsel item 3 SaMD "reference information with doctor deciding" (:192); D2 seed-never-authority (:21); R-009 retention; the dataset licence is procurement (owner item 4, roadmap v2 :450).
**Tier:** an automation-grade rule inside the transaction — not an agent, no tier; hard-warn → override-with-reason → KPI.
**Mission vs spec:** the mission's "real-time before adverse events" is *exactly* law 2 (issue-time, inside the transaction) and is met; the spec is stricter than "beta" (hard block at dispense). The honest gap is **data, not code**: an empty `formulary_interactions` table on production makes 4b a green suite protecting nobody. Measurable parameters: rows in `formulary_interactions` on prod; `formulary` coverage of the last 30 days' Rx lines; `interactionOverrideCount` per week; pharmacy `interaction_block` refusals.

## 5b — 100 % real-time protocol auditing

**Home:** Protocol-Adherence Evaluator (roadmap note 12): brainstorm 12 roster — automation, **T0**, "Events vs rules → Deviation task → Named clinical reviewer → ships with the NABH pack" (`12-agentic-copilot-layer.md:393`); quality brainstorm 22 — automation, "**T2-capped (deviation *review tasks*)**", "machine-readable protocol rules in Expertise store × patient-context events", manual path = sampling audits, "deviation ≠ correction", "**gated on knowledge sourcing**" (`22-quality-nabh-incidents-infection-control-governance.md:460`); Plan **28d** "Protocol-Adherence Evaluator only when machine-readable protocols exist" (:537); 28e the inference lane for quality (:538); index row 28 (`00-INDEX-AND-SYNTHESIS.md:74`).
**Status:** brainstormed; Plan 28 is "Phase-2 fast-follow" and roadmap v2 §5 says "not 28" this quarter (:416-418).
**What is real-time and 100 % today (deployed):** every state change is a guarded workflow transition (`kernel/workflow/instances.ts`, `roles.ts:8-27`) and an append-only event; gates already encode protocols as *blocks*: OPD danger protocol (`modules/opd/escalation.ts`), Form F / PCPNDT lockout, AERB licence gate, lab critical read-back, OT counts hard stop (roster :412 "module rule, not agent"); PHI access log with care context (`kernel/phi/audit.ts:244`); a T0 Notes-vs-Orders Cross-check (fix 20) is rostered for IPD (:446).
**Gaps:** (1) machine-readable protocols do not exist — the Expertise store is R-260 (shared `documents` table with SOPs, decide at 28c authoring); (2) the quality brainstorm claims `audit.control_tested` "(exists)" (:125) — **it does not**: `grep control_tested apps/core/src` is empty; (3) reviewer identity — "every agent report lands as a task with a named reviewer" (E-18) needs the tasks engine and Plan 20; (4) the evaluator's outputs are *review tasks*, never blocks — a block is the module's own gate.
**Rulings:** R-247 single-approver honesty (28a, 12a); R-240 just-culture (28a, pre-plan-authoring) — a deviation register without a just-culture policy is a disciplinary tool; R-244 no meeting audio (28e); R-260 Expertise store. DPIA staff-data flag (§4 :38) — deviation tasks are staff behavioural data, "never individual performance evaluation".
**Tier:** T0 report / T2-capped review tasks; never T3+.
**Where the mission exceeds the spec:** "100 % of patient interactions against clinical protocols in real time" — the spec gives 100 % *event capture* and 100 % *gate enforcement* on transitions (real, live), and a T0 evaluator over rules that do not yet exist. **Reconciliation:** (i) say plainly that every interaction is already an evented, replayable, guarded transition (that is the 100 %); (ii) the first "protocol set" is the gates already built — count them and their refusals as the audit; (iii) the evaluator arrives with 28d after (a) an Expertise store, (b) a just-culture ruling, (c) a named reviewer via Plan 20, with rule versions and unit tests per rule. Measurable parameter: number of protocol rules with a version id; deviation tasks opened/closed per day; one run reconstructed end-to-end (brainstorm 12 §7 :291 "what an inspector demands at the door").

---

## Facts

1. Spec §16 caps clinical agents at T2–T3 permanently (`…architecture-design.md:778`); T4 is operational only.
2. The DPIA is one unsigned draft labelled v0.2 inside a v0.1 filename, scope = 12a + two proofs; the sign-off table is empty (`docs/compliance/2026-08-23-dpia-agentic-runtime-v0.1.md:3, 5, 68-72`). It is the PRE-12a gate (spec :839) and roadmap v2 :405.
3. Code holds identity + key + kill switch + `transcribe()` only; `complete()` is deliberately undeclared (`kernel/inference/types.ts:9-11`); PermissionGuard refuses every agent (`kernel/auth/guards.ts:99-101`).
4. Roadmap v2 §5: "No 12a agent runtime" this quarter (:428); the Hermes T0 bridge and UAT both need the second server the owner has not ruled on (:440).
5. 16a (DDI) is merged, closed, and — by migration count 0027 ≤ 56 — deployed; the engine returns nothing with zero pairs (`rx-checks.ts:186`) and the 26-pair seed's execution on prod is unrecorded.
6. 16b (snapshot card) is PROVISIONAL, unapproved, unbuilt (`…16b….md:3`; §6 CLOSE empty :166); it needs no DPIA gate (copilot spec :172).
7. No plan, roster row or spec schedules a prescription drafter or an ambient scribe; index theme 4: "no transcript-to-note agent is scheduled in any plan" (`00-INDEX-AND-SYNTHESIS.md:144`); 05 brainstorm :459 blocks the ambient scribe on R-059.
8. Voice: push-to-talk ≤ 15 s, `hi|en` only, transcript never chained to a model, flag `false`, route 503 (`speech.controller.ts:37-55`, `voice-flag.ts:27`, `config.ts:170`).
9. Human task assignment resolves to everyone holding a role until Plan 20 (`kernel/workflow/roles.ts:29-32`); no kernel `tasks` table; pool engine is 19a (R-094), not this quarter (roadmap v2 :414).
10. Protocol-Adherence Evaluator is T0/T2-capped review tasks gated on machine-readable protocols (brainstorm 12 :393; 22 :460, :537); Plan 28 is not this quarter (roadmap v2 :416).
11. The radiology drafter seam exists and is explicitly not a model; the offline drafter never writes a finding (`modules/radiology/drafter.ts:7-25`).
12. Design law 4: the LLM narrates, never originates; hard alerts come only from deterministic checks (copilot spec :29); uncited claims are dropped (:114).

## Surprises

1. **The quality brainstorm asserts `audit.control_tested` "(exists)"** (`22-…md:125`) and no such event is in `apps/core/src`. A brainstorm claim about code, unverified, now sits under a mission pillar.
2. **4b is deployed and probably empty.** The most mission-ready capability is a green, closed, deployed engine whose only data source is a 26-pair seed script no runbook names; the measurable parameter is a row count on production, not a test count.
3. **Nothing in 22 brainstorm documents names an Rx drafter or an ambient scribe.** The two most visible promises of pillar 2b have no home, no roster row and no ruling id — the register's nearest rows (R-059, R-050, R-129) are all *legal*.
4. **The one autonomous clinical act in production is a rule, not an agent**: the double-confirmed danger reading bumps the queue by itself with a 10-s human cancel (`escalation.ts:21-33`) — a signed-off per-seat autonomy ladder that predates the 12a runtime and is the pattern the mission's "real-time task assignment" already follows.
5. **The DPIA's filename says v0.1, its header says v0.2 DRAFT**, roadmap v2 waits on "v0.2", and 16b T7 would write v0.3. Three versions, one unsigned file.
6. **Plan 16b sits un-approved eleven days after its only precondition (16a's CLOSE) was met**, with its seam drift already recorded by 16a (:383-386) — the cheapest 2b deliverable is waiting on nobody.
7. **The Hermes bridge would discharge the Plan-02 seam without 12a's runtime** (`00-BRAINSTORM.md:13, 175`): 12a's "first activation" becomes an external agent that never calls a model inside HMIS; the Digest Writer becomes a Hermes cron over a Class-0 fact sheet.
